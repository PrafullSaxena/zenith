---
phase: 11-full-ui-ux-revamp
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
  - src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx
  - src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
  - src/renderer/src/plugins/launchpad/ProviderSelector.tsx
  - src/renderer/src/plugins/launchpad/AiAdvisor.tsx
  - src/renderer/src/plugins/launchpad/EstimationHistory.tsx
  - src/renderer/src/plugins/launchpad/ComparisonView.tsx
  - src/renderer/src/components/dashboard/MissionControl.tsx
  - src/renderer/src/plugins/stubs/AstroPatchView.tsx
  - src/renderer/src/plugins/stubs/PromptBuilderView.tsx
autonomous: true
requirements:
  - SHELL-07

must_haves:
  truths:
    - "CodeReviewBot, DbInspector, and Launchpad views have stagger-children entrance animations on their top-level sections"
    - "PR cards, QuickStat cards, history entries, and provider cards lift on hover"
    - "Stub plugins (AstroPatch, PromptBuilder) have entrance animations on icons and subtle glow effects"
    - "Launchpad ComparisonView has alternating row shading for readability"
  artifacts:
    - path: "src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx"
      provides: "stagger-children on view container"
      contains: "stagger-children"
    - path: "src/renderer/src/plugins/db-inspector/DbInspectorView.tsx"
      provides: "stagger-children on left panel"
      contains: "stagger-children"
    - path: "src/renderer/src/plugins/launchpad/ProviderSelector.tsx"
      provides: "stagger-children + hover-lift on provider cards"
      contains: "hover-lift"
    - path: "src/renderer/src/plugins/stubs/AstroPatchView.tsx"
      provides: "Entrance animation and glow effect"
    - path: "src/renderer/src/plugins/stubs/PromptBuilderView.tsx"
      provides: "Entrance animation and glow effect"
    - path: "src/renderer/src/components/dashboard/MissionControl.tsx"
      provides: "hover-lift on QuickStat cards"
      contains: "hover-lift"
  key_links:
    - from: "src/renderer/src/assets/main.css"
      to: "All modified component files"
      via: "CSS stagger-children, hover-lift, animate-fade-in-up utilities"
      pattern: "(stagger-children|hover-lift|animate-fade-in-up)"
---

<objective>
Add entrance animations, stagger effects, hover micro-interactions, and visual polish across CodeReviewBot, DbInspector, Launchpad, Dashboard, and stub plugin views.

Purpose: Make the application feel alive and responsive. Every view should have entrance animations on mount, cards should lift on hover, and lists should stagger their children. This brings the rest of the app up to the quality level already established by Dashboard and Nebula (which already use stagger-children, hover-lift, and animate-page-enter).

Output: 10 files updated with animation classes and micro-interactions. No logic changes -- visual/CSS classes only.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-full-ui-ux-revamp/11-RESEARCH.md
@.planning/phases/11-full-ui-ux-revamp/11-01-SUMMARY.md
@src/renderer/src/assets/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stagger-children and hover-lift to CodeReviewBot, DbInspector, Launchpad, and Dashboard</name>
  <files>
    src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
    src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx
    src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
    src/renderer/src/plugins/launchpad/ProviderSelector.tsx
    src/renderer/src/plugins/launchpad/AiAdvisor.tsx
    src/renderer/src/plugins/launchpad/EstimationHistory.tsx
    src/renderer/src/plugins/launchpad/ComparisonView.tsx
    src/renderer/src/components/dashboard/MissionControl.tsx
  </files>
  <action>
Add CSS animation classes to existing elements. Do NOT add new elements or change component structure. Only add/modify className strings.

**1. CodeReviewBotView.tsx:**
Add `stagger-children` to the top-level container div that wraps the view's main sections (header, tab bar, content). This is the outermost div inside the component return. The children (header section, tab bar, tab content panel) will get staggered entrance animations.

**2. ReviewPanel.tsx:**
Find the container that renders the list of AI review finding cards. Add `stagger-children` to that container. IMPORTANT: Only do this if the findings list is typically <=10 items. If the list can be large (variable length from AI), do NOT add stagger-children -- instead add `animate-fade-in-up` to the container itself for a single entrance animation.

**3. DbInspectorView.tsx:**
Find the left panel (sidebar with connection info, schema sections). Add `stagger-children` to the left panel container so its sections (connection selector, schema tree, etc.) stagger in. Do NOT add stagger-children to the main content area (query results can have many rows).

**4. ProviderSelector.tsx:**
- Add `stagger-children` to the grid/flex container that holds the provider cards (AWS, GCP, Azure)
- Add `hover-lift` class to each individual provider card element
- These are <=3 cards so stagger-children is safe

**5. AiAdvisor.tsx:**
Add hover animation to example prompt buttons/chips. Add `hover-lift` class to each example prompt button if they are rendered as cards/buttons. If they are simple text buttons, add `transition-colors duration-200` instead of hover-lift (hover-lift is for card-like elements with shadows).

**6. EstimationHistory.tsx:**
Add `hover-lift` to each history entry card/row. If entries are rendered as a flat list, add hover-lift to each list item's wrapper div.

**7. ComparisonView.tsx:**
Add alternating row shading to the comparison table for readability. Find the table body rows and add conditional className:
- Even rows: `bg-surface/30` (subtle background)
- Odd rows: transparent (default)
Pattern: On the `<tr>` element, add `className={index % 2 === 0 ? 'bg-surface/30' : ''}` or use `even:bg-surface/30` if using Tailwind's even: modifier on the row.

Also add `hover:bg-surface-elevated/30` to table rows for hover highlighting.

**8. MissionControl.tsx:**
Find the QuickStat cards section (likely StatsCards or individual stat card components). Add `hover-lift` class to each QuickStat card wrapper. The main MissionControl container likely already has `stagger-children` -- verify and keep it.

**CRITICAL CONSTRAINTS:**
- Do NOT apply `stagger-children` to containers with more than 10 dynamic children (PR lists, query results, schema tables). It causes performance issues.
- Only use `stagger-children` on static or small-count containers (view sections, card grids of 3-6 items).
- `hover-lift` is for card-like elements (border, rounded corners, shadow). Not for text buttons or inline links.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && echo "Checking stagger-children additions..." && grep -l 'stagger-children' src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx src/renderer/src/plugins/db-inspector/DbInspectorView.tsx src/renderer/src/plugins/launchpad/ProviderSelector.tsx 2>/dev/null | wc -l | xargs -I{} echo "Files with stagger-children: {}/3" && echo "Checking hover-lift additions..." && grep -l 'hover-lift' src/renderer/src/plugins/launchpad/ProviderSelector.tsx src/renderer/src/plugins/launchpad/EstimationHistory.tsx src/renderer/src/components/dashboard/MissionControl.tsx 2>/dev/null | wc -l | xargs -I{} echo "Files with hover-lift: {}/3"</automated>
    <manual>Open CodeReviewBot, DbInspector, and Launchpad -- verify sections animate in with a stagger effect on mount. Hover provider cards in Launchpad to see lift effect.</manual>
  </verify>
  <done>stagger-children added to CodeReviewBot, DbInspector, and Launchpad view containers. hover-lift added to ProviderSelector cards, EstimationHistory entries, MissionControl QuickStat cards. ComparisonView has alternating row shading. No logic changes.</done>
</task>

<task type="auto">
  <name>Task 2: Add entrance animations and glow effects to stub plugins</name>
  <files>
    src/renderer/src/plugins/stubs/AstroPatchView.tsx
    src/renderer/src/plugins/stubs/PromptBuilderView.tsx
  </files>
  <action>
Both stub plugins currently show a centered layout with an icon, title, and "Coming Soon" badge. Enhance them with entrance animations and a subtle glow effect to make them feel polished rather than placeholder.

**For BOTH AstroPatchView.tsx and PromptBuilderView.tsx, apply identical changes:**

1. **Icon entrance animation:** Wrap the main icon in a div with `animate-fade-in-up` class and add an inline style for a subtle scale animation. Or more simply, add `animate-fade-in-up` to the icon wrapper and add a CSS animation for scale-in:
   - Add className `animate-fade-in-up` to the icon container
   - The icon should appear to float in from below on mount

2. **Subtle glow effect on the icon:** Add a background glow behind the icon. This is a div behind/around the icon with:
   ```
   className="absolute inset-0 rounded-full bg-accent/[0.06] blur-xl"
   ```
   The icon container needs `relative` positioning. Add a sibling `<div>` with the glow behind the icon.

   If the icon wrapper already has a background circle (like `bg-accent/[0.06]`), add a second larger glow element:
   ```tsx
   <div className="relative mb-4">
     {/* Glow */}
     <div className="absolute -inset-4 rounded-full bg-accent/[0.04] blur-2xl" />
     {/* Icon circle */}
     <div className="relative flex h-16 w-16 items-center justify-center rounded-2xl bg-accent/[0.08]">
       <IconComponent size={28} className="text-accent/60" />
     </div>
   </div>
   ```

3. **"Coming Soon" badge:** Add `hover-lift` class to the badge element so it lifts slightly on hover, giving it an interactive feel even though it's not functional yet.

4. **Stagger the content:** Add `stagger-children` to the parent container so the icon, title, description, and badge animate in sequentially.

5. **Pulsing animation on icon:** Add a very subtle pulse to the icon using an inline animation or by adding `animate-[pulse_3s_ease-in-out_infinite]` with low opacity variation. Alternatively, use the `animate-status-pulse` class on the glow div (not the icon itself) for a breathing effect that hints at future availability.

**Structure target for each stub:**
```tsx
<div className="stagger-children flex flex-col items-center justify-center h-full">
  <div className="relative mb-4">
    <div className="animate-status-pulse absolute -inset-4 rounded-full bg-accent/[0.04] blur-2xl" />
    <div className="relative flex h-16 w-16 items-center justify-center rounded-2xl bg-accent/[0.08]">
      <IconComponent size={28} className="text-accent/60" />
    </div>
  </div>
  <h2 className="text-lg font-semibold text-text-primary">Plugin Name</h2>
  <p className="text-sm text-text-secondary/70">Description text</p>
  <span className="hover-lift mt-3 rounded-full bg-warning-muted px-3 py-1 text-xs font-medium text-warning">
    Coming Soon
  </span>
</div>
```

Adapt this structure to match the existing component's content and layout. Do NOT change the actual text content or component name -- only add animation classes and the glow div.

Use `bg-warning-muted text-warning` for the "Coming Soon" badge (semantic token from Plan 01) instead of any hardcoded amber/yellow colors.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && grep -q 'stagger-children' src/renderer/src/plugins/stubs/AstroPatchView.tsx && grep -q 'stagger-children' src/renderer/src/plugins/stubs/PromptBuilderView.tsx && grep -q 'hover-lift' src/renderer/src/plugins/stubs/AstroPatchView.tsx && grep -q 'blur-' src/renderer/src/plugins/stubs/AstroPatchView.tsx && echo "PASS: All stub animations added" || echo "FAIL: Missing animation classes"</automated>
    <manual>Navigate to AstroPatch and PromptBuilder in the sidebar. Verify: icon fades in from below, glow pulses subtly behind icon, badge lifts on hover, content staggers in sequentially.</manual>
  </verify>
  <done>Both AstroPatchView and PromptBuilderView have stagger-children entrance, icon glow with pulse, hover-lift on Coming Soon badge, and animate-fade-in-up on icon. Coming Soon badge uses bg-warning-muted/text-warning semantic tokens.</done>
</task>

</tasks>

<verification>
1. `grep -l 'stagger-children' src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx src/renderer/src/plugins/db-inspector/DbInspectorView.tsx src/renderer/src/plugins/launchpad/ProviderSelector.tsx src/renderer/src/plugins/stubs/AstroPatchView.tsx src/renderer/src/plugins/stubs/PromptBuilderView.tsx` returns all 5 files
2. `grep -l 'hover-lift' src/renderer/src/plugins/launchpad/ProviderSelector.tsx src/renderer/src/plugins/launchpad/EstimationHistory.tsx src/renderer/src/components/dashboard/MissionControl.tsx src/renderer/src/plugins/stubs/AstroPatchView.tsx` returns all 4 files
3. `grep -l 'blur-' src/renderer/src/plugins/stubs/AstroPatchView.tsx src/renderer/src/plugins/stubs/PromptBuilderView.tsx` returns both files (glow effect)
4. `npx tsc --noEmit` passes
</verification>

<success_criteria>
- CodeReviewBotView, DbInspectorView, ProviderSelector containers have stagger-children
- ReviewPanel findings list has stagger-children or animate-fade-in-up (depending on list size)
- ProviderSelector cards, EstimationHistory entries, MissionControl QuickStat cards have hover-lift
- ComparisonView table has alternating row shading and hover highlighting
- Both stub plugins have stagger-children, icon glow, hover-lift badge, entrance animation
- Stub Coming Soon badges use bg-warning-muted/text-warning
- TypeScript compilation succeeds
- No component logic changed -- visual/CSS classes only
</success_criteria>

<output>
After completion, create `.planning/phases/11-full-ui-ux-revamp/11-03-SUMMARY.md`
</output>
