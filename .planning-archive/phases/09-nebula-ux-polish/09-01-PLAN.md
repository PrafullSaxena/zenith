---
phase: 09-nebula-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/nebula.ts
  - src/renderer/src/types/electron.d.ts
  - src/main/nebula/database.ts
  - src/main/nebula/file-storage.ts
  - src/main/ipc-handlers.ts
  - src/preload/index.ts
  - src/preload/index.d.ts
  - src/renderer/src/stores/nebula-store.ts
  - package.json
autonomous: true
requirements:
  - NEBL-03

must_haves:
  truths:
    - "Pinned state persists across app restart (stored in SQLite)"
    - "Content preview text available for note list items"
    - "Audio files saved to disk alongside notes and loadable for playback"
    - "New Tiptap extensions installed and importable"
  artifacts:
    - path: "src/renderer/src/types/nebula.ts"
      provides: "Extended NoteListItem with pinned, hasDrawing, contentPreview, audioPath fields"
      contains: "pinned"
    - path: "src/main/nebula/database.ts"
      provides: "pinned column and content_preview column in notes table"
      contains: "pinned"
    - path: "src/main/ipc-handlers.ts"
      provides: "nebula:togglePin, nebula:saveAudio, nebula:loadAudio IPC handlers"
      contains: "togglePin"
  key_links:
    - from: "src/renderer/src/stores/nebula-store.ts"
      to: "window.api.nebula.togglePin"
      via: "togglePin store action"
      pattern: "togglePin"
    - from: "src/main/ipc-handlers.ts"
      to: "src/main/nebula/database.ts"
      via: "getNebulaInstances().db"
      pattern: "getNebulaInstances"
---

<objective>
Install new dependencies, extend Nebula data layer with pinned state, content preview, and audio file persistence for the UX polish phase.

Purpose: All UI plans (02, 03, 04) depend on these schema extensions, IPC handlers, and installed packages. This plan provides the shared data foundation.
Output: Extended types, SQLite schema with pinned/content_preview columns, audio save/load IPC, new npm packages installed.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-nebula-ux-polish/09-RESEARCH.md
@.planning/phases/08-nebula-plugin/08-03-SUMMARY.md
@src/renderer/src/types/nebula.ts
@src/renderer/src/types/electron.d.ts
@src/main/nebula/database.ts
@src/main/nebula/file-storage.ts
@src/main/ipc-handlers.ts
@src/preload/index.ts
@src/preload/index.d.ts
@src/renderer/src/stores/nebula-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and extend Nebula types and SQLite schema</name>
  <files>
    package.json
    src/renderer/src/types/nebula.ts
    src/renderer/src/types/electron.d.ts
    src/main/nebula/database.ts
    src/main/nebula/file-storage.ts
  </files>
  <action>
1. Install new dependencies:
   ```
   npm install @tiptap/extension-table @tiptap/extension-table-row @tiptap/extension-table-cell @tiptap/extension-table-header @tiptap/extension-image react-resizable-panels
   ```

2. Extend `src/renderer/src/types/nebula.ts`:
   - Add to NoteListItem interface: `pinned: boolean`, `hasDrawing: boolean`, `contentPreview: string | null`, `audioPath: string | null`
   - Add new type `NoteTag = { id: string; label: string; color: string }`
   - Add `tags: NoteTag[]` to NoteFile interface
   - Add `PanelLayout` type: `{ sidebarSize: number; editorSize: number; drawingCollapsed: boolean }`
   - Add `ToastMessage` type: `{ id: string; message: string; noteId?: string; type: 'info' | 'success' }`

3. Extend `src/main/nebula/database.ts`:
   - In the `initializeDatabase()` method (or constructor where tables are created), add migration logic: `ALTER TABLE notes ADD COLUMN pinned INTEGER DEFAULT 0` and `ALTER TABLE notes ADD COLUMN content_preview TEXT DEFAULT ''` — wrap each in try/catch to handle "duplicate column" error gracefully (column may already exist on re-run).
   - Add `togglePin(noteId: string, pinned: boolean)` method: `UPDATE notes SET pinned = ? WHERE id = ?`
   - Update `listNotes()` to include `pinned` and `content_preview` in the SELECT and return them. Order by `pinned DESC, updated_at DESC`.
   - Update `upsertNote()` to accept and store a `contentPreview` string parameter alongside existing fields.
   - Add `getContentPreview(content: unknown): string` private method that extracts first ~150 chars of plain text from Tiptap JSON (reuse the existing `extractPlainText` logic if available, or walk the JSON tree collecting text nodes).

4. Extend `src/main/nebula/file-storage.ts`:
   - Add `saveAudio(noteId: string, audioBuffer: Buffer): string` method that writes to `{storagePath}/audio/{noteId}.webm`, creates the `audio/` subdirectory if needed, returns the file path.
   - Add `loadAudio(noteId: string): Buffer | null` method that reads `{storagePath}/audio/{noteId}.webm`, returns null if file doesn't exist.
   - Add `deleteAudio(noteId: string): void` method that removes the audio file if it exists.

5. Update `src/renderer/src/types/electron.d.ts` nebula namespace:
   - Add `togglePin: (noteId: string, pinned: boolean) => Promise<void>`
   - Add `saveAudio: (noteId: string, audioBuffer: number[]) => Promise<{ audioPath: string }>`
   - Add `loadAudio: (noteId: string) => Promise<number[] | null>`
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check that @tiptap/extension-table and react-resizable-panels appear in package.json dependencies</manual>
  </verify>
  <done>New npm packages installed. NoteListItem has pinned/hasDrawing/contentPreview/audioPath fields. SQLite schema migrated with pinned and content_preview columns. Audio file storage methods exist. IPC type declarations updated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new IPC handlers and extend nebula store actions</name>
  <files>
    src/main/ipc-handlers.ts
    src/preload/index.ts
    src/preload/index.d.ts
    src/renderer/src/stores/nebula-store.ts
  </files>
  <action>
1. Add IPC handlers in `src/main/ipc-handlers.ts`:
   - `nebula:togglePin` — calls `db.togglePin(noteId, pinned)`, returns void
   - `nebula:saveAudio` — receives `(noteId: string, audioBuffer: number[])`, converts number[] to Buffer, calls `fileStorage.saveAudio(noteId, buffer)`, returns `{ audioPath }`
   - `nebula:loadAudio` — receives `(noteId: string)`, calls `fileStorage.loadAudio(noteId)`, converts Buffer to number[] via `Array.from(new Uint8Array(buffer))` (same pattern as transcription), returns number[] or null

2. Update `src/preload/index.ts` nebula namespace:
   - Add `togglePin: (noteId: string, pinned: boolean) => ipcRenderer.invoke('nebula:togglePin', noteId, pinned)`
   - Add `saveAudio: (noteId: string, audioBuffer: number[]) => ipcRenderer.invoke('nebula:saveAudio', noteId, audioBuffer)`
   - Add `loadAudio: (noteId: string) => ipcRenderer.invoke('nebula:loadAudio', noteId)`

3. Update `src/preload/index.d.ts` to match preload additions.

4. Extend `src/renderer/src/stores/nebula-store.ts`:
   - Add `togglePin: (noteId: string) => Promise<void>` action: reads current pinned state from notes array, flips it, calls `window.api.nebula.togglePin(noteId, newState)`, updates the note in local state, re-sorts notes (pinned first, then by updatedAt).
   - Add `toasts: ToastMessage[]` state field (initially empty).
   - Add `addToast: (toast: Omit<ToastMessage, 'id'>) => void` action that generates a unique id (e.g., `toast-${Date.now()}`), pushes to toasts array, and auto-removes after 5 seconds via setTimeout.
   - Add `removeToast: (id: string) => void` action.
   - Update `loadNotes` action to use the returned `pinned`, `contentPreview`, `hasDrawing` fields from IPC.
   - Ensure notes sorting in store is always: pinned first (by updatedAt desc), then unpinned (by updatedAt desc).
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify nebula-store.ts exports togglePin, addToast, removeToast actions</manual>
  </verify>
  <done>togglePin/saveAudio/loadAudio IPC fully wired from main through preload to renderer. Nebula store has togglePin action with optimistic sort, toast state management, and notes sorted pinned-first.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npm ls @tiptap/extension-table react-resizable-panels` shows both packages installed
- nebula-store.ts compiles with new togglePin, addToast, removeToast actions
- electron.d.ts has togglePin, saveAudio, loadAudio in nebula namespace
</verification>

<success_criteria>
- All 6 new npm packages installed and importable
- SQLite schema extended with pinned + content_preview columns
- Audio file save/load IPC fully wired end-to-end
- Pin toggle IPC fully wired with store action
- Toast state management in store
- TypeScript compilation passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/09-nebula-ux-polish/09-01-SUMMARY.md`
</output>
