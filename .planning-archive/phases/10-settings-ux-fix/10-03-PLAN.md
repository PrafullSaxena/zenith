---
phase: 10-settings-ux-fix
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/renderer/src/components/settings/ConnectionListEditor.tsx
  - src/renderer/src/components/settings/RepoListEditor.tsx
  - src/renderer/src/components/settings/SettingsLayout.tsx
  - src/renderer/src/components/settings/GeneralSettings.tsx
  - src/renderer/src/components/settings/PluginSettings.tsx
autonomous: true
requirements:
  - SETT-UX-03

must_haves:
  truths:
    - "ConnectionListEditor and RepoListEditor tables have rounded-lg border-border/50 containers"
    - "ConnectionListEditor and RepoListEditor buttons use padding-based sizing (no h-[38px])"
    - "Table rows in ConnectionListEditor and RepoListEditor have hover states"
    - "Empty states in both editors use dashed border with py-8 and icon"
    - "Delete buttons in both editors use hover:bg-red-500/10 rounded-lg"
    - "SettingsLayout sidebar buttons use rounded-lg instead of rounded-r-lg"
    - "GeneralSettings description uses text-xs"
    - "PluginSettings default agent select uses standardized classes"
  artifacts:
    - path: "src/renderer/src/components/settings/ConnectionListEditor.tsx"
      provides: "Connection list with unified table container, buttons, hover, empty state"
      contains: "rounded-lg border border-border/50"
    - path: "src/renderer/src/components/settings/RepoListEditor.tsx"
      provides: "Repo list with unified table container, buttons, hover, empty state"
      contains: "rounded-lg border border-border/50"
    - path: "src/renderer/src/components/settings/SettingsLayout.tsx"
      provides: "Settings sidebar with rounded-lg buttons"
      contains: "rounded-lg"
    - path: "src/renderer/src/components/settings/GeneralSettings.tsx"
      provides: "General settings header section"
      contains: "text-xs text-text-secondary"
    - path: "src/renderer/src/components/settings/PluginSettings.tsx"
      provides: "Plugin settings with unified select styling"
      contains: "rounded-lg border border-border/50"
  key_links:
    - from: "ConnectionListEditor.tsx"
      to: "SettingsField.tsx"
      via: "rendered by SettingsField for connection-list type"
      pattern: "rounded-lg"
    - from: "RepoListEditor.tsx"
      to: "SettingsField.tsx"
      via: "rendered by SettingsField for repo-list type"
      pattern: "rounded-lg"
---

<objective>
Unify remaining settings files (ConnectionListEditor, RepoListEditor, SettingsLayout, GeneralSettings, PluginSettings) with standardized design tokens for tables, buttons, empty states, sidebar, and section headers.

Purpose: These files contain table containers, add buttons, empty states, delete buttons, and sidebar navigation that all use slightly different styles. This plan brings them in line with the tokens established in Plan 01.

Output: Five updated files with unified Tailwind classes. No logic changes.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-settings-ux-fix/10-RESEARCH.md
@src/renderer/src/components/settings/ConnectionListEditor.tsx
@src/renderer/src/components/settings/RepoListEditor.tsx
@src/renderer/src/components/settings/SettingsLayout.tsx
@src/renderer/src/components/settings/GeneralSettings.tsx
@src/renderer/src/components/settings/PluginSettings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify ConnectionListEditor and RepoListEditor tables, buttons, empty states, and inputs</name>
  <files>src/renderer/src/components/settings/ConnectionListEditor.tsx, src/renderer/src/components/settings/RepoListEditor.tsx</files>
  <action>
This is a VISUAL-ONLY change. Modify Tailwind CSS classes only. Do NOT change any logic, state, props, imports, or component structure.

### ConnectionListEditor.tsx

**1. Update `inputClass` constant (line 31-32):**

OLD:
```
'w-full rounded-md border border-border bg-surface-elevated px-3 py-2 text-sm text-text-primary placeholder:text-text-secondary/50 focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition'
```

NEW:
```
'w-full rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary placeholder:text-text-secondary/50 focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30 transition'
```

Same as SettingsField inputClass.

**2. Update `selectClass` constant (line 34-35):**

OLD:
```
'w-full rounded-md border border-border bg-surface-elevated px-3 py-2 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition appearance-none'
```

NEW:
```
'w-full rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30 transition appearance-none'
```

Same as SettingsField selectClass.

**3. Update table container (line 157):**

OLD: `"rounded-md border border-border overflow-hidden"`
NEW: `"rounded-lg border border-border/50 overflow-hidden"`

Changes: `rounded-md` -> `rounded-lg`, `border-border` -> `border-border/50`

**4. Add hover state to table rows (line 180):**

OLD: `"border-b border-border last:border-b-0"`
NEW: `"border-b border-border/30 last:border-b-0 transition-colors hover:bg-surface-elevated/30"`

Changes: `border-border` -> `border-border/30`, added `transition-colors hover:bg-surface-elevated/30`

**5. Update delete button in table rows (line 192):**

OLD: `"text-text-secondary transition hover:text-red-400"`
NEW: `"rounded-lg p-1 text-text-secondary transition-colors hover:text-red-400 hover:bg-red-500/10"`

Changes: added `rounded-lg p-1`, `transition` -> `transition-colors`, added `hover:bg-red-500/10`

**6. Update empty state (line 207):**

OLD: `"rounded-md border border-dashed border-border px-4 py-3 text-center text-xs text-text-secondary"`
NEW: `"rounded-lg border border-dashed border-border/50 px-4 py-8 text-center text-xs text-text-secondary"`

Changes: `rounded-md` -> `rounded-lg`, `border-border` -> `border-border/50`, `py-3` -> `py-8`

Also update the empty state content. Currently it's just text. Add an icon before the text by importing `Database` from lucide-react (add to existing import line). Replace the single text with structured content:

OLD content inside the empty div:
```
No connections configured. Add one below.
```

NEW content inside the empty div:
```jsx
<Database size={24} className="mx-auto mb-2 text-text-secondary/30" />
<p className="text-sm text-text-secondary">No connections configured</p>
<p className="mt-1 text-xs text-text-secondary">Add one below to get started.</p>
```

Add `Database` to the lucide-react import on line 2.

**7. Update form container (line 214):**

OLD: `"rounded-md border border-border bg-surface-elevated/30 p-4 space-y-3"`
NEW: `"rounded-xl border border-border/50 bg-surface-elevated/30 p-4 space-y-3"`

Changes: `rounded-md` -> `rounded-xl`, `border-border` -> `border-border/50`

**8. Update Test button (line 328):**

OLD: `"flex items-center gap-1.5 rounded-md border border-border bg-surface px-3 py-1.5 text-xs font-medium text-text-secondary hover:text-text-primary hover:bg-surface-elevated transition disabled:opacity-50"`
NEW: `"flex items-center gap-1.5 rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-xs font-medium text-text-secondary hover:text-text-primary hover:bg-surface-elevated transition disabled:opacity-50"`

Changes: `rounded-md` -> `rounded-lg`, `border-border` -> `border-border/50`

**9. Update Add button (line 341):**

OLD: `"flex items-center gap-1.5 rounded-md bg-accent px-3 py-1.5 text-xs font-medium text-background hover:bg-accent/90 transition disabled:opacity-50"`
NEW: `"flex items-center gap-1.5 rounded-lg bg-accent px-3 py-1.5 text-xs font-medium text-background hover:bg-accent/90 transition disabled:opacity-50"`

Changes: `rounded-md` -> `rounded-lg`

**10. Update Cancel button (line 356):**

OLD: `"rounded-md px-3 py-1.5 text-xs font-medium text-text-secondary hover:text-text-primary transition"`
NEW: `"rounded-lg px-3 py-1.5 text-xs font-medium text-text-secondary hover:text-text-primary transition"`

Changes: `rounded-md` -> `rounded-lg`

**11. Update "Add Connection" button (line 369):**

OLD: `"flex h-[38px] items-center gap-1 rounded-md bg-accent px-3 text-sm font-medium text-background transition hover:bg-accent/90"`
NEW: `"flex items-center gap-1.5 rounded-lg bg-accent px-3 py-1.5 text-sm font-medium text-background transition hover:bg-accent/90"`

Changes: remove `h-[38px]`, `rounded-md` -> `rounded-lg`, `gap-1` -> `gap-1.5`, add `py-1.5`

### RepoListEditor.tsx

**12. Update `inputClass` constant (line 15-16):**

OLD:
```
'w-full rounded-md border border-border bg-surface-elevated px-3 py-2 text-sm text-text-primary placeholder:text-text-secondary/50 focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition'
```

NEW:
```
'w-full rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary placeholder:text-text-secondary/50 focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30 transition'
```

Same as SettingsField inputClass.

**13. Update table container (line 69):**

OLD: `"rounded-md border border-border overflow-hidden"`
NEW: `"rounded-lg border border-border/50 overflow-hidden"`

Changes: `rounded-md` -> `rounded-lg`, `border-border` -> `border-border/50`

**14. Add hover state to table rows (line 86):**

OLD: `"border-b border-border last:border-b-0"`
NEW: `"border-b border-border/30 last:border-b-0 transition-colors hover:bg-surface-elevated/30"`

Changes: `border-border` -> `border-border/30`, added `transition-colors hover:bg-surface-elevated/30`

**15. Update delete button in table rows (line 92):**

OLD: `"text-text-secondary transition hover:text-red-400"`
NEW: `"rounded-lg p-1 text-text-secondary transition-colors hover:text-red-400 hover:bg-red-500/10"`

Changes: added `rounded-lg p-1`, `transition` -> `transition-colors`, added `hover:bg-red-500/10`

**16. Update empty state (line 109):**

OLD: `"rounded-md border border-dashed border-border px-4 py-3 text-center text-xs text-text-secondary"`
NEW: `"rounded-lg border border-dashed border-border/50 px-4 py-8 text-center text-xs text-text-secondary"`

Changes: `rounded-md` -> `rounded-lg`, `border-border` -> `border-border/50`, `py-3` -> `py-8`

Also update the empty state content. Add `GitBranch` to the lucide-react import (line 2). Replace the single text with structured content:

OLD content inside the empty div:
```
No repositories configured. Add one below.
```

NEW content inside the empty div:
```jsx
<GitBranch size={24} className="mx-auto mb-2 text-text-secondary/30" />
<p className="text-sm text-text-secondary">No repositories configured</p>
<p className="mt-1 text-xs text-text-secondary">Add one below to get started.</p>
```

**17. Update "Add" button (line 149):**

OLD: `"flex h-[38px] items-center gap-1 rounded-md bg-accent px-3 text-sm font-medium text-background transition hover:bg-accent/90"`
NEW: `"flex items-center gap-1.5 rounded-lg bg-accent px-3 py-1.5 text-sm font-medium text-background transition hover:bg-accent/90"`

Changes: remove `h-[38px]`, `rounded-md` -> `rounded-lg`, `gap-1` -> `gap-1.5`, add `py-1.5`
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Open Settings > DbInspector plugin. Connection list should have rounded-lg border container. Table rows highlight on hover. Empty state should have Database icon with dashed border. "Add Connection" button should be padding-based (no fixed height). Open Settings > CodeReviewBot. Repo list should have same styling with GitBranch icon empty state.</manual>
  </verify>
  <done>ConnectionListEditor and RepoListEditor have unified table containers (rounded-lg border-border/50), hover rows, dashed empty states with icons, rounded-lg buttons with padding-based sizing, and unified inputClass/selectClass.</done>
</task>

<task type="auto">
  <name>Task 2: Polish SettingsLayout sidebar, GeneralSettings header, and PluginSettings select</name>
  <files>src/renderer/src/components/settings/SettingsLayout.tsx, src/renderer/src/components/settings/GeneralSettings.tsx, src/renderer/src/components/settings/PluginSettings.tsx</files>
  <action>
This is a VISUAL-ONLY change. Modify Tailwind CSS classes only. Do NOT change any logic, state, props, imports, or component structure.

### SettingsLayout.tsx

**1. Update sidebar category buttons — both instances (lines 66 and 90):**

Both category button elements have the same class pattern. Update BOTH:

OLD (each):
```
`w-full rounded-r-lg px-3 py-2 text-left text-sm transition-colors ${
  activeCategory === cat.id
    ? 'border-l-2 border-accent bg-surface-elevated text-text-primary font-medium'
    : 'text-text-secondary hover:text-text-primary hover:bg-surface-elevated/50'
}`
```

NEW (each):
```
`w-full rounded-lg px-3 py-2 text-left text-sm transition-colors ${
  activeCategory === cat.id
    ? 'border-l-2 border-accent bg-surface-elevated text-text-primary font-medium'
    : 'text-text-secondary hover:text-text-primary hover:bg-surface-elevated/50'
}`
```

Changes: `rounded-r-lg` -> `rounded-lg`

### GeneralSettings.tsx

**2. Add description below heading (line 70):**

Currently line 70 is:
```jsx
<h2 className="mb-6 text-lg font-semibold text-text-primary">General</h2>
```

Change `mb-6` to `mb-1` on the h2, and add a description paragraph after it:

NEW:
```jsx
<h2 className="mb-1 text-lg font-semibold text-text-primary">General</h2>
<p className="mb-6 text-xs text-text-secondary">Application-wide preferences and defaults.</p>
```

### PluginSettings.tsx

**3. Update the standalone select for Default AI Agent (line 91):**

OLD:
```
"w-full rounded-md border border-border bg-surface-elevated px-3 py-2 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition appearance-none"
```

NEW:
```
"w-full rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30 transition appearance-none"
```

Same changes as SettingsField selectClass.

**4. Update plugin description (line 73):**

OLD: `"mb-6 text-sm text-text-secondary"`
NEW: `"mb-6 text-xs text-text-secondary"`

Changes: `text-sm` -> `text-xs`
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Open Settings. Sidebar buttons should use rounded-lg (not just right side rounded). General section should show "Application-wide preferences and defaults." subtitle. Navigate to any plugin settings -- Default AI Agent select should have rounded-lg and subtle focus ring.</manual>
  </verify>
  <done>SettingsLayout sidebar uses rounded-lg buttons. GeneralSettings has description subtitle. PluginSettings default agent select uses standardized rounded-lg/border-border/50/focus:ring-1 classes. Plugin description uses text-xs.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors across all 5 files
- All table containers use rounded-lg border-border/50
- All table rows have hover:bg-surface-elevated/30
- All buttons use rounded-lg (no more rounded-md)
- No h-[38px] fixed heights remain -- all buttons use padding
- Empty states have icons, dashed borders, py-8
- Sidebar buttons are fully rounded-lg
- Section descriptions are text-xs
- Default AI Agent select matches SettingsField selectClass
</verification>

<success_criteria>
- ConnectionListEditor: rounded-lg table, hover rows, Database icon empty state, padding-based buttons
- RepoListEditor: rounded-lg table, hover rows, GitBranch icon empty state, padding-based buttons
- SettingsLayout: rounded-lg sidebar buttons (not rounded-r-lg)
- GeneralSettings: text-xs description subtitle added
- PluginSettings: unified select classes, text-xs description
- No logic changes -- only Tailwind class modifications and empty state content updates
</success_criteria>

<output>
After completion, create `.planning/phases/10-settings-ux-fix/10-03-SUMMARY.md`
</output>
