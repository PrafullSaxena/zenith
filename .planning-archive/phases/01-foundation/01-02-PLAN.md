---
phase: 01-foundation
plan: "02"
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/components/Sidebar.tsx
  - src/renderer/src/components/AppLayout.tsx
  - src/renderer/src/components/ErrorBoundary.tsx
  - src/renderer/src/main.tsx
  - src/main/index.ts
autonomous: true
requirements:
  - SHELL-02
  - SHELL-03
  - SHELL-05
  - SHELL-06
  - SHELL-07
  - SHELL-09

must_haves:
  truths:
    - "App shows a ~56px narrow sidebar with plugin icons stacked at top and settings gear at bottom"
    - "Clicking a sidebar icon switches the main content area to that plugin's stub view"
    - "Active sidebar icon has a left accent border (neon cyan)"
    - "Hovering a sidebar icon shows a tooltip with the plugin name"
    - "Window size and position persist across app restarts via electron-win-state"
    - "Sidebar and content area are responsive — sidebar stays fixed width, content fills remaining space"
    - "An unhandled error in a plugin view shows recovery UI, not a white screen"
    - "The app uses dark-only theme with neon cyan accents throughout"
  artifacts:
    - path: "src/renderer/src/components/Sidebar.tsx"
      provides: "Icon rail navigation driven by PLUGINS array"
      contains: "PLUGINS"
    - path: "src/renderer/src/components/AppLayout.tsx"
      provides: "Sidebar + content area flex layout with drag region"
      contains: "AppLayout"
    - path: "src/renderer/src/components/ErrorBoundary.tsx"
      provides: "React error boundary with recovery UI"
      contains: "ErrorBoundary"
    - path: "src/renderer/src/App.tsx"
      provides: "React Router setup with plugin routes and Suspense"
      contains: "BrowserRouter"
    - path: "src/main/index.ts"
      provides: "electron-win-state integration for window persistence"
      contains: "WinState"
  key_links:
    - from: "src/renderer/src/components/Sidebar.tsx"
      to: "src/renderer/src/plugins/registry.ts"
      via: "Sidebar maps over PLUGINS to render icon buttons with NavLink"
      pattern: "PLUGINS\\.map"
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/plugins/registry.ts"
      via: "Router maps PLUGINS to Route elements using plugin.route and plugin.component"
      pattern: "PLUGINS\\.map.*Route"
    - from: "src/renderer/src/components/AppLayout.tsx"
      to: "src/renderer/src/components/Sidebar.tsx"
      via: "AppLayout renders Sidebar in flex row with Outlet for content"
      pattern: "Sidebar.*Outlet"
---

<objective>
Build the app shell: narrow sidebar with icon rail navigation driven by PLUGINS array, React Router for view switching, responsive layout with drag region for custom titlebar, window state persistence via electron-win-state, and error boundary for crash recovery.

Purpose: This is the visual skeleton users interact with. Without the sidebar and routing, plugins have nowhere to render. Window state persistence (SHELL-05) and error boundaries (SHELL-09) are foundational UX requirements.
Output: Working sidebar navigation that switches between plugin stub views, window remembers its size/position, dark theme applied, and errors caught gracefully.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-03-PLAN.md
@.planning/phases/01-foundation/01-CONTEXT.md
@src/renderer/src/assets/main.css
@src/renderer/src/types/electron.d.ts
@src/main/index.ts
@src/renderer/src/plugins/registry.ts
@src/renderer/src/types/plugin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app shell layout with sidebar navigation and React Router</name>
  <files>
    src/renderer/src/components/Sidebar.tsx
    src/renderer/src/components/AppLayout.tsx
    src/renderer/src/components/ErrorBoundary.tsx
    src/renderer/src/App.tsx
    src/renderer/src/main.tsx
  </files>
  <action>
**ErrorBoundary** (`src/renderer/src/components/ErrorBoundary.tsx`):
- Class component implementing `componentDidCatch` and `getDerivedStateFromError`
- When error caught: render a recovery UI centered in the content area with:
  - Error icon (lucide-react `AlertTriangle`)
  - "Something went wrong" heading
  - Error message text (from error.message)
  - "Try Again" button that calls `this.setState({ hasError: false })` to retry
  - Use Tailwind classes: bg-surface, text-text-primary, border-border, rounded
- When no error: render `this.props.children`
- Accept `children: React.ReactNode` prop

**Sidebar** (`src/renderer/src/components/Sidebar.tsx`):
- Width: fixed w-14 (56px) per user decision
- Background: bg-surface (oklch 14%)
- Border: border-r border-border (oklch 20%)
- Layout: flex flex-col h-full — plugin icons at top (flex-1), settings gear at bottom
- **Plugin icons section:** Map over `PLUGINS` from registry. For each plugin:
  - Render a `NavLink` (from react-router-dom) to `plugin.route`
  - Icon: dynamically render lucide-react icon by name. Use a simple icon map object mapping icon name strings to imported lucide components (GitPullRequest, Database, Wrench, MessageSquare, Settings). Do NOT use dynamic import — import all needed icons statically and map by string.
  - Size: 20px icon inside a 40px hit area (w-10 h-10 flex items-center justify-center)
  - Active state: left-2 border-l-2 border-accent (neon cyan) when NavLink isActive. Active icon color: text-accent. Inactive: text-text-secondary.
  - Hover: bg-surface-elevated (oklch 18%) rounded transition
  - **Tooltip:** Use a simple CSS tooltip approach — relative positioning with a hidden span that appears on group-hover. Tooltip shows `plugin.name`. Position: to the right of the icon (left-full ml-2), bg-surface-elevated text-text-primary text-xs rounded px-2 py-1 whitespace-nowrap. Use `group` and `group-hover:opacity-100 opacity-0` pattern with transition.
- **Settings gear:** At bottom of sidebar, same icon button pattern. NavLink to `/settings`. Icon: Settings from lucide-react. Same active/hover styling as plugin icons.
- **Drag region:** Top section of sidebar (first ~32px) should be `-webkit-app-region: drag` for macOS traffic light alignment. The icon buttons themselves must be `-webkit-app-region: no-drag`.

**AppLayout** (`src/renderer/src/components/AppLayout.tsx`):
- Flex row: `flex h-screen w-screen overflow-hidden bg-background`
- Left: `<Sidebar />`
- Right: flex-1 overflow container wrapping `<Outlet />` (from react-router-dom)
- Top of content area: a thin drag region div (~32px height) with `-webkit-app-region: drag` for the custom titlebar. This is transparent/invisible — just for dragging.
- Content area below drag region: flex-1 overflow-y-auto p-4

**App.tsx** — Complete rewrite:
- Install react-router-dom: **IMPORTANT** — check if react-router-dom is already in package.json. If NOT, run `npm install react-router-dom` first.
- Wrap in `<BrowserRouter>` (use `<HashRouter>` instead — Electron file:// protocol requires hash router)
- Define `<Routes>`:
  - `<Route element={<AppLayout />}>` as parent layout route
    - Map PLUGINS to: `<Route path={plugin.route} element={<Suspense fallback={<LoadingFallback />}><ErrorBoundary><plugin.component /></ErrorBoundary></Suspense>} />`
    - `<Route path="/settings" element={<div className="flex h-full items-center justify-center text-text-secondary">Settings</div>} />` — placeholder, will be replaced by 01-04
    - `<Route path="/" element={<Navigate to={PLUGINS[0].route} replace />} />` — default redirect to first plugin
    - `<Route path="*" element={<Navigate to={PLUGINS[0].route} replace />} />` — catch-all
  - Create a simple `LoadingFallback` component: centered spinner or "Loading..." text with text-text-secondary

**main.tsx** — Update to import `App` (should already do this, just verify no changes needed).

Use `import { HashRouter, Routes, Route, NavLink, Navigate, Outlet } from 'react-router-dom'`. Electron's file:// protocol does not work with BrowserRouter history API — HashRouter is mandatory.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npm run typecheck:web 2>&1 | tail -5</automated>
    <manual>Run `npm run dev`, verify sidebar appears with 4 plugin icons + settings gear. Click icons to switch views.</manual>
  </verify>
  <done>Sidebar renders with 4 plugin icons (correct order: CodeReviewBot, DbInspector, AstroPatch, PromptBuilder) and settings gear at bottom. Clicking icons navigates to stub views via HashRouter. Active icon shows neon cyan left border. Tooltips appear on hover. ErrorBoundary catches render errors and shows recovery UI. App defaults to first plugin route on launch. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate electron-win-state for window persistence and apply dark theme polish</name>
  <files>
    src/main/index.ts
    src/renderer/src/assets/main.css
  </files>
  <action>
**electron-win-state** (`src/main/index.ts`):
- Import `WinState` from `electron-win-state` (already installed per package.json)
- In `createWindow()`, BEFORE creating BrowserWindow:
  ```ts
  const winState = new WinState({
    defaultWidth: 1280,
    defaultHeight: 800,
  })
  ```
- Replace hardcoded `width: 1280, height: 800` with `...winState.winOptions` spread (provides x, y, width, height from previous session)
- After creating the BrowserWindow, call `winState.manage(mainWindow)` to track position/size changes
- Keep `minWidth: 900, minHeight: 600` — these are NOT managed by winState

**Dark theme CSS polish** (`src/renderer/src/assets/main.css`):
- Add scrollbar styling for dark theme:
  ```css
  ::-webkit-scrollbar { width: 8px; height: 8px; }
  ::-webkit-scrollbar-track { background: var(--color-surface); }
  ::-webkit-scrollbar-thumb { background: var(--color-border); border-radius: 4px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--color-text-secondary); }
  ```
- Add transition utilities for sidebar hover states (already available via Tailwind, but ensure smooth 150ms transitions)
- Add `-webkit-app-region` utility classes if needed:
  ```css
  .drag-region { -webkit-app-region: drag; }
  .no-drag { -webkit-app-region: no-drag; }
  ```
- Ensure the sidebar's fixed width doesn't collapse — `flex-shrink-0` on sidebar container

Note: SHELL-07 (dark-only theme) is satisfied by the existing @theme tokens from 01-01 plus the sidebar/layout components using those tokens consistently. No light mode, no toggle. Just verify all new components use the established color tokens.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npm run build 2>&1 | tail -5</automated>
    <manual>Run `npm run dev`, resize window, close app, reopen — window should restore to previous size/position. Scrollbars should be dark-styled.</manual>
  </verify>
  <done>Window state (size, position) persists across sessions. Dark scrollbars render with surface/border colors. Sidebar is flex-shrink-0 and never collapses below 56px. All components use established @theme color tokens. `npm run build` passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` and `npm run typecheck:web` pass with zero errors
2. App launches with sidebar (56px) showing 4 plugin icons + settings gear
3. Clicking each icon switches content area to corresponding stub view
4. Active icon shows left cyan border accent
5. Window remembers size/position after restart
6. Throwing an error in a stub view shows ErrorBoundary recovery UI (not white screen)
7. Sidebar tooltips appear on hover
8. All colors use @theme dark tokens — no hardcoded colors except where inline style existed already
</verification>

<success_criteria>
- Sidebar-driven navigation works for all 4 plugins + settings
- Window state persistence verified across restart
- ErrorBoundary catches and recovers from render errors
- Dark-only theme consistently applied — no light mode, no toggles
- Responsive: sidebar fixed, content fills remaining space
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
