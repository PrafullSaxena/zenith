---
phase: 02-mission-control-activity-log
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/renderer/src/components/Sidebar.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/components/activity/ActivityLog.tsx
  - src/renderer/src/components/settings/GeneralSettings.tsx
  - src/main/settings-store.ts
autonomous: true
requirements:
  - DASH-01
  - ACTV-03

must_haves:
  truths:
    - "Mission Control is the default landing view when the app launches"
    - "Sidebar has a Home/LayoutDashboard icon at the top that navigates to /dashboard"
    - "Clicking the sidebar dashboard icon shows the Mission Control view"
    - "A dedicated /activity route shows the full activity log with filtering"
    - "Activity log is accessible from both the dashboard 'View All' link and the sidebar"
    - "GeneralSettings Default View dropdown includes 'Mission Control' option"
    - "Default view setting defaults to 'dashboard' (not 'code-review-bot')"
  artifacts:
    - path: "src/renderer/src/components/activity/ActivityLog.tsx"
      provides: "Full dedicated activity log view with plugin and status filters"
      exports: ["default"]
    - path: "src/renderer/src/App.tsx"
      provides: "Updated routing with /dashboard (default), /activity routes"
      contains: "dashboard"
    - path: "src/renderer/src/components/Sidebar.tsx"
      provides: "Updated sidebar with Dashboard and Activity icons above plugin icons"
      contains: "LayoutDashboard"
  key_links:
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/components/dashboard/MissionControl.tsx"
      via: "React.lazy import and Route element for /dashboard"
      pattern: "import.*MissionControl"
    - from: "src/renderer/src/App.tsx"
      to: "src/renderer/src/components/activity/ActivityLog.tsx"
      via: "React.lazy import and Route element for /activity"
      pattern: "import.*ActivityLog"
    - from: "src/renderer/src/components/Sidebar.tsx"
      to: "/dashboard"
      via: "SidebarIcon NavLink to='/dashboard'"
      pattern: "to=./dashboard"
    - from: "src/renderer/src/App.tsx"
      to: "/dashboard"
      via: "Navigate default redirect to /dashboard"
      pattern: "Navigate to=./dashboard"
---

<objective>
Wire Mission Control as the default landing view, add dashboard and activity sidebar icons, create the dedicated activity log view, and update routing and settings to make everything navigable.

Purpose: Completes DASH-01 (Mission Control as default landing) and ACTV-03 (activity viewable from dashboard AND dedicated section). This is the final integration step that connects the data layer (Plan 01) and UI components (Plan 02) into the app shell.

Output: Updated `App.tsx` (routing), `Sidebar.tsx` (icons), new `ActivityLog.tsx` view, updated `GeneralSettings.tsx` and `settings-store.ts` defaults.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mission-control-activity-log/02-RESEARCH.md
@.planning/phases/02-mission-control-activity-log/02-01-SUMMARY.md
@.planning/phases/02-mission-control-activity-log/02-02-SUMMARY.md

@src/renderer/src/App.tsx
@src/renderer/src/components/Sidebar.tsx
@src/renderer/src/components/settings/GeneralSettings.tsx
@src/main/settings-store.ts
@src/renderer/src/stores/activity-store.ts
@src/renderer/src/components/dashboard/MissionControl.tsx
@src/renderer/src/components/dashboard/ActivityFeed.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dedicated ActivityLog view and update sidebar with dashboard/activity icons</name>
  <files>
    src/renderer/src/components/activity/ActivityLog.tsx
    src/renderer/src/components/Sidebar.tsx
  </files>
  <action>
**ActivityLog view** (`src/renderer/src/components/activity/ActivityLog.tsx`):

Create the full dedicated activity log view for ACTV-03:
- Import `useEffect, useState` from React
- Import `useActivityStore` from `'../../stores/activity-store'`
- Import `ActivityFeed` from `'../dashboard/ActivityFeed'` (reuse the same component)
- Import `PLUGINS` from `'../../plugins/registry'`
- Import `type { ActivityStatus }` from `'../../types/activity'`

Features:
1. **Filter bar** at top with:
   - Plugin filter: `<select>` dropdown with "All Plugins" + each plugin from PLUGINS array. Filter value stored in `useState<string>('all')`.
   - Status filter: `<select>` dropdown with "All Statuses", "Success", "Failure", "Pending". Filter value stored in `useState<ActivityStatus | 'all'>('all')`.
   - Style selects with: `rounded border border-border bg-surface-elevated px-3 py-1.5 text-sm text-text-primary focus:border-accent focus:outline-none`

2. **Filtered entries**: Apply plugin and status filters to entries from the store. Use `useMemo` or inline filter.

3. **Activity list**: Render `<ActivityFeed entries={filteredEntries} />` (no showViewAll, no truncation -- show all entries).

4. **Header**: "Activity Log" title with total count badge.

5. **Empty state**: "No activity entries match your filters" if filtered list is empty.

6. **Clear button**: Small "Clear All" button that calls `clearEntries()` from the store. Style: `text-xs text-text-secondary hover:text-red-400 transition`.

7. Default export the component (for React.lazy in App.tsx).

8. Call `loadEntries()` in useEffect on mount.

**Sidebar update** (`src/renderer/src/components/Sidebar.tsx`):

Update sidebar to add dashboard and activity icons above plugin icons:

1. Add imports: `LayoutDashboard, Activity` from `'lucide-react'`
2. Add to ICON_MAP: `LayoutDashboard, Activity`
3. Restructure the nav section to have:
   - Dashboard icon at very top: `<SidebarIcon iconName="LayoutDashboard" label="Mission Control" to="/dashboard" />`
   - Activity icon below dashboard: `<SidebarIcon iconName="Activity" label="Activity Log" to="/activity" />`
   - A subtle visual separator: `<div className="mx-auto my-1 h-px w-6 bg-border" />` between activity icon and plugin icons
   - Then the existing `PLUGINS.map(...)` loop for plugin icons

This mirrors the research recommendation: dashboard icon at top, separator, then plugins, then settings at bottom.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify Sidebar shows Dashboard + Activity icons above plugins with separator. Verify ActivityLog view has filter dropdowns and renders full entry list.</manual>
  </verify>
  <done>Sidebar has LayoutDashboard and Activity icons at top with a divider before plugin icons. Dedicated ActivityLog view exists with plugin and status filter dropdowns, clear button, and full entry list display.</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx routing and GeneralSettings default view</name>
  <files>
    src/renderer/src/App.tsx
    src/renderer/src/components/settings/GeneralSettings.tsx
    src/main/settings-store.ts
  </files>
  <action>
**App.tsx routing update** (`src/renderer/src/App.tsx`):

1. Add lazy imports at top (after existing imports):
   ```
   const MissionControl = React.lazy(() => import('./components/dashboard/MissionControl'))
   const ActivityLog = React.lazy(() => import('./components/activity/ActivityLog'))
   ```
   Import `React` (already used for JSX) for `React.lazy`.

2. Add two new Route elements BEFORE the catch-all routes, inside the `<Route element={<AppLayout />}>`:
   ```
   <Route path="/dashboard" element={
     <Suspense fallback={<LoadingFallback />}>
       <ErrorBoundary><MissionControl /></ErrorBoundary>
     </Suspense>
   } />
   <Route path="/activity" element={
     <Suspense fallback={<LoadingFallback />}>
       <ErrorBoundary><ActivityLog /></ErrorBoundary>
     </Suspense>
   } />
   ```
   Place these AFTER the plugin routes but BEFORE the settings route (or after -- order doesn't matter since paths are distinct, but logically group with other app-level routes near settings).

3. Change default redirect from `PLUGINS[0].route` to `'/dashboard'`:
   - `<Route path="/" element={<Navigate to="/dashboard" replace />} />`
   - `<Route path="*" element={<Navigate to="/dashboard" replace />} />`

4. Ensure `import React` is present at top (needed for `React.lazy`). It may already be implicit with JSX transform, but `React.lazy` needs the explicit import.

**GeneralSettings update** (`src/renderer/src/components/settings/GeneralSettings.tsx`):

Update the Default View dropdown to include Mission Control:

1. Change `defaultViewField.options` from just `PLUGINS.map(...)` to:
   ```
   options: [
     { label: 'Mission Control', value: 'dashboard' },
     ...PLUGINS.map((p) => ({ label: p.name, value: p.id }))
   ]
   ```

2. Change `defaultViewField.defaultValue` from `'code-review-bot'` to `'dashboard'`.

**Main process settings defaults** (`src/main/settings-store.ts`):

Update the DEFAULTS object:
- Change `general.defaultView` from `'code-review-bot'` to `'dashboard'`

This ensures new installs default to Mission Control as the landing view (DASH-01).

**IMPORTANT:** The catch-all route `path="*"` must redirect to `/dashboard`, NOT to `PLUGINS[0].route`. This ensures DASH-01 is satisfied: Mission Control is ALWAYS the default landing view. The settings `general.defaultView` dropdown is for future use when the app reads that setting to determine the initial redirect -- for now the hardcoded `/dashboard` redirect satisfies the requirement.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Launch the app -- it should land on Mission Control. Click sidebar icons to verify navigation works. Check that the GeneralSettings Default View dropdown shows "Mission Control" as first option.</manual>
  </verify>
  <done>App launches to /dashboard (Mission Control) by default. /dashboard and /activity routes are registered with Suspense + ErrorBoundary wrapping. GeneralSettings Default View includes "Mission Control" option. Main process defaults set defaultView to 'dashboard'. Back button from plugin returns to dashboard (no redirect loops).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project src/renderer/tsconfig.json` passes with zero errors
2. App default route redirects to `/dashboard` (not `PLUGINS[0].route`)
3. Sidebar shows: LayoutDashboard icon, Activity icon, divider, then plugin icons, then settings
4. Clicking sidebar LayoutDashboard icon navigates to Mission Control
5. Clicking sidebar Activity icon navigates to dedicated activity log
6. Dashboard "View All Activity" link navigates to `/activity` route
7. Dedicated activity log has plugin and status filter dropdowns
8. GeneralSettings Default View dropdown includes "Mission Control" as first option
9. `src/main/settings-store.ts` DEFAULTS has `general.defaultView: 'dashboard'`
10. No redirect loops when navigating back from plugins to dashboard
</verification>

<success_criteria>
- Mission Control is the default landing view (DASH-01)
- Activity log is viewable from BOTH dashboard feed (via "View All") and dedicated /activity route (ACTV-03)
- Sidebar navigation correctly routes to /dashboard, /activity, all plugin routes, and /settings
- Sidebar has visual separator between app-level icons (dashboard, activity) and plugin icons
- TypeScript compilation succeeds with no errors
- All routing uses Suspense + ErrorBoundary wrapping (consistent with existing plugin route pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/02-mission-control-activity-log/02-03-SUMMARY.md`
</output>
