---
phase: 02-mission-control-activity-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/activity.ts
  - src/renderer/src/stores/activity-store.ts
autonomous: true
requirements:
  - ACTV-01
  - ACTV-02

must_haves:
  truths:
    - "ActivityEntry type defines all required fields: id, pluginId, operation, status, durationMs, timestamp, detail"
    - "Activity store can add entries with automatic id and timestamp generation"
    - "Activity store persists entries to electron-store via window.api.settings"
    - "Activity store caps entries at 500 to prevent unbounded growth"
    - "Activity store loads persisted entries on initialization"
    - "Activity store provides getRecentEntries and getEntriesByPlugin selectors"
  artifacts:
    - path: "src/renderer/src/types/activity.ts"
      provides: "ActivityEntry and ActivityStatus type definitions"
      contains: "ActivityEntry"
    - path: "src/renderer/src/stores/activity-store.ts"
      provides: "Zustand activity store with CRUD and persistence"
      exports: ["useActivityStore"]
  key_links:
    - from: "src/renderer/src/stores/activity-store.ts"
      to: "window.api.settings"
      via: "settings.get('activity.log') and settings.set('activity.log', entries)"
      pattern: "window\\.api\\.settings\\.(get|set)"
    - from: "src/renderer/src/stores/activity-store.ts"
      to: "src/renderer/src/types/activity.ts"
      via: "import type { ActivityEntry }"
      pattern: "import.*ActivityEntry.*from.*types/activity"
---

<objective>
Create the centralized activity log data layer -- types and Zustand store -- that all dashboard and activity UI components will consume.

Purpose: Establishes the data foundation for ACTV-01 (centralized activity log with timestamps) and ACTV-02 (structured entries with plugin source, operation type, status, duration). Must exist before any UI components can render activity data.

Output: `src/renderer/src/types/activity.ts` (types) and `src/renderer/src/stores/activity-store.ts` (Zustand store with IPC persistence).
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mission-control-activity-log/02-RESEARCH.md

@src/renderer/src/types/plugin.ts
@src/renderer/src/stores/settings-store.ts
@src/renderer/src/stores/agent-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ActivityEntry type definitions</name>
  <files>src/renderer/src/types/activity.ts</files>
  <action>
Create `src/renderer/src/types/activity.ts` with the following type definitions:

1. `ActivityStatus` type alias: `'success' | 'failure' | 'pending'`

2. `ActivityEntry` interface with fields:
   - `id: string` -- Unique ID in format `'act-{timestamp}-{random5chars}'`
   - `pluginId: PluginId` -- Import `PluginId` from `'./plugin'`
   - `operation: string` -- Human-readable operation name (e.g., 'PR Review', 'Query Executed')
   - `status: ActivityStatus` -- success, failure, or pending
   - `durationMs: number | null` -- Duration in milliseconds, null if not measured
   - `timestamp: string` -- ISO 8601 timestamp string
   - `detail?: string` -- Optional detail/error message

Follow the same export style as `src/renderer/src/types/plugin.ts` (named exports, JSDoc comments on interface and fields).
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify types/activity.ts exists with correct fields</manual>
  </verify>
  <done>ActivityEntry and ActivityStatus types are defined, importable, and pass TypeScript compilation. PluginId is properly imported from the plugin types module.</done>
</task>

<task type="auto">
  <name>Task 2: Create activity Zustand store with IPC persistence</name>
  <files>src/renderer/src/stores/activity-store.ts</files>
  <action>
Create `src/renderer/src/stores/activity-store.ts` following the exact pattern from `agent-store.ts` and `settings-store.ts`:

1. Import `create` from `'zustand'` and `ActivityEntry` from `'../types/activity'`.

2. Define `ActivityStoreState` interface:
   - `entries: ActivityEntry[]`
   - `isLoading: boolean`
   - `loadEntries: () => Promise<void>`
   - `addEntry: (entry: Omit<ActivityEntry, 'id' | 'timestamp'>) => Promise<void>`
   - `clearEntries: () => Promise<void>`
   - `getRecentEntries: (limit?: number) => ActivityEntry[]`
   - `getEntriesByPlugin: (pluginId: string) => ActivityEntry[]`

3. Implement store with `create<ActivityStoreState>((set, get) => ({...}))`:

   - `entries: []` and `isLoading: true` as initial state
   - `loadEntries`: Read from `window.api.settings.get('activity.log')`, parse as `ActivityEntry[]`, set to state. Handle null/non-array case by defaulting to `[]`.
   - `addEntry`: Generate `id` as `act-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`. Set `timestamp` to `new Date().toISOString()`. Prepend to entries array. Cap at 500 entries via `.slice(0, 500)`. Update state optimistically, then persist via `window.api.settings.set('activity.log', updated)`.
   - `clearEntries`: Set entries to `[]`, persist empty array.
   - `getRecentEntries(limit = 20)`: Return `get().entries.slice(0, limit)`.
   - `getEntriesByPlugin(pluginId)`: Return `get().entries.filter(e => e.pluginId === pluginId)`.

4. Export as `useActivityStore`.

Key patterns to follow (from existing stores):
- Optimistic local update before IPC call (same as settings-store)
- `isLoading` flag pattern (same as agent-store)
- `window.api.settings` for persistence (NOT localStorage, NOT new IPC channel)
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify store exports useActivityStore, has loadEntries/addEntry/clearEntries/getRecentEntries/getEntriesByPlugin methods</manual>
  </verify>
  <done>Activity store is created with all methods, compiles without errors, follows established Zustand + IPC persistence pattern, caps entries at 500, and provides getRecentEntries and getEntriesByPlugin selectors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project src/renderer/tsconfig.json` passes with zero errors
2. `src/renderer/src/types/activity.ts` exports `ActivityEntry` and `ActivityStatus`
3. `src/renderer/src/stores/activity-store.ts` exports `useActivityStore`
4. Activity store imports `ActivityEntry` from types (not using `any`)
5. Activity store uses `window.api.settings.get/set` for persistence (not localStorage)
6. Entry cap at 500 is implemented in `addEntry`
</verification>

<success_criteria>
- TypeScript compilation succeeds with no errors
- ActivityEntry type has all 7 fields (id, pluginId, operation, status, durationMs, timestamp, detail)
- ActivityStatus is a 3-member union type
- Zustand store follows established project patterns (create, optimistic update, IPC persist)
- Store provides load, add, clear, getRecent, getByPlugin operations
</success_criteria>

<output>
After completion, create `.planning/phases/02-mission-control-activity-log/02-01-SUMMARY.md`
</output>
