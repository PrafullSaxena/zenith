---
phase: 07-launchpad-plugin
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - src/renderer/src/plugins/launchpad/AiAdvisor.tsx
  - src/renderer/src/plugins/launchpad/EstimationHistory.tsx
  - src/renderer/src/plugins/launchpad/ComparisonView.tsx
  - src/renderer/src/plugins/launchpad/LaunchpadView.tsx
autonomous: false
requirements:
  - LNCH-06
  - LNCH-07
  - LNCH-09
  - LNCH-10

must_haves:
  truths:
    - "User can ask AI for cloud infrastructure recommendations and see streaming response"
    - "AI suggestions can be applied to auto-populate the estimator with recommended services"
    - "User can save estimations and reload them from history"
    - "User can compare equivalent service costs side-by-side across AWS, GCP, and Azure"
  artifacts:
    - path: "src/renderer/src/plugins/launchpad/AiAdvisor.tsx"
      provides: "AI chat panel with streaming and suggestion parsing"
      contains: "AiAdvisor"
    - path: "src/renderer/src/plugins/launchpad/EstimationHistory.tsx"
      provides: "Saved estimations list with reload and delete"
      contains: "EstimationHistory"
    - path: "src/renderer/src/plugins/launchpad/ComparisonView.tsx"
      provides: "Side-by-side provider comparison table"
      contains: "ComparisonView"
  key_links:
    - from: "src/renderer/src/plugins/launchpad/AiAdvisor.tsx"
      to: "src/renderer/src/stores/launchpad-store.ts"
      via: "startAiChat and applySuggestions actions"
      pattern: "startAiChat|applySuggestions"
    - from: "src/renderer/src/plugins/launchpad/EstimationHistory.tsx"
      to: "src/renderer/src/stores/launchpad-store.ts"
      via: "loadEstimation and deleteHistoryEntry actions"
      pattern: "loadEstimation|deleteHistoryEntry"
    - from: "src/renderer/src/plugins/launchpad/ComparisonView.tsx"
      to: "src/renderer/src/data/cloud-pricing/equivalences.ts"
      via: "SERVICE_EQUIVALENCES for cross-provider mapping"
      pattern: "SERVICE_EQUIVALENCES|getEquivalentServiceId"
---

<objective>
Build the three secondary Launchpad tabs: AI Advisor (chat with streaming + suggestion auto-populate), Estimation History (save/load/delete), and Comparison View (side-by-side multi-provider costs). Then wire them into LaunchpadView replacing the placeholder divs.

Purpose: These tabs complete the full Launchpad feature set beyond the core estimator -- AI-driven recommendations, persistence, and cross-provider comparison.
Output: 3 new components + updated LaunchpadView with all tabs functional.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-launchpad-plugin/07-RESEARCH.md
@.planning/phases/07-launchpad-plugin/07-01-SUMMARY.md
@.planning/phases/07-launchpad-plugin/07-02-SUMMARY.md
@.planning/phases/07-launchpad-plugin/07-03-SUMMARY.md
@src/renderer/src/plugins/db-inspector/AskAI.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AiAdvisor chat component with streaming and suggestion application</name>
  <files>
    src/renderer/src/plugins/launchpad/AiAdvisor.tsx
  </files>
  <action>
    Create `src/renderer/src/plugins/launchpad/AiAdvisor.tsx` following the AskAI.tsx pattern from DbInspector:
    - Import useLaunchpadStore (aiSession, pendingSuggestions, startAiChat, cancelAiChat, applySuggestions, dismissSuggestions)
    - Import useAgentStore for agent selection
    - Import useSettingsStore for plugin default agent
    - Import lucide icons: Send, Loader, Square (stop), Sparkles, X
    - Import useRef for auto-scroll

    Layout:
    - Full-height flex column
    - Header: "AI Cloud Advisor" title with Sparkles icon
    - Description text: "Describe your infrastructure needs and get service recommendations with cost estimates"
    - Chat input area at bottom:
      - Textarea for user question (multiline, placeholder: "e.g., I need a web app with 2 servers, a database, and file storage...")
      - Send button (disabled when streaming or empty input)
      - Cancel button (shown only during streaming)
    - Response area (scrollable, above input):
      - If aiSession is null: show example prompts as clickable suggestions (3-4 common scenarios like "Small web application", "Data processing pipeline", "Microservices architecture")
      - If aiSession.status === 'streaming': show rawText with pulse animation, auto-scroll via useRef+useEffect
      - If aiSession.status === 'complete': show rawText rendered as plain text (or basic markdown-like formatting with whitespace preserved)
      - If aiSession.status === 'error': show error message with retry button

    Suggestion banner (shown when pendingSuggestions is not null):
    - Highlighted card below the response text: "AI has suggested a configuration"
    - Show summary: provider + number of services suggested
    - Two buttons:
      - "Apply Suggestions" (accent color): calls applySuggestions(), then switches to estimator tab via setActiveTab('estimator')
      - "Dismiss" (ghost button): calls dismissSuggestions()
    - IMPORTANT: Never auto-apply suggestions (per research recommendation). User must click "Apply".

    Agent selection:
    - Use same pattern as DbInspector AskAI: get defaultAgent from plugin settings, fall back to first configured provider
    - If no agent configured: show warning "Configure an AI agent in Settings to use the advisor"

    startAiChat call:
    - Get selected agent (id, model, command)
    - Call store.startAiChat(question, agentId, model, command)
    - Clear the input textarea after sending
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
    <manual>Verify AiAdvisor renders with chat input, example prompts, and handles streaming display</manual>
  </verify>
  <done>AI Advisor tab renders with chat input, streams AI responses, shows parsed suggestion banner with Apply/Dismiss buttons. Agent selection follows existing pattern with fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Create EstimationHistory, ComparisonView, and wire all tabs into LaunchpadView</name>
  <files>
    src/renderer/src/plugins/launchpad/EstimationHistory.tsx
    src/renderer/src/plugins/launchpad/ComparisonView.tsx
    src/renderer/src/plugins/launchpad/LaunchpadView.tsx
  </files>
  <action>
    Create `src/renderer/src/plugins/launchpad/EstimationHistory.tsx`:
    - Import useLaunchpadStore (history, loadEstimation, deleteHistoryEntry, setActiveTab)
    - Import lucide icons: Clock, Trash2, RotateCcw, FileText
    - If history is empty: show empty state with "No saved estimations yet" message and a subtle icon
    - Otherwise render a list of estimation entries:
      - Each entry as a card with:
        - Name (bold)
        - Provider badge (AWS/GCP/Azure with provider-specific color)
        - Saved date (formatted with Intl.DateTimeFormat or relative time)
        - Total monthly cost: "$X.XX/mo"
        - Number of services: "N services"
        - Action buttons:
          - "Load" button (RotateCcw icon): calls loadEstimation(entry), then setActiveTab('estimator') to switch back
          - "Delete" button (Trash2 icon): confirm dialog (simple window.confirm), then deleteHistoryEntry(entry.id)
    - Sort entries by savedAt descending (most recent first -- store should already maintain this order)
    - Show count in header: "Estimation History (N)"

    Create `src/renderer/src/plugins/launchpad/ComparisonView.tsx`:
    - Import useLaunchpadStore (provider, selectedServices)
    - Import getCatalog from data/cloud-pricing/index
    - Import SERVICE_EQUIVALENCES, getEquivalentServiceId from equivalences
    - Import calculateServiceCost from calculator
    - Import lucide icons: BarChart3, AlertCircle

    Logic:
    - Take current provider's selectedServices as the template
    - For each selected service, find equivalent services in the other two providers via getEquivalentServiceId
    - Calculate costs for each provider using the equivalent service with default config from that provider's catalog
    - The current provider uses the user's actual config; other providers use default configs
    - If no equivalent exists for a service in a provider, show "N/A" in that column

    Layout:
    - If no provider selected or no services selected: show "Select a provider and add services in the Estimator tab to compare costs"
    - Otherwise:
      - Header: "Multi-Provider Comparison"
      - Subtitle showing base provider: "Based on your {provider} configuration"
      - Comparison table with columns: Service | AWS | GCP | Azure
      - Each row: service name, then cost per provider ($X.XX/mo)
      - Highlight the current provider's column (subtle accent background)
      - Total row at bottom with sum per provider
      - Color-code cheapest option per row (green text or checkmark)
    - Note at bottom: "Comparison uses default configurations for non-selected providers. Actual costs may vary."

    Update `src/renderer/src/plugins/launchpad/LaunchpadView.tsx`:
    - Replace the placeholder divs for 'ai-advisor', 'history', and 'compare' tabs with actual components:
      - 'ai-advisor': AiAdvisor component
      - 'history': EstimationHistory component
      - 'compare': ComparisonView component
    - Add imports for AiAdvisor, EstimationHistory, ComparisonView
    - Ensure all tab content components are rendered conditionally based on activeTab
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
    <manual>Verify all 4 tabs in LaunchpadView render their actual content, not placeholders</manual>
  </verify>
  <done>EstimationHistory shows saved entries with load/delete. ComparisonView renders side-by-side provider cost table. LaunchpadView has all 4 tabs fully functional with real components.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Launchpad plugin visually</name>
  <files>src/renderer/src/plugins/launchpad/LaunchpadView.tsx</files>
  <action>
    Human verifies the complete Launchpad plugin works end-to-end by running the app and testing all 4 tabs.
    Steps:
    1. Run `npm run dev` to start the app
    2. Verify "Launchpad" appears in the sidebar with a Rocket icon
    3. Click Launchpad and verify 4 tabs show: Estimator, AI Advisor, History, Compare
    4. In Estimator tab:
       a. Select AWS -- verify service catalog appears with categories
       b. Toggle on EC2 -- verify ResourceConfigurator shows config fields
       c. Select t3.medium, quantity 2, 730 hrs -- verify cost shows ~$60.74/mo
       d. Toggle monthly/yearly -- verify yearly shows ~$728.88
       e. Add S3 (100GB, 10GB transfer) -- verify cost updates
       f. Click "Save Estimation" -- enter name, save
       g. Click "Export PDF" -- verify save dialog and PDF file
       h. Click "Change Provider" -- verify returns to selection
    5. In AI Advisor tab: verify chat input and example prompts appear
    6. In History tab: verify saved estimation appears, load and delete work
    7. In Compare tab: verify side-by-side provider comparison table
    8. Verify no console errors throughout
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project tsconfig.json 2>&1 | head -5</automated>
    <manual>Complete visual and functional verification of all Launchpad tabs as described in action</manual>
  </verify>
  <done>All 4 Launchpad tabs verified functional: estimator calculates costs correctly, AI advisor streams responses, history saves/loads/deletes, comparison shows cross-provider costs. No console errors.</done>
</task>

</tasks>

<verification>
- All Launchpad plugin components compile and render
- AI Advisor streams responses and parses suggestion blocks
- History persists across app restarts (uses electron-store via settings IPC)
- Comparison view maps services across providers using equivalence map
- PDF export generates a valid PDF file
- All 4 tabs are accessible and functional
</verification>

<success_criteria>
- AI Advisor: user can ask questions, see streaming responses, and apply AI suggestions to the estimator
- History: user can save, load, and delete estimations; data persists via electron-store
- Comparison: user sees side-by-side costs across AWS/GCP/Azure for their selected services
- All LNCH requirements (01-10) are fully addressed across Plans 01-04
- The complete plugin is visually verified by the user
</success_criteria>

<output>
After completion, create `.planning/phases/07-launchpad-plugin/07-04-SUMMARY.md`
</output>
