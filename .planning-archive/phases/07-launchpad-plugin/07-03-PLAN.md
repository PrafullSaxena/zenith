---
phase: 07-launchpad-plugin
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - src/renderer/src/plugins/launchpad/LaunchpadView.tsx
  - src/renderer/src/plugins/launchpad/ProviderSelector.tsx
  - src/renderer/src/plugins/launchpad/ServiceCatalog.tsx
  - src/renderer/src/plugins/launchpad/ResourceConfigurator.tsx
  - src/renderer/src/plugins/launchpad/EstimationSummary.tsx
autonomous: true
requirements:
  - LNCH-01
  - LNCH-02
  - LNCH-03
  - LNCH-05

must_haves:
  truths:
    - "User can select AWS, GCP, or Azure as cloud provider via clickable cards"
    - "User can browse service catalog organized by category and toggle services on/off"
    - "User can configure resource parameters (instance type, quantity, storage size, etc.) per selected service"
    - "User sees itemized cost breakdown with subtotals per service and grand total with monthly/yearly toggle"
  artifacts:
    - path: "src/renderer/src/plugins/launchpad/LaunchpadView.tsx"
      provides: "Main plugin view with tab navigation"
      min_lines: 80
    - path: "src/renderer/src/plugins/launchpad/ProviderSelector.tsx"
      provides: "AWS/GCP/Azure card selector"
      contains: "ProviderSelector"
    - path: "src/renderer/src/plugins/launchpad/ServiceCatalog.tsx"
      provides: "Browsable service list with categories"
      contains: "ServiceCatalog"
    - path: "src/renderer/src/plugins/launchpad/ResourceConfigurator.tsx"
      provides: "Per-service configuration form"
      contains: "ResourceConfigurator"
    - path: "src/renderer/src/plugins/launchpad/EstimationSummary.tsx"
      provides: "Itemized cost breakdown with totals"
      contains: "EstimationSummary"
  key_links:
    - from: "src/renderer/src/plugins/launchpad/LaunchpadView.tsx"
      to: "src/renderer/src/stores/launchpad-store.ts"
      via: "useLaunchpadStore import"
      pattern: "useLaunchpadStore"
    - from: "src/renderer/src/plugins/launchpad/ServiceCatalog.tsx"
      to: "src/renderer/src/data/cloud-pricing/index.ts"
      via: "getCatalog for selected provider"
      pattern: "getCatalog"
    - from: "src/renderer/src/plugins/launchpad/EstimationSummary.tsx"
      to: "src/renderer/src/data/cloud-pricing/calculator.ts"
      via: "calculateTotalCost for display"
      pattern: "calculateTotalCost"
---

<objective>
Build the core estimator UI: the main LaunchpadView with tab navigation, provider selection cards, browsable service catalog with category grouping, per-service resource configuration forms, and the itemized estimation summary with monthly/yearly toggle.

Purpose: This is the primary user interaction surface — the estimator tab where users select a provider, pick services, configure resources, and see costs in real time.
Output: 5 React components forming the complete estimator workflow.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-launchpad-plugin/07-RESEARCH.md
@.planning/phases/07-launchpad-plugin/07-01-SUMMARY.md
@.planning/phases/07-launchpad-plugin/07-02-SUMMARY.md
@src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
@src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LaunchpadView main component with tab navigation and ProviderSelector</name>
  <files>
    src/renderer/src/plugins/launchpad/LaunchpadView.tsx
    src/renderer/src/plugins/launchpad/ProviderSelector.tsx
  </files>
  <action>
    Create `src/renderer/src/plugins/launchpad/LaunchpadView.tsx`:
    - Default export function (for React.lazy compatibility)
    - Import useLaunchpadStore, useAgentStore, useActivityStore
    - Import LaunchpadTab type
    - Import child components: ProviderSelector, ServiceCatalog, ResourceConfigurator, EstimationSummary (the latter 3 will exist after Task 2 — import them now, they will be created in Task 2)
    - Import lucide icons: Rocket, Calculator, MessageSquare, History, GitCompare (or similar)
    - Define TABS array: [{ id: 'estimator', label: 'Estimator', icon: Calculator }, { id: 'ai-advisor', label: 'AI Advisor', icon: MessageSquare }, { id: 'history', label: 'History', icon: History }, { id: 'compare', label: 'Compare', icon: GitCompare }]
    - Use activeTab from store, setActiveTab for switching
    - useEffect on mount: call loadHistory()
    - Layout (following DbInspectorView pattern):
      - Full-height flex column
      - Top bar with plugin title "Launchpad" + Rocket icon + tab navigation row
      - Tab bar: horizontal row of tab buttons with border-b-2 border-accent for active tab (per CodeReviewBot pattern)
      - Content area: conditionally render based on activeTab
        - 'estimator': render the estimator flow (ProviderSelector if no provider selected, otherwise ServiceCatalog + ResourceConfigurator + EstimationSummary in a split layout)
        - 'ai-advisor': placeholder div "AI Advisor" (built in Plan 04)
        - 'history': placeholder div "History" (built in Plan 04)
        - 'compare': placeholder div "Compare" (built in Plan 04)
    - Estimator tab layout:
      - If provider is null: show ProviderSelector full-width
      - If provider is set: two-column layout (left: ServiceCatalog + ResourceConfigurator, right: EstimationSummary)
      - Show a "Change Provider" button/link when provider is set (calls clearEstimation)

    Styling: Use Tailwind @theme tokens (text-text-primary, text-text-secondary, bg-bg-primary, bg-bg-secondary, border-border, text-accent). Dark-only theme. Follow existing plugin view styling patterns.

    Create `src/renderer/src/plugins/launchpad/ProviderSelector.tsx`:
    - Props: onSelect(provider: CloudProvider)
    - Import PROVIDER_INFO from data/cloud-pricing/index (display names + descriptions)
    - Import Cloud, Server, Globe icons from lucide-react (or use provider-specific imagery)
    - Render 3 clickable cards in a responsive grid (grid-cols-1 md:grid-cols-3 gap-4):
      - AWS card: orange/amber accent, "Amazon Web Services" label, brief description
      - GCP card: blue accent, "Google Cloud Platform" label
      - Azure card: cyan accent, "Microsoft Azure" label
    - Each card: hover effect (scale-[1.02] transition, border-accent highlight), onClick calls onSelect with the provider
    - Title above cards: "Select Cloud Provider"
    - Cards should have: provider name (large text), provider description, a subtle icon
    - Active state not needed here (selection navigates to catalog view)
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
    <manual>Verify LaunchpadView renders with tab bar and ProviderSelector shows 3 provider cards</manual>
  </verify>
  <done>LaunchpadView renders with 4-tab navigation bar. Estimator tab shows ProviderSelector with 3 clickable provider cards when no provider is selected. Tab switching works via store.</done>
</task>

<task type="auto">
  <name>Task 2: Create ServiceCatalog, ResourceConfigurator, and EstimationSummary components</name>
  <files>
    src/renderer/src/plugins/launchpad/ServiceCatalog.tsx
    src/renderer/src/plugins/launchpad/ResourceConfigurator.tsx
    src/renderer/src/plugins/launchpad/EstimationSummary.tsx
  </files>
  <action>
    Create `src/renderer/src/plugins/launchpad/ServiceCatalog.tsx`:
    - Props: provider (CloudProvider)
    - Import getCatalog from data/cloud-pricing/index
    - Import useLaunchpadStore for selectedServices, addService, removeService
    - Get catalog via getCatalog(provider)
    - Render categories as collapsible sections (use useState for expanded/collapsed per category):
      - Category header: bold name with chevron icon (ChevronDown/ChevronRight)
      - Under each category: list of services as rows/cards
      - Each service row: checkbox or toggle to add/remove, service name, brief description
      - Selected services get a highlighted border/background (border-accent or bg-accent/10)
    - When user checks a service: call addService(categoryId, serviceId) — store should add with default config from catalog
    - When user unchecks: call removeService(serviceId)
    - Show selected count badge per category header (e.g., "Compute (2 selected)")

    Create `src/renderer/src/plugins/launchpad/ResourceConfigurator.tsx`:
    - Props: none (reads from store)
    - Import useLaunchpadStore for selectedServices, updateServiceConfig, provider
    - Import getCatalog to get config schema definitions
    - For each selected service, render a configuration card:
      - Service name as card header
      - For each field in configSchema:
        - type 'select': dropdown with options (for instance types, regions, etc.)
        - type 'number': number input with min/max, label, default
      - On change: call updateServiceConfig(serviceId, { ...currentConfig, [fieldKey]: newValue })
    - If no services selected: show empty state message "Select services from the catalog to configure"
    - Cards should be scrollable if many services are selected
    - Use Tailwind form styling: bg-bg-secondary rounded-lg p-4, input with bg-bg-primary border-border

    Create `src/renderer/src/plugins/launchpad/EstimationSummary.tsx`:
    - Props: none (reads from store)
    - Import useLaunchpadStore for selectedServices, provider
    - Import calculateTotalCost from calculator
    - Import getCatalog from index
    - Local state: displayMode ('monthly' | 'yearly') with toggle switch
    - Compute totals using calculateTotalCost(selectedServices, getCatalog(provider))
    - Render:
      - Header: "Cost Estimation" with monthly/yearly toggle (button group or switch)
      - Itemized table/list:
        - Each service: name, configuration summary (e.g., "2x t3.medium, 730 hrs/mo"), monthly cost, yearly cost
        - Subtotal per category (optional but nice)
      - Separator line
      - Grand total row (bold, larger text): "$X.XX / month" or "$X.XX / year"
      - Action buttons at bottom:
        - "Save Estimation" button: opens a small name input modal/inline, calls saveEstimation(name)
        - "Export PDF" button: calls exportPdf() from store
        - "Clear All" button: calls clearEstimation()
    - Format all currency values with `$${value.toFixed(2)}`
    - If no services selected: show "No services selected" placeholder
    - Summary should auto-update as user changes configs (reactive via store)
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
    <manual>Verify all 3 components render without errors. ServiceCatalog shows categories, ResourceConfigurator shows form fields, EstimationSummary shows cost table.</manual>
  </verify>
  <done>ServiceCatalog displays provider services grouped by category with toggles. ResourceConfigurator renders dynamic forms per selected service. EstimationSummary shows itemized breakdown with monthly/yearly toggle, save, export, and clear buttons.</done>
</task>

</tasks>

<verification>
- All 5 components in src/renderer/src/plugins/launchpad/ compile without TypeScript errors
- LaunchpadView has 4 tabs (estimator, ai-advisor, history, compare)
- Estimator tab flow: ProviderSelector -> ServiceCatalog + ResourceConfigurator + EstimationSummary
- All components connect to useLaunchpadStore (not local state for cross-component data)
- Costs auto-update as user modifies resource configurations
</verification>

<success_criteria>
- User can select a cloud provider via cards
- User can browse and toggle services from the catalog
- User can configure instance types, quantities, storage sizes per service
- User sees real-time cost calculations with monthly/yearly toggle
- Save Estimation and Export PDF buttons are wired to store actions
</success_criteria>

<output>
After completion, create `.planning/phases/07-launchpad-plugin/07-03-SUMMARY.md`
</output>
