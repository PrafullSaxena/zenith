---
phase: 07-micro-interactions-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/pages/GeneralSettings.tsx
  - src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx
  - src/renderer/src/components/StatsCards.tsx
  - src/renderer/src/plugins/cortex/components/InsightsPanel.tsx
  - src/renderer/src/plugins/cortex/components/DesignDocTab.tsx
  - src/renderer/src/plugins/cortex/components/ValidationPanel.tsx
  - src/renderer/src/plugins/db-inspector/components/ConnectionManager.tsx
  - src/renderer/src/plugins/db-inspector/components/QueryTab.tsx
  - src/renderer/src/pages/MCPSettings.tsx
  - src/renderer/src/App.tsx
autonomous: true
requirements:
  - MICRO-03
  - MICRO-05

must_haves:
  truths:
    - "All card grids in the app use staggered entrance animations from shared motion.ts variants"
    - "All standalone loading states use GlassSkeleton instead of bare Loader2 spinners"
    - "Inline button loading spinners remain unchanged (not converted to skeletons)"
  artifacts:
    - path: "src/renderer/src/pages/GeneralSettings.tsx"
      provides: "Theme grid with stagger animation"
      contains: "staggerContainer"
    - path: "src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx"
      provides: "Architecture card grid with stagger animation"
      contains: "staggerContainer"
    - path: "src/renderer/src/components/StatsCards.tsx"
      provides: "Stats cards with stagger animation"
      contains: "staggerContainer"
  key_links:
    - from: "src/renderer/src/pages/GeneralSettings.tsx"
      to: "src/renderer/src/lib/motion.ts"
      via: "staggerContainer/staggerItem import"
      pattern: "import.*staggerContainer.*motion"
---

<objective>
Audit and fix stagger animation gaps across all card grids, and replace standalone Loader2 spinners with contextual GlassSkeleton loaders.

Purpose: MICRO-03 requires all card grids to use stagger animations. MICRO-05 requires all standalone loading states to use GlassSkeleton. Research identified specific gaps: GeneralSettings theme grid, ArchitectureDashboard, and StatsCards lack stagger. ~40+ Loader2 instances exist, with standalone ones needing conversion.

Output: All card grids use staggerContainer/staggerItem, and standalone loading states use GlassSkeleton.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-micro-interactions-and-polish/07-RESEARCH.md
@src/renderer/src/lib/motion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stagger animations to remaining card grids</name>
  <files>
    src/renderer/src/pages/GeneralSettings.tsx
    src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx
    src/renderer/src/components/StatsCards.tsx
  </files>
  <action>
First, grep the full codebase to confirm the three gaps from research and find any additional card grids missing stagger:
```
grep -rn "grid-cols" src/renderer/src/ --include="*.tsx" | grep -v node_modules | grep -v stagger
```

For each gap, apply the standard stagger pattern:

**GeneralSettings.tsx** — Theme grid (grid-cols-3):
- Import `staggerContainer`, `staggerItem` from `@renderer/lib/motion` and `motion` from `framer-motion`
- Wrap the theme grid container div with `motion.div` using `variants={staggerContainer}`, `initial="hidden"`, `animate="visible"`
- Wrap each ThemeCard in `motion.div` with `variants={staggerItem}`

**ArchitectureDashboard.tsx** — Card grid (grid-cols-1/2/3):
- Same pattern: import stagger variants, wrap grid container, wrap each card item

**StatsCards.tsx** — Stat cards grid (grid-cols-2/4):
- Same pattern: import stagger variants, wrap grid container, wrap each stat card
- Use `useRef` mount-only pattern if this component re-renders on data updates (same as ActivityFeed from Phase 03-02)

For any additional gaps found by the grep, apply the same pattern. If a grid renders a static/fixed number of items (like 4 stat cards), stagger is still appropriate for entrance animation.

IMPORTANT: Preserve existing className props on grid containers — just change the element from `div` to `motion.div` and add the variants/initial/animate props.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>grep -rn "staggerContainer" src/renderer/src/pages/GeneralSettings.tsx src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx src/renderer/src/components/StatsCards.tsx — all three should match</manual>
  </verify>
  <done>GeneralSettings theme grid, ArchitectureDashboard card grid, and StatsCards all use staggerContainer/staggerItem entrance animations</done>
</task>

<task type="auto">
  <name>Task 2: Replace standalone Loader2 spinners with GlassSkeleton</name>
  <files>
    src/renderer/src/plugins/cortex/components/InsightsPanel.tsx
    src/renderer/src/plugins/cortex/components/DesignDocTab.tsx
    src/renderer/src/plugins/cortex/components/ValidationPanel.tsx
    src/renderer/src/plugins/db-inspector/components/ConnectionManager.tsx
    src/renderer/src/plugins/db-inspector/components/QueryTab.tsx
    src/renderer/src/pages/MCPSettings.tsx
    src/renderer/src/App.tsx
  </files>
  <action>
First, grep to find ALL Loader2 + animate-spin instances:
```
grep -rn "animate-spin\|Loader2" src/renderer/src/ --include="*.tsx" | grep -v node_modules
```

Classify each instance:
- **STANDALONE loading state** (Loader2 IS the content area — replace): Full-page loaders, panel loading states, tab content loaders. These render a centered spinner as the sole content while data loads.
- **INLINE button spinner** (Loader2 inside a button during an action — KEEP): Button loading indicators where the spinner appears inside a `<button>` or `<GlassButton>` alongside text like "Loading..." or "Analyzing...". These are legitimate UX patterns.

For each STANDALONE instance, replace with the appropriate GlassSkeleton variant:
- **Panel/card content loading** → `<GlassSkeleton variant="card" />` (or multiple stacked)
- **Text content loading** → `<GlassSkeleton variant="text" />` (3-4 lines with varying widths)
- **Table data loading** → `<GlassSkeleton variant="table" />`
- **Full-page loading** (App.tsx suspense fallback) → Stack of `<GlassSkeleton variant="card" />` elements to simulate page layout

Import GlassSkeleton from `@renderer/components/ui`. Remove unused Loader2 imports after replacement (keep if file still uses it for inline button spinners).

Expected standalone replacements based on research:
- InsightsPanel.tsx — panel loading state → GlassSkeleton variant="text"
- DesignDocTab.tsx — panel loading state → GlassSkeleton variant="text"
- ValidationPanel.tsx — panel loading state → GlassSkeleton variant="text"
- ConnectionManager.tsx — connection list loading → GlassSkeleton variant="card"
- QueryTab.tsx — results loading → GlassSkeleton variant="table"
- MCPSettings.tsx — settings loading → GlassSkeleton variant="card"
- App.tsx — suspense fallback → GlassSkeleton variant="card" stack

Wrap skeleton containers in a div with `p-4 space-y-3` for consistent spacing.

DO NOT replace Loader2 instances inside GlassButton or button elements (e.g., RepoCard "Analyzing..." button, AgentRow action buttons).
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Count remaining standalone Loader2 spinners: grep -rn "animate-spin" src/renderer/src/ --include="*.tsx" | grep -v "button\|Button\|btn" — standalone instances should be significantly reduced</manual>
  </verify>
  <done>All standalone loading states use contextual GlassSkeleton. Inline button spinners remain as Loader2. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `grep -rn "staggerContainer" src/renderer/src/pages/GeneralSettings.tsx` returns matches
3. `grep -rn "staggerContainer" src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx` returns matches
4. `grep -rn "staggerContainer" src/renderer/src/components/StatsCards.tsx` returns matches
5. `grep -rn "GlassSkeleton" src/renderer/src/App.tsx` returns matches for suspense fallback
</verification>

<success_criteria>
- All card grids across the app (including previously-missed GeneralSettings, ArchitectureDashboard, StatsCards) use staggerContainer/staggerItem
- Standalone Loader2 loading states replaced with contextual GlassSkeleton variants
- Inline button spinners preserved as-is
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/07-micro-interactions-and-polish/07-02-SUMMARY.md`
</output>
