---
phase: 03-core-pages-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/renderer/src/components/dashboard/MissionControl.tsx
  - src/renderer/src/components/dashboard/StatsCards.tsx
  - src/renderer/src/components/dashboard/PluginCard.tsx
  - src/renderer/src/components/dashboard/ActivityFeed.tsx
  - src/renderer/src/components/dashboard/StatusBadge.tsx
  - src/renderer/src/components/dashboard/HealthPanel.tsx
  - src/renderer/src/components/dashboard/TokenChart.tsx
  - src/renderer/src/components/activity/ActivityLog.tsx
autonomous: true
requirements:
  - CORE-01
  - CORE-02

must_haves:
  truths:
    - "Dashboard stat cards render as GlassCards with animated counters that count from 0 to value"
    - "Dashboard plugin overview cards use GlassCard interactive with staggered entrance animation"
    - "Dashboard activity feed entries are GlassCards with left accent bar colored by plugin"
    - "Dashboard TokenChart and HealthPanel are wrapped in GlassCard default"
    - "Activity Log toolbar is a GlassSurface with GlassSelect filter dropdowns"
    - "Activity Log entries use GlassCard with GlassBadge for status indicators"
    - "Activity Log shows EmptyState when no activities match filters"
  artifacts:
    - path: "src/renderer/src/components/dashboard/StatsCards.tsx"
      provides: "GlassCard stat cards with AnimatedCounter"
      contains: "GlassCard"
    - path: "src/renderer/src/components/dashboard/PluginCard.tsx"
      provides: "GlassCard interactive plugin overview cards"
      contains: "staggerItem"
    - path: "src/renderer/src/components/activity/ActivityLog.tsx"
      provides: "Glass-styled activity log with GlassSelect filters and GlassBadge status"
      contains: "GlassSurface"
  key_links:
    - from: "src/renderer/src/components/dashboard/StatsCards.tsx"
      to: "@renderer/components/ui"
      via: "GlassCard + AnimatedCounter imports"
      pattern: "import.*GlassCard.*AnimatedCounter"
    - from: "src/renderer/src/components/dashboard/MissionControl.tsx"
      to: "@renderer/lib/motion"
      via: "staggerContainer for card grids"
      pattern: "staggerContainer"
    - from: "src/renderer/src/components/activity/ActivityLog.tsx"
      to: "@renderer/components/ui"
      via: "GlassSurface + GlassSelect + GlassCard + GlassBadge imports"
      pattern: "GlassSurface|GlassSelect|GlassBadge"
---

<objective>
Migrate the Mission Control Dashboard and Activity Log to glass components — the two highest-traffic core pages.

Purpose: Dashboard is the first thing users see; Activity Log is the primary operational view. Migrating these creates the immediate "wow" moment of the Obsidian Glass design system.

Output: Dashboard and Activity Log fully using GlassCard, GlassSurface, GlassBadge, GlassSelect, AnimatedCounter, and stagger animations.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages-migration/03-RESEARCH.md
@.planning/phases/03-core-pages-migration/03-01-SUMMARY.md

@src/renderer/src/components/dashboard/MissionControl.tsx
@src/renderer/src/components/dashboard/StatsCards.tsx
@src/renderer/src/components/dashboard/PluginCard.tsx
@src/renderer/src/components/dashboard/ActivityFeed.tsx
@src/renderer/src/components/dashboard/StatusBadge.tsx
@src/renderer/src/components/dashboard/HealthPanel.tsx
@src/renderer/src/components/dashboard/TokenChart.tsx
@src/renderer/src/components/activity/ActivityLog.tsx
@src/renderer/src/components/ui/index.ts
@src/renderer/src/lib/motion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Dashboard (MissionControl) to glass components</name>
  <files>
    src/renderer/src/components/dashboard/MissionControl.tsx
    src/renderer/src/components/dashboard/StatsCards.tsx
    src/renderer/src/components/dashboard/PluginCard.tsx
    src/renderer/src/components/dashboard/ActivityFeed.tsx
    src/renderer/src/components/dashboard/StatusBadge.tsx
    src/renderer/src/components/dashboard/HealthPanel.tsx
    src/renderer/src/components/dashboard/TokenChart.tsx
  </files>
  <action>
    **MissionControl.tsx (parent layout):**
    - Wrap the hero/greeting section in `<GlassSurface>` — keep existing layout (greeting, date, stats grid) inside
    - Wrap the plugin overview cards grid in `<motion.div variants={staggerContainer} initial="hidden" animate="visible">` and each card in `<motion.div variants={staggerItem}>`
    - Wrap the activity feed section similarly with staggerContainer/staggerItem
    - Import staggerContainer, staggerItem from `@renderer/lib/motion`

    **StatsCards.tsx (QuickStat component):**
    - Replace inline Tailwind card markup (`hover-lift group relative flex items-center gap-3 rounded-xl border border-border/60 bg-surface-elevated/60...`) with `<GlassCard className="flex items-center gap-3 px-4 py-3">`
    - Add AnimatedCounter for the stat value: `<AnimatedCounter value={typeof value === 'number' ? value : 0} className="text-lg font-semibold leading-tight text-text-primary" />`
    - Import GlassCard and AnimatedCounter from `@renderer/components/ui`
    - Remove any manual hover-lift CSS class usage (GlassCard default handles this)

    **PluginCard.tsx:**
    - Replace outer card div with `<GlassCard variant="interactive" className="...">` — preserve internal layout
    - Remove redundant border/bg/backdrop-blur Tailwind classes that GlassCard already provides
    - Watch for double-padding: GlassCard has `p-4` default, so remove inner `p-4`/`p-5` if present, or override with `className="p-0"` if keeping inner padding structure

    **ActivityFeed.tsx:**
    - Each activity entry becomes `<GlassCard className="flex items-center gap-3 px-4 py-2.5">`
    - Add a left accent bar: `<div className="h-full w-[3px] rounded-full" style={{ backgroundColor: pluginColor }} />`
    - Replace StatusBadge usage with `<GlassBadge variant={STATUS_TO_BADGE[status]}>` — create a mapping: success→'success', failure→'error', pending→'warning'
    - Wrap the feed list in staggerContainer/staggerItem motion.divs

    **StatusBadge.tsx:**
    - This component is now replaced by GlassBadge. Either delete it or leave it as a thin wrapper that renders GlassBadge for backward compatibility. Prefer deletion if no other files import it besides ActivityFeed and MissionControl.

    **HealthPanel.tsx:**
    - Replace the component's own outer div styling (rounded-xl border border-border/60 bg-surface-elevated/70 p-5) with `<GlassCard className="flex h-full flex-col p-5">` as root element
    - Keep all chart internals unchanged per user decision

    **TokenChart.tsx:**
    - Same pattern as HealthPanel: replace outer div styling with `<GlassCard className="flex h-full flex-col p-5">` as root
    - Keep all chart internals unchanged per user decision

    **Anti-patterns to avoid:**
    - Do NOT wrap existing components in GlassCard externally — replace the component's own outer div
    - Do NOT mix old CSS stagger-children with new framer-motion stagger — use framer-motion exclusively
    - Remove all `hover-lift` CSS class usage — GlassCard interactive variant handles this
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Dashboard should show stat cards as glass cards with animated counters, plugin cards with staggered entrance, and activity feed with colored accent bars and glass badges</manual>
  </verify>
  <done>All 7 dashboard files use glass components. No inline glass Tailwind patterns remain. StatsCards show AnimatedCounter. PluginCards use GlassCard interactive with stagger. ActivityFeed uses GlassBadge for status. TokenChart and HealthPanel wrapped in GlassCard.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Activity Log to glass components</name>
  <files>
    src/renderer/src/components/activity/ActivityLog.tsx
  </files>
  <action>
    **ActivityLog.tsx:**
    - **Toolbar:** Replace the filter bar container with `<GlassSurface className="...">` — this wraps the plugin filter, status filter, activity count, and clear button
    - **Filter dropdowns:** Replace `<select>` elements with `<GlassSelect>` — map the existing options array to GlassSelect's options format. GlassSelect accepts `options: { value: string; label: string }[]`, `value`, and `onChange`
    - **Clear button:** Replace with `<GlassButton variant="ghost" size="sm">Clear</GlassButton>`
    - **Activity entries:** Each entry becomes a `<GlassCard>` with:
      - Left accent bar colored by the entry's plugin color: `<div className="w-[3px] self-stretch rounded-full" style={{ backgroundColor: pluginColor }} />`
      - Status indicator using `<GlassBadge variant={statusMap[entry.status]}>` where statusMap = { success: 'success', failure: 'error', pending: 'warning' }
    - **Stagger animation:** Wrap the entry list in `<motion.div variants={staggerContainer} initial="hidden" animate="visible" key={filterKey}>` where filterKey changes only on page mount (NOT on filter change — see pitfall 6 from research). Use a `useRef(true)` to track initial mount and pass `initial={isMounted.current ? false : "hidden"}` to prevent re-animation on filter changes.
    - **Empty state:** When filtered results are empty, show `<EmptyState title="No activities found" description="Try adjusting your filters" />` instead of a bare "No results" text
    - Import GlassSurface, GlassCard, GlassBadge, GlassSelect, GlassButton, EmptyState from `@renderer/components/ui`
    - Import staggerContainer, staggerItem from `@renderer/lib/motion`
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Activity Log should show glass toolbar with glass select dropdowns, glass card entries with accent bars and badges, and EmptyState when no results</manual>
  </verify>
  <done>Activity Log toolbar uses GlassSurface. Filters use GlassSelect. Entries use GlassCard with left accent bar and GlassBadge status. Empty state shows EmptyState component. Stagger animation plays on mount but not on filter change.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- No remaining inline glass Tailwind patterns (bg-surface-elevated/60 backdrop-blur-sm border-border/60) in any dashboard file
- No remaining `<select>` elements in ActivityLog.tsx
- StatusBadge component deleted or deprecated
- All card grids use staggerContainer/staggerItem from lib/motion.ts
</verification>

<success_criteria>
- Dashboard stat cards show GlassCard with AnimatedCounter counting from 0 to value
- Dashboard plugin cards use GlassCard interactive with staggered fade-in entrance
- Dashboard activity feed entries are GlassCards with left accent bar by plugin color
- TokenChart and HealthPanel wrapped in GlassCard default (chart internals unchanged)
- Activity Log toolbar is GlassSurface with GlassSelect dropdowns
- Activity Log entries use GlassCard + GlassBadge status indicators
- Activity Log empty state shows EmptyState component
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages-migration/03-02-SUMMARY.md`
</output>
