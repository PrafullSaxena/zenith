---
phase: 03-core-pages-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/ui/AnimatedCounter.tsx
  - src/renderer/src/components/ui/index.ts
  - src/renderer/src/plugins/cortex/components/AnimatedCounter.tsx
  - src/renderer/src/components/ui/GlassTab.tsx
  - src/renderer/src/lib/theme-metadata.ts
  - src/renderer/src/components/AppLayout.tsx
autonomous: true
requirements:
  - CORE-01
  - CORE-04
  - CORE-05

must_haves:
  truths:
    - "AnimatedCounter is importable from @renderer/components/ui without Cortex coupling"
    - "GlassTab renders in vertical orientation with left-bar active indicator"
    - "Theme metadata provides color dots for all 12 classic themes"
    - "Navigating between pages plays a framer-motion crossfade transition with exit animation"
  artifacts:
    - path: "src/renderer/src/components/ui/AnimatedCounter.tsx"
      provides: "Shared animated number counter"
      min_lines: 30
    - path: "src/renderer/src/lib/theme-metadata.ts"
      provides: "Theme metadata with color dots for selector grid"
      contains: "THEME_METADATA"
    - path: "src/renderer/src/components/AppLayout.tsx"
      provides: "AnimatePresence page transitions"
      contains: "AnimatePresence"
  key_links:
    - from: "src/renderer/src/plugins/cortex/components/AnimatedCounter.tsx"
      to: "src/renderer/src/components/ui/AnimatedCounter.tsx"
      via: "re-export for backward compatibility"
      pattern: "export.*from.*@renderer/components/ui/AnimatedCounter"
    - from: "src/renderer/src/components/ui/index.ts"
      to: "src/renderer/src/components/ui/AnimatedCounter.tsx"
      via: "barrel export"
      pattern: "AnimatedCounter"
---

<objective>
Set up shared utilities and cross-cutting concerns that unblock all page migrations in Plans 03-02 and 03-03.

Purpose: The dashboard needs AnimatedCounter (currently locked in Cortex), Settings needs GlassTab vertical orientation and theme metadata, and all pages need framer-motion page transitions. Doing this first avoids duplication and dependency conflicts.

Output: AnimatedCounter in shared ui/, GlassTab with orientation prop, THEME_METADATA constant, AnimatePresence page transitions in AppLayout.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages-migration/03-RESEARCH.md

@src/renderer/src/plugins/cortex/components/AnimatedCounter.tsx
@src/renderer/src/components/ui/GlassTab.tsx
@src/renderer/src/components/ui/index.ts
@src/renderer/src/components/AppLayout.tsx
@src/renderer/src/lib/motion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate AnimatedCounter to shared ui/ and extend GlassTab with vertical orientation</name>
  <files>
    src/renderer/src/components/ui/AnimatedCounter.tsx
    src/renderer/src/components/ui/index.ts
    src/renderer/src/plugins/cortex/components/AnimatedCounter.tsx
    src/renderer/src/components/ui/GlassTab.tsx
  </files>
  <action>
    1. **Move AnimatedCounter to shared location:**
       - Copy `src/renderer/src/plugins/cortex/components/AnimatedCounter.tsx` to `src/renderer/src/components/ui/AnimatedCounter.tsx`
       - In the new file, update the import path for `usePrefersReducedMotion` to `@renderer/lib/useReducedMotion` (the Cortex version already re-exports from there, so the API is identical)
       - Replace the OLD file content with a single re-export: `export { default } from '@renderer/components/ui/AnimatedCounter'` — this preserves backward compatibility for any existing Cortex imports
       - Add `export { default as AnimatedCounter } from './AnimatedCounter'` to `src/renderer/src/components/ui/index.ts` in a new "Utilities" section comment

    2. **Add vertical orientation to GlassTab:**
       - Add an `orientation?: 'horizontal' | 'vertical'` prop to GlassTab (default: 'horizontal')
       - When `orientation="vertical"`:
         - Switch the outer container from `flex gap-1` (row) to `flex flex-col gap-1`
         - Change the active indicator from a bottom underline (`bottom-0 left-0 right-0 h-0.5`) to a left bar (`left-0 top-0 bottom-0 w-0.5 rounded-r`)
         - Keep the same layoutId="activeTab" and spring animation
         - Tab buttons should be `w-full text-left` instead of centered
       - Ensure existing horizontal usage is unaffected (default prop value)
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify AnimatedCounter imports resolve from both old Cortex path and new ui/ path</manual>
  </verify>
  <done>AnimatedCounter importable from `@renderer/components/ui`. Old Cortex path re-exports without breakage. GlassTab accepts orientation="vertical" prop and renders left-bar active indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Create theme metadata and add page transitions</name>
  <files>
    src/renderer/src/lib/theme-metadata.ts
    src/renderer/src/components/AppLayout.tsx
  </files>
  <action>
    1. **Create theme metadata module:**
       - Create `src/renderer/src/lib/theme-metadata.ts`
       - Define a `ThemeMeta` type: `{ value: string; label: string; section: 'classic' | 'new'; colors: { bg: string; surface: string; accent: string; text: string } }`
       - Export a `THEME_METADATA` array containing all 12 classic themes. Extract the 4 color values (bg, surface, accent, text) from the CSS `[data-theme]` blocks in main.css. Read `src/renderer/src/assets/main.css` to get the actual hex/oklch values for each theme.
       - Include placeholder entries for 6 new themes with `section: 'new'` — these will be filled in Phase 5. Use empty/placeholder color values marked with a TODO comment.
       - Export helper functions: `getClassicThemes()` returns themes with section='classic', `getNewThemes()` returns section='new'

    2. **Add AnimatePresence page transitions to AppLayout:**
       - Replace the current CSS `animate-page-enter` approach with framer-motion AnimatePresence
       - Import `AnimatePresence, motion` from 'framer-motion' and `pageTransition` from `@renderer/lib/motion`
       - Wrap `<Outlet />` in `<AnimatePresence mode="wait"><motion.div key={location.pathname} variants={pageTransition} initial="initial" animate="animate" exit="exit" className="h-full"><Outlet /></motion.div></AnimatePresence>`
       - Use `useLocation()` from react-router-dom for the key (already likely available in AppLayout)
       - Remove the old `animate-page-enter` CSS class from the wrapper div
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Navigating between pages should show a crossfade/slide transition instead of instant swap</manual>
  </verify>
  <done>THEME_METADATA constant exists with 12 classic theme entries containing accurate color values. AppLayout uses AnimatePresence with pageTransition variants for route changes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `import { AnimatedCounter } from '@renderer/components/ui'` resolves
- `import { THEME_METADATA } from '@renderer/lib/theme-metadata'` resolves and returns 12+ entries
- AppLayout.tsx contains `AnimatePresence` and `pageTransition`
- GlassTab.tsx accepts `orientation` prop
</verification>

<success_criteria>
- AnimatedCounter relocated with backward-compatible re-export
- GlassTab supports vertical orientation with left-bar active indicator
- Theme metadata module provides color data for all 12 classic themes
- Page transitions use framer-motion AnimatePresence (not CSS keyframes)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages-migration/03-01-SUMMARY.md`
</output>
