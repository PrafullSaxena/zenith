---
phase: 03-core-pages-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/renderer/src/components/Sidebar.tsx
  - src/renderer/src/components/about/AboutView.tsx
  - src/renderer/src/components/settings/SettingsLayout.tsx
  - src/renderer/src/components/settings/GeneralSettings.tsx
autonomous: true
requirements:
  - CORE-03
  - CORE-04
  - CORE-05

must_haves:
  truths:
    - "Sidebar icons show scale(1.08) + accent glow ring on hover"
    - "Sidebar active indicator bar slides vertically between icons with spring animation"
    - "Sidebar tooltips appear after 400ms delay with translateX fade-in and glass styling"
    - "About View capability cards are GlassCard interactive with staggered entrance"
    - "About View getting-started section uses glass timeline with numbered step indicators"
    - "Settings sidebar uses GlassTab vertical orientation with sliding left-bar indicator"
    - "Settings theme selector is a 3-column grid of GlassCards with color dots and active glow"
  artifacts:
    - path: "src/renderer/src/components/Sidebar.tsx"
      provides: "Glass-upgraded sidebar with hover glow, active bar slide, glass tooltips"
      contains: "layoutId"
    - path: "src/renderer/src/components/about/AboutView.tsx"
      provides: "Glass-styled About page with GlassCard capabilities and glass timeline"
      contains: "GlassCard"
    - path: "src/renderer/src/components/settings/SettingsLayout.tsx"
      provides: "Settings with GlassTab vertical sidebar navigation"
      contains: "GlassTab"
    - path: "src/renderer/src/components/settings/GeneralSettings.tsx"
      provides: "Theme selector grid with GlassCard theme cards"
      contains: "THEME_METADATA"
  key_links:
    - from: "src/renderer/src/components/Sidebar.tsx"
      to: "framer-motion"
      via: "LayoutGroup + layoutId for active bar sliding"
      pattern: "LayoutGroup|sidebarActiveBar"
    - from: "src/renderer/src/components/settings/GeneralSettings.tsx"
      to: "src/renderer/src/lib/theme-metadata.ts"
      via: "THEME_METADATA import for grid cards"
      pattern: "THEME_METADATA|getClassicThemes"
    - from: "src/renderer/src/components/settings/SettingsLayout.tsx"
      to: "@renderer/components/ui"
      via: "GlassTab vertical orientation"
      pattern: "orientation.*vertical"
---

<objective>
Migrate the Sidebar, About View, and Settings pages to glass components — completing all 5 core page migration targets.

Purpose: Sidebar touches every page (it's the app shell), About View showcases capabilities, and Settings contains the new theme selector grid that users will interact with to explore themes in Phase 5. This plan delivers the remaining 3 of 5 migration targets.

Output: Sidebar with hover glow + sliding active bar, About View with glass cards + timeline, Settings with vertical GlassTab navigation + visual theme selector grid.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-pages-migration/03-RESEARCH.md
@.planning/phases/03-core-pages-migration/03-01-SUMMARY.md

@src/renderer/src/components/Sidebar.tsx
@src/renderer/src/components/about/AboutView.tsx
@src/renderer/src/components/settings/SettingsLayout.tsx
@src/renderer/src/components/settings/GeneralSettings.tsx
@src/renderer/src/components/ui/GlassTab.tsx
@src/renderer/src/lib/theme-metadata.ts
@src/renderer/src/lib/motion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Sidebar with hover glow, active bar slide, and glass tooltips</name>
  <files>
    src/renderer/src/components/Sidebar.tsx
  </files>
  <action>
    **Sidebar icon hover effect:**
    - Add `scale(1.08)` + accent glow ring on hover to each SidebarIcon
    - Use CSS transition (150ms smooth): `transition-all duration-150 hover:scale-[1.08] hover:shadow-[0_0_12px_var(--color-accent)/20]`
    - Keep sidebar background opaque/solid dark per user decision — NO glass treatment on the sidebar background itself

    **Active indicator bar with layoutId:**
    - Import `motion, LayoutGroup` from 'framer-motion'
    - Wrap the entire sidebar nav section in `<LayoutGroup>`
    - For each SidebarIcon, when `isActive` is true, render: `<motion.div layoutId="sidebarActiveBar" className="absolute left-0 h-6 w-[3px] rounded-r bg-accent" transition={{ type: 'spring', stiffness: 500, damping: 30 }} />`
    - Remove the existing CSS-transition active indicator div and replace with the motion.div above
    - IMPORTANT: Use layoutId="sidebarActiveBar" (NOT "activeTab") to avoid conflict with GlassTab's layoutId
    - The SidebarIcon container needs `position: relative` for absolute positioning of the bar

    **Glass-styled tooltips with delay:**
    - Replace the existing tooltip `<span>` with a CSS-only glass tooltip:
      `<span className="pointer-events-none absolute left-full z-50 ml-2 whitespace-nowrap rounded-lg bg-[var(--glass-bg)] border border-[var(--glass-border)] px-2.5 py-1 text-xs text-text-primary opacity-0 shadow-lg backdrop-blur-sm transition-all duration-150 delay-[400ms] translate-x-0 group-hover:opacity-100 group-hover:translate-x-1">`
    - The 400ms delay before appearing is handled by `delay-[400ms]` on the transition
    - The translateX(4px) entrance is via `translate-x-0 group-hover:translate-x-1`

    **Preserve existing behavior:**
    - Keep existing framer-motion Reorder drag behavior completely unchanged
    - Keep the existing expand/collapse sidebar toggle
    - Keep existing icon rendering and NavLink routing
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Sidebar icons should show scale + glow on hover, the active bar should slide between icons on navigation, and tooltips should appear after 400ms with a slide-in effect</manual>
  </verify>
  <done>Sidebar icons have scale(1.08) + accent glow ring hover. Active indicator slides between icons via framer-motion layoutId spring animation. Tooltips have 400ms delay, fade + translateX entrance, and glass styling. Drag reorder preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate About View and Settings to glass components with theme selector grid</name>
  <files>
    src/renderer/src/components/about/AboutView.tsx
    src/renderer/src/components/settings/SettingsLayout.tsx
    src/renderer/src/components/settings/GeneralSettings.tsx
  </files>
  <action>
    **AboutView.tsx:**
    - **Capability cards:** Replace div cards with `<GlassCard variant="interactive">` — there are 6 capability items. Wrap the grid in `<motion.div variants={staggerContainer} initial="hidden" animate="visible">` and each card in `<motion.div variants={staggerItem}>`
    - **Getting started section:** Convert the getting-started list items into a glass timeline:
      - Each step wrapped in `<GlassCard className="relative pl-10">` with a numbered step indicator: `<div className="absolute left-3 top-3 flex h-6 w-6 items-center justify-center rounded-full bg-accent text-xs font-bold text-white">{stepNumber}</div>`
      - Vertical connecting line between steps: `<div className="absolute left-[21px] top-9 bottom-0 w-px bg-border/40" />` (on all but the last item)
    - **Author section:** Wrap in `<GlassCard>`. Convert social link anchors to `<GlassButton variant="ghost" size="sm">` with the appropriate icons
    - **Header section:** Wrap the top hero/header area in `<GlassSurface>` for consistency
    - Import GlassCard, GlassSurface, GlassButton from `@renderer/components/ui`
    - Import staggerContainer, staggerItem from `@renderer/lib/motion`

    **SettingsLayout.tsx:**
    - Replace the sidebar navigation buttons (currently plain buttons with border-l-2 active indicator) with `<GlassTab orientation="vertical" tabs={settingsTabs} activeTab={activeSection} onTabChange={setActiveSection} />`
    - Map existing section names to the GlassTab `tabs` format: `{ id: string; label: string }[]`
    - The GlassTab vertical mode (from Plan 03-01) renders with flex-col and left-bar active indicator, matching the settings sidebar pattern
    - Remove the old manual button rendering and border-l-2 active styling
    - Keep the right-side content area and its CSS `animate-tab-enter` animation (or replace with a simple fade if desired)

    **GeneralSettings.tsx — Theme Selector Grid (NEW UI):**
    - Import `THEME_METADATA, getClassicThemes, getNewThemes` from `@renderer/lib/theme-metadata`
    - Replace the current `<select>` theme dropdown with a visual grid:
      ```
      <div className="space-y-6">
        <div>
          <p className="text-xs font-semibold uppercase tracking-wider text-text-secondary mb-3">Classic Themes</p>
          <div className="grid grid-cols-3 gap-3">
            {getClassicThemes().map(theme => <ThemeCard key={theme.value} ... />)}
          </div>
        </div>
        {getNewThemes().length > 0 && (
          <div>
            <p className="text-xs font-semibold uppercase tracking-wider text-text-secondary mb-3">New Collection</p>
            <div className="grid grid-cols-3 gap-3">
              {getNewThemes().map(theme => <ThemeCard key={theme.value} ... />)}
            </div>
          </div>
        )}
      </div>
      ```
    - **ThemeCard** (inline or small sub-component):
      - Use `<GlassCard variant={isActive ? 'selected' : 'interactive'} onClick={() => setTheme(theme.value)} className={isActive ? 'border-accent shadow-[0_0_8px_var(--color-accent)/20]' : ''} >`
      - Content: theme name as `<span className="text-sm font-medium">{theme.label}</span>`
      - 4 color dots row: `<div className="mt-2 flex gap-1.5">{theme.colors map to <span className="h-3 w-3 rounded-full" style={{ backgroundColor: color }} />}</div>`
      - Active check icon: `{isActive && <Check size={14} className="absolute top-2 right-2 text-accent" />}`
      - Import Check from lucide-react
    - **Other settings sections:** Wrap each settings group (AI Agents, MCP, Plugin settings) in `<GlassCard>` where they currently use plain div containers
    - Replace any remaining `<input>` form elements with `<GlassInput>` and `<select>` with `<GlassSelect>` where applicable in GeneralSettings
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>About View should show glass capability cards with stagger, glass timeline for getting started, glass author card with button links. Settings should show GlassTab vertical sidebar and 3-column theme grid with color dots and active glow.</manual>
  </verify>
  <done>About View uses GlassCard interactive for capabilities with stagger entrance, glass timeline for getting-started, GlassCard author section with GlassButton social links. Settings uses GlassTab vertical for sidebar navigation. Theme selector is a 3-column grid of GlassCards with color dots, theme name, and accent glow on active theme. Other settings sections wrapped in GlassCard.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Sidebar.tsx contains `layoutId="sidebarActiveBar"` and `LayoutGroup`
- AboutView.tsx contains no inline glass Tailwind patterns — all using GlassCard/GlassSurface
- SettingsLayout.tsx uses GlassTab with orientation="vertical"
- GeneralSettings.tsx uses THEME_METADATA for theme grid (no hardcoded theme lists)
- No remaining `<select>` elements for theme selection in GeneralSettings
</verification>

<success_criteria>
- Sidebar icons have hover glow ring and the active bar slides with spring animation
- Sidebar tooltips have 400ms delay with slide entrance and glass styling
- About View capability cards are GlassCard interactive with stagger entrance
- About View getting-started section shows glass timeline with numbered steps
- Settings sidebar uses GlassTab vertical with sliding left-bar indicator
- Theme selector is a 3-column grid with color dot previews and accent glow on active
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-pages-migration/03-03-SUMMARY.md`
</output>
