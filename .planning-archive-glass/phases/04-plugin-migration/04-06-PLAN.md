---
phase: 04-plugin-migration
plan: 6
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/renderer/src/plugins/nebula/NebulaView.tsx
  - src/renderer/src/plugins/nebula/NoteList.tsx
  - src/renderer/src/plugins/nebula/NoteEditor.tsx
  - src/renderer/src/plugins/nebula/NoteContextMenu.tsx
  - src/renderer/src/plugins/nebula/DrawingCanvas.tsx
  - src/renderer/src/plugins/nebula/VoiceRecorder.tsx
  - src/renderer/src/plugins/nebula/SearchView.tsx
  - src/renderer/src/plugins/nebula/KnowledgeGraph.tsx
  - src/renderer/src/plugins/nebula/DeleteConfirmDialog.tsx
  - src/renderer/src/plugins/nebula/LinkDialog.tsx
  - src/renderer/src/plugins/nebula/FloatingToolbar.tsx
  - src/renderer/src/plugins/nebula/ToastContainer.tsx
autonomous: true
requirements: [PLUG-05]

must_haves:
  truths:
    - "Nebula renders a PluginHeader with BookOpen icon, gradient title, and GlassTab bar (Notes, Search, Knowledge tabs)"
    - "Note list sidebar shows compact GlassCard per note with accent left bar on active note"
    - "Editor wrapper uses GlassSurface around Tiptap editor WITHOUT modifying editor internals"
    - "Drawing canvas panel uses GlassCard wrapper with GlassResizeHandle for panel resize"
    - "Voice recorder FAB is a circular GlassButton with accent glow when recording"
    - "Search results show GlassCard per result with title, snippet highlight, and relevance GlassBadge"
    - "All loading states use GlassSkeleton, empty states use EmptyState"
    - "Knowledge graph is left unchanged (Phase 6 handles 3D upgrade)"
  artifacts:
    - path: "src/renderer/src/plugins/nebula/NebulaView.tsx"
      provides: "Main view with PluginHeader"
      min_lines: 40
    - path: "src/renderer/src/plugins/nebula/NoteList.tsx"
      provides: "Glass note list with active indicator"
      min_lines: 30
  key_links:
    - from: "src/renderer/src/plugins/nebula/NebulaView.tsx"
      to: "PluginHeader"
      via: "import from ui/"
      pattern: "import.*PluginHeader.*from.*ui"
    - from: "src/renderer/src/plugins/nebula/DrawingCanvas.tsx"
      to: "GlassResizeHandle"
      via: "import from ui/"
      pattern: "import.*GlassResizeHandle.*from.*ui"
---

<objective>
Migrate all Nebula component files to the Obsidian Glass design system. Critical constraint: do NOT touch Tiptap editor or tldraw canvas internals — only wrap their containers. Knowledge graph is unchanged (Phase 6).

Purpose: Nebula is the note-taking plugin — glass styling elevates the writing experience without disrupting the editor.
Output: All 12 Nebula files migrated with glass components.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-migration/04-00-SUMMARY.md
@.planning/phases/04-plugin-migration/04-01-SUMMARY.md
@src/renderer/src/plugins/nebula/NebulaView.tsx
@src/renderer/src/plugins/nebula/NoteList.tsx
@src/renderer/src/plugins/nebula/NoteEditor.tsx
@src/renderer/src/plugins/nebula/DrawingCanvas.tsx
@src/renderer/src/plugins/nebula/VoiceRecorder.tsx
@src/renderer/src/plugins/nebula/SearchView.tsx
@src/renderer/src/stores/nebula-store.ts
@src/renderer/src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate NebulaView, NoteList, NoteEditor, DrawingCanvas, and VoiceRecorder</name>
  <files>
    src/renderer/src/plugins/nebula/NebulaView.tsx
    src/renderer/src/plugins/nebula/NoteList.tsx
    src/renderer/src/plugins/nebula/NoteEditor.tsx
    src/renderer/src/plugins/nebula/DrawingCanvas.tsx
    src/renderer/src/plugins/nebula/VoiceRecorder.tsx
    src/renderer/src/plugins/nebula/FloatingToolbar.tsx
  </files>
  <action>
**NebulaView.tsx:**
- Import `PluginHeader` from `@renderer/components/ui`. Import `BookOpen` from `lucide-react`.
- Replace header + custom tab bar with `<PluginHeader icon={BookOpen} title="Nebula" tabs={[{ id: 'notes', label: 'Notes', icon: FileText }, { id: 'search', label: 'Search', icon: Search }, { id: 'knowledge', label: 'Knowledge', icon: Network }]} activeTab={activeTab} onTabChange={setActiveTab} />`.
- Replace `animate-tab-enter` CSS class with framer-motion `AnimatePresence` + `pageTransition` variants.
- Notes tab layout: keep existing sidebar (NoteList) + editor split. Wrap the split in appropriate containers.
- Preserve ALL store connections, sidebar collapse, and routing logic.

**NoteList.tsx:**
- Import `GlassCard, GlassBadge, GlassSkeleton, EmptyState` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Wrap list in `motion.div variants={staggerContainer} initial="hidden" animate="visible"`.
- Each note: `<motion.div variants={staggerItem}><GlassCard variant="interactive" className="cursor-pointer p-3">` containing:
  - Note title (truncated), preview text in `text-xs text-text-secondary line-clamp-2`, last modified timestamp in `text-xs text-text-secondary/60`.
  - Tags: row of `GlassBadge variant="accent"` pills (compact size).
  - Active note: add `border-l-2 border-accent` to the card (accent left bar).
- Loading: `GlassSkeleton variant="card"` (3 skeleton cards).
- Empty: `<EmptyState icon={FileText} title="No notes yet" description="Create your first note" actionLabel="New Note" onAction={createNote} />`
- Preserve all note selection, right-click context, and drag-reorder logic.

**NoteEditor.tsx:**
- Wrap the Tiptap editor area in `<GlassSurface className="flex-1 overflow-hidden">`.
- Do NOT modify Tiptap editor initialization, extensions, content handling, or any internal rendering.
- Only change the outer container wrapper.

**DrawingCanvas.tsx:**
- Import `GlassCard, GlassResizeHandle, GlassButton` from `@renderer/components/ui`.
- Wrap the tldraw canvas in `<GlassCard className="overflow-hidden p-0">`.
- Between editor and drawing panel, use `<GlassResizeHandle onResize={...}>` for panel resizing.
- Toggle button to show/hide drawing: circular `GlassButton variant="ghost"` with Pencil icon.
- Do NOT modify tldraw canvas internals, tools, or event handling.

**VoiceRecorder.tsx:**
- Import `GlassButton` from `@renderer/components/ui`.
- Replace the FAB button with a circular `GlassButton` using custom className for circular shape: `className="rounded-full w-12 h-12 p-0 flex items-center justify-center"`.
- When recording: add accent glow with `shadow-[0_0_20px_rgba(var(--color-accent-rgb),0.4)]` and a subtle scale-up.
- Keep equalizer animation unchanged.
- Preserve all recording state, audio handling, and transcription logic.

**FloatingToolbar.tsx:**
- Wrap in `GlassSurface className="rounded-xl"` for the toolbar container.
- Keep all toolbar buttons and their handlers unchanged. Style them with glass appearance if currently plain.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Nebula shows PluginHeader, note list has interactive GlassCards with accent bar, editor wrapped in GlassSurface</manual>
  </verify>
  <done>NebulaView uses PluginHeader. NoteList shows compact GlassCards with accent left bar and tags. NoteEditor wrapped in GlassSurface (Tiptap untouched). DrawingCanvas in GlassCard with resize handle (tldraw untouched). VoiceRecorder is circular GlassButton with recording glow.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate SearchView, KnowledgeGraph wrapper, dialogs, and remaining components</name>
  <files>
    src/renderer/src/plugins/nebula/SearchView.tsx
    src/renderer/src/plugins/nebula/KnowledgeGraph.tsx
    src/renderer/src/plugins/nebula/DeleteConfirmDialog.tsx
    src/renderer/src/plugins/nebula/LinkDialog.tsx
    src/renderer/src/plugins/nebula/NoteContextMenu.tsx
    src/renderer/src/plugins/nebula/ToastContainer.tsx
  </files>
  <action>
**SearchView.tsx:**
- Import `GlassCard, GlassInput, GlassBadge, GlassSkeleton, EmptyState` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Search bar: `GlassInput` with search icon, full width at top.
- Results list: `motion.div variants={staggerContainer}` wrapping result cards.
- Each result: `<motion.div variants={staggerItem}><GlassCard variant="interactive" className="cursor-pointer">` containing:
  - Note title, snippet with highlighted match terms (keep existing highlight logic), relevance score as `GlassBadge`.
  - Tags as `GlassBadge variant="accent"` pills.
- Loading: `GlassSkeleton variant="card"` (3 skeleton cards).
- Empty/no results: `<EmptyState icon={Search} title="No results found" description="Try a different search term" />`
- Preserve all search logic, debounce, and result selection.

**KnowledgeGraph.tsx:**
- MINIMAL changes. This component is upgraded in Phase 6.
- Only wrap the outer container in `GlassSurface` if not already styled.
- Keep ALL graph rendering, force simulation, node interaction, and 3D logic completely unchanged.
- If there's a toolbar, style it with `GlassSurface` + `GlassButton` for controls.

**DeleteConfirmDialog.tsx:**
- Replace custom modal/overlay with `GlassModal isOpen={...} onClose={...} title="Delete Note"`.
- Replace buttons with `GlassButton` (danger for delete, ghost for cancel).
- Preserve confirmation logic.

**LinkDialog.tsx:**
- Replace custom modal/overlay with `GlassModal isOpen={...} onClose={...} title="Insert Link"`.
- Replace inputs with `GlassInput`.
- Replace buttons with `GlassButton` (primary for insert, ghost for cancel).
- Preserve link creation logic.

**NoteContextMenu.tsx:**
- Style the context menu container with glass appearance: `bg-surface-elevated/80 backdrop-blur-xl border border-white/[0.08] rounded-xl shadow-lg`. Menu items get `hover:bg-white/[0.06] transition-colors rounded-lg px-3 py-2`.
- Preserve all menu items, icons, and handlers.

**ToastContainer.tsx:**
- If Nebula has its own toast implementation, migrate to use the shared `GlassToast` system from `@renderer/components/ui` and the toast store. If it already delegates to the shared system, leave it.
- Preserve all toast trigger logic.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Search shows GlassInput + stagger results, dialogs use GlassModal, knowledge graph minimally wrapped</manual>
  </verify>
  <done>SearchView uses GlassInput search bar with stagger result GlassCards. KnowledgeGraph minimally wrapped (Phase 6 does full upgrade). Dialogs use GlassModal. Context menu has glass styling. Toast delegated to shared system. All loading/empty states handled.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- No `animate-tab-enter` CSS class references remain in Nebula files
- Tiptap editor and tldraw canvas internals are NOT modified (only outer wrappers changed)
- Knowledge graph logic is unchanged (only container styling if needed)
- All 12 files use shared ui/ components where applicable
- All loading states use GlassSkeleton
- All empty states use EmptyState with context-specific CTAs
</verification>

<success_criteria>
- Nebula is visually consistent with all other migrated plugins
- No inline glass patterns — all use shared ui/ components
- Tiptap editor, tldraw canvas, and knowledge graph internals completely untouched
- VoiceRecorder has circular glass FAB with recording glow
- Note list shows active indicator accent bar
- All existing note creation, editing, search, and voice recording functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-migration/04-06-SUMMARY.md`
</output>
