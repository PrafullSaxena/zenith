---
phase: 04-plugin-migration
plan: 4
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
  - src/renderer/src/plugins/db-inspector/ConnectionManager.tsx
  - src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx
  - src/renderer/src/plugins/db-inspector/QueryConsole.tsx
  - src/renderer/src/plugins/db-inspector/QueryTab.tsx
  - src/renderer/src/plugins/db-inspector/ResultsGrid.tsx
  - src/renderer/src/plugins/db-inspector/AskAI.tsx
  - src/renderer/src/plugins/db-inspector/QueryOptimizer.tsx
  - src/renderer/src/plugins/db-inspector/ERDiagram.tsx
  - src/renderer/src/plugins/db-inspector/DbHistory.tsx
  - src/renderer/src/plugins/db-inspector/SavedQueriesPanel.tsx
  - src/renderer/src/plugins/db-inspector/CellModal.tsx
autonomous: true
requirements: [PLUG-03]

must_haves:
  truths:
    - "DbInspector renders a PluginHeader with Database icon, gradient title, and GlassTab bar with icons+labels (Console, Ask AI, Optimizer, ER Diagram, History)"
    - "Connection manager uses GlassSelect with status dot indicator"
    - "Schema explorer uses GlassSurface container with hover-highlighted tree items"
    - "Query results use GlassTable with sortable columns and striped rows"
    - "Ask AI uses GlassChat component for the AI chat interface"
    - "Query optimizer shows stacked GlassCards per section with stagger entrance"
    - "All loading states use GlassSkeleton, empty states use EmptyState"
  artifacts:
    - path: "src/renderer/src/plugins/db-inspector/DbInspectorView.tsx"
      provides: "Main view with PluginHeader"
      min_lines: 40
    - path: "src/renderer/src/plugins/db-inspector/ResultsGrid.tsx"
      provides: "Glass table results display"
      min_lines: 30
  key_links:
    - from: "src/renderer/src/plugins/db-inspector/DbInspectorView.tsx"
      to: "PluginHeader"
      via: "import from ui/"
      pattern: "import.*PluginHeader.*from.*ui"
    - from: "src/renderer/src/plugins/db-inspector/AskAI.tsx"
      to: "GlassChat"
      via: "import from ui/"
      pattern: "import.*GlassChat.*from.*ui"
    - from: "src/renderer/src/plugins/db-inspector/ResultsGrid.tsx"
      to: "GlassTable"
      via: "import from ui/"
      pattern: "import.*GlassTable.*from.*ui"
---

<objective>
Migrate all DbInspector component files to the Obsidian Glass design system. DbInspector is the most tab-heavy plugin (5 tabs) with a collapsible left panel. Replace all custom styling with shared glass components. Replace Ask AI with GlassChat. Replace query results with GlassTable.

Purpose: DbInspector is a power-user tool — glass styling makes data exploration feel premium without sacrificing density.
Output: All 12 DbInspector files migrated with glass components.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-migration/04-00-SUMMARY.md
@.planning/phases/04-plugin-migration/04-01-SUMMARY.md
@src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
@src/renderer/src/plugins/db-inspector/ConnectionManager.tsx
@src/renderer/src/plugins/db-inspector/QueryConsole.tsx
@src/renderer/src/plugins/db-inspector/AskAI.tsx
@src/renderer/src/plugins/db-inspector/ResultsGrid.tsx
@src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx
@src/renderer/src/stores/db-store.ts
@src/renderer/src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DbInspectorView, ConnectionManager, SchemaExplorer, and QueryConsole</name>
  <files>
    src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
    src/renderer/src/plugins/db-inspector/ConnectionManager.tsx
    src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx
    src/renderer/src/plugins/db-inspector/QueryConsole.tsx
    src/renderer/src/plugins/db-inspector/QueryTab.tsx
    src/renderer/src/plugins/db-inspector/ResultsGrid.tsx
  </files>
  <action>
**DbInspectorView.tsx:**
- Import `PluginHeader` from `@renderer/components/ui`. Import `Database` from `lucide-react`.
- Replace header + custom tab bar with `<PluginHeader icon={Database} title="DB Inspector" tabs={[{ id: 'console', label: 'Console', icon: Terminal }, { id: 'ask-ai', label: 'Ask AI', icon: MessageSquare }, { id: 'optimizer', label: 'Optimizer', icon: Zap }, { id: 'er-diagram', label: 'ER Diagram', icon: Network }, { id: 'history', label: 'History', icon: Clock }]} activeTab={activeTab} onTabChange={setActiveTab} statusIndicator={connectionBadge} />`.
- Connection status: `GlassBadge variant="success"` (Connected) or `GlassBadge variant="error"` (Disconnected) with pulsing animation when connecting (`className="animate-pulse"`).
- Layout: Keep existing collapsible left panel (SchemaExplorer) + right content area structure.
- Left panel: wrap SchemaExplorer in `GlassSurface className="h-full overflow-hidden"`.
- Replace `animate-tab-enter` CSS class with framer-motion `AnimatePresence` + `pageTransition` variants for tab content switching.
- Preserve ALL store connections, panel collapse logic, and keyboard shortcuts.

**ConnectionManager.tsx:**
- Import `GlassSelect, GlassBadge, GlassButton` from `@renderer/components/ui`.
- Replace connection dropdown with `GlassSelect`. Add a custom render for the selected option showing a status dot (green/red circle) next to the connection name.
- Replace connect/disconnect buttons with `GlassButton` (primary for connect, danger for disconnect).
- Preserve all connection state, retry logic, and error handling.

**SchemaExplorer.tsx:**
- Wrap in `GlassSurface` (already done from parent).
- Tree items: each item gets `hover:bg-white/[0.04] transition-colors rounded-lg px-2 py-1` on hover.
- Indent lines: thin `border-l border-white/[0.06]` vertical lines.
- Expand/collapse: keep existing animation, add smooth `transition-all duration-200`.
- If schema is loading, show `GlassSkeleton variant="text" lines={8}`.
- If no connection, show `EmptyState icon={Database} title="Connect a database" description="Select a connection to explore schema"`.

**QueryConsole.tsx + QueryTab.tsx:**
- Wrap query editor area in `GlassCard className="overflow-hidden p-0"`. Do NOT touch SqlEditor/CodeMirror internals.
- Query action bar: `GlassSurface` with `GlassButton` for Run, Format, Save. Run button uses `variant="primary"`.
- Tab management for multiple queries: use existing tab system but style tab headers with glass appearance (subtle glass bg, active accent indicator).

**ResultsGrid.tsx:**
- Import `GlassTable` from `@renderer/components/ui`.
- Replace custom table rendering with `<GlassTable columns={...} data={...} onSort={...} sortKey={...} sortDirection={...} emptyMessage="Run a query to see results" />`.
- Map existing column definitions to GlassTable's `columns` prop format.
- If results are loading, show `GlassSkeleton variant="table"`.
- Preserve all cell rendering, copy functionality, and CellModal trigger logic.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>DbInspector shows PluginHeader with 5 tabs, glass schema explorer, GlassTable results</manual>
  </verify>
  <done>DbInspectorView uses PluginHeader with 5 icon+label tabs. ConnectionManager uses GlassSelect with status indicator. SchemaExplorer has hover-highlighted tree items in GlassSurface. QueryConsole wraps editor in GlassCard. ResultsGrid uses GlassTable.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AskAI, QueryOptimizer, ERDiagram, DbHistory, and remaining components</name>
  <files>
    src/renderer/src/plugins/db-inspector/AskAI.tsx
    src/renderer/src/plugins/db-inspector/QueryOptimizer.tsx
    src/renderer/src/plugins/db-inspector/ERDiagram.tsx
    src/renderer/src/plugins/db-inspector/DbHistory.tsx
    src/renderer/src/plugins/db-inspector/SavedQueriesPanel.tsx
    src/renderer/src/plugins/db-inspector/CellModal.tsx
  </files>
  <action>
**AskAI.tsx:**
- Import `GlassChat` from `@renderer/components/ui`.
- Replace custom chat UI with `<GlassChat messages={...} onSend={...} isStreaming={...} placeholder="Ask about your database..." renderContent={markdownRenderer} />`.
- Map existing message data to GlassChat's message format. Keep all store connections and streaming logic.
- If no connection established, show `EmptyState` instead of chat.

**QueryOptimizer.tsx:**
- Import `GlassCard, GlassSkeleton, EmptyState, GlassBadge` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Stacked sections: EXPLAIN analysis, suggestions, tradeoffs, optimized query — each in a `GlassCard`.
- Wrap all sections in stagger container for entrance animation.
- Each suggestion: collapsible `GlassCard` with toggle (use simple state toggle — expand/collapse with `AnimatePresence` height animation).
- Severity indicators: `GlassBadge` (error for critical issues, warning for suggestions, info for notes).
- Loading: `GlassSkeleton variant="card"` (4 skeleton cards stacked).
- Empty: `<EmptyState icon={Zap} title="No query to optimize" description="Run a query first, then optimize it here" />`

**ERDiagram.tsx:**
- Glass split view: `GlassCard` for the diagram viewer area, `GlassSurface` for the syntax editor area.
- Zoom controls: `GlassButton` (ghost variant, icon-only) for zoom in/out/reset.
- Table selector: `GlassSelect` for filtering which tables to show.
- Do NOT modify MermaidRenderer internals.
- Preserve all diagram rendering, zoom state, and table selection logic.

**DbHistory.tsx:**
- Import `GlassCard, GlassSkeleton, EmptyState` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Each history entry: `GlassCard variant="interactive"` with query preview, timestamp, execution time, and row count `GlassBadge`.
- Stagger entrance animation.
- Loading: `GlassSkeleton variant="card"`.
- Empty: `<EmptyState icon={Clock} title="No query history" description="Executed queries will appear here" />`

**SavedQueriesPanel.tsx:**
- Wrap in `GlassSurface`. Each saved query: `GlassCard variant="interactive"` with name, preview, and action buttons (`GlassButton` ghost for edit/delete).
- Empty: `<EmptyState icon={Save} title="No saved queries" description="Save a query to access it later" />`

**CellModal.tsx:**
- Replace custom modal with `GlassModal`. Keep cell content display logic unchanged.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>AskAI uses GlassChat, QueryOptimizer has stacked GlassCards, ERDiagram has glass split view</manual>
  </verify>
  <done>AskAI uses GlassChat. QueryOptimizer shows stacked collapsible GlassCards with severity badges. ERDiagram has glass split view. DbHistory and SavedQueriesPanel have stagger GlassCards. CellModal uses GlassModal. All loading/empty states handled.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- No `animate-tab-enter` CSS class references remain in DbInspector files
- All 12+ files use shared ui/ components
- Ask AI uses GlassChat component
- Query results use GlassTable component
- All loading states use GlassSkeleton
- All empty states use EmptyState with context-specific CTAs
</verification>

<success_criteria>
- DbInspector is visually consistent with all other migrated plugins
- No inline glass patterns — all use shared ui/ components
- SqlEditor/CodeMirror internals untouched
- MermaidRenderer internals untouched
- All 5 tab views fully migrated with glass components
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-migration/04-04-SUMMARY.md`
</output>
