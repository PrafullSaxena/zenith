---
phase: 04-plugin-migration
plan: 3
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
  - src/renderer/src/plugins/code-review-bot/PRList.tsx
  - src/renderer/src/plugins/code-review-bot/PRDiffView.tsx
  - src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx
  - src/renderer/src/plugins/code-review-bot/ReviewHistory.tsx
  - src/renderer/src/plugins/code-review-bot/SettingsPanel.tsx
autonomous: true
requirements: [PLUG-02]

must_haves:
  truths:
    - "CodeReviewBot renders a PluginHeader with GitPullRequest icon, gradient title, and GlassTab bar (Diff, Review, History tabs)"
    - "PR list shows GlassCard per PR with hover lift and accent left bar on selected"
    - "Review comments render as GlassCard entries with GlassBadge severity indicators"
    - "Diff viewer is wrapped in GlassCard without modifying syntax highlighting internals"
    - "Settings panel uses GlassCard sections with GlassInput/GlassSelect"
    - "Loading states use GlassSkeleton, empty PR list shows EmptyState"
  artifacts:
    - path: "src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx"
      provides: "Main view with PluginHeader"
      min_lines: 40
    - path: "src/renderer/src/plugins/code-review-bot/PRList.tsx"
      provides: "Glass PR list with interactive cards"
      min_lines: 30
  key_links:
    - from: "src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx"
      to: "PluginHeader"
      via: "import from ui/"
      pattern: "import.*PluginHeader.*from.*ui"
---

<objective>
Migrate all 6 CodeReviewBot files to the Obsidian Glass design system. Replace custom tab bars, plain divs, and inline styles with shared glass components.

Purpose: CodeReviewBot's PR list and review comments are high-visibility screens — glass styling makes code review feel polished.
Output: All 6 CodeReviewBot files migrated with glass components, loading states, and empty states.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-migration/04-00-SUMMARY.md
@.planning/phases/04-plugin-migration/04-01-SUMMARY.md
@src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
@src/renderer/src/plugins/code-review-bot/PRList.tsx
@src/renderer/src/plugins/code-review-bot/PRDiffView.tsx
@src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx
@src/renderer/src/plugins/code-review-bot/ReviewHistory.tsx
@src/renderer/src/plugins/code-review-bot/SettingsPanel.tsx
@src/renderer/src/stores/review-store.ts
@src/renderer/src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CodeReviewBotView with PluginHeader and PRList with glass cards</name>
  <files>
    src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
    src/renderer/src/plugins/code-review-bot/PRList.tsx
    src/renderer/src/plugins/code-review-bot/PRDiffView.tsx
  </files>
  <action>
**CodeReviewBotView.tsx:**
- Import `PluginHeader` from `@renderer/components/ui`. Import `GitPullRequest` from `lucide-react`.
- Replace existing header + custom tab bar with `<PluginHeader icon={GitPullRequest} title="Code Review" tabs={[{ id: 'diff', label: 'Diff' }, { id: 'review', label: 'Review' }, { id: 'history', label: 'History' }]} activeTab={activeTab} onTabChange={setActiveTab} statusIndicator={connectionBadge} />`.
- The status indicator on the right should be a `GlassBadge` showing connection status (green "Connected" or red "Disconnected").
- Replace the existing header area with a `GlassSurface` containing the `GlassSelect` repo selector and settings `GlassButton`.
- Add `AnimatePresence mode="wait"` around tab content with `motion.div` keyed by `activeTab` using `pageTransition` variants.
- Replace any `animate-tab-enter` CSS class usage with framer-motion page transitions.
- Preserve ALL store connections, routing logic, and state management.

**PRList.tsx:**
- Import `GlassCard, GlassBadge, GlassSkeleton, EmptyState` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Wrap the PR list in `motion.div variants={staggerContainer} initial="hidden" animate="visible"`.
- Each PR entry: `<motion.div variants={staggerItem}><GlassCard variant="interactive" className="cursor-pointer">` containing:
  - Author avatar (existing), PR title, file count, and `GlassBadge` for PR status (open/merged/declined).
  - Selected PR: add `border-l-2 border-accent` class to the GlassCard (or use `variant="selected"` if applicable).
- Loading state: `<GlassSkeleton variant="card" />` (3 skeleton cards).
- Empty state: `<EmptyState icon={GitPullRequest} title="No pull requests" description="Connect to Bitbucket to see PRs" actionLabel="Connect" onAction={openSettings} />`
- Preserve ALL click handlers, selection logic, and data fetching.

**PRDiffView.tsx:**
- Wrap the entire diff viewer in `<GlassCard className="overflow-hidden p-0">`.
- Do NOT modify diff syntax highlighting, line numbers, or any internal diff rendering logic.
- Add a `GlassSurface` toolbar above the diff with file name and any existing controls.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>CodeReviewBot shows PluginHeader with GlassTab, PR list has interactive GlassCards with stagger</manual>
  </verify>
  <done>CodeReviewBotView uses PluginHeader with gradient title and connection status badge. PRList shows GlassCards with stagger animation, skeleton loading, and empty state. PRDiffView wrapped in GlassCard preserving diff internals.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ReviewPanel, ReviewHistory, and SettingsPanel with glass components</name>
  <files>
    src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx
    src/renderer/src/plugins/code-review-bot/ReviewHistory.tsx
    src/renderer/src/plugins/code-review-bot/SettingsPanel.tsx
  </files>
  <action>
**ReviewPanel.tsx:**
- Import `GlassCard, GlassBadge, GlassSkeleton` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Each review comment: `GlassCard` with severity `GlassBadge` (error=red for critical, warning=yellow for suggestion, info=blue for note).
- Wrap comments list in stagger container.
- If review is in progress/streaming, show `GlassSkeleton variant="text" lines={3}` as placeholder.
- Preserve all comment data rendering, inline diff annotations, and action handlers.

**ReviewHistory.tsx:**
- Import `GlassCard, GlassSkeleton, EmptyState` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Each history entry: `GlassCard variant="interactive"` with review title, date in `text-xs text-text-secondary`, file count, and a GlassBadge for review score/status.
- Stagger entrance animation on the list.
- Loading: `GlassSkeleton variant="card"`.
- Empty: `<EmptyState icon={History} title="No review history" description="Reviews will appear here after completion" />`

**SettingsPanel.tsx:**
- Import `GlassCard, GlassInput, GlassSelect, GlassButton, GlassSurface` from `@renderer/components/ui`.
- Group settings into `GlassCard` sections (e.g., Connection, Review Preferences, Advanced).
- Replace native inputs with `GlassInput` and selects with `GlassSelect`.
- Replace action buttons with `GlassButton` (primary for save/connect, danger for disconnect/reset).
- Preserve all form state, validation, and save handlers.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Review comments show severity badges, history has stagger cards, settings uses glass form elements</manual>
  </verify>
  <done>ReviewPanel shows GlassCard comments with severity GlassBadge. ReviewHistory has staggered GlassCards with empty state. SettingsPanel uses GlassCard sections with GlassInput/GlassSelect/GlassButton. All existing functionality preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- No `animate-tab-enter` CSS class references remain in CodeReviewBot files
- All 6 files use shared ui/ components
- PR list has interactive GlassCards with stagger animation
- Review comments have severity GlassBadge indicators
- All loading states use GlassSkeleton
- Empty states use EmptyState component
</verification>

<success_criteria>
- CodeReviewBot is visually consistent with TextCraft and core pages
- No inline glass patterns — all use shared ui/ components
- Tab switching uses framer-motion page transitions (no animate-tab-enter)
- All existing PR review, diff viewing, and settings functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-migration/04-03-SUMMARY.md`
</output>
