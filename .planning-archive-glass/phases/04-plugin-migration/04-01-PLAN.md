---
phase: 04-plugin-migration
plan: 1
type: execute
wave: 2
depends_on: [04-00]
files_modified:
  - src/renderer/src/plugins/textcraft/TextCraftView.tsx
  - src/renderer/src/plugins/textcraft/InputPanel.tsx
  - src/renderer/src/plugins/textcraft/ControlsPanel.tsx
  - src/renderer/src/plugins/textcraft/OutputPanel.tsx
  - src/renderer/src/plugins/textcraft/HistoryPanel.tsx
autonomous: true
requirements: [PLUG-06]

must_haves:
  truths:
    - "TextCraft renders a PluginHeader with Wand2 icon, gradient title, and GlassTab bar (Refine + History tabs)"
    - "The 3-column layout (Input, Controls, Output) uses GlassCard panels with GlassResizeHandle between them"
    - "Controls panel uses GlassSelect for tone/format and GlassButton primary for the Refine action"
    - "History tab shows GlassCard entries with staggered entrance animation"
    - "Loading state shows GlassSkeleton shimmer instead of bare spinner"
    - "Empty history shows EmptyState with 'Paste text to refine' CTA"
  artifacts:
    - path: "src/renderer/src/plugins/textcraft/TextCraftView.tsx"
      provides: "Main view with PluginHeader and tab routing"
      min_lines: 40
    - path: "src/renderer/src/plugins/textcraft/InputPanel.tsx"
      provides: "Glass-wrapped text input panel"
      min_lines: 20
    - path: "src/renderer/src/plugins/textcraft/ControlsPanel.tsx"
      provides: "Glass controls with GlassSelect and GlassButton"
      min_lines: 30
    - path: "src/renderer/src/plugins/textcraft/OutputPanel.tsx"
      provides: "Glass-wrapped output panel"
      min_lines: 20
    - path: "src/renderer/src/plugins/textcraft/HistoryPanel.tsx"
      provides: "Glass history entries with stagger animation"
      min_lines: 30
  key_links:
    - from: "src/renderer/src/plugins/textcraft/TextCraftView.tsx"
      to: "PluginHeader, GlassTab"
      via: "imports from ui/"
      pattern: "import.*PluginHeader.*from.*ui"
    - from: "src/renderer/src/plugins/textcraft/ControlsPanel.tsx"
      to: "GlassSelect, GlassButton"
      via: "imports from ui/"
      pattern: "import.*GlassSelect.*from.*ui"
---

<objective>
Migrate TextCraft (smallest plugin, 5 files) to the Obsidian Glass design system. This is the Wave 1 validation plugin — establishes the migration pattern for the remaining 5 plugins.

Purpose: Validate the shared glass component migration workflow on the simplest plugin before tackling larger ones.
Output: All 5 TextCraft files migrated to glass components with full UX polish.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-migration/04-00-SUMMARY.md
@src/renderer/src/plugins/textcraft/TextCraftView.tsx
@src/renderer/src/plugins/textcraft/InputPanel.tsx
@src/renderer/src/plugins/textcraft/ControlsPanel.tsx
@src/renderer/src/plugins/textcraft/OutputPanel.tsx
@src/renderer/src/plugins/textcraft/HistoryPanel.tsx
@src/renderer/src/stores/textcraft-store.ts
@src/renderer/src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TextCraftView with PluginHeader and resizable 3-column layout</name>
  <files>
    src/renderer/src/plugins/textcraft/TextCraftView.tsx
    src/renderer/src/plugins/textcraft/InputPanel.tsx
    src/renderer/src/plugins/textcraft/OutputPanel.tsx
  </files>
  <action>
**TextCraftView.tsx** — Full rewrite of the main view:
- Import `PluginHeader` from `@renderer/components/ui`. Import `GlassResizeHandle` from `@renderer/components/ui`. Import `AnimatePresence` and `motion` from `framer-motion`. Import `pageTransition` from `@renderer/lib/motion`. Import `Wand2` from `lucide-react`.
- Define tabs: `[{ id: 'refine', label: 'Refine', icon: Wand2 }, { id: 'history', label: 'History', icon: Clock }]`
- State: `activeTab` (default 'refine'), `leftWidth` (default 33, percentage), `rightWidth` (default 33, percentage). Middle column is `100 - leftWidth - rightWidth`.
- Render:
  1. `PluginHeader` with `icon={Wand2}` `title="TextCraft"` and the tabs/activeTab/onTabChange props
  2. Below header, `AnimatePresence mode="wait"` wrapping tab content with `motion.div` using `pageTransition` variants keyed by `activeTab`
  3. Refine tab content: a `flex flex-1 overflow-hidden` row containing:
     - `InputPanel` at `style={{ width: leftWidth + '%' }}`
     - `GlassResizeHandle onResize={(dx) => setLeftWidth(prev => clamp(prev + dx * 100 / containerWidth, 20, 50))}`
     - `ControlsPanel` at flex-1
     - `GlassResizeHandle onResize={(dx) => setRightWidth(prev => clamp(prev - dx * 100 / containerWidth, 20, 50))}`
     - `OutputPanel` at `style={{ width: rightWidth + '%' }}`
  4. History tab content: `HistoryPanel` component
  - Use a `useRef` on the flex container to get `containerWidth` via `ResizeObserver` or `offsetWidth`.
  - Helper `clamp(value, min, max)` = `Math.min(Math.max(value, min), max)`
- Preserve ALL existing event handlers, store connections, and callback logic from the current implementation.

**InputPanel.tsx** — Wrap in GlassCard:
- Import `GlassCard` from `@renderer/components/ui`
- Replace outer container with `<GlassCard className="flex flex-col h-full overflow-hidden">`. Keep the textarea and all existing handler logic intact.
- Add a small header inside the card: `<div className="text-xs font-medium text-text-secondary uppercase tracking-wider px-1 pb-2">Input</div>`

**OutputPanel.tsx** — Wrap in GlassCard:
- Import `GlassCard` from `@renderer/components/ui`
- Replace outer container with `<GlassCard className="flex flex-col h-full overflow-hidden">`.
- Add header: `<div className="text-xs font-medium text-text-secondary uppercase tracking-wider px-1 pb-2">Output</div>`
- If output is loading/streaming, show `GlassSkeleton variant="text" lines={6}` instead of a spinner.
- If no output yet, show `EmptyState icon={FileText} title="No output yet" description="Paste text and click Refine to see results"` (compact, no action button needed).
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>TextCraft renders with PluginHeader, 3-column resizable layout, glass input/output panels</manual>
  </verify>
  <done>TextCraftView uses PluginHeader with gradient title and GlassTab, 3-column layout has GlassResizeHandle dividers, InputPanel and OutputPanel wrapped in GlassCard with proper loading/empty states</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ControlsPanel and HistoryPanel with glass components</name>
  <files>
    src/renderer/src/plugins/textcraft/ControlsPanel.tsx
    src/renderer/src/plugins/textcraft/HistoryPanel.tsx
  </files>
  <action>
**ControlsPanel.tsx** — Full glass migration:
- Import `GlassCard, GlassSelect, GlassButton` from `@renderer/components/ui`
- Replace outer container with `<GlassCard className="flex flex-col h-full gap-4">`.
- Add header: `<div className="text-xs font-medium text-text-secondary uppercase tracking-wider px-1">Controls</div>`
- Replace any native `<select>` elements with `GlassSelect` for tone and format selectors. Map existing option arrays to GlassSelect's `options` prop format `{ value: string; label: string }`.
- If there's a length/creativity slider, keep it as-is (no GlassSlider component exists) but wrap it with glass-style labels.
- Replace the main Refine/Generate button with `<GlassButton variant="primary" size="lg" className="w-full">`. Add `isLoading` prop to show shimmer state during processing. Preserve the existing onClick handler.
- Preserve ALL existing form state, callbacks, and store connections.

**HistoryPanel.tsx** — Glass migration with stagger animation:
- Import `GlassCard, GlassSkeleton, EmptyState` from `@renderer/components/ui`. Import `motion` from `framer-motion`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`. Import `Clock` from `lucide-react`.
- If history is loading, render `GlassSkeleton variant="card"` (3 skeleton cards stacked).
- If history is empty, render `<EmptyState icon={Clock} title="No history yet" description="Paste text to refine" actionLabel="Go to Refine" onAction={() => { /* switch to refine tab via callback */ }} />`
- If history has entries, render a `motion.div variants={staggerContainer} initial="hidden" animate="visible"` wrapper containing `motion.div variants={staggerItem}` for each entry.
- Each history entry: `<GlassCard variant="interactive" className="cursor-pointer">` containing the entry title, preview text (truncated), timestamp in `text-xs text-text-secondary`, and a GlassBadge showing the tone/format used. On click, load the entry (preserve existing click handler).
- Preserve ALL existing data loading, click handlers, and store interactions.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>ControlsPanel shows GlassSelect dropdowns and primary GlassButton; HistoryPanel shows staggered GlassCards with empty state</manual>
  </verify>
  <done>ControlsPanel uses GlassSelect for all dropdowns and GlassButton for Refine action; HistoryPanel uses staggered GlassCards for entries, GlassSkeleton for loading, and EmptyState for zero-data view. All existing behavior preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- TextCraft has PluginHeader with gradient title and GlassTab (Refine + History)
- 3-column layout uses GlassCard panels with GlassResizeHandle dividers
- All dropdowns use GlassSelect, main action uses GlassButton primary
- Loading states use GlassSkeleton, empty states use EmptyState
- History entries render with stagger animation
- All existing functionality preserved (form submission, history loading, text processing)
</verification>

<success_criteria>
- TextCraft is visually consistent with core pages migrated in Phase 3
- No inline Tailwind glass patterns — all use shared ui/ components
- Tab switching has AnimatePresence page transition
- Loading and empty states are handled with glass components
- Resizable panels work smoothly with drag handles
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-migration/04-01-SUMMARY.md`
</output>
