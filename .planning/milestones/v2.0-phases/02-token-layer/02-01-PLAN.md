---
phase: 02-token-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/ui/button.tsx
  - src/renderer/src/components/ui/card.tsx
  - src/renderer/src/components/ui/input.tsx
  - src/renderer/src/components/ui/label.tsx
  - src/renderer/src/components/ui/select.tsx
  - src/renderer/src/components/ui/textarea.tsx
  - src/renderer/src/components/ui/switch.tsx
  - src/renderer/src/components/ui/badge.tsx
  - src/renderer/src/components/ui/skeleton.tsx
  - src/renderer/src/components/ui/separator.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-06, COMP-09]

must_haves:
  truths:
    - "Button renders with 4 named variants: default (primary), secondary, destructive, ghost — each with zenith-violet token colors"
    - "Card renders with 28px rounded corners, card bg, 1px border"
    - "Input shows primary focus ring and supports error state styling"
    - "Select opens a dropdown with card bg and supports keyboard navigation"
    - "Badge renders as pill with 13% tinted status colors (success, warning, destructive, info)"
    - "Skeleton renders with shimmer animation"
  artifacts:
    - path: "src/renderer/src/components/ui/card.tsx"
      provides: "Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter"
      exports: ["Card", "CardHeader", "CardTitle", "CardDescription", "CardContent", "CardFooter"]
    - path: "src/renderer/src/components/ui/input.tsx"
      provides: "Input component with focus ring"
      exports: ["Input"]
    - path: "src/renderer/src/components/ui/label.tsx"
      provides: "Label component"
      exports: ["Label"]
    - path: "src/renderer/src/components/ui/select.tsx"
      provides: "Select with all subcomponents"
      exports: ["Select", "SelectTrigger", "SelectContent", "SelectItem", "SelectValue"]
    - path: "src/renderer/src/components/ui/badge.tsx"
      provides: "Badge with status variants"
      exports: ["Badge", "badgeVariants"]
    - path: "src/renderer/src/components/ui/skeleton.tsx"
      provides: "Skeleton with shimmer"
      exports: ["Skeleton"]
  key_links:
    - from: "src/renderer/src/components/ui/card.tsx"
      to: "@renderer/lib/utils"
      via: "cn() import"
      pattern: "import.*cn.*from.*@renderer/lib/utils"
    - from: "src/renderer/src/components/ui/badge.tsx"
      to: "CSS custom properties"
      via: "hsl(var(--success)) etc"
      pattern: "success|warning|destructive|info"
---

<objective>
Generate core shadcn form and display components customized with zenith-violet tokens.

Purpose: Provide the primary building blocks (Card, Input, Select, Badge, Skeleton, etc.) that every shared component and screen will consume.
Output: 10 component files in src/renderer/src/components/ui/ ready for import.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@components.json
@src/renderer/src/components/ui/button.tsx
@design-system/zenith/MASTER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate shadcn core components via CLI</name>
  <files>
    src/renderer/src/components/ui/card.tsx
    src/renderer/src/components/ui/input.tsx
    src/renderer/src/components/ui/label.tsx
    src/renderer/src/components/ui/select.tsx
    src/renderer/src/components/ui/textarea.tsx
    src/renderer/src/components/ui/switch.tsx
    src/renderer/src/components/ui/skeleton.tsx
    src/renderer/src/components/ui/separator.tsx
  </files>
  <action>
    Generate shadcn components using the CLI. Run each individually to catch errors:

    ```bash
    npx pnpm dlx shadcn@latest add card --yes --overwrite
    npx pnpm dlx shadcn@latest add input --yes --overwrite
    npx pnpm dlx shadcn@latest add label --yes --overwrite
    npx pnpm dlx shadcn@latest add select --yes --overwrite
    npx pnpm dlx shadcn@latest add textarea --yes --overwrite
    npx pnpm dlx shadcn@latest add switch --yes --overwrite
    npx pnpm dlx shadcn@latest add skeleton --yes --overwrite
    npx pnpm dlx shadcn@latest add separator --yes --overwrite
    ```

    IMPORTANT: shadcn CLI may generate files at wrong paths due to Electron @renderer aliases. After each generation:
    1. Check if file landed at the correct path: `src/renderer/src/components/ui/<name>.tsx`
    2. If it landed at `@renderer/components/ui/` (literal), move it to the correct path (this was encountered in Phase 1)
    3. Verify each file imports cn from `@renderer/lib/utils`

    If the CLI fails entirely, manually create each component following the exact pattern from button.tsx:
    - Import React, cn from @renderer/lib/utils
    - Use cva for variants where applicable
    - Export named component + variants

    After generation, customize each component to match zenith-violet design tokens from MASTER.md:

    **Card**: Ensure rounded-[28px] (--radius-lg) on Card root, p-5 padding on CardContent. The generated shadcn Card should already use bg-card and border-border tokens.

    **Input**: Ensure rounded-2xl (--radius/16px), bg-card background, border-border, focus state with `focus-visible:border-primary focus-visible:ring-2 focus-visible:ring-primary/25`. Add `aria-invalid:border-destructive` for error state.

    **Select**: Ensure SelectTrigger has rounded-2xl, bg-card. SelectContent has bg-card (popover bg). Verify keyboard navigation works (this is built into Radix Select).

    **Skeleton**: Add shimmer animation if not present. The animation should be a gradient moving left-to-right, 1.5s loop, linear easing. Check main.css — if a shimmer @keyframes already exists, use it; otherwise add one.

    **Separator**: Standard shadcn separator, bg-border color.

    **Textarea**: Ensure matches Input styling (rounded-2xl, bg-card, same focus ring).

    **Switch**: Ensure uses primary color when checked.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && ls -la src/renderer/src/components/ui/{card,input,label,select,textarea,switch,skeleton,separator}.tsx && grep -l "cn" src/renderer/src/components/ui/{card,input,label,select,textarea,switch,skeleton,separator}.tsx | wc -l</automated>
    <manual>All 8 files exist and import cn()</manual>
  </verify>
  <done>8 shadcn components generated at correct paths, each importing cn() from @renderer/lib/utils and using HSL token custom properties</done>
</task>

<task type="auto">
  <name>Task 2: Customize Button variants and create Badge with status colors</name>
  <files>
    src/renderer/src/components/ui/button.tsx
    src/renderer/src/components/ui/badge.tsx
  </files>
  <action>
    **Button (COMP-01)**: The current button.tsx was generated by shadcn and has variants: default, destructive, outline, secondary, ghost, link. Per the design spec, we need 4 primary variants: default (primary), secondary, destructive, ghost. The existing file already has these. Customize to match MASTER.md spec exactly:

    - default (primary): `bg-primary text-primary-foreground hover:bg-primary/90 rounded-2xl` (16px radius per spec)
    - secondary: `bg-secondary text-secondary-foreground border border-border hover:bg-secondary/80 rounded-2xl`
    - destructive: `bg-destructive text-white hover:bg-destructive/90 rounded-2xl`
    - ghost: `bg-transparent text-muted-foreground hover:bg-secondary rounded-2xl`

    Update the base class in cva to use `rounded-2xl` instead of `rounded-md`. Keep outline and link variants as-is (useful utilities). Add active:scale-[0.97] transition for press feedback per MASTER.md animation spec (150ms ease-out). Ensure `transition-all duration-150 ease-out` is in base.

    **Badge (COMP-06)**: Generate via CLI first:
    ```bash
    npx pnpm dlx shadcn@latest add badge --yes --overwrite
    ```

    Then customize badge.tsx to add zenith-violet status variants. Use cva pattern matching button.tsx. Required variants:

    - default: `bg-primary/13 text-primary` (violet tint)
    - secondary: `bg-secondary text-secondary-foreground`
    - destructive: `bg-destructive/13 text-destructive`
    - success: `bg-[hsl(var(--success)/0.13)] text-[hsl(var(--success))]`
    - warning: `bg-[hsl(var(--warning)/0.13)] text-[hsl(var(--warning))]`
    - info: `bg-[hsl(var(--info)/0.13)] text-[hsl(var(--info))]`
    - outline: `border border-border text-foreground`

    All badge variants: `rounded-full px-3 py-1 text-xs font-medium` (pill shape per MASTER.md).

    Ensure both files use: `import { cn } from "@renderer/lib/utils"` and follow the exact pattern of the existing button.tsx (React.ComponentProps, data-slot, cva).
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && grep -c "rounded-2xl" src/renderer/src/components/ui/button.tsx && grep -c "success\|warning\|info" src/renderer/src/components/ui/badge.tsx</automated>
    <manual>Button has rounded-2xl in base, Badge has success/warning/info variants</manual>
  </verify>
  <done>Button has 4+ variants with 16px radius and press scale animation. Badge has 7 variants including status colors with 13% tinted backgrounds and pill shape.</done>
</task>

</tasks>

<verification>
- All 10 component files exist in src/renderer/src/components/ui/
- Each file imports cn from @renderer/lib/utils
- Card uses rounded-[28px], Input/Select use rounded-2xl
- Badge has success/warning/info/destructive status variants
- Skeleton has shimmer animation
- TypeScript compiles without errors: `cd src/renderer && npx tsc --noEmit 2>&1 | head -20`
</verification>

<success_criteria>
- COMP-01: Button renders with default, secondary, destructive, ghost variants + press scale
- COMP-02: Card renders with 28px radius, card bg, 1px border
- COMP-03: Input + Label exist with focus ring and error state
- COMP-04: Select opens dropdown with card bg and keyboard nav
- COMP-06: Badge renders pill shape with 6 status color variants
- COMP-09: Skeleton renders with shimmer animation
</success_criteria>

<output>
After completion, create `.planning/phases/02-token-layer/02-01-SUMMARY.md`
</output>
