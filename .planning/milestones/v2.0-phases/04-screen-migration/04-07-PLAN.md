---
phase: 04-screen-migration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/nebula/NebulaView.tsx
  - src/renderer/src/plugins/nebula/NoteList.tsx
  - src/renderer/src/plugins/nebula/NoteEditor.tsx
  - src/renderer/src/plugins/nebula/SearchView.tsx
  - src/renderer/src/plugins/nebula/KnowledgeGraph.tsx
  - src/renderer/src/plugins/nebula/VoiceRecorder.tsx
  - src/renderer/src/plugins/nebula/DrawingCanvas.tsx
  - src/renderer/src/plugins/nebula/FloatingToolbar.tsx
  - src/renderer/src/plugins/nebula/TranscriptionBlock.tsx
  - src/renderer/src/plugins/nebula/DeleteConfirmDialog.tsx
  - src/renderer/src/plugins/nebula/NoteContextMenu.tsx
  - src/renderer/src/plugins/nebula/LinkDialog.tsx
  - src/renderer/src/plugins/nebula/CodeBlockNodeView.tsx
  - src/renderer/src/plugins/nebula/CodeBlockControls.tsx
  - src/renderer/src/plugins/nebula/TableControls.tsx
  - src/renderer/src/plugins/nebula/ToastContainer.tsx
autonomous: true
requirements:
  - NEBL-01
  - NEBL-02
  - NEBL-03
  - NEBL-04
  - NEBL-05
  - NEBL-06

must_haves:
  truths:
    - "NebulaView uses PluginShell with 3 tabs (Notes, Search, Knowledge)"
    - "NoteList shows Card sidebar with ScrollArea, pinned section, DropdownMenu context actions"
    - "NoteEditor uses shared RichTextEditor (full mode) with auto-save StatusBadge"
    - "SearchView uses shared SearchInput + ChatInterface for Q&A"
    - "KnowledgeGraph renders 2D force graph only (no KnowledgeGraph3D)"
    - "All dialogs use Animate-UI Dialog/AlertDialog/DropdownMenu"
  artifacts:
    - path: "src/renderer/src/plugins/nebula/NebulaView.tsx"
      provides: "Nebula plugin shell with 3 tabs"
      contains: "import.*PluginShell"
    - path: "src/renderer/src/plugins/nebula/NoteEditor.tsx"
      provides: "Note editing with RichTextEditor"
      contains: "import.*RichTextEditor"
    - path: "src/renderer/src/plugins/nebula/KnowledgeGraph.tsx"
      provides: "2D knowledge graph"
      contains: "react-force-graph-2d"
  key_links:
    - from: "src/renderer/src/plugins/nebula/NoteEditor.tsx"
      to: "shared rich-text-editor"
      via: "RichTextEditor full mode"
      pattern: "import.*RichTextEditor.*from.*shared"
    - from: "src/renderer/src/plugins/nebula/SearchView.tsx"
      to: "shared chat-interface"
      via: "ChatInterface for search Q&A"
      pattern: "import.*ChatInterface.*from.*shared"
---

<objective>
Migrate the entire Nebula plugin from Glass components to shadcn/shared components. This is the largest migration with 16 files.

Purpose: Nebula has the most varied component usage (editor, graph, voice, drawing, dialogs) and needs thorough migration.
Output: 16 rewritten Nebula component files using PluginShell, RichTextEditor, ChatInterface, Dialog, DropdownMenu.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/plugin-shell.tsx
@src/renderer/src/components/shared/rich-text-editor.tsx
@src/renderer/src/components/shared/chat-interface.tsx
@src/renderer/src/components/shared/search-input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate NebulaView + NoteList + NoteEditor + SearchView + KnowledgeGraph</name>
  <files>
    src/renderer/src/plugins/nebula/NebulaView.tsx
    src/renderer/src/plugins/nebula/NoteList.tsx
    src/renderer/src/plugins/nebula/NoteEditor.tsx
    src/renderer/src/plugins/nebula/SearchView.tsx
    src/renderer/src/plugins/nebula/KnowledgeGraph.tsx
  </files>
  <action>
**NebulaView.tsx:**
1. Read existing file to understand tab structure and layout
2. Remove Glass imports
3. Import PluginShell from @renderer/components/shared/plugin-shell
4. Wrap in PluginShell with: title="Nebula", icon=BookOpen or Sparkles (from lucide), tabs for Notes/Search/Knowledge
5. Notes tab: SplitPanel with NoteList (left, 30%) + NoteEditor (right, 70%)
6. Search tab: SearchView
7. Knowledge tab: KnowledgeGraph
8. Keep ALL store connections (useNebulaStore or equivalent)

**NoteList.tsx:**
1. Read existing file
2. Replace Glass components with Card sidebar layout
3. Use ScrollArea from ui/scroll-area for the note list
4. Pinned notes section at top with Separator
5. Each note item: clickable div with rounded-[16px], title (font-medium), date (text-muted-foreground text-xs), snippet
6. Active note: bg-primary/18 border border-primary/35
7. Context actions via DropdownMenu from ui/dropdown-menu (pin, duplicate, delete)
8. "New Note" Button (primary) at top
9. Keep ALL note store connections, sorting, filtering

**NoteEditor.tsx:**
1. Read existing file
2. Replace existing Tiptap setup with RichTextEditor from @renderer/components/shared/rich-text-editor
3. Pass mode="full", content from store, onChange to save to store
4. Add auto-save Badge at top: Badge (success) showing "Saved" or Badge (warning) showing "Saving..."
5. Keep FloatingToolbar reference (migrated in Task 2)
6. Keep ALL auto-save logic, store connections, Tiptap extensions

**SearchView.tsx:**
1. Read existing file
2. Use SearchInput from @renderer/components/shared/search-input at top
3. Search results as Card list below
4. Use ChatInterface from @renderer/components/shared/chat-interface for Q&A section
5. Keep ALL search logic, AI Q&A streaming, store connections

**KnowledgeGraph.tsx:**
1. Read existing file -- currently may import KnowledgeGraph3D
2. REMOVE any KnowledgeGraph3D import
3. Use ONLY 2D rendering with react-force-graph-2d
4. Style: node color based on note type (primary for main, info for linked, muted for orphan), link color hsl(var(--border))
5. Keep ALL graph data generation, node/link computation from store
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Nebula shows 3 tabs, Notes tab has list + editor, Search has ChatInterface, Knowledge has 2D graph</manual>
  </verify>
  <done>NebulaView uses PluginShell with 3 tabs. NoteList uses ScrollArea+DropdownMenu. NoteEditor uses shared RichTextEditor (full mode). SearchView uses SearchInput+ChatInterface. KnowledgeGraph is 2D only. All store connections preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate VoiceRecorder, DrawingCanvas, FloatingToolbar, all dialogs, and editor node views</name>
  <files>
    src/renderer/src/plugins/nebula/VoiceRecorder.tsx
    src/renderer/src/plugins/nebula/DrawingCanvas.tsx
    src/renderer/src/plugins/nebula/FloatingToolbar.tsx
    src/renderer/src/plugins/nebula/TranscriptionBlock.tsx
    src/renderer/src/plugins/nebula/DeleteConfirmDialog.tsx
    src/renderer/src/plugins/nebula/NoteContextMenu.tsx
    src/renderer/src/plugins/nebula/LinkDialog.tsx
    src/renderer/src/plugins/nebula/CodeBlockNodeView.tsx
    src/renderer/src/plugins/nebula/CodeBlockControls.tsx
    src/renderer/src/plugins/nebula/TableControls.tsx
    src/renderer/src/plugins/nebula/ToastContainer.tsx
  </files>
  <action>
**VoiceRecorder.tsx:**
1. Read existing file
2. Restyle as FAB Button (primary color, rounded-full, fixed position)
3. Recording state: pulsing ring animation with destructive color
4. Use Button from ui/button
5. Keep ALL voice recording logic (Web Audio API, IPC calls)

**DrawingCanvas.tsx:**
1. Read existing file -- uses tldraw
2. Keep tldraw usage, just restyle the wrapper Card (rounded-[28px], border)
3. Keep ALL drawing logic

**FloatingToolbar.tsx:**
1. Read existing file
2. Replace with Popover from ui/popover
3. Button groups inside: formatting buttons (bold, italic, etc.) using Button (ghost variant, size="sm")
4. Keep ALL toolbar positioning and Tiptap command execution

**TranscriptionBlock.tsx:**
1. Read existing file
2. Restyle with Card wrapper, Badge for speaker labels
3. Editable text area with Input or inline editing
4. Keep ALL transcription logic

**DeleteConfirmDialog.tsx:**
1. Read existing file
2. Replace GlassModal with AlertDialog from ui/alert-dialog
3. Destructive action styling: AlertDialogAction with destructive variant
4. Keep ALL delete confirmation logic

**NoteContextMenu.tsx:**
1. Read existing file
2. Replace with DropdownMenu from ui/dropdown-menu
3. Menu items: Pin, Duplicate, Export, Delete (destructive)
4. Keep ALL context menu actions

**LinkDialog.tsx:**
1. Read existing file
2. Replace GlassModal with Dialog from ui/dialog
3. Input for URL, Input for display text, Button submit
4. Keep ALL link insertion logic

**CodeBlockNodeView.tsx + CodeBlockControls.tsx:**
1. Read existing files
2. Restyle code blocks with theme colors (bg-secondary for code block bg, text-foreground)
3. Copy Button overlay using Button (ghost, size="sm")
4. Language selector using Select if present
5. Keep ALL Tiptap node view logic

**TableControls.tsx:**
1. Read existing file
2. Replace with inline Button components from ui/button (ghost variant)
3. Keep ALL table manipulation logic

**ToastContainer.tsx:**
1. Read existing file
2. Replace with Sonner -- since Sonner is set up globally, this file may become a thin wrapper or can be simplified
3. If it just wraps toast rendering, replace GlassToast calls with toast() from sonner
4. Keep ALL toast trigger logic but redirect to global Sonner
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Voice recorder shows FAB button, dialogs use Dialog/AlertDialog, code blocks have theme colors</manual>
  </verify>
  <done>VoiceRecorder is FAB Button. DrawingCanvas wraps tldraw in Card. FloatingToolbar uses Popover. All dialogs use Dialog/AlertDialog/DropdownMenu. CodeBlock/Table controls restyled. Toasts use Sonner. All logic preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassModal\|GlassButton\|GlassTab\|GlassChat\|GlassToast\|KnowledgeGraph3D" src/renderer/src/plugins/nebula/` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- Nebula renders with PluginShell, 3 tabs, all sub-components migrated
- NoteEditor uses shared RichTextEditor (full mode)
- SearchView uses shared ChatInterface
- KnowledgeGraph is 2D only
- All dialogs use Animate-UI Dialog/AlertDialog
- Voice recorder, drawing canvas, toolbar all restyled
- Zero Glass*/3D component imports
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-07-SUMMARY.md`
</output>
