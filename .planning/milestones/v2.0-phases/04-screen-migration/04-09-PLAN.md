---
phase: 04-screen-migration
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
  - src/renderer/src/plugins/code-review-bot/PRList.tsx
  - src/renderer/src/plugins/code-review-bot/PRDiffView.tsx
  - src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx
  - src/renderer/src/plugins/code-review-bot/ReviewHistory.tsx
  - src/renderer/src/plugins/code-review-bot/SettingsPanel.tsx
autonomous: true
requirements:
  - CRVW-01
  - CRVW-02
  - CRVW-03
  - CRVW-04
  - CRVW-05

must_haves:
  truths:
    - "CodeReviewBotView uses PluginShell with 3 tabs (Reviews, History, Settings)"
    - "PRList shows Card list with author Badge and file count StatusBadge"
    - "PRDiffView renders diffs with theme diff colors (add=success tint, del=destructive tint)"
    - "ReviewPanel uses shared ContentRenderer with severity StatusBadges"
    - "ReviewHistory uses shared HistoryList"
    - "SettingsPanel uses shared RichTextEditor (minimal mode) for guidelines"
  artifacts:
    - path: "src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx"
      provides: "Code Review Bot plugin shell"
      contains: "import.*PluginShell"
    - path: "src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx"
      provides: "Review findings display"
      contains: "import.*ContentRenderer"
    - path: "src/renderer/src/plugins/code-review-bot/ReviewHistory.tsx"
      provides: "Review history"
      contains: "import.*HistoryList"
  key_links:
    - from: "src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx"
      to: "shared content-renderer"
      via: "ContentRenderer for review findings"
      pattern: "import.*ContentRenderer.*from.*shared"
    - from: "src/renderer/src/plugins/code-review-bot/SettingsPanel.tsx"
      to: "shared rich-text-editor"
      via: "RichTextEditor for guidelines editing"
      pattern: "import.*RichTextEditor.*from.*shared"
---

<objective>
Migrate Code Review Bot plugin from Glass components to shadcn/shared components.

Purpose: Code Review Bot needs theme-aware diff rendering and shared ContentRenderer for review output.
Output: 6 rewritten Code Review Bot component files.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/plugin-shell.tsx
@src/renderer/src/components/shared/content-renderer.tsx
@src/renderer/src/components/shared/history-list.tsx
@src/renderer/src/components/shared/rich-text-editor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CodeReviewBotView + PRList + PRDiffView</name>
  <files>
    src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
    src/renderer/src/plugins/code-review-bot/PRList.tsx
    src/renderer/src/plugins/code-review-bot/PRDiffView.tsx
  </files>
  <action>
**CodeReviewBotView.tsx:**
1. Read existing file to understand tab structure and store connections
2. Remove Glass imports
3. Import PluginShell from @renderer/components/shared/plugin-shell
4. Wrap in PluginShell with: title="Code Review", icon=GitPullRequest (from lucide), tabs for Reviews/History/Settings
5. Reviews tab: SplitPanel or flex layout with PRList on left + PRDiffView/ReviewPanel on right
6. History tab: ReviewHistory
7. Settings tab: SettingsPanel
8. Keep ALL store connections, active PR state

**PRList.tsx:**
1. Read existing file
2. Replace Glass components with Card list layout
3. Each PR: Card (rounded-[22px]) with PR title (font-medium), author Badge, file count Badge, status Badge (open/merged/closed with appropriate status color)
4. Clickable to select PR and show diff/review
5. Use ScrollArea for the list
6. Keep ALL PR data fetching, Bitbucket integration, store connections

**PRDiffView.tsx:**
1. Read existing file
2. Keep diff rendering logic but restyle with theme diff colors:
   - Added lines: bg-[hsl(var(--success)/0.1)] text-[hsl(var(--success))]
   - Deleted lines: bg-[hsl(var(--destructive)/0.1)] text-[hsl(var(--destructive))]
   - Context lines: bg-transparent text-foreground
   - Line numbers: text-muted-foreground
   - Diff header: bg-secondary
3. Wrap in Card or bare div with proper font-mono styling
4. Keep ALL diff parsing and rendering logic
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Code Review Bot shows PluginShell, PR list with badges, diff with theme colors</manual>
  </verify>
  <done>CodeReviewBotView uses PluginShell. PRList shows Card list with Badges. PRDiffView uses theme diff colors. All store connections preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ReviewPanel + ReviewHistory + SettingsPanel</name>
  <files>
    src/renderer/src/plugins/code-review-bot/ReviewPanel.tsx
    src/renderer/src/plugins/code-review-bot/ReviewHistory.tsx
    src/renderer/src/plugins/code-review-bot/SettingsPanel.tsx
  </files>
  <action>
**ReviewPanel.tsx:**
1. Read existing file
2. Replace Glass components
3. Use ContentRenderer from @renderer/components/shared/content-renderer for review findings display
4. Add severity Badges above or inline: Badge with destructive for critical, warning for medium, info for low
5. Each finding rendered as a section with severity badge + file reference + description
6. Keep ALL review data handling, store connections

**ReviewHistory.tsx:**
1. Read existing file
2. Replace with HistoryList from @renderer/components/shared/history-list
3. Map review history entries: { id, title (PR name), subtitle (repo + branch), timestamp, badge (findings count) }
4. onRestore: loads review back into ReviewPanel
5. onDelete: removes history entry
6. Keep ALL history store connections

**SettingsPanel.tsx:**
1. Read existing file
2. Replace Glass components with Card sections
3. Form fields: Input from ui/input for Bitbucket credentials/URL, Switch from ui/switch for toggles
4. Guidelines editor: RichTextEditor from @renderer/components/shared/rich-text-editor with mode="minimal"
5. Save Button (primary)
6. Keep ALL settings store connections and IPC calls
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Review panel shows ContentRenderer findings, History shows HistoryList, Settings has RichTextEditor</manual>
  </verify>
  <done>ReviewPanel uses ContentRenderer with severity Badges. ReviewHistory uses shared HistoryList. SettingsPanel uses Input+Switch+RichTextEditor. All store connections preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassTab\|GlassModal\|PluginHeader\|GlassSurface" src/renderer/src/plugins/code-review-bot/` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- Code Review Bot renders with PluginShell and 3 tabs
- PRList shows Cards with author/status Badges
- PRDiffView uses theme diff colors (success/destructive tints)
- ReviewPanel uses shared ContentRenderer
- ReviewHistory uses shared HistoryList
- SettingsPanel uses shared RichTextEditor (minimal)
- Zero Glass* imports
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-09-SUMMARY.md`
</output>
