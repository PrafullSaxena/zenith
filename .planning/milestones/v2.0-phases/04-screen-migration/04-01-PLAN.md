---
phase: 04-screen-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/dashboard/MissionControl.tsx
  - src/renderer/src/components/dashboard/StatsCards.tsx
  - src/renderer/src/components/dashboard/TokenChart.tsx
  - src/renderer/src/components/dashboard/HealthPanel.tsx
  - src/renderer/src/components/dashboard/ActivityFeed.tsx
  - src/renderer/src/components/dashboard/PluginCard.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Dashboard renders MissionControl header with radial gradient, greeting, SearchInput, and primary action button"
    - "4 MetricCards display with status-colored icon containers and delta indicators"
    - "TokenChart renders in a Card wrapper with theme colors"
    - "HealthPanel shows glowing status dots and grouped resources"
    - "ActivityFeed shows nested rows with StatusBadges"
    - "PluginCards grid renders with hover lift animation"
  artifacts:
    - path: "src/renderer/src/components/dashboard/MissionControl.tsx"
      provides: "Dashboard main view with header, stats, charts, activity, plugins"
      contains: "import.*from.*@renderer/components/ui"
    - path: "src/renderer/src/components/dashboard/StatsCards.tsx"
      provides: "4 MetricCards with status colors"
      contains: "import.*Card"
    - path: "src/renderer/src/components/dashboard/HealthPanel.tsx"
      provides: "Health panel with glowing dots"
      contains: "shadow-\\[0_0_20px"
    - path: "src/renderer/src/components/dashboard/PluginCard.tsx"
      provides: "Plugin card with hover lift"
      contains: "import.*Card"
  key_links:
    - from: "src/renderer/src/components/dashboard/MissionControl.tsx"
      to: "stores"
      via: "useActivityStore, useTokenStore, useHealthStore, useDbStore"
      pattern: "use(Activity|Token|Health|Db)Store"
    - from: "src/renderer/src/components/dashboard/MissionControl.tsx"
      to: "sub-components"
      via: "imports TokenChart, HealthPanel, PluginCard, StatsCards"
      pattern: "import.*from.*\\./"
---

<objective>
Migrate the Dashboard screen (MissionControl + all sub-components) from Glass components to shadcn/ui primitives.

Purpose: Dashboard is the app landing page and must showcase the new zenith-violet design system.
Output: 6 rewritten dashboard component files using Card, Badge, SearchInput, and theme tokens.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/search-input.tsx
@src/renderer/src/components/shared/plugin-shell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate MissionControl header + StatsCards + PluginCard</name>
  <files>
    src/renderer/src/components/dashboard/MissionControl.tsx
    src/renderer/src/components/dashboard/StatsCards.tsx
    src/renderer/src/components/dashboard/PluginCard.tsx
  </files>
  <action>
**MissionControl.tsx:**
1. Read the existing file fully to understand all store connections, state, effects, and layout
2. Remove imports: GlassCard, GlassSurface, AnimatedCounter, Scene3DWrapper, ActivityMesh3D lazy import
3. Add imports: Card/CardContent/CardHeader from @renderer/components/ui/card, Button from @renderer/components/ui/button, Badge from @renderer/components/ui/badge, SearchInput from @renderer/components/shared/search-input, cn from @renderer/lib/utils
4. Rewrite the hero header section: Card with rounded-[28px], backdrop-blur, radial gradient bg. Include greeting text, SearchInput, and a primary action Button ("New Project" or similar based on existing)
5. Replace ActivityMesh3D section with a simple "Recent Operations" summary Card showing last 5 activity items from useActivityStore
6. Keep ALL useEffect hooks, store subscriptions (useActivityStore, useTokenStore, useHealthStore, useDbStore), and data fetching logic EXACTLY as-is
7. Keep the motion.div wrappers and staggerContainer/staggerItem animations
8. Keep the layout grid structure (TokenChart spanning 2 cols, HealthPanel 1 col, PluginCards grid below)

**StatsCards.tsx:**
1. Read existing file to understand current StatsCard structure and data
2. Replace GlassCard with Card from ui/card
3. Each MetricCard: Card with rounded-[22px], status-colored icon container (bg-[hsl(var(--success)/0.13)] etc.), AnimatedCounter for value, delta indicator showing +/- change with appropriate color
4. Keep AnimatedCounter component -- just restyle its wrapper
5. Layout: 4 cards in a grid (grid-cols-2 lg:grid-cols-4)

**PluginCard.tsx:**
1. Read existing file
2. Replace GlassCard with Card from ui/card
3. Add hover lift: className="transition-all duration-200 hover:-translate-y-0.5 hover:border-muted-foreground/30"
4. Keep plugin icon, name, description, navigation onClick
5. Use rounded-[22px] for card radius
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Dashboard renders with new Card-based layout, no Glass imports remain</manual>
  </verify>
  <done>MissionControl renders header Card with SearchInput, 4 MetricCards with status colors, PluginCards with hover lift. Zero GlassCard/GlassSurface/Scene3DWrapper imports. All store connections preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TokenChart, HealthPanel, ActivityFeed</name>
  <files>
    src/renderer/src/components/dashboard/TokenChart.tsx
    src/renderer/src/components/dashboard/HealthPanel.tsx
    src/renderer/src/components/dashboard/ActivityFeed.tsx
  </files>
  <action>
**TokenChart.tsx:**
1. Read existing file -- this likely has a custom SVG chart
2. Wrap in Card from ui/card with rounded-[28px]
3. Replace any GlassCard/GlassSurface usage
4. Restyle chart colors to use CSS custom property theme colors: hsl(var(--primary)) for primary line, hsl(var(--muted-foreground)) for axis/grid
5. Keep ALL chart logic, data processing, useTokenStore connection intact

**HealthPanel.tsx:**
1. Read existing file
2. Replace GlassCard with Card from ui/card
3. Implement glowing status dots: w-3 h-3 rounded-full with bg color and shadow-[0_0_20px_color] per MASTER.md spec
4. Use Badge from ui/badge for StatusBadges (success/warning/destructive variants)
5. Group resources by category with section headers
6. Keep useHealthStore connection and all health data logic

**ActivityFeed.tsx:**
1. Read existing file (or create if it does not exist as a separate component -- it may be inline in MissionControl)
2. If inline in MissionControl, extract to ActivityFeed.tsx
3. Use Card wrapper, nested rows with rounded-[22px] bg-background
4. Each row: timestamp, description, Badge for plugin name with plugin accent color, Badge for status
5. Use ScrollArea from ui/scroll-area for the list
6. Keep useActivityStore connection
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>TokenChart shows in Card wrapper, HealthPanel has glowing dots, ActivityFeed shows badges</manual>
  </verify>
  <done>TokenChart wrapped in themed Card. HealthPanel has glowing status dots and StatusBadges. ActivityFeed renders nested rows with plugin accent badges. All store connections preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassSurface\|GlassButton\|Scene3DWrapper\|ActivityMesh3D" src/renderer/src/components/dashboard/` returns no matches
- `grep -r "from.*components/ui/card\|from.*components/ui/badge" src/renderer/src/components/dashboard/` returns matches in all rewritten files
- TypeScript compiles with no errors: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- Dashboard renders MissionControl with Card-based header, SearchInput, 4 MetricCards, TokenChart in Card, HealthPanel with glowing dots, ActivityFeed with badges, PluginCards with hover lift
- Zero Glass* or 3D component imports in dashboard/
- All store connections (activity, token, health, db) preserved and functional
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-01-SUMMARY.md`
</output>
