---
phase: 04-screen-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/cortex/CortexView.tsx
  - src/renderer/src/plugins/cortex/components/RepoManager.tsx
  - src/renderer/src/plugins/cortex/components/RepoCard.tsx
  - src/renderer/src/plugins/cortex/components/AddRepoDialog.tsx
  - src/renderer/src/plugins/cortex/components/CodePanel.tsx
  - src/renderer/src/plugins/cortex/components/CodeTabs.tsx
  - src/renderer/src/plugins/cortex/components/CodeViewer.tsx
  - src/renderer/src/plugins/cortex/components/QAPanel.tsx
  - src/renderer/src/plugins/cortex/components/ExportDialog.tsx
autonomous: true
requirements:
  - CRTX-01
  - CRTX-07
  - CRTX-08
  - CRTX-09

must_haves:
  truths:
    - "CortexView uses PluginShell with 4 main tabs (Repos, Insights, Code, Q&A)"
    - "RepoManager shows Card grid with StatusBadge and analyze Button"
    - "CodePanel uses shared FileTree + CodeEditor in SplitPanel"
    - "QAPanel uses shared ChatInterface with citation renderActions"
    - "ExportDialog uses Dialog + PdfExporter trigger"
  artifacts:
    - path: "src/renderer/src/plugins/cortex/CortexView.tsx"
      provides: "Cortex plugin shell with 4 tabs"
      contains: "import.*PluginShell"
    - path: "src/renderer/src/plugins/cortex/components/CodePanel.tsx"
      provides: "File tree + code editor split"
      contains: "import.*FileTree|import.*CodeEditor"
    - path: "src/renderer/src/plugins/cortex/components/QAPanel.tsx"
      provides: "AI Q&A chat interface"
      contains: "import.*ChatInterface"
  key_links:
    - from: "src/renderer/src/plugins/cortex/CortexView.tsx"
      to: "shared plugin-shell"
      via: "PluginShell wrapper"
      pattern: "import.*PluginShell.*from.*shared"
    - from: "src/renderer/src/plugins/cortex/components/QAPanel.tsx"
      to: "shared chat-interface"
      via: "ChatInterface for Q&A"
      pattern: "import.*ChatInterface.*from.*shared"
---

<objective>
Migrate Cortex CortexView, RepoManager, CodePanel, QAPanel, and ExportDialog from Glass components to shadcn/shared components.

Purpose: These are the main Cortex tabs (Repos, Code, Q&A) plus the export dialog, using the heaviest shared components.
Output: 9 rewritten Cortex component files using PluginShell, FileTree, CodeEditor, ChatInterface, PdfExporter.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/plugin-shell.tsx
@src/renderer/src/components/shared/chat-interface.tsx
@src/renderer/src/components/shared/code-editor.tsx
@src/renderer/src/components/shared/file-tree.tsx
@src/renderer/src/components/shared/split-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CortexView + RepoManager + RepoCard + AddRepoDialog</name>
  <files>
    src/renderer/src/plugins/cortex/CortexView.tsx
    src/renderer/src/plugins/cortex/components/RepoManager.tsx
    src/renderer/src/plugins/cortex/components/RepoCard.tsx
    src/renderer/src/plugins/cortex/components/AddRepoDialog.tsx
  </files>
  <action>
**CortexView.tsx:**
1. Read existing file to understand tab structure and store connections
2. Remove Glass imports (PluginHeader, GlassTab, etc.)
3. Import PluginShell from @renderer/components/shared/plugin-shell
4. Wrap entire view in PluginShell with: title="Cortex", icon=Brain (from lucide-react), tabs for Repos/Insights/Code/Q&A
5. Each tab.content renders the corresponding panel component
6. Keep ALL store connections, active repo state, analysis state

**RepoManager.tsx:**
1. Read existing file
2. Replace with grid of RepoCard components (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)
3. Add a header area with "Add Repository" Button (primary) that opens AddRepoDialog
4. Keep ALL repo store connections and analysis trigger logic

**RepoCard.tsx:**
1. Read existing file
2. Replace GlassCard with Card from ui/card (rounded-[22px])
3. Show: repo name (font-semibold), path (text-muted-foreground text-sm), Badge for analysis status (success=analyzed, warning=pending, info=analyzing)
4. "Analyze" Button (primary variant, small) to trigger analysis
5. Keep onClick for selecting repo and ALL store connections

**AddRepoDialog.tsx:**
1. Read existing file
2. Replace GlassModal with Dialog from ui/dialog
3. Form inside: Input for repo path (or file picker Button), Select for repo type if applicable
4. Submit Button (primary), Cancel Button (ghost)
5. Keep ALL IPC calls for adding repos
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>CortexView shows PluginShell with 4 tabs, Repos tab shows card grid</manual>
  </verify>
  <done>CortexView uses PluginShell. RepoManager shows Card grid. RepoCard has Badge + analyze Button. AddRepoDialog uses Dialog. All store/IPC connections preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CodePanel + CodeTabs + CodeViewer + QAPanel + ExportDialog</name>
  <files>
    src/renderer/src/plugins/cortex/components/CodePanel.tsx
    src/renderer/src/plugins/cortex/components/CodeTabs.tsx
    src/renderer/src/plugins/cortex/components/CodeViewer.tsx
    src/renderer/src/plugins/cortex/components/QAPanel.tsx
    src/renderer/src/plugins/cortex/components/ExportDialog.tsx
  </files>
  <action>
**CodePanel.tsx:**
1. Read existing file
2. Replace with SplitPanel from @renderer/components/shared/split-panel (direction="horizontal", sizes=[30, 70])
3. Left panel: FileTree from @renderer/components/shared/file-tree (pass nodes from store, onSelect to load file)
4. Right panel: CodeEditor from @renderer/components/shared/code-editor (readOnly=true, language detected from file extension)
5. Keep ALL file loading logic, store connections for codebase tree and file content

**CodeTabs.tsx:**
1. Read existing file
2. Replace with Tabs from ui/tabs for multiple open files
3. Each TabsTrigger shows filename with close button
4. TabsContent renders CodeEditor (readOnly)
5. Keep ALL tab management state

**CodeViewer.tsx:**
1. Read existing file
2. This may be replaced entirely by CodeEditor (readOnly mode) -- if CodePanel directly uses CodeEditor, mark CodeViewer as deprecated but keep it compiling
3. If other components still import CodeViewer, make it a thin wrapper around CodeEditor (readOnly=true)

**QAPanel.tsx:**
1. Read existing file
2. Replace with ChatInterface from @renderer/components/shared/chat-interface
3. Pass: messages from store, onSend handler, isStreaming from store, suggestedQuestions
4. For renderActions: add citation links if the response has citations -- render as clickable links that navigate to the source file in CodePanel
5. Keep ALL AI chat logic, streaming handlers, store connections

**ExportDialog.tsx:**
1. Read existing file
2. Replace with Dialog from ui/dialog
3. Inside: format selector (Select with report/document/diagram), title Input
4. Export Button triggers PdfExporter from @renderer/components/shared/pdf-exporter (call exportPdf with content + metadata)
5. Keep ALL export logic and content gathering
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Code tab shows FileTree + CodeEditor split, Q&A tab shows ChatInterface, Export dialog works</manual>
  </verify>
  <done>CodePanel uses SplitPanel+FileTree+CodeEditor. QAPanel uses ChatInterface with citations. ExportDialog uses Dialog+PdfExporter. All store/AI streaming connections preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassModal\|GlassTab\|PluginHeader\|GlassChat" src/renderer/src/plugins/cortex/CortexView.tsx src/renderer/src/plugins/cortex/components/RepoManager.tsx src/renderer/src/plugins/cortex/components/CodePanel.tsx src/renderer/src/plugins/cortex/components/QAPanel.tsx src/renderer/src/plugins/cortex/components/ExportDialog.tsx` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- CortexView wrapped in PluginShell with 4 tabs
- RepoManager shows Card grid with analyze buttons
- CodePanel uses shared FileTree + CodeEditor (readOnly)
- QAPanel uses shared ChatInterface with citation actions
- ExportDialog uses Dialog + PdfExporter
- Zero Glass* imports in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-04-SUMMARY.md`
</output>
