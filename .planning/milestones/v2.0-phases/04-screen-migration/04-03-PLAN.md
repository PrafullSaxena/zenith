---
phase: 04-screen-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/cortex/components/InsightsPanel.tsx
  - src/renderer/src/plugins/cortex/components/OverviewTab.tsx
  - src/renderer/src/plugins/cortex/components/APIListTab.tsx
  - src/renderer/src/plugins/cortex/components/FlowsTab.tsx
  - src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx
  - src/renderer/src/plugins/cortex/components/DiagramsTab.tsx
  - src/renderer/src/plugins/cortex/components/MindGraphTab.tsx
  - src/renderer/src/plugins/cortex/components/InsightCard.tsx
  - src/renderer/src/plugins/cortex/components/ValidationPanel.tsx
  - src/renderer/src/plugins/cortex/components/DonutChart.tsx
autonomous: true
requirements:
  - CRTX-02
  - CRTX-03
  - CRTX-04
  - CRTX-05
  - CRTX-06

must_haves:
  truths:
    - "InsightsPanel renders 6 sub-tabs (Overview, APIs, Flows, Architecture, Diagrams, Graph) with animated tab indicator"
    - "Overview tab shows MetricCards, DonutChart, and ContentRenderer for documentation"
    - "APIs tab shows DataTable with method Badges and sortable headers"
    - "Flows/Architecture/Diagrams tabs render React Flow with theme-token-styled nodes"
    - "Graph tab renders 2D force graph only (no MindGraph3D)"
  artifacts:
    - path: "src/renderer/src/plugins/cortex/components/InsightsPanel.tsx"
      provides: "6-tab insights container"
      contains: "import.*Tabs.*from.*@renderer/components/ui/tabs"
    - path: "src/renderer/src/plugins/cortex/components/APIListTab.tsx"
      provides: "API list with DataTable"
      contains: "import.*DataTable"
    - path: "src/renderer/src/plugins/cortex/components/MindGraphTab.tsx"
      provides: "2D-only force graph"
      contains: "react-force-graph-2d"
  key_links:
    - from: "src/renderer/src/plugins/cortex/components/InsightsPanel.tsx"
      to: "sub-tabs"
      via: "Tabs rendering 6 content panels"
      pattern: "TabsContent"
    - from: "src/renderer/src/plugins/cortex/components/OverviewTab.tsx"
      to: "shared content-renderer"
      via: "ContentRenderer for documentation display"
      pattern: "import.*ContentRenderer"
---

<objective>
Migrate Cortex InsightsPanel and all 6 sub-tabs from Glass components to shadcn/ui primitives.

Purpose: InsightsPanel is the most tab-heavy component in the app with 6 sub-tabs requiring different component patterns.
Output: 10 rewritten Cortex insight component files using Tabs, DataTable, ContentRenderer, Card, Badge.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/content-renderer.tsx
@src/renderer/src/components/shared/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate InsightsPanel + OverviewTab + APIListTab + InsightCard + DonutChart + ValidationPanel</name>
  <files>
    src/renderer/src/plugins/cortex/components/InsightsPanel.tsx
    src/renderer/src/plugins/cortex/components/OverviewTab.tsx
    src/renderer/src/plugins/cortex/components/APIListTab.tsx
    src/renderer/src/plugins/cortex/components/InsightCard.tsx
    src/renderer/src/plugins/cortex/components/DonutChart.tsx
    src/renderer/src/plugins/cortex/components/ValidationPanel.tsx
  </files>
  <action>
**InsightsPanel.tsx:**
1. Read existing file to understand current tab structure and data flow
2. Remove Glass imports (GlassTab, GlassCard, etc.)
3. Import Tabs, TabsList, TabsTrigger, TabsContent from @renderer/components/ui/tabs
4. Build 6 sub-tabs: Overview, APIs, Flows, Architecture, Diagrams, Graph
5. Each TabsTrigger with appropriate Lucide icon + label
6. Keep ALL store connections and data fetching

**OverviewTab.tsx:**
1. Read existing file
2. Replace Glass components with: Card for sections, Badge for status indicators
3. Use MetricCards pattern (Card + status-colored icon container + AnimatedCounter) for key metrics
4. Use ContentRenderer from @renderer/components/shared/content-renderer for documentation/HLD display
5. Keep DonutChart component reference (restyled separately)

**APIListTab.tsx:**
1. Read existing file
2. Replace table with DataTable from @renderer/components/shared/data-table
3. Columns: method (Badge with color: GET=info, POST=success, PUT=warning, DELETE=destructive), path, description, parameters count
4. Add sortable: true for relevant columns
5. Keep ALL data fetching and filtering logic

**InsightCard.tsx:**
1. Read existing file
2. Replace with Card (rounded-[22px]) + Badge for type/severity
3. Keep insight data structure intact

**DonutChart.tsx:**
1. Read existing file -- likely custom SVG
2. Restyle colors to use theme tokens (primary, success, warning, info, destructive)
3. Keep chart logic, wrap in Card if not already

**ValidationPanel.tsx:**
1. Read existing file
2. Restyle with Card, Badge for validation status
3. Keep validation logic and store connections
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>InsightsPanel shows 6 tabs, Overview shows metrics + docs, APIs shows DataTable</manual>
  </verify>
  <done>InsightsPanel renders 6 animated sub-tabs. Overview uses MetricCards + ContentRenderer. APIs uses DataTable with method Badges. All store connections preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate FlowsTab + ArchitectureDashboard + DiagramsTab + MindGraphTab</name>
  <files>
    src/renderer/src/plugins/cortex/components/FlowsTab.tsx
    src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx
    src/renderer/src/plugins/cortex/components/DiagramsTab.tsx
    src/renderer/src/plugins/cortex/components/MindGraphTab.tsx
  </files>
  <action>
**FlowsTab.tsx:**
1. Read existing file -- uses React Flow
2. Replace Glass components wrapping the flow diagram
3. Restyle React Flow node/edge styles to use theme tokens: node bg = hsl(var(--card)), node border = hsl(var(--border)), edge stroke = hsl(var(--muted-foreground)), selected node border = hsl(var(--primary))
4. Use Tabs from ui/tabs for flow type selector if multiple flow types exist
5. Keep ALL React Flow setup, node data, edge data, layout logic

**ArchitectureDashboard.tsx:**
1. Read existing file
2. Replace Glass components with Card sections, Badge for entity types
3. If it has a React Flow entity diagram, restyle nodes/edges with theme tokens
4. If it uses ContentRenderer for HLD output, import from shared
5. Keep ALL data fetching and store connections

**DiagramsTab.tsx:**
1. Read existing file
2. Replace Glass wrappers with Card
3. Restyle React Flow diagrams with theme tokens (same pattern as FlowsTab)
4. Keep ALL diagram generation logic

**MindGraphTab.tsx:**
1. Read existing file -- currently imports MindGraph3D
2. REMOVE the MindGraph3D import and 3D rendering entirely
3. Replace with 2D force graph using react-force-graph-2d (check if already a dependency, if not note it)
4. Style: node color = hsl(var(--primary)), link color = hsl(var(--border)), bg = transparent
5. Add SearchInput from shared for graph search/filter if the 3D version had search
6. Keep ALL data connections (nodes, links from store/analysis)
7. If react-force-graph-2d is not installed, use a simple 2D rendering with d3-force or comment with TODO for Phase 5
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Flow tabs render React Flow with theme colors, Graph tab shows 2D graph (no 3D)</manual>
  </verify>
  <done>FlowsTab/DiagramsTab render React Flow with theme tokens. ArchitectureDashboard uses Card+Badge. MindGraphTab renders 2D force graph only, no MindGraph3D import. All store connections preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassTab\|GlassSurface\|MindGraph3D" src/renderer/src/plugins/cortex/components/InsightsPanel.tsx src/renderer/src/plugins/cortex/components/OverviewTab.tsx src/renderer/src/plugins/cortex/components/APIListTab.tsx src/renderer/src/plugins/cortex/components/FlowsTab.tsx src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx src/renderer/src/plugins/cortex/components/DiagramsTab.tsx src/renderer/src/plugins/cortex/components/MindGraphTab.tsx` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- InsightsPanel shows 6 animated sub-tabs
- Overview uses MetricCards + ContentRenderer, APIs uses DataTable with method Badges
- Flow/Architecture/Diagrams use theme-styled React Flow
- Graph tab uses 2D force graph only (no 3D imports)
- Zero Glass*/3D component imports
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-03-SUMMARY.md`
</output>
