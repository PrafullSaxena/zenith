---
phase: 04-screen-migration
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/launchpad/LaunchpadView.tsx
  - src/renderer/src/plugins/launchpad/ProviderSelector.tsx
  - src/renderer/src/plugins/launchpad/ServiceCatalog.tsx
  - src/renderer/src/plugins/launchpad/ResourceConfigurator.tsx
  - src/renderer/src/plugins/launchpad/EstimationSummary.tsx
  - src/renderer/src/plugins/launchpad/AiAdvisor.tsx
  - src/renderer/src/plugins/launchpad/EstimationHistory.tsx
  - src/renderer/src/plugins/launchpad/ComparisonView.tsx
  - src/renderer/src/components/activity/ActivityLog.tsx
  - src/renderer/src/components/about/AboutView.tsx
autonomous: true
requirements:
  - LNCH-01
  - LNCH-02
  - LNCH-03
  - LNCH-04
  - LNCH-05
  - LNCH-06
  - LNCH-07
  - MISC-01
  - MISC-02

must_haves:
  truths:
    - "LaunchpadView uses PluginShell with 4 tabs (Estimator, AI Advisor, History, Compare)"
    - "ProviderSelector shows Card buttons with provider brand colors"
    - "ServiceCatalog uses shared SearchInput + ScrollArea grouped by category"
    - "EstimationSummary shows cost breakdown Card with PdfExporter trigger"
    - "AiAdvisor uses shared ChatInterface with suggestion action buttons"
    - "EstimationHistory uses shared HistoryList"
    - "No CostTreemap3D import exists"
    - "ActivityLog uses shared DataTable with Select filters and StatusBadges"
    - "AboutView shows Cards and capability Badges"
  artifacts:
    - path: "src/renderer/src/plugins/launchpad/LaunchpadView.tsx"
      provides: "Launchpad plugin shell"
      contains: "import.*PluginShell"
    - path: "src/renderer/src/plugins/launchpad/AiAdvisor.tsx"
      provides: "AI advisor chat"
      contains: "import.*ChatInterface"
    - path: "src/renderer/src/components/activity/ActivityLog.tsx"
      provides: "Activity log with DataTable"
      contains: "import.*DataTable"
    - path: "src/renderer/src/components/about/AboutView.tsx"
      provides: "About page with Cards"
      contains: "import.*Card"
  key_links:
    - from: "src/renderer/src/plugins/launchpad/AiAdvisor.tsx"
      to: "shared chat-interface"
      via: "ChatInterface for AI advisor"
      pattern: "import.*ChatInterface.*from.*shared"
    - from: "src/renderer/src/components/activity/ActivityLog.tsx"
      to: "shared data-table"
      via: "DataTable for activity display"
      pattern: "import.*DataTable.*from.*shared"
---

<objective>
Migrate Launchpad plugin, Activity Log, and About page from Glass components to shadcn/shared components. Also remove CostTreemap3D.

Purpose: This plan covers the final 3 screens -- Launchpad (largest), Activity (DataTable), and About (Cards+Badges).
Output: 10 rewritten component files.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/plugin-shell.tsx
@src/renderer/src/components/shared/chat-interface.tsx
@src/renderer/src/components/shared/data-table.tsx
@src/renderer/src/components/shared/history-list.tsx
@src/renderer/src/components/shared/search-input.tsx
@src/renderer/src/components/shared/pdf-exporter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate LaunchpadView + ProviderSelector + ServiceCatalog + ResourceConfigurator + EstimationSummary</name>
  <files>
    src/renderer/src/plugins/launchpad/LaunchpadView.tsx
    src/renderer/src/plugins/launchpad/ProviderSelector.tsx
    src/renderer/src/plugins/launchpad/ServiceCatalog.tsx
    src/renderer/src/plugins/launchpad/ResourceConfigurator.tsx
    src/renderer/src/plugins/launchpad/EstimationSummary.tsx
  </files>
  <action>
**LaunchpadView.tsx:**
1. Read existing file to understand tab structure and store connections
2. Remove Glass imports and CostTreemap3D lazy import if present
3. Import PluginShell from @renderer/components/shared/plugin-shell
4. Wrap in PluginShell with: title="Launchpad", icon=Rocket (from lucide), tabs for Estimator/AI Advisor/History/Compare
5. Estimator tab: vertical flow of ProviderSelector -> ServiceCatalog -> ResourceConfigurator -> EstimationSummary
6. AI Advisor tab: AiAdvisor
7. History tab: EstimationHistory
8. Compare tab: ComparisonView
9. Keep ALL store connections (useLaunchpadStore or equivalent)

**ProviderSelector.tsx:**
1. Read existing file
2. Replace Glass components with Card buttons (rounded-[22px])
3. Each provider: Card with brand color accent (AWS=orange, Azure=blue, GCP=red etc.), provider logo/icon, name
4. Active provider: border-primary bg-primary/10
5. Keep ALL provider selection logic

**ServiceCatalog.tsx:**
1. Read existing file
2. Add SearchInput from @renderer/components/shared/search-input at top for filtering
3. Use ScrollArea from ui/scroll-area for the service list
4. Group services by category with section headers (text-xs font-semibold text-muted-foreground uppercase)
5. Each service: clickable row with icon, name, description
6. Keep ALL service data and selection logic

**ResourceConfigurator.tsx:**
1. Read existing file
2. Replace Glass form fields with: Input, Select, Label from ui/
3. Dynamic form based on selected service's configuration schema
4. Each field in a Card section or simple form group
5. Keep ALL configuration state management and validation

**EstimationSummary.tsx:**
1. Read existing file
2. Replace Glass components with Card (rounded-[28px])
3. Cost breakdown: rows with service name, quantity, unit cost, total -- styled as a simple table or Card list
4. Total: prominent display with large font
5. "Export PDF" Button triggering PdfExporter from @renderer/components/shared/pdf-exporter (exportPdf with format="report")
6. REMOVE any CostTreemap3D reference -- replace with DataTable or simple bar comparison if 3D was used for cost visualization
7. Keep ALL cost calculation logic
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Launchpad shows PluginShell with 4 tabs, Estimator flow works end to end</manual>
  </verify>
  <done>LaunchpadView uses PluginShell. ProviderSelector uses Card buttons. ServiceCatalog uses SearchInput+ScrollArea. ResourceConfigurator uses shadcn form components. EstimationSummary has PDF export. No CostTreemap3D. All store connections preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AiAdvisor + EstimationHistory + ComparisonView + ActivityLog + AboutView</name>
  <files>
    src/renderer/src/plugins/launchpad/AiAdvisor.tsx
    src/renderer/src/plugins/launchpad/EstimationHistory.tsx
    src/renderer/src/plugins/launchpad/ComparisonView.tsx
    src/renderer/src/components/activity/ActivityLog.tsx
    src/renderer/src/components/about/AboutView.tsx
  </files>
  <action>
**AiAdvisor.tsx:**
1. Read existing file
2. Replace Glass chat component with ChatInterface from @renderer/components/shared/chat-interface
3. Pass: messages from store, onSend handler, isStreaming, placeholder="Ask about cloud costs..."
4. For renderActions: suggestion buttons that auto-fill optimization recommendations
5. Pass suggestedQuestions like ["How can I reduce costs?", "Compare AWS vs GCP for this workload"]
6. Keep ALL AI streaming logic, store connections

**EstimationHistory.tsx:**
1. Read existing file
2. Replace with HistoryList from @renderer/components/shared/history-list
3. Map estimation history: { id, title (project name or provider), subtitle (total cost), timestamp, badge (provider name) }
4. onRestore: loads estimation config back into Estimator tab
5. onDelete: removes entry
6. Keep ALL history store connections

**ComparisonView.tsx:**
1. Read existing file
2. Replace Glass components with side-by-side Card layout
3. Each provider gets a Card (rounded-[22px]) showing: provider name, service breakdown, total cost
4. Use DataTable from shared for detailed service comparison if applicable
5. Highlight the cheapest option with success border/badge
6. Keep ALL comparison data and calculation logic

**ActivityLog.tsx:**
1. Read existing file
2. Replace Glass components with Card wrapper (rounded-[28px])
3. Use DataTable from @renderer/components/shared/data-table
4. Columns: timestamp, plugin (Badge with plugin accent), action, status (Badge with status color), details
5. Add filter row at top: Select filters for plugin and status
6. Keep ALL activity store connections, data fetching

**AboutView.tsx:**
1. Read existing file
2. Replace Glass components with Card layout
3. App info Card (rounded-[28px]): logo, version, description
4. Capabilities: grid of Badge components showing each capability
5. Simplified layout -- keep it clean and minimal
6. If there's a timeline, use a vertical list with small dots (like status dots)
7. Keep ALL version info and capability data
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>AI Advisor shows ChatInterface, History shows HistoryList, Activity Log shows DataTable, About shows Cards</manual>
  </verify>
  <done>AiAdvisor uses ChatInterface. EstimationHistory uses HistoryList. ComparisonView uses Cards+DataTable. ActivityLog uses DataTable with filters. AboutView uses Cards+Badges. All store connections preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassTab\|GlassChat\|GlassSurface\|GlassTable\|CostTreemap3D\|PluginHeader" src/renderer/src/plugins/launchpad/ src/renderer/src/components/activity/ src/renderer/src/components/about/` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- Launchpad renders with PluginShell and 4 tabs
- AiAdvisor uses shared ChatInterface
- EstimationHistory uses shared HistoryList
- No CostTreemap3D import anywhere
- ActivityLog uses shared DataTable with filters
- AboutView uses Cards and Badges
- Zero Glass* imports in all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-10-SUMMARY.md`
</output>
