---
phase: 04-screen-migration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/db-inspector/AskAI.tsx
  - src/renderer/src/plugins/db-inspector/QueryOptimizer.tsx
  - src/renderer/src/plugins/db-inspector/ERDiagram.tsx
  - src/renderer/src/plugins/db-inspector/MermaidRenderer.tsx
  - src/renderer/src/plugins/db-inspector/DbHistory.tsx
  - src/renderer/src/plugins/db-inspector/SavedQueriesPanel.tsx
autonomous: true
requirements:
  - DBIP-04
  - DBIP-05
  - DBIP-06
  - DBIP-07

must_haves:
  truths:
    - "AskAI uses shared ChatInterface with run-sql action buttons on responses"
    - "QueryOptimizer uses ContentRenderer with mermaid rendering for execution plans"
    - "ERDiagram renders Mermaid ER diagrams (2D only, no SchemaOrb3D)"
    - "DbHistory uses shared HistoryList"
  artifacts:
    - path: "src/renderer/src/plugins/db-inspector/AskAI.tsx"
      provides: "AI Q&A for database"
      contains: "import.*ChatInterface"
    - path: "src/renderer/src/plugins/db-inspector/ERDiagram.tsx"
      provides: "ER diagram viewer"
      contains: "MermaidRenderer"
    - path: "src/renderer/src/plugins/db-inspector/DbHistory.tsx"
      provides: "Query history list"
      contains: "import.*HistoryList"
  key_links:
    - from: "src/renderer/src/plugins/db-inspector/AskAI.tsx"
      to: "shared chat-interface"
      via: "ChatInterface with run-sql actions"
      pattern: "import.*ChatInterface.*from.*shared"
    - from: "src/renderer/src/plugins/db-inspector/DbHistory.tsx"
      to: "shared history-list"
      via: "HistoryList for query history"
      pattern: "import.*HistoryList.*from.*shared"
---

<objective>
Migrate DB Inspector AI/ER/History tabs from Glass components to shadcn/shared components.

Purpose: These tabs use the heaviest shared components (ChatInterface, ContentRenderer, HistoryList) and must remove 3D SchemaOrb.
Output: 6 rewritten DB Inspector component files.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/chat-interface.tsx
@src/renderer/src/components/shared/content-renderer.tsx
@src/renderer/src/components/shared/history-list.tsx
@src/renderer/src/components/shared/search-input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate AskAI + QueryOptimizer + SavedQueriesPanel</name>
  <files>
    src/renderer/src/plugins/db-inspector/AskAI.tsx
    src/renderer/src/plugins/db-inspector/QueryOptimizer.tsx
    src/renderer/src/plugins/db-inspector/SavedQueriesPanel.tsx
  </files>
  <action>
**AskAI.tsx:**
1. Read existing file to understand chat structure, message format, store connections
2. Remove Glass imports (GlassChat or equivalent)
3. Import ChatInterface from @renderer/components/shared/chat-interface
4. Pass: messages from store, onSend handler, isStreaming from store, placeholder="Ask about your database..."
5. For renderActions: implement run-sql action -- when AI response contains SQL, show a "Run Query" Button that executes the SQL via the existing query execution IPC
6. Pass suggestedQuestions like ["Explain the schema", "Write a query to...", "Optimize this query"]
7. Keep ALL AI streaming logic, message persistence, store connections

**QueryOptimizer.tsx:**
1. Read existing file
2. Replace Glass components with Card wrapper
3. Use ContentRenderer from @renderer/components/shared/content-renderer for the optimization output (markdown + mermaid for execution plan diagrams + code blocks for SQL)
4. Input area: CodeEditor (readOnly or editable) for the SQL to optimize, Button to trigger optimization
5. Suggestion Cards below with optimization tips
6. Keep ALL optimization IPC calls and result handling

**SavedQueriesPanel.tsx:**
1. Read existing file
2. Restyle with Card list or use a simple ScrollArea with query items
3. Each saved query: Card (rounded-[22px]) with name, preview snippet, Badge for DB type
4. Click to load into QueryConsole
5. Keep ALL saved query management logic
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>AskAI shows ChatInterface with run-sql buttons, QueryOptimizer shows markdown output</manual>
  </verify>
  <done>AskAI uses ChatInterface with run-sql renderActions. QueryOptimizer uses ContentRenderer with mermaid. SavedQueriesPanel restyled. All store/IPC connections preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ERDiagram + MermaidRenderer + DbHistory</name>
  <files>
    src/renderer/src/plugins/db-inspector/ERDiagram.tsx
    src/renderer/src/plugins/db-inspector/MermaidRenderer.tsx
    src/renderer/src/plugins/db-inspector/DbHistory.tsx
  </files>
  <action>
**ERDiagram.tsx:**
1. Read existing file -- may reference SchemaOrb3D for 3D mode
2. REMOVE any SchemaOrb3D import and 3D rendering toggle
3. Keep ONLY the 2D Mermaid ER diagram rendering
4. Wrap in Card (rounded-[28px])
5. Add SearchInput from shared for table search/filter at the top
6. Use Tabs from ui/tabs if there are multiple view modes (e.g., full ER vs filtered ER)
7. Keep ALL ER diagram generation logic (relationship-inference.ts usage)

**MermaidRenderer.tsx:**
1. Read existing file
2. Restyle the mermaid rendering wrapper with theme-appropriate colors
3. This may use mermaid.js directly -- keep that, just update the mermaid theme config to use zenith-violet colors (dark background, violet accents)
4. Ensure the rendered SVG sits inside a Card or has proper rounded corners
5. Keep ALL mermaid initialization and rendering logic

**DbHistory.tsx:**
1. Read existing file
2. Replace with HistoryList from @renderer/components/shared/history-list
3. Map existing history entries to HistoryEntry format: { id, title (query snippet), subtitle (database name), timestamp, type (query/mutation), badge (status) }
4. onRestore: loads query back into QueryConsole
5. onDelete: removes history entry
6. Keep ALL history store connections
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>ER tab shows Mermaid diagram with search, History tab shows HistoryList</manual>
  </verify>
  <done>ERDiagram renders 2D Mermaid only (no SchemaOrb3D). MermaidRenderer uses theme colors. DbHistory uses shared HistoryList. All store connections preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassChat\|SchemaOrb3D\|GlassSurface" src/renderer/src/plugins/db-inspector/AskAI.tsx src/renderer/src/plugins/db-inspector/QueryOptimizer.tsx src/renderer/src/plugins/db-inspector/ERDiagram.tsx src/renderer/src/plugins/db-inspector/DbHistory.tsx` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- AskAI uses shared ChatInterface with run-sql actions
- QueryOptimizer uses shared ContentRenderer with mermaid
- ERDiagram shows 2D Mermaid only (no 3D)
- DbHistory uses shared HistoryList
- Zero Glass*/3D component imports in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-06-SUMMARY.md`
</output>
