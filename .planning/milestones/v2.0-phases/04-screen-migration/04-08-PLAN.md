---
phase: 04-screen-migration
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/textcraft/TextCraftView.tsx
  - src/renderer/src/plugins/textcraft/InputPanel.tsx
  - src/renderer/src/plugins/textcraft/ControlsPanel.tsx
  - src/renderer/src/plugins/textcraft/OutputPanel.tsx
  - src/renderer/src/plugins/textcraft/HistoryPanel.tsx
autonomous: true
requirements:
  - TXCR-01
  - TXCR-02
  - TXCR-03
  - TXCR-04
  - TXCR-05

must_haves:
  truths:
    - "TextCraftView uses PluginShell with 2 tabs (Refine, History)"
    - "Refine tab has 3-panel SplitPanel layout (input, controls, output)"
    - "InputPanel uses shared RichTextEditor (minimal mode)"
    - "ControlsPanel uses shadcn ToggleGroup, Select, Textarea, Button"
    - "OutputPanel uses shared ContentRenderer with collapsible+copy+export-pdf+save-as-note actions"
    - "HistoryPanel uses shared HistoryList"
  artifacts:
    - path: "src/renderer/src/plugins/textcraft/TextCraftView.tsx"
      provides: "TextCraft plugin shell"
      contains: "import.*PluginShell"
    - path: "src/renderer/src/plugins/textcraft/InputPanel.tsx"
      provides: "Text input with RichTextEditor"
      contains: "import.*RichTextEditor"
    - path: "src/renderer/src/plugins/textcraft/OutputPanel.tsx"
      provides: "AI output with ContentRenderer"
      contains: "import.*ContentRenderer"
    - path: "src/renderer/src/plugins/textcraft/HistoryPanel.tsx"
      provides: "Refinement history"
      contains: "import.*HistoryList"
  key_links:
    - from: "src/renderer/src/plugins/textcraft/TextCraftView.tsx"
      to: "shared plugin-shell + split-panel"
      via: "PluginShell wrapper and SplitPanel for 3-panel layout"
      pattern: "import.*(PluginShell|SplitPanel)"
    - from: "src/renderer/src/plugins/textcraft/OutputPanel.tsx"
      to: "shared content-renderer"
      via: "ContentRenderer for AI output"
      pattern: "import.*ContentRenderer.*from.*shared"
---

<objective>
Migrate TextCraft plugin from Glass components to shadcn/shared components with 3-panel SplitPanel layout.

Purpose: TextCraft is a clean 3-panel layout that showcases SplitPanel, RichTextEditor, and ContentRenderer together.
Output: 5 rewritten TextCraft component files.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/plugin-shell.tsx
@src/renderer/src/components/shared/split-panel.tsx
@src/renderer/src/components/shared/rich-text-editor.tsx
@src/renderer/src/components/shared/content-renderer.tsx
@src/renderer/src/components/shared/history-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate TextCraftView + InputPanel + ControlsPanel</name>
  <files>
    src/renderer/src/plugins/textcraft/TextCraftView.tsx
    src/renderer/src/plugins/textcraft/InputPanel.tsx
    src/renderer/src/plugins/textcraft/ControlsPanel.tsx
  </files>
  <action>
**TextCraftView.tsx:**
1. Read existing file to understand the 3-panel resizable layout, tab structure, store connections
2. Remove Glass imports (PluginHeader, GlassResizeHandle, GlassTab, etc.)
3. Import PluginShell from @renderer/components/shared/plugin-shell
4. Import SplitPanel from @renderer/components/shared/split-panel
5. Wrap in PluginShell with: title="TextCraft", icon=Wand2 (from lucide), tabs for Refine/History
6. Refine tab content: SplitPanel (direction="horizontal", sizes=[33, 34, 33]) with 3 children: InputPanel, ControlsPanel, OutputPanel
7. History tab content: HistoryPanel
8. REMOVE the custom clamp/resize logic -- SplitPanel handles resizing internally via react-resizable-panels
9. Keep ALL store connections (useTextCraftStore), state management

**InputPanel.tsx:**
1. Read existing file
2. Remove Glass imports
3. Wrap in Card (rounded-[22px]) with CardHeader "Input" label
4. Use RichTextEditor from @renderer/components/shared/rich-text-editor with mode="minimal"
5. Pass content from store, onChange to update store
6. Word count footer: show character/word count below editor (text-xs text-muted-foreground)
7. Keep ALL store connections

**ControlsPanel.tsx:**
1. Read existing file to understand tone selection, format, instructions, trigger button
2. Remove Glass imports
3. Wrap in Card (rounded-[22px])
4. Tone selector: use a grid of small Button components or a toggle group pattern -- check if ToggleGroup is available as a shadcn component, if not use a grid of Buttons with active state (bg-primary/18)
5. Format selector: Select from ui/select (e.g., "Formal", "Casual", "Technical")
6. Custom instructions: Textarea from ui/textarea
7. "Refine" Button (primary variant, full width) at bottom
8. Keep ALL refinement trigger logic, AI call, store connections
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>TextCraft shows 3-panel layout with RichTextEditor input, controls, and output</manual>
  </verify>
  <done>TextCraftView uses PluginShell+SplitPanel. InputPanel uses RichTextEditor (minimal). ControlsPanel uses Select+Textarea+Button. Zero Glass imports.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate OutputPanel + HistoryPanel</name>
  <files>
    src/renderer/src/plugins/textcraft/OutputPanel.tsx
    src/renderer/src/plugins/textcraft/HistoryPanel.tsx
  </files>
  <action>
**OutputPanel.tsx:**
1. Read existing file
2. Remove Glass imports
3. Wrap in Card (rounded-[22px]) with CardHeader "Output" label
4. Use ContentRenderer from @renderer/components/shared/content-renderer
5. Pass: content from store (refined text), isStreaming from store, collapsible=true, actions=['copy-raw', 'copy-md', 'export-pdf', 'save-as-note']
6. Implement onAction handler: copy-raw copies plain text, copy-md copies markdown, export-pdf triggers PdfExporter, save-as-note creates a Nebula note via IPC
7. Keep ALL streaming display logic, store connections

**HistoryPanel.tsx:**
1. Read existing file
2. Remove Glass imports
3. Use HistoryList from @renderer/components/shared/history-list
4. Map refinement history entries to HistoryEntry format: { id, title (first line of input), subtitle (tone + format), timestamp, badge (status) }
5. onRestore: loads the input+output back into the panels
6. onDelete: removes history entry
7. Keep ALL history store connections
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Output panel shows ContentRenderer with actions, History tab shows HistoryList</manual>
  </verify>
  <done>OutputPanel uses ContentRenderer with collapsible+copy+export+save actions. HistoryPanel uses shared HistoryList. All store connections preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassTab\|PluginHeader\|GlassResizeHandle\|GlassSurface" src/renderer/src/plugins/textcraft/` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- TextCraft shows PluginShell with Refine/History tabs
- Refine tab has 3-panel SplitPanel layout
- InputPanel uses shared RichTextEditor (minimal)
- OutputPanel uses shared ContentRenderer with all actions
- HistoryPanel uses shared HistoryList
- Zero Glass* imports
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-08-SUMMARY.md`
</output>
