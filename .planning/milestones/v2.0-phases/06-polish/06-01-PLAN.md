---
phase: 06-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/ui/card.tsx
  - src/renderer/src/components/ui/EmptyState.tsx
  - src/renderer/src/lib/micro-interactions.ts
  - src/renderer/src/plugins/cortex/CortexView.tsx
  - src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
  - src/renderer/src/plugins/nebula/NebulaView.tsx
  - src/renderer/src/plugins/textcraft/TextCraftView.tsx
  - src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
  - src/renderer/src/plugins/launchpad/LaunchpadView.tsx
autonomous: true
requirements:
  - POLS-01
  - POLS-02

must_haves:
  truths:
    - "Buttons scale to 0.97 on press with 150ms ease-out"
    - "Cards lift 1px on hover with border brighten, 200ms transition"
    - "Dialogs open with scale+fade animation"
    - "Every plugin shows an EmptyState component when it has no data"
    - "EmptyState shows icon, title, description, and action button"
  artifacts:
    - path: "src/renderer/src/lib/micro-interactions.ts"
      provides: "Shared CSS class constants and Framer Motion variants for micro-interactions"
      exports: ["cardHoverClasses", "interactiveCardClasses"]
    - path: "src/renderer/src/components/ui/EmptyState.tsx"
      provides: "Updated EmptyState with zenith tokens instead of old Glass CSS vars"
      contains: "text-muted-foreground"
    - path: "src/renderer/src/components/ui/card.tsx"
      provides: "Card with optional interactive hover variant"
      contains: "hover:-translate-y-px"
  key_links:
    - from: "src/renderer/src/plugins/*/View.tsx"
      to: "src/renderer/src/components/ui/EmptyState.tsx"
      via: "import { EmptyState }"
      pattern: "EmptyState"
    - from: "src/renderer/src/components/ui/card.tsx"
      to: "src/renderer/src/lib/micro-interactions.ts"
      via: "import interactiveCardClasses"
      pattern: "micro-interactions"
---

<objective>
Add micro-interaction animations to all interactive elements and wire EmptyState into every plugin.

Purpose: POLS-01 (micro-interactions on all interactive elements) and POLS-02 (empty states for every plugin). Makes the app feel polished and guides users when data is absent.
Output: micro-interactions utility, updated EmptyState, Card interactive variant, empty states wired into all 6 plugins.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@design-system/zenith/MASTER.md (Animation Rules + Component Specs sections)
@src/renderer/src/components/ui/button.tsx (already has active:scale-[0.97] and focus-visible)
@src/renderer/src/components/ui/card.tsx (needs interactive hover variant)
@src/renderer/src/components/ui/EmptyState.tsx (exists, needs token update)
@src/renderer/src/plugins/registry.ts (plugin list for empty state wiring)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add micro-interaction utilities and update Card + EmptyState</name>
  <files>
    src/renderer/src/lib/micro-interactions.ts
    src/renderer/src/components/ui/card.tsx
    src/renderer/src/components/ui/EmptyState.tsx
  </files>
  <action>
1. Create `src/renderer/src/lib/micro-interactions.ts`:
   - Export `interactiveCardClasses` = `"transition-all duration-200 ease-out hover:-translate-y-px hover:border-muted-foreground/30 hover:shadow-lg cursor-pointer"`
   - Export `pressScaleClasses` = `"active:scale-[0.97] transition-transform duration-150 ease-out"`
   - Export Framer Motion variant objects for dialog (scale+fade) and accordion (height) for consumers that need motion variants

2. Update `src/renderer/src/components/ui/card.tsx`:
   - Add an optional `interactive` boolean prop to `Card` (default false)
   - When `interactive=true`, merge the `interactiveCardClasses` from micro-interactions.ts into className
   - Keep existing non-interactive Card behavior unchanged

3. Update `src/renderer/src/components/ui/EmptyState.tsx`:
   - Replace old CSS variable references: `var(--text-secondary)` -> `text-muted-foreground`, `var(--text-primary)` -> `text-foreground`, `var(--accent)` -> `bg-primary`
   - Replace the raw `<button>` with the project's `Button` component (import from `@renderer/components/ui/button`)
   - Add `secondaryActionLabel?: string` and `onSecondaryAction?: () => void` optional props for a ghost-variant secondary button
   - Use `usePrefersReducedMotion` from `@renderer/lib/useReducedMotion` -- if reduced motion, skip the parallax mouse-move effect (set offset to {x:0,y:0} always)
   - Keep the existing framer-motion icon parallax for non-reduced-motion users
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify card.tsx exports Card with interactive prop, EmptyState uses design tokens</manual>
  </verify>
  <done>micro-interactions.ts exports utility classes; Card supports interactive prop; EmptyState uses zenith tokens, Button component, and respects reduced motion</done>
</task>

<task type="auto">
  <name>Task 2: Wire EmptyState into all 6 plugin views</name>
  <files>
    src/renderer/src/plugins/cortex/CortexView.tsx
    src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
    src/renderer/src/plugins/nebula/NebulaView.tsx
    src/renderer/src/plugins/textcraft/TextCraftView.tsx
    src/renderer/src/plugins/code-review-bot/CodeReviewBotView.tsx
    src/renderer/src/plugins/launchpad/LaunchpadView.tsx
  </files>
  <action>
For each plugin View, add an EmptyState that renders when the plugin has no data. Import `EmptyState` from `@renderer/components/ui/EmptyState` and relevant Lucide icons.

**Cortex** (`CortexView.tsx`):
- Condition: no repos analyzed (check the store/state for empty repos list)
- Icon: `Brain`, Title: "No repositories analyzed", Description: "Add a repository to get started with code analysis, insights, and documentation."
- Action: "Add Repository" -> triggers the add repo flow

**DB Inspector** (`DbInspectorView.tsx`):
- Condition: no database connections configured
- Icon: `Database`, Title: "No database connections", Description: "Connect to a PostgreSQL database to explore schemas, run queries, and get AI-powered insights."
- Action: "Add Connection" -> triggers add connection flow

**Nebula** (`NebulaView.tsx`):
- Condition: no notes exist
- Icon: `BookOpen`, Title: "No notes yet", Description: "Create your first note to start building your knowledge base with AI-powered search and summarization."
- Action: "Create Note" -> triggers new note creation

**TextCraft** (`TextCraftView.tsx`):
- Condition: no history entries exist (show in history panel only, not the main view)
- Icon: `PenLine`, Title: "No refinements yet", Description: "Write or paste text in the input panel and refine it with AI to see your history here."
- Action: none (no action button needed, user uses the input panel)

**Code Review Bot** (`CodeReviewBotView.tsx`):
- Condition: no PRs loaded or no repos configured
- Icon: `GitPullRequest`, Title: "No pull requests", Description: "Configure your Bitbucket repositories in Settings to start reviewing pull requests with AI."
- Action: "Open Settings" -> navigate to /settings

**Launchpad** (`LaunchpadView.tsx`):
- Condition: no estimations exist
- Icon: `Rocket`, Title: "No estimations yet", Description: "Select a cloud provider and configure services to generate your first cost estimation."
- Action: none (the main UI already has the provider selector)

For each plugin: Find the primary content area, wrap it in a conditional that checks the relevant data source. If empty, render `<EmptyState>`. If data exists, render the normal view. Do NOT restructure the existing component layout -- just add the conditional at the appropriate point.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Each plugin View imports EmptyState and conditionally renders it when data is empty</manual>
  </verify>
  <done>All 6 plugins show contextual EmptyState when they have no data, with appropriate icons, descriptions, and actions</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `grep -r "EmptyState" src/renderer/src/plugins/ --include="*.tsx"` shows imports in all 6 plugin View files
- `grep -r "var(--text-" src/renderer/src/components/ui/EmptyState.tsx` returns no matches (old Glass tokens removed)
- Card component accepts `interactive` prop without type error
</verification>

<success_criteria>
1. Button press animation (scale 0.97) already exists; Card now has hover lift animation when interactive=true
2. EmptyState uses zenith design tokens and Button component
3. All 6 plugin views conditionally show EmptyState with plugin-specific content
4. Reduced motion preference disables parallax effect in EmptyState
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-01-SUMMARY.md`
</output>
