---
phase: 06-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/layout/app-layout.tsx
  - src/renderer/src/hooks/useCommandPaletteItems.ts
  - src/renderer/src/hooks/useGlobalKeyboardShortcuts.ts
  - src/renderer/src/components/shared/command-palette.tsx
autonomous: true
requirements:
  - POLS-03
  - POLS-04

must_haves:
  truths:
    - "Cmd+K opens command palette with searchable list of all plugin routes"
    - "Command palette shows recent activity entries as searchable items"
    - "Command palette shows settings sections as searchable items"
    - "Cmd+B toggles sidebar collapse"
    - "Cmd+N creates new item in applicable plugins (Nebula note)"
    - "Escape closes open modals and command palette"
  artifacts:
    - path: "src/renderer/src/hooks/useCommandPaletteItems.ts"
      provides: "Hook that builds CommandPaletteItem[] from routes, activity, settings"
      exports: ["useCommandPaletteItems"]
    - path: "src/renderer/src/hooks/useGlobalKeyboardShortcuts.ts"
      provides: "Central keyboard shortcut handler for Cmd+B, Cmd+N, Escape"
      exports: ["useGlobalKeyboardShortcuts"]
    - path: "src/renderer/src/components/layout/app-layout.tsx"
      provides: "AppLayout wrapping children in CommandPaletteProvider"
      contains: "CommandPaletteProvider"
  key_links:
    - from: "src/renderer/src/components/layout/app-layout.tsx"
      to: "src/renderer/src/components/shared/command-palette.tsx"
      via: "CommandPaletteProvider wrapping Outlet"
      pattern: "CommandPaletteProvider"
    - from: "src/renderer/src/hooks/useCommandPaletteItems.ts"
      to: "src/renderer/src/plugins/registry.ts"
      via: "PLUGINS array for route items"
      pattern: "PLUGINS"
    - from: "src/renderer/src/hooks/useGlobalKeyboardShortcuts.ts"
      to: "src/renderer/src/components/layout/sidebar.tsx"
      via: "useSidebarCollapsed for Cmd+B"
      pattern: "useSidebarCollapsed"
---

<objective>
Wire the CommandPalette to all routes, activity, and settings, and audit all keyboard shortcuts.

Purpose: POLS-03 (command palette wired to all routes/activity/settings) and POLS-04 (keyboard shortcuts Cmd+K, Cmd+B, Cmd+N, Escape). The command palette already exists as a component but is not mounted or populated with real data.
Output: CommandPaletteProvider mounted in AppLayout, items hook populating routes/activity/settings, global keyboard shortcut handler.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/renderer/src/components/shared/command-palette.tsx (CommandPaletteProvider, useCommandPalette, CommandPaletteItem type)
@src/renderer/src/components/layout/app-layout.tsx (needs to wrap with CommandPaletteProvider)
@src/renderer/src/components/layout/sidebar.tsx (useSidebarCollapsed export, Cmd+B already handled here)
@src/renderer/src/plugins/registry.ts (PLUGINS array with id, name, icon, route)
@src/renderer/src/App.tsx (routes: /dashboard, /activity, /about, /settings, plugin routes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useCommandPaletteItems hook and useGlobalKeyboardShortcuts</name>
  <files>
    src/renderer/src/hooks/useCommandPaletteItems.ts
    src/renderer/src/hooks/useGlobalKeyboardShortcuts.ts
  </files>
  <action>
1. Create `src/renderer/src/hooks/useCommandPaletteItems.ts`:
   - Import `PLUGINS` from `@renderer/plugins/registry`, `useNavigate` from `react-router-dom`, and Lucide icons (`LayoutDashboard`, `Activity`, `Info`, `Settings`, `Search`).
   - Build and return a `CommandPaletteItem[]` with three groups:

   **Group "Navigation" (all routes):**
   - Dashboard: id=`nav-dashboard`, label="Dashboard", icon=LayoutDashboard, route=/dashboard
   - Activity Log: id=`nav-activity`, label="Activity Log", icon=Activity, route=/activity
   - About: id=`nav-about`, label="About", icon=Info, route=/about
   - Settings: id=`nav-settings`, label="Settings", icon=Settings, route=/settings, shortcut="Cmd+,"
   - Each plugin from PLUGINS: id=`nav-${plugin.id}`, label=plugin.name, icon from ICON_MAP (duplicate the icon map from sidebar or import it), route=plugin.route
   - Each item's `onSelect` calls `navigate(route)`

   **Group "Settings" (settings sections):**
   - General: id=`settings-general`, label="General Settings", onSelect -> navigate("/settings") (settings uses tabs internally)
   - AI Agents: id=`settings-ai-agents`, label="AI Agents Settings"
   - MCP Servers: id=`settings-mcp`, label="MCP Server Settings"
   - Plugins: id=`settings-plugins`, label="Plugin Settings"
   - Connections: id=`settings-connections`, label="Connection Settings"

   **Group "Actions":**
   - New Note: id=`action-new-note`, label="New Note", shortcut="Cmd+N", onSelect -> navigate("/nebula") (Nebula handles new note creation)
   - Toggle Sidebar: id=`action-sidebar`, label="Toggle Sidebar", shortcut="Cmd+B"

   Return items via `useMemo` keyed on `navigate` reference.

2. Create `src/renderer/src/hooks/useGlobalKeyboardShortcuts.ts`:
   - Export `useGlobalKeyboardShortcuts()` hook
   - Uses `useEffect` to register a single `keydown` listener on `window`
   - Import `useNavigate` from react-router-dom
   - Import `useLocation` from react-router-dom
   - Shortcuts:
     - `Cmd+N`: Navigate to /nebula (new note). Only if current route is not already on a text input (check `document.activeElement?.tagName` is not INPUT/TEXTAREA/[contenteditable])
     - `Escape`: If no dialog/modal is open (check `document.querySelector('[role="dialog"]')`), do nothing. The command palette and dialogs already handle their own Escape.
   - NOTE: Cmd+K is already handled by CommandPaletteProvider. Cmd+B is already handled by Sidebar. This hook only handles shortcuts not covered elsewhere.
   - Keep it minimal -- don't duplicate existing handlers
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Hooks export correctly, items cover all routes and settings sections</manual>
  </verify>
  <done>useCommandPaletteItems returns items for all routes + settings + actions; useGlobalKeyboardShortcuts handles Cmd+N</done>
</task>

<task type="auto">
  <name>Task 2: Mount CommandPaletteProvider in AppLayout and wire items</name>
  <files>
    src/renderer/src/components/layout/app-layout.tsx
    src/renderer/src/components/shared/command-palette.tsx
  </files>
  <action>
1. Update `src/renderer/src/components/layout/app-layout.tsx`:
   - Import `CommandPaletteProvider` from `@renderer/components/shared/command-palette`
   - Import `useCommandPaletteItems` from `@renderer/hooks/useCommandPaletteItems`
   - Import `useGlobalKeyboardShortcuts` from `@renderer/hooks/useGlobalKeyboardShortcuts`
   - Call `useGlobalKeyboardShortcuts()` at the top of AppLayout
   - Call `const paletteItems = useCommandPaletteItems()`
   - Wrap the entire AppLayout return (the flex container with Sidebar + content) inside `<CommandPaletteProvider defaultItems={paletteItems}>`
   - Keep existing Sidebar, drag region, AnimatePresence, and Outlet structure unchanged

2. Minor update to `command-palette.tsx` if needed:
   - Verify the Dialog gets `aria-label="Command palette"` for accessibility
   - Ensure Escape closes the palette (already handled by Dialog component's onOpenChange)
   - Add a visual shortcut hint in the input: show a faded "Cmd+K" badge in the command input area to hint at the shortcut
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Start app, press Cmd+K -- palette opens with Navigation, Settings, and Actions groups. Search filters across all groups. Selecting a route navigates there. Escape closes.</manual>
  </verify>
  <done>CommandPalette is mounted globally, populated with all routes + settings + actions, Cmd+K opens it, all keyboard shortcuts (Cmd+K, Cmd+B, Cmd+N, Escape) work</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep -r "CommandPaletteProvider" src/renderer/src/components/layout/app-layout.tsx` shows the provider is mounted
- `grep -r "useCommandPaletteItems" src/renderer/src/hooks/` confirms hook exists
- `grep -r "useGlobalKeyboardShortcuts" src/renderer/src/hooks/` confirms hook exists
</verification>

<success_criteria>
1. Cmd+K opens command palette with all plugin routes, system routes, settings sections, and actions
2. Search filters items across all groups
3. Selecting a navigation item navigates to that route
4. Cmd+B toggles sidebar (already working, verified not broken)
5. Cmd+N navigates to Nebula (new note context)
6. Escape closes command palette and modals
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-02-SUMMARY.md`
</output>
