---
phase: 17-integrations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/integrations/credentials.ts
autonomous: true
requirements: [TDATA-03]

must_haves:
  truths:
    - "Jira, Confluence, and Google credentials can be saved encrypted and retrieved in main process"
    - "A stored credential returns its decrypted value; an absent key returns null"
    - "Helpers are callable from any main-process module (IPC handlers, client files)"
  artifacts:
    - path: "src/main/integrations/credentials.ts"
      provides: "saveIntegrationCredential, getIntegrationCredential, hasIntegrationCredential, deleteIntegrationCredential, getIntegrationCredentialMasked"
      exports: ["saveIntegrationCredential", "getIntegrationCredential", "hasIntegrationCredential", "deleteIntegrationCredential", "getIntegrationCredentialMasked", "CRED_JIRA_BASE_URL", "CRED_JIRA_EMAIL", "CRED_JIRA_API_TOKEN", "CRED_JIRA_PROJECTS", "CRED_CONFLUENCE_BASE_URL", "CRED_CONFLUENCE_EMAIL", "CRED_CONFLUENCE_API_TOKEN"]
  key_links:
    - from: "src/main/integrations/credentials.ts"
      to: "electron safeStorage"
      via: "safeStorage.encryptString / decryptString"
      pattern: "safeStorage\\.encryptString"
---

<objective>
Create the integrations credential store module — a thin safeStorage wrapper for Jira, Confluence, and Google credential persistence.

Purpose: Phase 17 API clients (Plans 02 and 03) and IPC handlers (Plan 04) need a single, isolated credential store. Mirror the pattern in src/main/pricing/credentials.ts exactly.
Output: src/main/integrations/credentials.ts with named key constants and five helper functions.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@src/main/pricing/credentials.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/main/integrations/credentials.ts</name>
  <files>src/main/integrations/credentials.ts</files>
  <action>
Create the directory src/main/integrations/ and write credentials.ts.

Pattern: copy the structure from src/main/pricing/credentials.ts exactly — same Store, same five functions (saveCredential, getCredential, hasCredential, deleteCredential, getCredentialMasked), same safeStorage pattern.

Use a dedicated electron-store instance named 'zenith-integrations-credentials' (isolated from other credential stores).

Export these named key constants:
- CRED_JIRA_BASE_URL = 'integrations.jira.baseUrl'
- CRED_JIRA_EMAIL = 'integrations.jira.email'
- CRED_JIRA_API_TOKEN = 'integrations.jira.apiToken'
- CRED_JIRA_PROJECTS = 'integrations.jira.projects'  (comma-separated project keys, not encrypted — plain setting but stored here for colocation)
- CRED_CONFLUENCE_BASE_URL = 'integrations.confluence.baseUrl'
- CRED_CONFLUENCE_EMAIL = 'integrations.confluence.email'
- CRED_CONFLUENCE_API_TOKEN = 'integrations.confluence.apiToken'

IMPORTANT: CRED_JIRA_PROJECTS is NOT a secret (project keys like "PROJ, ENG" are not sensitive). Store it as plaintext via a separate Store.set call (not via safeStorage). Add a separate pair of helpers: saveIntegrationSetting(key, value) / getIntegrationSetting(key): string | null that use the same store but WITHOUT safeStorage encryption. This handles the non-secret project keys field.

Export the five safeStorage helpers with the 'Integration' suffix to avoid collision with pricing/credentials.ts if both are ever imported together:
- saveIntegrationCredential(key, value)
- getIntegrationCredential(key): string | null
- hasIntegrationCredential(key): boolean
- deleteIntegrationCredential(key): void
- getIntegrationCredentialMasked(key): string | null (last 4 chars visible, rest bullet-masked)

And the two plaintext helpers:
- saveIntegrationSetting(key, value)
- getIntegrationSetting(key): string | null
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | grep -E "integrations/credentials" | head -20; echo "TypeScript exit: $?"</automated>
    <manual>File exists at src/main/integrations/credentials.ts with all seven exports and two store helpers</manual>
  </verify>
  <done>
    - src/main/integrations/credentials.ts exists
    - Five safeStorage helpers exported with Integration suffix
    - Two plaintext helpers exported (saveIntegrationSetting, getIntegrationSetting)
    - Seven CRED_* key constants exported
    - TypeScript compiles with zero errors in this file
  </done>
</task>

</tasks>

<verification>
Run: cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30
Expected: zero errors referencing integrations/credentials.ts
</verification>

<success_criteria>
- src/main/integrations/credentials.ts exists and exports all named constants and helpers
- TypeScript compilation passes with no errors in this file
- Pattern mirrors src/main/pricing/credentials.ts
</success_criteria>

<output>
After completion, create .planning/phases/17-integrations/17-01-SUMMARY.md
</output>
