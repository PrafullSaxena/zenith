---
phase: 11-visualizations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/launchpad/charts/ComparisonBarChart.tsx
  - src/renderer/src/plugins/launchpad/charts/HistoryTrendLine.tsx
  - src/renderer/src/plugins/launchpad/ComparisonView.tsx
  - src/renderer/src/plugins/launchpad/EstimationHistory.tsx
autonomous: true
requirements: [VIZ-04, VIZ-05, VIZ-06]

must_haves:
  truths:
    - "ComparisonView renders a horizontal grouped bar chart with one group per service family and three bars per group (AWS/GCP/Azure)"
    - "The cheapest provider bar in each group is highlighted green"
    - "History tab renders a multi-line trend chart plotting saved estimations over time with one line per provider"
    - "Hovering a trend point shows the estimation name and cost breakdown"
    - "All chart colors use CSS custom properties or the shared provider brand colors"
  artifacts:
    - path: "src/renderer/src/plugins/launchpad/charts/ComparisonBarChart.tsx"
      provides: "Horizontal grouped bar chart for provider comparison"
      min_lines: 60
    - path: "src/renderer/src/plugins/launchpad/charts/HistoryTrendLine.tsx"
      provides: "Multi-line trend chart for estimation history"
      min_lines: 60
    - path: "src/renderer/src/plugins/launchpad/ComparisonView.tsx"
      provides: "Updated ComparisonView integrating the bar chart"
    - path: "src/renderer/src/plugins/launchpad/EstimationHistory.tsx"
      provides: "Updated History tab with trend line above the card list"
  key_links:
    - from: "src/renderer/src/plugins/launchpad/ComparisonView.tsx"
      to: "charts/ComparisonBarChart.tsx"
      via: "import and render with rows comparison data"
      pattern: "import.*ComparisonBarChart"
    - from: "src/renderer/src/plugins/launchpad/EstimationHistory.tsx"
      to: "charts/HistoryTrendLine.tsx"
      via: "import and render with history entries"
      pattern: "import.*HistoryTrendLine"
    - from: "charts/ComparisonBarChart.tsx"
      to: "charts/chart-utils.ts"
      via: "PROVIDER_COLORS for bar fills"
      pattern: "import.*PROVIDER_COLORS|chart-utils"
---

<objective>
Build the ComparisonView grouped bar chart and the History trend line chart. The bar chart replaces the current card-based comparison layout with a visual chart plus the existing detail below. The trend line sits above the history card list.

Purpose: Makes provider cost comparison and historical trends instantly scannable with rich charts.
Output: Two new chart components wired into existing views.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-visualizations/11-CONTEXT.md
@src/renderer/src/plugins/launchpad/ComparisonView.tsx
@src/renderer/src/plugins/launchpad/EstimationHistory.tsx
@src/renderer/src/types/launchpad.ts
@src/renderer/src/data/cloud-pricing/equivalences.ts
@src/renderer/src/data/cloud-pricing/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ComparisonBarChart and wire into ComparisonView</name>
  <files>
    src/renderer/src/plugins/launchpad/charts/ComparisonBarChart.tsx
    src/renderer/src/plugins/launchpad/ComparisonView.tsx
  </files>
  <action>
1. Create `charts/ComparisonBarChart.tsx`:
   - Props: `{ data: Array<{ serviceName: string; aws: number | null; gcp: number | null; azure: number | null }> }`
   - Uses Recharts `<BarChart>` with `layout="vertical"` (horizontal bars, service names as Y-axis labels per user decision).
   - `<YAxis dataKey="serviceName" type="category" width={140} tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 11 }} />`
   - `<XAxis type="number" tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 10 }} tickFormatter={(v) => '$' + v.toFixed(0)} />`
   - Three `<Bar>` components: `dataKey="aws"`, `dataKey="gcp"`, `dataKey="azure"` with fills from `PROVIDER_COLORS` in chart-utils.ts (import from `./chart-utils`).
   - Custom `<Cell>` rendering: for each bar group, find the cheapest non-null provider. The cheapest bar gets `fill="hsl(var(--success))"` (green highlight) with a subtle glow via `filter: drop-shadow(0 0 4px hsl(var(--success) / 0.3))`. Other bars keep their provider brand color but slightly desaturated for dark theme readability.
   - Custom tooltip: shows service name, then each provider's cost on separate lines with provider color dots. Shows "N/A" for null values.
   - No legend (per user decision: inline labels + tooltips for detail). Provider color indicators in the tooltip suffice.
   - `barGap={2}` `barCategoryGap="20%"` for visual separation between groups.
   - Height: dynamic based on data length — `Math.max(300, data.length * 60)` px.
   - Grid: `<CartesianGrid strokeDasharray="3 3" stroke="hsl(var(--border))" opacity={0.3} />`

2. Update `ComparisonView.tsx`:
   - Import `ComparisonBarChart` from `./charts/ComparisonBarChart`.
   - Keep the existing `rows` computation and `totals`/`cheapestProvider` logic.
   - Transform `rows` into chart data: `rows.map(row => ({ serviceName: row.sourceName, aws: row.costs.aws, gcp: row.costs.gcp, azure: row.costs.azure }))`.
   - Add the `<ComparisonBarChart>` above the existing side-by-side cards layout. Wrap in a section with heading "Cost Comparison" and a thin border-bottom separator.
   - Keep the existing provider cards below as the detail view (they provide per-service breakdown with green/red highlights that the chart supplements).
   - The bar chart section should be scrollable if it exceeds viewport height (wrap in `overflow-auto max-h-[400px]`).
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Open Launchpad Compare tab with services selected. Verify horizontal grouped bar chart renders above the provider cards. Verify cheapest bar is green. Hover bars to see tooltip with provider costs.</manual>
  </verify>
  <done>ComparisonView shows a horizontal grouped bar chart with provider-colored bars; cheapest provider bar highlighted green; existing card detail view preserved below</done>
</task>

<task type="auto">
  <name>Task 2: Create HistoryTrendLine and wire into EstimationHistory</name>
  <files>
    src/renderer/src/plugins/launchpad/charts/HistoryTrendLine.tsx
    src/renderer/src/plugins/launchpad/EstimationHistory.tsx
  </files>
  <action>
1. Create `charts/HistoryTrendLine.tsx`:
   - Props: `{ entries: EstimationEntry[] }` (import `EstimationEntry` from `../../types/launchpad`).
   - Data transformation: group entries by provider, plot X = savedAt (parsed as Date), Y = totalMonthly. Each provider becomes a separate `<Line>` with its brand color from `PROVIDER_COLORS`.
   - Uses Recharts `<LineChart>` with `<ResponsiveContainer width="100%" height={250}>`.
   - `<XAxis dataKey="date" type="number" domain={['dataMin', 'dataMax']} scale="time" tickFormatter={(ts) => new Date(ts).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })} tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 10 }} />`
   - `<YAxis tickFormatter={(v) => '$' + v.toFixed(0)} tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 10 }} />`
   - One `<Line>` per provider that has entries: `type="monotone"`, `strokeWidth={2}`, `dot={{ r: 4, fill: providerColor }}`, `activeDot={{ r: 6, stroke: providerColor, strokeWidth: 2 }}`.
   - Per user decision: when 3+ providers overlap, use slight line thickness variation (AWS: 2.5, GCP: 2, Azure: 1.5) and dash patterns (AWS: solid, GCP: "5 3", Azure: "2 2") for readability.
   - Custom tooltip: dark card showing estimation name, date, provider, and totalMonthly. If an estimation has multiple services, show top 3 services with costs in the tooltip breakdown.
   - Grid: `<CartesianGrid strokeDasharray="3 3" stroke="hsl(var(--border))" opacity={0.3} />`
   - Data preparation: sort entries by `savedAt` ascending, convert to `{ date: Date.parse(entry.savedAt), name: entry.name, provider: entry.provider, totalMonthly: entry.totalMonthly, services: entry.services }`. Create separate data arrays per provider for multi-line rendering.
   - Handle scrollable X-axis for many entries: show last 15 by default. If more than 15 entries exist, only render the most recent 15 (user decision says "last 10-15 by default"). Add a small "Show all" toggle button that expands to full dataset.
   - Empty guard: if entries.length < 2, render a subtle message "Need at least 2 saved estimations to show trends" instead of the chart.

2. Update `EstimationHistory.tsx`:
   - Import `HistoryTrendLine` from `./charts/HistoryTrendLine`.
   - Above the existing `motion.div` card list (inside the `history.length > 0` branch), add the trend line chart:
     ```
     <div className="mb-4 rounded-xl border border-white/5 bg-black/20 backdrop-blur-md p-4">
       <p className="text-xs font-semibold text-[hsl(var(--foreground))] mb-3">Cost Trend</p>
       <HistoryTrendLine entries={history} />
     </div>
     ```
   - Keep all existing history card rendering, load/delete functionality unchanged.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Save 3+ estimations across different providers. Switch to History tab. Verify multi-line trend chart renders above the card list. Hover data points to see estimation name and cost breakdown. Verify provider lines use distinct colors/dash patterns.</manual>
  </verify>
  <done>History tab shows a trend line chart above the card list; each provider has its own colored line; hovering a point shows estimation name + cost breakdown; chart handles fewer than 2 entries gracefully</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- TypeScript compilation passes (`npx tsc --noEmit`)
- ComparisonView renders grouped bar chart above existing cards
- Cheapest provider bar highlighted green in each service group
- History tab renders multi-line trend chart when 2+ estimations saved
- Tooltips work on both charts with dark card styling
- No hardcoded colors in chart components except provider brand colors from chart-utils
</verification>

<success_criteria>
- ComparisonView has a horizontal grouped bar chart with AWS/GCP/Azure bars per service family
- Cheapest provider bar is green-highlighted
- History tab has a multi-provider trend line above the card list
- Hover on trend points shows estimation name and cost breakdown
- Provider lines are visually distinguishable (color + dash pattern)
- All chart theming uses CSS custom properties
</success_criteria>

<output>
After completion, create `.planning/phases/11-visualizations/11-02-SUMMARY.md`
</output>
