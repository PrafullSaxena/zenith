---
phase: 11-visualizations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/renderer/src/plugins/launchpad/charts/CostTreemap.tsx
  - src/renderer/src/plugins/launchpad/charts/CategoryDonut.tsx
  - src/renderer/src/plugins/launchpad/charts/chart-utils.ts
  - src/renderer/src/plugins/launchpad/EstimationSummary.tsx
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-06]

must_haves:
  truths:
    - "EstimationSummary renders a treemap showing cost distribution by service with category color-coding"
    - "Hovering a treemap cell shows service name, cost, and percentage of total"
    - "EstimationSummary renders a donut chart showing spending split by category alongside the treemap"
    - "Treemap and donut cross-highlight: hovering a category in one dims non-matching in the other"
    - "All chart colors come from CSS custom properties, no hardcoded hex in chart components"
  artifacts:
    - path: "src/renderer/src/plugins/launchpad/charts/CostTreemap.tsx"
      provides: "Recharts Treemap wrapper for cost distribution"
      min_lines: 60
    - path: "src/renderer/src/plugins/launchpad/charts/CategoryDonut.tsx"
      provides: "Recharts PieChart donut for category spending"
      min_lines: 50
    - path: "src/renderer/src/plugins/launchpad/charts/chart-utils.ts"
      provides: "Shared chart color mapping and tooltip styling"
      min_lines: 30
    - path: "src/renderer/src/plugins/launchpad/EstimationSummary.tsx"
      provides: "Updated layout with treemap + donut side-by-side"
  key_links:
    - from: "src/renderer/src/plugins/launchpad/EstimationSummary.tsx"
      to: "charts/CostTreemap.tsx"
      via: "import and render with fullResult.items + selectedServices"
      pattern: "import.*CostTreemap"
    - from: "src/renderer/src/plugins/launchpad/EstimationSummary.tsx"
      to: "charts/CategoryDonut.tsx"
      via: "import and render with category-aggregated data"
      pattern: "import.*CategoryDonut"
    - from: "charts/chart-utils.ts"
      to: "CSS custom properties"
      via: "getComputedStyle reading --chart-1 through --chart-5"
      pattern: "getComputedStyle|--chart-"
---

<objective>
Install recharts and build the EstimationSummary charts: a Treemap showing per-service cost distribution (color-coded by category) and a Donut chart showing per-category spending. Replace the existing CostTreemapFallback bar chart.

Purpose: Makes cost distribution immediately scannable with rich interactive visualizations.
Output: Two reusable chart components, shared chart utilities, updated EstimationSummary layout.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-visualizations/11-CONTEXT.md
@src/renderer/src/plugins/launchpad/EstimationSummary.tsx
@src/renderer/src/types/launchpad.ts
@src/renderer/src/data/cloud-pricing/calculator.ts
@src/renderer/src/assets/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts + create chart utilities and chart components</name>
  <files>
    package.json
    src/renderer/src/plugins/launchpad/charts/chart-utils.ts
    src/renderer/src/plugins/launchpad/charts/CostTreemap.tsx
    src/renderer/src/plugins/launchpad/charts/CategoryDonut.tsx
  </files>
  <action>
1. Install recharts: `npm install recharts` (no @types needed, recharts ships its own types).

2. Create `charts/chart-utils.ts` with:
   - `CATEGORY_COLORS` constant: maps category IDs (compute, containers, storage, database, networking, serverless, ml-ai, analytics) to CSS variable references. Use `--chart-1` through `--chart-5` for the first 5 categories, then cycle with opacity variations (0.7, 0.5) per user decision.
   - `getCategoryColor(categoryId: string, index: number): string` — returns `hsl(var(--chart-N))` for the category, with opacity cycling for overflow categories.
   - `PROVIDER_COLORS` constant: `{ aws: '#FF9900', gcp: '#4285F4', azure: '#0078D4' }` — brand colors per user decision. These are the ONLY hardcoded hex values, and only used for provider identity, not chart theming.
   - `CustomTooltip` shared styled wrapper: compact dark card (`bg-black/90 backdrop-blur-md border border-white/10 rounded-lg px-3 py-2 shadow-xl text-xs`). Takes children as render prop.
   - Type: `ChartDataItem = { name: string; value: number; categoryId: string; serviceId: string; fill: string }`

3. Create `charts/CostTreemap.tsx`:
   - Props: `{ items: Array<{ serviceId: string; serviceName: string; monthly: number; categoryId?: string }>; highlightCategory: string | null; onCategoryHover: (categoryId: string | null) => void }`
   - Uses Recharts `<Treemap>` component with `dataKey="value"`, `aspectRatio={4/3}`.
   - Map `items` to treemap data: `{ name: serviceName, value: monthly, categoryId, fill: getCategoryColor(categoryId) }`.
   - Custom content renderer (`<rect>`) that renders service name + cost inside each cell. Cells with `categoryId` not matching `highlightCategory` (when set) render at 30% opacity (cross-highlight dimming per user decision).
   - Custom tooltip: shows service name, `$XX.XX`, and `XX.X%` of total cost.
   - Height: 350px per user decision.
   - No animation on initial render (set `isAnimationActive={false}` for treemap to avoid layout jank).

4. Create `charts/CategoryDonut.tsx`:
   - Props: `{ items: Array<{ serviceId: string; serviceName: string; monthly: number; categoryId?: string }>; highlightCategory: string | null; onCategoryHover: (categoryId: string | null) => void }`
   - Aggregate items by `categoryId` to produce `{ name: categoryName, value: totalMonthlyCost, categoryId, fill }`.
   - Uses Recharts `<PieChart>` with `<Pie>` configured as donut (`innerRadius="55%"` `outerRadius="85%"`).
   - Each `<Cell>` uses `getCategoryColor()` fill. When `highlightCategory` is set, non-matching cells render at 30% opacity.
   - `onMouseEnter` on each cell calls `onCategoryHover(categoryId)`, `onMouseLeave` calls `onCategoryHover(null)`.
   - Center label: total cost formatted as `$X,XXX`.
   - Custom tooltip: category name, cost, percentage.
   - Height: 350px.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify recharts is in package.json dependencies, all three chart files exist and export their components</manual>
  </verify>
  <done>recharts installed, CostTreemap and CategoryDonut components compile without errors, chart-utils exports shared color/tooltip utilities</done>
</task>

<task type="auto">
  <name>Task 2: Wire charts into EstimationSummary with cross-highlighting</name>
  <files>
    src/renderer/src/plugins/launchpad/EstimationSummary.tsx
  </files>
  <action>
1. In `EstimationSummary.tsx`:
   - Add `import CostTreemap from './charts/CostTreemap'` and `import CategoryDonut from './charts/CategoryDonut'`.
   - Add state: `const [highlightCategory, setHighlightCategory] = useState<string | null>(null)`.
   - Remove the entire `CostTreemapFallback` function and its constants (`FALLBACK_CATEGORY_COLORS`, `FALLBACK_DEFAULT_COLOR`).
   - Replace the "Cost Distribution (2D fallback)" section (the `border-t` div containing `CostTreemapFallback`) with a new chart section:

   ```
   {/* Charts — treemap ~60% width, donut ~40% per user decision */}
   {fullResult && selectedServices.length > 0 && (
     <div className="border-t border-white/6 px-4 pt-3 pb-2">
       <p className="text-[10px] text-[hsl(var(--muted-foreground))] mb-2">Cost Distribution</p>
       <div className="flex gap-4">
         <div className="w-[60%]">
           <CostTreemap
             items={enrichedItems}
             highlightCategory={highlightCategory}
             onCategoryHover={setHighlightCategory}
           />
         </div>
         <div className="w-[40%]">
           <CategoryDonut
             items={enrichedItems}
             highlightCategory={highlightCategory}
             onCategoryHover={setHighlightCategory}
           />
         </div>
       </div>
     </div>
   )}
   ```

   - Create `enrichedItems` by joining `fullResult.items` with `selectedServices` to get `categoryId`:
     ```
     const enrichedItems = fullResult ? fullResult.items.map(item => ({
       ...item,
       categoryId: selectedServices.find(s => s.serviceId === item.serviceId)?.categoryId
     })) : []
     ```

2. Verify the cross-highlighting works: hovering treemap cell sets `highlightCategory`, which dims non-matching donut segments, and vice versa.

3. Keep all existing functionality (line items, totals, save, export, region picker) unchanged.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Open Launchpad, select a provider, add 3+ services from different categories. Verify treemap and donut render side-by-side. Hover a treemap cell and confirm donut dims non-matching segments. Hover donut segment and confirm treemap dims non-matching cells.</manual>
  </verify>
  <done>EstimationSummary shows treemap (60% width) and donut (40% width) side-by-side with cross-highlighting; CostTreemapFallback is removed; all existing functionality preserved</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- TypeScript compilation passes (`npx tsc --noEmit`)
- EstimationSummary renders treemap + donut when services are selected
- Tooltips show service name, cost, percentage
- Cross-highlighting between treemap and donut works
- No hardcoded hex colors in chart components (except provider brand colors in chart-utils.ts)
</verification>

<success_criteria>
- recharts is installed and importable
- Treemap replaces the old CostTreemapFallback bar chart
- Donut chart shows category spending breakdown
- Both charts render at ~350px height, side-by-side (60/40 split)
- Hovering one chart cross-highlights the other
- All colors derived from CSS custom properties
</success_criteria>

<output>
After completion, create `.planning/phases/11-visualizations/11-01-SUMMARY.md`
</output>
