---
phase: 10-settings-ux-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/settings/SettingsField.tsx
autonomous: true
requirements:
  - SETT-UX-01

must_haves:
  truths:
    - "All text inputs, selects, and textareas rendered by SettingsField use rounded-lg with consistent focus ring"
    - "Toggle switch uses unified h-5 w-10 sizing with translate-x-5 on state"
    - "Password visibility icon is size 14"
    - "All buttons in SettingsField (directory browse, reset) use rounded-lg"
  artifacts:
    - path: "src/renderer/src/components/settings/SettingsField.tsx"
      provides: "Universal form field component with unified design tokens"
      contains: "rounded-lg"
  key_links:
    - from: "src/renderer/src/components/settings/SettingsField.tsx"
      to: "all settings views"
      via: "imported and rendered by GeneralSettings, PluginSettings, etc."
      pattern: "rounded-lg border-border/50"
---

<objective>
Unify SettingsField.tsx -- the universal form component used by all settings views -- with standardized design tokens for inputs, selects, toggles, buttons, and password icon.

Purpose: SettingsField is the foundation component. Fixing it first propagates consistent styling to GeneralSettings, PluginSettings, and any plugin-specific settings that use it. All downstream plans depend on these tokens being correct.

Output: Updated SettingsField.tsx with unified Tailwind classes.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-settings-ux-fix/10-RESEARCH.md
@src/renderer/src/components/settings/SettingsField.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update inputClass, selectClass, and all button/toggle classes in SettingsField.tsx</name>
  <files>src/renderer/src/components/settings/SettingsField.tsx</files>
  <action>
This is a VISUAL-ONLY change. Modify Tailwind CSS classes only. Do NOT change any logic, state, props, imports, or component structure.

**1. Replace the `inputClass` constant (line 16-17):**

OLD:
```
'w-full rounded-md border border-border bg-surface-elevated px-3 py-2 text-sm text-text-primary placeholder:text-text-secondary/50 focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition'
```

NEW:
```
'w-full rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary placeholder:text-text-secondary/50 focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30 transition'
```

Changes: `rounded-md` -> `rounded-lg`, `border-border` -> `border-border/50`, `bg-surface-elevated` -> `bg-surface`, `py-2` -> `py-1.5`, `focus:ring-2 focus:ring-accent focus:border-transparent` -> `focus:border-accent focus:ring-1 focus:ring-accent/30`

**2. Replace the `selectClass` constant (line 19-20):**

OLD:
```
'w-full rounded-md border border-border bg-surface-elevated px-3 py-2 text-sm text-text-primary focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition appearance-none'
```

NEW:
```
'w-full rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30 transition appearance-none'
```

Same changes as inputClass plus keeping `appearance-none`.

**3. Update the boolean/toggle switch (lines 76-86):**

OLD outer button classes:
```
`relative inline-flex h-6 w-11 items-center rounded-full transition ${
  value ? 'bg-accent' : 'bg-surface-elevated border border-border'
}`
```

NEW outer button classes:
```
`relative inline-flex h-5 w-10 items-center rounded-full transition ${
  value ? 'bg-accent' : 'bg-surface-elevated border border-border'
}`
```

Changes: `h-6 w-11` -> `h-5 w-10`

OLD inner span classes:
```
`inline-block h-4 w-4 transform rounded-full bg-text-primary transition ${
  value ? 'translate-x-6' : 'translate-x-1'
}`
```

NEW inner span classes:
```
`inline-block h-4 w-4 transform rounded-full bg-text-primary transition ${
  value ? 'translate-x-5' : 'translate-x-0.5'
}`
```

Changes: `translate-x-6` -> `translate-x-5`, `translate-x-1` -> `translate-x-0.5`

**4. Update the password visibility icon size (line 54):**

OLD: `{showPassword ? <EyeOff size={16} /> : <Eye size={16} />}`
NEW: `{showPassword ? <EyeOff size={14} /> : <Eye size={14} />}`

**5. Update directory browse button (line 143):**

OLD:
```
"flex items-center gap-1.5 rounded-md border border-border bg-surface-elevated px-3 py-2 text-sm text-text-secondary hover:text-text-primary transition"
```

NEW:
```
"flex items-center gap-1.5 rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-secondary hover:text-text-primary transition"
```

Changes: `rounded-md` -> `rounded-lg`, `border-border` -> `border-border/50`, `bg-surface-elevated` -> `bg-surface`, `py-2` -> `py-1.5`

**6. Update directory reset button (line 157):**

OLD:
```
"rounded-md border border-border bg-surface-elevated px-2 py-2 text-sm text-text-secondary hover:text-red-400 transition"
```

NEW:
```
"rounded-lg border border-border/50 bg-surface px-2 py-1.5 text-sm text-text-secondary hover:text-red-400 hover:bg-red-500/10 transition"
```

Changes: `rounded-md` -> `rounded-lg`, `border-border` -> `border-border/50`, `bg-surface-elevated` -> `bg-surface`, `py-2` -> `py-1.5`, added `hover:bg-red-500/10`
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Open Settings > General. Verify inputs have subtle rounded-lg corners, lighter background. Toggle "Show welcome on start" -- switch should be slightly smaller (h-5 w-10) with correct dot travel. Directory browse button should match input styling.</manual>
  </verify>
  <done>SettingsField.tsx uses unified design tokens: rounded-lg borders, border-border/50, bg-surface, py-1.5 for inputs/selects, h-5 w-10 toggle, size-14 password icon, rounded-lg buttons with consistent hover states.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- All SettingsField renderings (GeneralSettings, PluginSettings) show updated styles
- Toggle switch animates correctly with new translate values
- Focus rings are subtle (ring-1 ring-accent/30) not heavy (ring-2 ring-accent)
</verification>

<success_criteria>
- inputClass and selectClass use rounded-lg, border-border/50, bg-surface, py-1.5, subtle focus ring
- Toggle is h-5 w-10 with translate-x-5/translate-x-0.5
- Password icon is size 14
- All buttons use rounded-lg
- No logic changes -- only Tailwind class modifications
</success_criteria>

<output>
After completion, create `.planning/phases/10-settings-ux-fix/10-01-SUMMARY.md`
</output>
