---
phase: 10-settings-ux-fix
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/renderer/src/components/settings/MCPSettings.tsx
  - src/renderer/src/components/settings/AgentRow.tsx
  - src/renderer/src/components/settings/AIAgentsSettings.tsx
  - src/renderer/src/components/settings/AddCustomAgentForm.tsx
autonomous: true
requirements:
  - SETT-UX-02

must_haves:
  truths:
    - "MCP toggle switch matches SettingsField toggle (h-5 w-10, translate-x-5/translate-x-0.5)"
    - "MCP form inputs use standardized focus ring (focus:ring-1 focus:ring-accent/30)"
    - "MCP form container uses rounded-xl border-border/50 bg-surface-elevated/30"
    - "MCP empty state uses dashed border with icon, title, and subtitle"
    - "MCP badge uses rounded-md"
    - "Agent table rows have hover:bg-surface-elevated/30 transition-colors"
    - "AgentRow API key input uses standardized focus style"
    - "AgentRow type badge uses text-[10px]"
    - "AgentRow delete button uses hover:bg-red-500/10 rounded-lg"
    - "AddCustomAgentForm container uses bg-surface-elevated/30"
    - "AddCustomAgentForm toggle buttons use rounded-lg"
    - "AddCustomAgentForm inputs use standardized classes"
    - "AIAgentsSettings description uses text-xs"
  artifacts:
    - path: "src/renderer/src/components/settings/MCPSettings.tsx"
      provides: "MCP settings with unified toggle, inputs, container, empty state"
      contains: "focus:ring-1 focus:ring-accent/30"
    - path: "src/renderer/src/components/settings/AgentRow.tsx"
      provides: "Agent row with hover state, unified badge, delete button"
      contains: "hover:bg-surface-elevated/30"
    - path: "src/renderer/src/components/settings/AIAgentsSettings.tsx"
      provides: "AI agents table with consistent description"
      contains: "text-xs text-text-secondary"
    - path: "src/renderer/src/components/settings/AddCustomAgentForm.tsx"
      provides: "Custom agent form with unified container and inputs"
      contains: "rounded-xl border border-border/50 bg-surface-elevated/30"
  key_links:
    - from: "MCPSettings.tsx toggle"
      to: "SettingsField.tsx toggle"
      via: "same h-5 w-10 sizing"
      pattern: "h-5 w-10"
    - from: "AgentRow.tsx"
      to: "AIAgentsSettings.tsx"
      via: "rendered in table body"
      pattern: "hover:bg-surface-elevated/30"
---

<objective>
Unify server and agent settings components (MCPSettings, AgentRow, AIAgentsSettings, AddCustomAgentForm) with standardized design tokens matching the updated SettingsField.

Purpose: These four files contain their own inline toggle, input, badge, container, and button styles that diverge from the SettingsField standard. Aligning them creates visual consistency across the entire AI/server configuration area.

Output: Four updated files with unified Tailwind classes. No logic changes.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-settings-ux-fix/10-RESEARCH.md
@src/renderer/src/components/settings/MCPSettings.tsx
@src/renderer/src/components/settings/AgentRow.tsx
@src/renderer/src/components/settings/AIAgentsSettings.tsx
@src/renderer/src/components/settings/AddCustomAgentForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify MCPSettings toggle, inputs, container, empty state, badge, and delete button</name>
  <files>src/renderer/src/components/settings/MCPSettings.tsx</files>
  <action>
This is a VISUAL-ONLY change. Modify Tailwind CSS classes only. Do NOT change any logic, state, props, imports, or component structure.

**1. Update empty state container (line 98):**

OLD:
```
"mb-4 rounded-lg border border-border bg-surface-elevated/30 px-4 py-6 text-center"
```

NEW:
```
"mb-4 rounded-lg border border-dashed border-border/50 px-4 py-8 text-center"
```

Changes: add `border-dashed`, `border-border` -> `border-border/50`, remove `bg-surface-elevated/30`, `py-6` -> `py-8`

**2. Update empty state subtitle (line 101):**

OLD: `"mt-1 text-xs text-text-secondary/60"`
NEW: `"mt-1 text-xs text-text-secondary"`

Changes: `text-text-secondary/60` -> `text-text-secondary`

**3. Update server row hover — add hover state (line 112):**

OLD: `"flex items-center gap-3 px-4 py-3"`
NEW: `"flex items-center gap-3 px-4 py-3 transition-colors hover:bg-surface-elevated/30"`

Changes: added `transition-colors hover:bg-surface-elevated/30`

**4. Update badge (line 131):**

OLD: `"shrink-0 rounded bg-surface-elevated px-1.5 py-0.5 text-[10px] text-text-secondary"`
NEW: `"shrink-0 rounded-md bg-surface-elevated px-1.5 py-0.5 text-[10px] text-text-secondary"`

Changes: `rounded` -> `rounded-md`

**5. Update toggle switch outer button (line 138):**

OLD:
```
`relative h-5 w-9 shrink-0 rounded-full transition-colors ${
  server.enabled ? 'bg-accent' : 'bg-surface-elevated'
}`
```

NEW:
```
`relative h-5 w-10 shrink-0 rounded-full transition-colors ${
  server.enabled ? 'bg-accent' : 'bg-surface-elevated border border-border'
}`
```

Changes: `w-9` -> `w-10`, added `border border-border` to disabled state

**6. Update toggle switch inner dot (line 143):**

OLD:
```
`absolute top-0.5 h-4 w-4 rounded-full bg-white shadow transition-transform ${
  server.enabled ? 'translate-x-4' : 'translate-x-0.5'
}`
```

NEW:
```
`absolute top-0.5 h-4 w-4 rounded-full bg-text-primary shadow transition-transform ${
  server.enabled ? 'translate-x-5' : 'translate-x-0.5'
}`
```

Changes: `bg-white` -> `bg-text-primary`, `translate-x-4` -> `translate-x-5`

**7. Update delete button (line 151):**

OLD: `"shrink-0 rounded p-1 text-text-secondary/40 transition-colors hover:bg-red-500/10 hover:text-red-400"`
NEW: `"shrink-0 rounded-lg p-1 text-text-secondary transition-colors hover:bg-red-500/10 hover:text-red-400"`

Changes: `rounded` -> `rounded-lg`, `text-text-secondary/40` -> `text-text-secondary`

**8. Update form container (line 172):**

OLD: `"rounded-lg border border-accent/20 bg-accent/5 p-4"`
NEW: `"rounded-xl border border-border/50 bg-surface-elevated/30 p-4"`

Changes: `rounded-lg` -> `rounded-xl`, `border-accent/20` -> `border-border/50`, `bg-accent/5` -> `bg-surface-elevated/30`

**9. Update all 3 form inputs (lines 192, 205, 218) — each has the same class string:**

OLD (each):
```
"w-full rounded-lg border border-border bg-surface px-3 py-1.5 text-sm text-text-primary placeholder:text-text-secondary/40 focus:border-accent focus:outline-none"
```

NEW (each):
```
"w-full rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary placeholder:text-text-secondary/40 focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30"
```

Changes: `border-border` -> `border-border/50`, added `focus:ring-1 focus:ring-accent/30`

**10. Update select element (line 229):**

OLD: `"rounded-lg border border-border bg-surface px-3 py-1.5 text-sm text-text-primary focus:border-accent focus:outline-none"`
NEW: `"rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30"`

Changes: `border-border` -> `border-border/50`, added `focus:ring-1 focus:ring-accent/30`

**11. Update header description (line 91):**

OLD: `"mb-6 text-xs text-text-secondary"`
NEW: `"mb-6 text-xs text-text-secondary"` (already correct -- no change needed)
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Open Settings > MCP Servers. Verify toggle matches SettingsField toggle size. Add form should have rounded-xl container with border-border/50. Inputs should have subtle focus ring. Empty state should have dashed border.</manual>
  </verify>
  <done>MCPSettings uses unified toggle (h-5 w-10, translate-x-5), standardized input focus, rounded-xl form container, dashed empty state, rounded-md badge, rounded-lg delete button, and row hover state.</done>
</task>

<task type="auto">
  <name>Task 2: Unify AgentRow, AIAgentsSettings, and AddCustomAgentForm styles</name>
  <files>src/renderer/src/components/settings/AgentRow.tsx, src/renderer/src/components/settings/AIAgentsSettings.tsx, src/renderer/src/components/settings/AddCustomAgentForm.tsx</files>
  <action>
This is a VISUAL-ONLY change. Modify Tailwind CSS classes only. Do NOT change any logic, state, props, imports, or component structure.

### AgentRow.tsx

**1. Update type badge (line 67):**

OLD: `"rounded-md bg-surface-elevated px-1.5 py-0.5 text-xs text-text-secondary"`
NEW: `"rounded-md bg-surface-elevated px-1.5 py-0.5 text-[10px] text-text-secondary"`

Changes: `text-xs` -> `text-[10px]`

**2. Update API key input (line 93):**

OLD:
```
"w-48 rounded border border-border bg-surface-elevated px-2 py-1 text-sm text-text-primary placeholder:text-text-secondary/50 focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent"
```

NEW:
```
"w-48 rounded-lg border border-border/50 bg-surface px-2 py-1 text-sm text-text-primary placeholder:text-text-secondary/50 focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30"
```

Changes: `rounded` -> `rounded-lg`, `border-border` -> `border-border/50`, `bg-surface-elevated` -> `bg-surface`, `focus:ring-2 focus:ring-accent focus:border-transparent` -> `focus:border-accent focus:ring-1 focus:ring-accent/30`

**3. Update Remove button for custom agents (line 147):**

OLD: `"px-2 py-1 text-xs text-red-400 transition hover:text-red-300"`
NEW: `"rounded-lg px-2 py-1 text-xs text-text-secondary transition hover:text-red-400 hover:bg-red-500/10"`

Changes: added `rounded-lg`, `text-red-400` -> `text-text-secondary`, `hover:text-red-300` -> `hover:text-red-400 hover:bg-red-500/10`

### AIAgentsSettings.tsx

**4. Update description paragraph (line 34):**

OLD: `"mb-6 text-sm text-text-secondary"`
NEW: `"mb-6 text-xs text-text-secondary"`

Changes: `text-sm` -> `text-xs`

**5. Wrap table in a styled container — update the overflow-x-auto div (line 38):**

OLD: `"overflow-x-auto"`
NEW: `"overflow-hidden rounded-lg border border-border/50"`

Changes: `overflow-x-auto` -> `overflow-hidden rounded-lg border border-border/50`

### AddCustomAgentForm.tsx

**6. Update the `inputClass` constant (line 61-62):**

OLD:
```
'w-full rounded-md border border-border bg-surface-elevated px-3 py-2 text-sm text-text-primary placeholder:text-text-secondary/50 focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition'
```

NEW:
```
'w-full rounded-lg border border-border/50 bg-surface px-3 py-1.5 text-sm text-text-primary placeholder:text-text-secondary/50 focus:border-accent focus:outline-none focus:ring-1 focus:ring-accent/30 transition'
```

Same changes as SettingsField inputClass.

**7. Update form container (line 65):**

OLD: `"mt-4 rounded-xl border border-border/50 bg-surface p-4"`
NEW: `"mt-4 rounded-xl border border-border/50 bg-surface-elevated/30 p-4"`

Changes: `bg-surface` -> `bg-surface-elevated/30`

**8. Update CLI/API toggle buttons (lines 73 and 84) -- both have identical class pattern:**

For each button, update the inactive state branch:

OLD (each inactive branch):
```
'border border-border text-text-secondary hover:border-accent hover:text-accent'
```

NEW (each inactive branch):
```
'border border-border/50 text-text-secondary hover:border-accent hover:text-accent'
```

Changes: `border-border` -> `border-border/50`

Also for each button, change `rounded-md` to `rounded-lg`:

OLD (each): `rounded-md px-3 py-1.5 text-xs font-medium transition`
NEW (each): `rounded-lg px-3 py-1.5 text-xs font-medium transition`
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Open Settings > AI Agents. Verify table has rounded-lg border container. Agent rows highlight on hover. Type badges are smaller (text-[10px]). Click "Add Custom Provider" -- form container should match MCP form styling. Toggle buttons should be rounded-lg.</manual>
  </verify>
  <done>AgentRow has hover states, unified API key input focus, smaller badge, rounded-lg delete button. AIAgentsSettings has text-xs description, bordered table container. AddCustomAgentForm has unified inputClass, bg-surface-elevated/30 container, rounded-lg toggle buttons.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors across all 4 files
- MCP toggle matches SettingsField toggle visually
- All inputs across MCPSettings, AgentRow, AddCustomAgentForm use the same focus style
- Form containers in MCP and AddCustomAgent use rounded-xl border-border/50 bg-surface-elevated/30
- Table rows have hover feedback
- Delete/remove buttons use consistent hover:bg-red-500/10 pattern
</verification>

<success_criteria>
- MCP toggle: h-5 w-10, translate-x-5/translate-x-0.5, bg-text-primary dot
- All inline inputs: rounded-lg, border-border/50, bg-surface, focus:ring-1 focus:ring-accent/30
- Form containers: rounded-xl border-border/50 bg-surface-elevated/30
- Empty state: dashed border, py-8
- Badges: rounded-md text-[10px]
- Delete buttons: rounded-lg hover:bg-red-500/10 hover:text-red-400
- Table hover: hover:bg-surface-elevated/30 transition-colors
- No logic changes
</success_criteria>

<output>
After completion, create `.planning/phases/10-settings-ux-fix/10-02-SUMMARY.md`
</output>
