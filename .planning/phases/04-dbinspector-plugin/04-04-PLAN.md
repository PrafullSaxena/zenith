---
phase: 04-dbinspector-plugin
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
  - src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx
  - src/renderer/src/plugins/db-inspector/QueryTab.tsx
autonomous: false
requirements:
  - DBIS-03
  - DBIS-04
  - DBIS-05
  - DBIS-07
  - DBIS-08

must_haves:
  truths:
    - "Query Console tab appears in DbInspectorView alongside existing tabs"
    - "Schema browser left panel is collapsible"
    - "Double-clicking a table or column in schema tree inserts its name at cursor in the active editor"
    - "Query history entries appear in the History tab with re-run capability"
    - "ResultsGrid replaces the placeholder in QueryTab"
    - "Connection loss triggers auto-reconnect and query retry"
    - "Saved queries panel is accessible from the query console"
  artifacts:
    - path: "src/renderer/src/plugins/db-inspector/DbInspectorView.tsx"
      provides: "Query Console tab added to TABS array, collapsible left panel, full wiring"
      contains: "query-console"
    - path: "src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx"
      provides: "onInsertAtCursor callback prop for double-click insert"
      contains: "onInsertAtCursor"
    - path: "src/renderer/src/plugins/db-inspector/QueryTab.tsx"
      provides: "Full QueryTab with ResultsGrid replacing placeholder, saved queries integration"
      contains: "ResultsGrid"
  key_links:
    - from: "src/renderer/src/plugins/db-inspector/DbInspectorView.tsx"
      to: "src/renderer/src/plugins/db-inspector/QueryConsole.tsx"
      via: "renders QueryConsole when activeTab is query-console"
      pattern: "QueryConsole|query-console"
    - from: "src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx"
      to: "SqlEditor.tsx"
      via: "double-click calls onInsertAtCursor which calls EditorView.dispatch"
      pattern: "onInsertAtCursor|insertAtCursor"
    - from: "src/renderer/src/plugins/db-inspector/QueryTab.tsx"
      to: "src/renderer/src/plugins/db-inspector/ResultsGrid.tsx"
      via: "renders ResultsGrid with query execution results"
      pattern: "ResultsGrid"
---

<objective>
Wire all query console components into the DbInspector view, integrate ResultsGrid into QueryTab, add schema browser enhancements, and add a visual verification checkpoint.

Purpose: Connect the editor (Plan 02) and results grid (Plan 03) into the full query console experience, wire schema browser double-click-to-insert, make the left panel collapsible, integrate saved queries, add query history entries, and verify the complete flow works visually.

Output: Fully functional query console tab in DbInspectorView with all components integrated.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dbinspector-plugin/04-CONTEXT.md
@.planning/phases/04-dbinspector-plugin/04-RESEARCH.md
@.planning/phases/04-dbinspector-plugin/04-01-SUMMARY.md
@.planning/phases/04-dbinspector-plugin/04-02-SUMMARY.md
@.planning/phases/04-dbinspector-plugin/04-03-SUMMARY.md
@src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
@src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx
@src/renderer/src/plugins/db-inspector/QueryTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire QueryConsole into DbInspectorView, integrate ResultsGrid into QueryTab, enhance SchemaExplorer</name>
  <files>
    src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
    src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx
    src/renderer/src/plugins/db-inspector/QueryTab.tsx
  </files>
  <action>
1. **Update DbInspectorView.tsx:**
   - Import `QueryConsole` from `./QueryConsole`
   - Import `Terminal` icon from lucide-react for the query console tab
   - Add `'query-console'` to the TABS array: `{ id: 'query-console', label: 'Query Console', icon: Terminal }` — place it FIRST in the array so it's the default tab (query console is the primary feature being added)
   - Change `activeTab` default in db-store to `'query-console'` instead of `'ask-ai'`
   - Render `<QueryConsole>` in the tab content area when `activeTab === 'query-console'`, passing: `connectionId={activeConnectionId}`, `isConnected={isConnected}`, `schema={activeSchema}`, `tables={tables}`, `engine={activeConnection?.engine ?? 'postgresql'}`
   - **Collapsible left panel:** Add a collapse/expand toggle button (ChevronLeft/ChevronRight icon) at the top-right corner of the left panel. When collapsed, the left panel shrinks to 0 width with `overflow-hidden` and a transition (`transition-all duration-200`). The toggle button stays visible at the edge. Store collapsed state in local `useState<boolean>(false)`.
   - Pass a ref or callback to SchemaExplorer for the double-click-to-insert feature. Create a state `editorViewRef` that gets set by the QueryConsole component when the active SqlEditor mounts. Pass this ref down to SchemaExplorer via `onInsertAtCursor` prop.
   - **Connection loss handling:** In `executeQuery` store action, if the query errors with a connection-related message (ECONNRESET, ETIMEDOUT, "Connection terminated", "Client has encountered a connection error"), attempt auto-reconnect: call `connectToDb(activeConnectionId)` then retry the query once. If retry also fails, show the error to the user. Add this logic directly in the catch block of `executeQuery` in db-store.
   - Wire the `getTabLabel` function to show streaming indicator for query console: if any tab has a running query, show "Query Console ●"

2. **Update SchemaExplorer.tsx:**
   - Add new optional prop: `onInsertAtCursor?: (text: string) => void`
   - On table name in the tree: add `onDoubleClick` handler that calls `onInsertAtCursor(tableName)` if the callback is provided. Prevent the default single-click expand behavior on double-click (use a timer pattern or `event.detail === 2` check).
   - On column name in the expanded table details: add `onDoubleClick` handler that calls `onInsertAtCursor(columnName)`.
   - Visual hint: when `onInsertAtCursor` is provided, show a subtle cursor pointer on table/column names to indicate they're double-clickable.

3. **Update QueryTab.tsx:**
   - Replace the placeholder `<pre>` results display with the actual `<ResultsGrid>` component
   - Import `ResultsGrid` from `./ResultsGrid`
   - Import `SavedQueriesPanel` from `./SavedQueriesPanel`
   - Pass `lastResult.rows`, `lastResult.fields`, `lastResult.hasMore`, execution status to ResultsGrid
   - Add a "Saved Queries" button in the toolbar that toggles a dropdown/panel showing `SavedQueriesPanel`. When a saved query is loaded, update the tab's SQL and the CodeMirror editor content.
   - Wire the "Save Query" action: toolbar button or Ctrl+S that opens a small name prompt (inline input in toolbar), then calls `saveQuery(name, sql)` from db-store
   - Wire the EXPLAIN floating icon: clicking it sets `activeTab` to `'query-optimizer'` and calls `startOptimization(currentSql)` passing the agent info (same pattern as existing handleStartOptimization in DbInspectorView)
   - Pass `errorLine` from `lastResult.errorLine` to SqlEditor to highlight the error line
   - Add `onEditorReady` prop handling: when SqlEditor reports its EditorView, store it so the parent can provide it for schema browser insert-at-cursor
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check DbInspectorView has query-console tab, SchemaExplorer has onInsertAtCursor, QueryTab uses ResultsGrid</manual>
  </verify>
  <done>Query Console tab appears first in DbInspectorView TABS. Left panel is collapsible with toggle button. SchemaExplorer supports double-click to insert table/column name at editor cursor. QueryTab renders ResultsGrid with real data, has saved queries button, EXPLAIN floating icon wired to Optimizer. Connection loss triggers auto-reconnect + retry. TypeScript compiles clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete query console</name>
  <files>none</files>
  <action>
Present the complete query console to the user for visual verification. All automation is complete in Task 1 — this checkpoint confirms the integrated experience works end-to-end.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -5</automated>
    <manual>
1. Launch the app: `npm run dev`
2. Navigate to DbInspector in the sidebar
3. Verify the "Query Console" tab appears first and is selected by default
4. Connect to a PostgreSQL database (use existing connection from settings)
5. Verify the CodeMirror editor loads with SQL syntax highlighting
6. Type a query (e.g., `SELECT * FROM users LIMIT 10`) — verify autocomplete suggests table names after "FROM "
7. Press Ctrl+Enter (Cmd+Enter on Mac) — verify the query executes and results appear in the grid below
8. Verify the status bar shows execution time and row count
9. Click a column header — verify sorting works
10. Click a cell — verify the value is copied to clipboard
11. Click "Export CSV" — verify native save dialog opens
12. Open multiple query tabs — verify tab switching works
13. Toggle write mode on a tab — verify the lock/unlock icon changes
14. Double-click a table name in the schema browser (left panel) — verify it inserts at cursor in the editor
15. Collapse the left panel — verify it hides and can be expanded again
16. Close the app and reopen — verify tabs and SQL content are restored
    </manual>
  </verify>
  <done>User has visually verified the complete query console works: editor with autocomplete, execution, results grid, tab management, schema browser insert, collapsible panel, export, and session persistence.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Query Console tab visible in DbInspectorView
- Full query flow works: type SQL -> execute -> see results in grid
- Schema browser double-click inserts at cursor
- Left panel collapses/expands
- Tabs persist across sessions
- Export CSV/JSON works via native dialog
</verification>

<success_criteria>
- Complete query console integrated into DbInspectorView as a new tab
- All CONTEXT.md locked decisions implemented (split layout, tab management, execution shortcuts, write toggle, autocomplete, schema browser insert, results grid, export, status bar)
- Deferred items NOT implemented (no visual query builder DBIS-09, no SQLite/MSSQL/Oracle)
- User can perform a full query workflow: connect -> write SQL -> execute -> view results -> export
</success_criteria>

<output>
After completion, create `.planning/phases/04-dbinspector-plugin/04-04-SUMMARY.md`
</output>
