---
phase: 01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/assets/main.css
  - src/renderer/src/assets/fonts/PlusJakartaSans-Variable.woff2
  - src/renderer/src/assets/fonts/GeistMono-Regular.woff2
  - src/renderer/src/assets/fonts/GeistMono-Medium.woff2
  - src/renderer/src/assets/fonts/GeistMono-SemiBold.woff2
  - src/renderer/src/assets/fonts/GeistMono-Bold.woff2
autonomous: true
requirements: [FOUND-01, FOUND-03, FOUND-04, FOUND-06, FOUND-07]

must_haves:
  truths:
    - "Glass CSS tokens (--glass-bg, --glass-border, --glass-blur, --glass-glow) resolve correctly on all 12 existing themes"
    - "App renders body text in Plus Jakarta Sans and code blocks in Geist Mono"
    - "Timing tokens (--duration-instant/fast/normal/slow/slower) and easing tokens (--ease-out/spring/smooth) are defined and usable"
    - "Two-tier blur strategy tokens exist: --glass-blur for top-level, translucent-only (no blur) for nested"
  artifacts:
    - path: "src/renderer/src/assets/main.css"
      provides: "Glass tokens, timing tokens, easing tokens, @font-face declarations, font stack updates, per-theme --glass-glow overrides"
      contains: "--glass-bg"
    - path: "src/renderer/src/assets/fonts/PlusJakartaSans-Variable.woff2"
      provides: "Variable sans font file covering weights 200-800"
    - path: "src/renderer/src/assets/fonts/GeistMono-Regular.woff2"
      provides: "Monospace font regular weight"
  key_links:
    - from: "src/renderer/src/assets/main.css"
      to: "src/renderer/src/assets/fonts/PlusJakartaSans-Variable.woff2"
      via: "@font-face src url"
      pattern: "url.*PlusJakartaSans-Variable"
    - from: "src/renderer/src/assets/main.css"
      to: "src/renderer/src/assets/fonts/GeistMono-Regular.woff2"
      via: "@font-face src url"
      pattern: "url.*GeistMono"
    - from: "@theme block"
      to: "[data-theme] selectors"
      via: "--glass-glow override per theme"
      pattern: "--glass-glow:"
---

<objective>
Add glass design tokens, timing/easing tokens, and font infrastructure to main.css. Download and install Plus Jakarta Sans Variable and Geist Mono font files. Configure per-theme --glass-glow overrides for all 12 themes. Remove JetBrains Mono font files.

Purpose: Establishes the CSS custom property foundation that every downstream glass component, animation, and typography class depends on. Without these tokens, Phase 2 components have nothing to consume.

Output: Updated main.css with all design tokens + font declarations, font woff2 files in assets/fonts/.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
@src/renderer/src/assets/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download font files and update font infrastructure</name>
  <files>
    src/renderer/src/assets/fonts/PlusJakartaSans-Variable.woff2
    src/renderer/src/assets/fonts/GeistMono-Regular.woff2
    src/renderer/src/assets/fonts/GeistMono-Medium.woff2
    src/renderer/src/assets/fonts/GeistMono-SemiBold.woff2
    src/renderer/src/assets/fonts/GeistMono-Bold.woff2
    src/renderer/src/assets/main.css
  </files>
  <action>
    1. **Download Plus Jakarta Sans Variable woff2:**
       - Source: Google Fonts API or fontsource npm package
       - Get the variable woff2 file covering weights 200-800 in a single file
       - Save as `src/renderer/src/assets/fonts/PlusJakartaSans-Variable.woff2`
       - Approach: Use `curl` to download from Google Fonts CDN, or extract from `@fontsource-variable/plus-jakarta-sans` npm package (install temporarily, copy woff2, uninstall). The Google Fonts API URL pattern is: `https://fonts.gstatic.com/s/plusjakartasans/v8/...woff2` — find the exact URL by checking fonts.google.com for Plus Jakarta Sans Variable.
       - Alternative: `npx @fontsource-variable/plus-jakarta-sans` or download from https://github.com/nicholasgasior/plus-jakarta-sans/tree/main/fonts/webfonts

    2. **Download Geist Mono woff2 files:**
       - Source: Vercel's Geist font GitHub repository (https://github.com/vercel/geist-font)
       - Download 4 weight files: Regular (400), Medium (500), SemiBold (600), Bold (700)
       - Save to `src/renderer/src/assets/fonts/GeistMono-{Regular,Medium,SemiBold,Bold}.woff2`
       - If a variable woff2 for Geist Mono exists in the repo, use that single file instead of 4 static files

    3. **Remove JetBrains Mono font files:**
       - Delete: `src/renderer/src/assets/fonts/JetBrainsMono-Regular.woff2`
       - Delete: `src/renderer/src/assets/fonts/JetBrainsMono-Bold.woff2`
       - Delete: `src/renderer/src/assets/fonts/JetBrainsMono-Italic.woff2`
       - Delete: `src/renderer/src/assets/fonts/JetBrainsMono-SemiBold.woff2`

    4. **Update @font-face declarations in main.css:**
       - Replace all 4 JetBrains Mono @font-face blocks with:
         - 1 @font-face for Plus Jakarta Sans Variable (font-weight: 200 800, format woff2-variations, font-display: swap)
         - 4 @font-face blocks for Geist Mono (Regular 400, Medium 500, SemiBold 600, Bold 700, format woff2, font-display: swap)
       - Add a fallback font metrics override for Inter to minimize CLS:
         ```css
         @font-face {
           font-family: "Inter Fallback";
           src: local("Inter");
           size-adjust: 104%;
           ascent-override: 92%;
           descent-override: 22%;
           line-gap-override: 0%;
         }
         ```

    5. **Update --font-sans and --font-mono in @theme block:**
       - Change `--font-sans` to: `"Plus Jakarta Sans", "Inter Fallback", "Inter", ui-sans-serif, system-ui, sans-serif`
       - Change `--font-mono` to: `"Geist Mono", ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, monospace`

    **Important:** Do NOT delete Inter font files from assets/fonts/ — Inter is kept as fallback per user decision. If Inter files do not exist locally (they may be loaded differently), that is fine.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && ls -la src/renderer/src/assets/fonts/ | grep -E "(PlusJakarta|GeistMono)" && ! ls src/renderer/src/assets/fonts/JetBrains* 2>/dev/null && grep "Plus Jakarta Sans" src/renderer/src/assets/main.css && grep "Geist Mono" src/renderer/src/assets/main.css && grep "font-weight: 200 800" src/renderer/src/assets/main.css</automated>
    <manual>Run the app and confirm body text renders in Plus Jakarta Sans (rounder letterforms than Inter) and code blocks render in Geist Mono (wider than JetBrains Mono)</manual>
  </verify>
  <done>Plus Jakarta Sans Variable and Geist Mono woff2 files exist in assets/fonts/. JetBrains Mono files are deleted. @font-face declarations updated. --font-sans and --font-mono in @theme reference the new fonts.</done>
</task>

<task type="auto">
  <name>Task 2: Add glass tokens, timing/easing tokens, and per-theme glass-glow overrides</name>
  <files>src/renderer/src/assets/main.css</files>
  <action>
    1. **Add glass tokens to @theme block** (inside the existing `@theme { ... }` block, after --radius):
       ```css
       /* Glass design tokens — two-tier blur strategy */
       --glass-bg: rgba(255, 255, 255, 0.03);
       --glass-border: rgba(255, 255, 255, 0.08);
       --glass-blur: 24px;
       --glass-glow: 0 0 0 2px oklch(72% 0.15 195 / 0.2);
       ```
       The default --glass-glow uses the Zenith theme accent (cyan). Per-theme overrides below.

    2. **Add timing and easing tokens to @theme block** (after glass tokens):
       ```css
       /* Timing tokens */
       --duration-instant: 100ms;
       --duration-fast: 150ms;
       --duration-normal: 250ms;
       --duration-slow: 400ms;
       --duration-slower: 600ms;

       /* Easing tokens */
       --ease-out: cubic-bezier(0.25, 0.46, 0.45, 0.94);
       --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
       --ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);
       ```

    3. **Add --glass-glow override to each [data-theme] selector.** For each of the 11 non-default themes, add a `--glass-glow` line that uses that theme's accent color at 20% opacity. The value pattern is `0 0 0 2px <accent-color-at-20%>`.

       Map each theme's accent to its glow:
       - `portfolio`: `0 0 0 2px rgba(245, 158, 11, 0.2)` (amber — portfolio uses hex, so use rgba)
       - `nord`: `0 0 0 2px oklch(72% 0.12 220 / 0.2)` (frost blue)
       - `rose-pine`: `0 0 0 2px oklch(68% 0.14 350 / 0.2)` (dusty rose)
       - `dracula`: `0 0 0 2px oklch(65% 0.18 290 / 0.2)` (purple)
       - `gruvbox`: `0 0 0 2px oklch(72% 0.16 60 / 0.2)` (warm orange)
       - `tokyo-night`: Read the accent from main.css and derive the glow at 20% opacity
       - `synthwave`: Same derivation from its accent
       - `catppuccin`: Same derivation from its accent
       - `emerald`: Same derivation from its accent
       - `solarized`: Same derivation from its accent
       - `crimson`: Same derivation from its accent

       Add the `--glass-glow` property inside each existing `[data-theme="..."]` block — do NOT create new selectors. Place it after the last existing property in each block.

       NOTE: --glass-bg, --glass-border, and --glass-blur use white/neutral values that are theme-independent. Only --glass-glow needs per-theme overrides.

    4. **Two-tier blur documentation comment** — add a CSS comment above the glass tokens in @theme:
       ```css
       /* Glass design tokens — two-tier blur strategy:
          Blur tier (backdrop-blur with --glass-blur): GlassCard, GlassModal, GlassToast
          Translucent tier (--glass-bg only, no blur): GlassSurface, GlassInput, GlassSelect, GlassButton, GlassBadge, GlassTab
          Nested surfaces always use translucent tier regardless of parent. */
       ```
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && grep -c "\-\-glass-glow:" src/renderer/src/assets/main.css | xargs -I{} test {} -ge 12 && grep "\-\-glass-bg:" src/renderer/src/assets/main.css && grep "\-\-glass-border:" src/renderer/src/assets/main.css && grep "\-\-glass-blur:" src/renderer/src/assets/main.css && grep "\-\-duration-instant:" src/renderer/src/assets/main.css && grep "\-\-ease-out:" src/renderer/src/assets/main.css && echo "All tokens present"</automated>
  </verify>
  <done>Glass tokens (--glass-bg, --glass-border, --glass-blur, --glass-glow) defined in @theme with per-theme --glass-glow overrides for all 12 themes. Timing tokens (5 durations) and easing tokens (3 curves) defined in @theme. Two-tier blur strategy documented.</done>
</task>

</tasks>

<verification>
1. `grep -c "\-\-glass-glow:" src/renderer/src/assets/main.css` returns >= 12 (1 default + 11 overrides)
2. `grep "\-\-duration-fast:" src/renderer/src/assets/main.css` returns "150ms"
3. `grep "\-\-ease-spring:" src/renderer/src/assets/main.css` returns the cubic-bezier value
4. Font files exist: `ls src/renderer/src/assets/fonts/{PlusJakartaSans,GeistMono}*`
5. JetBrains Mono files removed: `ls src/renderer/src/assets/fonts/JetBrains*` returns nothing
6. `grep "Plus Jakarta Sans" src/renderer/src/assets/main.css` confirms font-face + font-sans
7. App builds without errors: `npm run build` succeeds
</verification>

<success_criteria>
- Glass tokens resolve on all 12 themes (--glass-glow uses each theme's accent color)
- Timing and easing tokens are available as Tailwind utilities (duration-fast, ease-out, etc.)
- Plus Jakarta Sans Variable font loads as --font-sans with Inter as fallback
- Geist Mono loads as --font-mono replacing JetBrains Mono
- Two-tier blur strategy is encoded in the token design (--glass-blur exists for top-level, components use --glass-bg without blur for nested)
- App builds cleanly with no font-related errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md`
</output>
