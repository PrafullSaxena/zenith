---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - electron.vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.web.json
  - src/main/main.ts
  - src/main/preload.ts
  - src/renderer/index.html
  - src/renderer/src/main.tsx
  - src/renderer/src/assets/main.css
autonomous: true
requirements:
  - SHELL-01

must_haves:
  truths:
    - "Running `npm run dev` launches an Electron window with a dark (#0f0f0f) background"
    - "The window has no native title bar (titleBarStyle: 'hidden')"
    - "nodeIntegration is false and contextIsolation is true (verified in webPreferences)"
    - "Content-Security-Policy header is set on all responses"
    - "window.api is defined in the renderer (contextBridge is working)"
    - "npm run build produces a dist/ output without TypeScript errors"
  artifacts:
    - path: "src/main/main.ts"
      provides: "BrowserWindow creation with secure webPreferences"
      contains: "nodeIntegration: false"
    - path: "src/main/preload.ts"
      provides: "contextBridge typed API surface"
      contains: "contextBridge.exposeInMainWorld"
    - path: "src/renderer/src/assets/main.css"
      provides: "Tailwind v4 CSS-first theme with dark palette"
      contains: "@import \"tailwindcss\""
    - path: "electron.vite.config.ts"
      provides: "electron-vite 5.0 build config with Tailwind v4"
      contains: "tailwindcss()"
  key_links:
    - from: "src/main/preload.ts"
      to: "src/renderer/src/types/electron.d.ts"
      via: "contextBridge exposeInMainWorld maps to window.api TypeScript interface"
      pattern: "window\\.api"
    - from: "electron.vite.config.ts"
      to: "src/renderer/src/assets/main.css"
      via: "tailwindcss() vite plugin processes @import tailwindcss"
      pattern: "@tailwindcss/vite"
---

<objective>
Scaffold the Electron application from scratch: install all dependencies, configure electron-vite 5.0 with React 19 + TypeScript + Tailwind v4, create a secure BrowserWindow with contextIsolation, and establish the typed contextBridge API surface. The window shows a dark background with the custom titlebar region — no content yet.

Purpose: Every subsequent plan depends on this scaffold existing. No IPC, no UI components, no plugin system can exist without the base Electron + Vite + React foundation.
Output: A runnable `npm run dev` that shows a dark Electron window; `npm run build` that produces a clean dist/ output; the typed window.api bridge that renderer plans build on.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold electron-vite project and install all dependencies</name>
  <done>npm run build exits 0, dist/ contains build artifacts, package.json has "type": "module", electron.vite.config.ts uses @tailwindcss/vite plugin</done>
</task>

<task type="auto">
  <name>Task 2: Configure secure BrowserWindow and typed contextBridge</name>
  <done>Electron window launches with dark background (#0f0f0f), titleBarStyle hidden, `npm run build` exits 0, `window.api` is accessible in renderer DevTools with settings/credentials/app namespaces</done>
</task>

</tasks>
