---
phase: 02-mission-control-activity-log
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/renderer/src/components/dashboard/MissionControl.tsx
  - src/renderer/src/components/dashboard/PluginCard.tsx
  - src/renderer/src/components/dashboard/ActivityFeed.tsx
  - src/renderer/src/components/dashboard/StatusBadge.tsx
autonomous: true
requirements:
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "Dashboard shows one summary card per plugin from the PLUGINS array"
    - "Each plugin card displays plugin name, icon, description, and recent activity count"
    - "Each plugin card has a primary action button that navigates to the plugin route"
    - "Activity feed displays recent operations with plugin icon, operation name, status badge, timestamp, and duration"
    - "Dashboard layout uses CSS Grid that is responsive to window size (1 col on small, 2-4 cols on large)"
  artifacts:
    - path: "src/renderer/src/components/dashboard/MissionControl.tsx"
      provides: "Main dashboard view assembling plugin cards grid and activity feed"
      exports: ["default"]
    - path: "src/renderer/src/components/dashboard/PluginCard.tsx"
      provides: "Individual plugin summary card with name, icon, activity count, and open button"
      exports: ["PluginCard"]
    - path: "src/renderer/src/components/dashboard/ActivityFeed.tsx"
      provides: "Scrollable activity feed showing recent entries with relative timestamps"
      exports: ["ActivityFeed"]
    - path: "src/renderer/src/components/dashboard/StatusBadge.tsx"
      provides: "Status badge component for success/failure/pending states"
      exports: ["StatusBadge"]
  key_links:
    - from: "src/renderer/src/components/dashboard/MissionControl.tsx"
      to: "src/renderer/src/stores/activity-store.ts"
      via: "useActivityStore for loading entries and passing to ActivityFeed"
      pattern: "useActivityStore"
    - from: "src/renderer/src/components/dashboard/PluginCard.tsx"
      to: "src/renderer/src/plugins/registry.ts"
      via: "Receives PluginDefinition prop, uses ICON_MAP for icon rendering"
      pattern: "PluginDefinition"
    - from: "src/renderer/src/components/dashboard/PluginCard.tsx"
      to: "react-router-dom"
      via: "useNavigate() for quick-action button navigation to plugin route"
      pattern: "useNavigate"
    - from: "src/renderer/src/components/dashboard/ActivityFeed.tsx"
      to: "src/renderer/src/types/activity.ts"
      via: "ActivityEntry type for rendering entries"
      pattern: "ActivityEntry"
---

<objective>
Build the Mission Control dashboard UI components -- plugin summary cards in a responsive CSS Grid, quick-action navigation buttons, and the recent activity feed.

Purpose: Implements the visual dashboard (DASH-02 through DASH-05). These components consume the activity store from Plan 01 and will be wired into routing by Plan 03.

Output: Four component files in `src/renderer/src/components/dashboard/` -- MissionControl, PluginCard, ActivityFeed, StatusBadge.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mission-control-activity-log/02-RESEARCH.md
@.planning/phases/02-mission-control-activity-log/02-01-SUMMARY.md

@src/renderer/src/plugins/registry.ts
@src/renderer/src/types/plugin.ts
@src/renderer/src/components/Sidebar.tsx
@src/renderer/src/assets/main.css
@src/renderer/src/stores/activity-store.ts
@src/renderer/src/types/activity.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusBadge, PluginCard, and relative time utility</name>
  <files>
    src/renderer/src/components/dashboard/StatusBadge.tsx
    src/renderer/src/components/dashboard/PluginCard.tsx
  </files>
  <action>
**StatusBadge component** (`src/renderer/src/components/dashboard/StatusBadge.tsx`):

Create a small presentational component that renders a colored badge for activity status:
- Import `ActivityStatus` from `'../../types/activity'`
- Props: `{ status: ActivityStatus }`
- Style map using Tailwind classes:
  - `success`: `'bg-green-500/10 text-green-400'`
  - `failure`: `'bg-red-500/10 text-red-400'`
  - `pending`: `'bg-yellow-500/10 text-yellow-400'`
- Render: `<span>` with `inline-flex items-center rounded-full px-2 py-0.5 text-[11px] font-medium` + status-specific classes
- Export as named export `StatusBadge`

**PluginCard component** (`src/renderer/src/components/dashboard/PluginCard.tsx`):

Create the individual plugin summary card:
- Import `useNavigate` from `'react-router-dom'`
- Import `PluginDefinition` from `'../../types/plugin'`
- Import `useActivityStore` from `'../../stores/activity-store'`
- Import lucide icons used in sidebar: copy the ICON_MAP pattern from Sidebar.tsx (import `GitPullRequest, Database, Wrench, MessageSquare` from `'lucide-react'` and create a local `ICON_MAP` record). Or better: extract ICON_MAP to a shared location -- but to avoid touching Sidebar.tsx in this plan, create a local copy. Add `LayoutDashboard` and `Activity` icons too for future use.
- Props: `{ plugin: PluginDefinition }`
- Show: plugin icon (from ICON_MAP), plugin name, plugin description
- Activity count: use `useActivityStore(s => s.getEntriesByPlugin(plugin.id))` to get entries, filter for last 24 hours (`Date.now() - new Date(e.timestamp).getTime() < 86400000`), show count as "{N} ops today"
- Quick-action button: "Open" button with `onClick={() => navigate(plugin.route)}` -- styled with `rounded bg-accent/10 px-3 py-1 text-xs font-medium text-accent hover:bg-accent/20 transition`
- Card wrapper: `rounded-lg border border-border bg-surface-elevated p-4` with hover effect `hover:border-accent/30 transition-colors`
- Export as named export `PluginCard`

**Relative time utility**: Add a `formatRelativeTime` function at the top of PluginCard.tsx (or in a small utility). Use `Intl.RelativeTimeFormat` as described in RESEARCH.md. This will also be used by ActivityFeed. To share it, put it in a small utility: `src/renderer/src/components/dashboard/utils.ts` with the `formatRelativeTime` function from the research doc. NO external library -- use built-in `Intl.RelativeTimeFormat`.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify PluginCard renders plugin name, icon, description, activity count, and Open button</manual>
  </verify>
  <done>StatusBadge renders colored badges for success/failure/pending. PluginCard shows plugin name, icon, description, 24h activity count, and Open navigation button. formatRelativeTime utility exists using Intl.RelativeTimeFormat.</done>
</task>

<task type="auto">
  <name>Task 2: Create ActivityFeed and MissionControl dashboard view</name>
  <files>
    src/renderer/src/components/dashboard/ActivityFeed.tsx
    src/renderer/src/components/dashboard/MissionControl.tsx
  </files>
  <action>
**ActivityFeed component** (`src/renderer/src/components/dashboard/ActivityFeed.tsx`):

Create the activity feed list component:
- Import `ActivityEntry` from `'../../types/activity'`
- Import `StatusBadge` from `'./StatusBadge'`
- Import `formatRelativeTime` from `'./utils'`
- Import lucide icons: use the same ICON_MAP pattern (or import from PluginCard's shared location) to show plugin icon per entry. Also import `Clock` from lucide-react for duration display.
- Import `PLUGINS` from `'../../plugins/registry'` and `getPluginById` to look up plugin name/icon from entry's `pluginId`.
- Props: `{ entries: ActivityEntry[]; showViewAll?: boolean; onViewAll?: () => void }`
- For each entry, render a row showing:
  - Plugin icon (looked up via getPluginById + ICON_MAP) -- small, 16px
  - Operation name (entry.operation) -- text-sm text-text-primary
  - StatusBadge with entry.status
  - Relative timestamp (formatRelativeTime(entry.timestamp)) -- text-xs text-text-secondary
  - Duration if present: `${(entry.durationMs! / 1000).toFixed(1)}s` -- text-xs text-text-secondary
- Empty state: if entries.length === 0, show "No recent activity" message with muted text
- If `showViewAll` is true and entries exist, show a "View All Activity" link/button at the bottom
- Scrollable container: `max-h-80 overflow-y-auto` for dashboard embed (or let parent control overflow)
- Export as named export `ActivityFeed`

**MissionControl component** (`src/renderer/src/components/dashboard/MissionControl.tsx`):

Create the main dashboard view:
- Import `useEffect` from React
- Import `PLUGINS` from `'../../plugins/registry'`
- Import `useActivityStore` from `'../../stores/activity-store'`
- Import `useNavigate` from `'react-router-dom'`
- Import `PluginCard` from `'./PluginCard'`
- Import `ActivityFeed` from `'./ActivityFeed'`

Structure:
1. Call `useActivityStore` for `entries`, `isLoading`, `loadEntries`
2. `useEffect(() => { loadEntries() }, [loadEntries])` to load activity on mount
3. `const navigate = useNavigate()` for "View All" navigation

Layout (responsive CSS Grid per DASH-05):
```
<div className="space-y-6">
  {/* Header */}
  <h1 className="text-xl font-semibold text-text-primary">Mission Control</h1>

  {/* Plugin summary cards -- responsive CSS grid */}
  <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-4">
    {PLUGINS.map(plugin => (
      <PluginCard key={plugin.id} plugin={plugin} />
    ))}
  </div>

  {/* Recent Activity section */}
  <div>
    <h2 className="mb-3 text-sm font-semibold text-text-secondary uppercase tracking-wider">
      Recent Activity
    </h2>
    {isLoading ? (
      <p className="text-sm text-text-secondary">Loading activity...</p>
    ) : (
      <ActivityFeed
        entries={entries.slice(0, 20)}
        showViewAll={entries.length > 0}
        onViewAll={() => navigate('/activity')}
      />
    )}
  </div>
</div>
```

- Default export the component (for React.lazy compatibility in App.tsx)
- Grid must be responsive: 1 col by default, 2 on sm, 4 on xl -- satisfies DASH-05
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project src/renderer/tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify MissionControl renders plugin cards grid and activity feed section</manual>
  </verify>
  <done>MissionControl renders a responsive CSS grid of PluginCards and a recent ActivityFeed. ActivityFeed renders entries with plugin icon, operation, status badge, timestamp, and duration. Dashboard loads activity on mount and shows loading state.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project src/renderer/tsconfig.json` passes with zero errors
2. All 4 component files exist in `src/renderer/src/components/dashboard/`
3. MissionControl uses CSS Grid with responsive breakpoints (grid-cols-1, sm:grid-cols-2, xl:grid-cols-4)
4. PluginCard uses `useNavigate()` for the Open button (DASH-03)
5. PluginCard shows activity count from `useActivityStore` (DASH-02)
6. ActivityFeed renders status badges, relative timestamps, and duration (DASH-04)
7. MissionControl calls `loadEntries()` in useEffect (handles race condition from RESEARCH pitfall 2)
</verification>

<success_criteria>
- TypeScript compilation succeeds with no errors
- Dashboard grid is responsive (uses Tailwind grid-cols responsive prefixes)
- Each plugin card shows: name, icon, description, 24h activity count, Open button
- Activity feed shows: plugin icon, operation, status badge, relative timestamp, optional duration
- MissionControl loads activity data on mount
- Components use project theme tokens (text-text-primary, bg-surface-elevated, border-border, text-accent)
</success_criteria>

<output>
After completion, create `.planning/phases/02-mission-control-activity-log/02-02-SUMMARY.md`
</output>
