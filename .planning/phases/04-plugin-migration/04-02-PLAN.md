---
phase: 04-plugin-migration
plan: 2
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/renderer/src/plugins/cortex/CortexView.tsx
  - src/renderer/src/plugins/cortex/cortex-theme.ts
  - src/renderer/src/plugins/cortex/components/AnalysisProgress.tsx
  - src/renderer/src/plugins/cortex/components/AddRepoDialog.tsx
  - src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx
  - src/renderer/src/plugins/cortex/components/DesignDocTab.tsx
  - src/renderer/src/plugins/cortex/components/ExportDialog.tsx
  - src/renderer/src/plugins/cortex/components/MindGraphTab.tsx
  - src/renderer/src/plugins/cortex/components/TestCoverageCard.tsx
  - src/renderer/src/plugins/cortex/components/APIListTab.tsx
  - src/renderer/src/plugins/cortex/components/OverviewTab.tsx
  - src/renderer/src/plugins/cortex/components/QAPanel.tsx
  - src/renderer/src/plugins/cortex/components/RepoCard.tsx
  - src/renderer/src/plugins/cortex/components/RepoManager.tsx
  - src/renderer/src/plugins/cortex/components/InsightsPanel.tsx
  - src/renderer/src/plugins/cortex/components/DiagramsTab.tsx
  - src/renderer/src/plugins/cortex/components/FlowsTab.tsx
  - src/renderer/src/plugins/cortex/components/ValidationPanel.tsx
autonomous: true
requirements: [PLUG-01]

must_haves:
  truths:
    - "Cortex renders a PluginHeader with Brain icon, gradient title, and GlassTab bar"
    - "GLASS_CARD, GLASS_SURFACE, cardVariants, and useCardVariants are deleted from cortex-theme.ts"
    - "All 18 cortex component files use shared GlassCard/GlassSurface/stagger imports instead of cortex-theme inline constants"
    - "KIND_COLORS, METHOD_COLORS, REPO_TYPE_GRADIENTS are preserved in cortex-theme.ts"
    - "QAPanel uses GlassChat component for the AI chat interface"
    - "ExportDialog uses GlassModal instead of custom overlay"
    - "All loading states use GlassSkeleton, all empty states use EmptyState"
  artifacts:
    - path: "src/renderer/src/plugins/cortex/cortex-theme.ts"
      provides: "Cleaned theme file — only functional color mappings remain"
      contains: "KIND_COLORS"
    - path: "src/renderer/src/plugins/cortex/CortexView.tsx"
      provides: "Main view with PluginHeader"
      min_lines: 40
  key_links:
    - from: "src/renderer/src/plugins/cortex/CortexView.tsx"
      to: "PluginHeader"
      via: "import from ui/"
      pattern: "import.*PluginHeader.*from.*ui"
    - from: "src/renderer/src/plugins/cortex/components/QAPanel.tsx"
      to: "GlassChat"
      via: "import from ui/"
      pattern: "import.*GlassChat.*from.*ui"
    - from: "src/renderer/src/plugins/cortex/components/RepoManager.tsx"
      to: "staggerContainer, staggerItem"
      via: "import from motion.ts"
      pattern: "import.*stagger.*from.*motion"
---

<objective>
Migrate all 18 Cortex component files from inline cortex-theme.ts constants to shared glass components. Delete GLASS_CARD, GLASS_SURFACE, cardVariants, and useCardVariants from cortex-theme.ts while preserving functional color mappings. Replace QAPanel chat with GlassChat. Replace ExportDialog with GlassModal.

Purpose: Cortex is the most important migration — it has the most inline glass patterns that diverge from the shared design system. After this, the biggest source of visual inconsistency is resolved.
Output: All 18 Cortex files migrated, cortex-theme.ts cleaned, zero inline glass patterns remaining.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-migration/04-00-SUMMARY.md
@.planning/phases/04-plugin-migration/04-01-SUMMARY.md
@src/renderer/src/plugins/cortex/cortex-theme.ts
@src/renderer/src/plugins/cortex/CortexView.tsx
@src/renderer/src/components/ui/index.ts
@src/renderer/src/lib/motion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace GLASS_CARD usages in all 11 component files + migrate QAPanel to GlassChat</name>
  <files>
    src/renderer/src/plugins/cortex/components/AnalysisProgress.tsx
    src/renderer/src/plugins/cortex/components/AddRepoDialog.tsx
    src/renderer/src/plugins/cortex/components/ArchitectureDashboard.tsx
    src/renderer/src/plugins/cortex/components/DesignDocTab.tsx
    src/renderer/src/plugins/cortex/components/ExportDialog.tsx
    src/renderer/src/plugins/cortex/components/MindGraphTab.tsx
    src/renderer/src/plugins/cortex/components/TestCoverageCard.tsx
    src/renderer/src/plugins/cortex/components/APIListTab.tsx
    src/renderer/src/plugins/cortex/components/OverviewTab.tsx
    src/renderer/src/plugins/cortex/components/QAPanel.tsx
    src/renderer/src/plugins/cortex/components/RepoCard.tsx
    src/renderer/src/plugins/cortex/components/RepoManager.tsx
  </files>
  <action>
For each file, perform these replacements:

**Pattern A — GLASS_CARD replacement (9 files):**
Files: AnalysisProgress, AddRepoDialog, ArchitectureDashboard, DesignDocTab, ExportDialog, MindGraphTab, TestCoverageCard, APIListTab, OverviewTab, QAPanel, RepoCard

1. Remove `GLASS_CARD` from the cortex-theme import. If GLASS_CARD was the only import, delete the entire import line.
2. Add `import { GlassCard } from '@renderer/components/ui'` (or add GlassCard to existing ui import).
3. Replace every `<div className={`${GLASS_CARD} ...`}>` with `<GlassCard className="...">` (move remaining classes to className prop). GlassCard already provides glass styling, border-radius, and backdrop-blur.
4. Replace corresponding `</div>` with `</GlassCard>`.
5. If the element needs interactive hover behavior, use `<GlassCard variant="interactive">`.

**Pattern B — cardVariants/useCardVariants replacement (2 files):**
Files: RepoManager, OverviewTab

1. Remove `cardVariants`/`useCardVariants` from cortex-theme import.
2. Add `import { staggerContainer, staggerItem } from '@renderer/lib/motion'`.
3. Replace container `variants={containerVariants}` with `variants={staggerContainer}`.
4. Replace item `variants={variants} custom={i}` with `variants={staggerItem}` (remove `custom` prop — staggerItem uses parent stagger, no index needed).

**Pattern C — QAPanel GlassChat migration:**
- Import `GlassChat` from `@renderer/components/ui`.
- Replace the custom message list + input area with `<GlassChat>`. Map existing message data to GlassChat's `messages` prop format (`{ id, role, content, citations }`). Pass existing `onSend` handler. Pass `isStreaming` from store state. Pass a `renderContent` prop that uses the existing markdown renderer if one exists.
- Keep existing store connections and streaming logic intact. Only the rendering layer changes.

**Pattern D — ExportDialog GlassModal migration:**
- Import `GlassModal, GlassInput, GlassSelect, GlassButton` from `@renderer/components/ui`.
- Replace custom overlay/modal wrapper with `<GlassModal isOpen={isOpen} onClose={onClose} title="Export Analysis">`.
- Replace form inputs with `GlassInput` and selects with `GlassSelect`.
- Replace action buttons with `GlassButton` (primary for export, ghost for cancel).
- Preserve all form state and submission logic.

**IMPORTANT for all files:**
- Keep ALL entity-specific colors: `getKindColor`, `getMethodColor`, `KIND_COLORS`, `METHOD_COLORS`, `REPO_TYPE_GRADIENTS` imports must remain unchanged.
- Keep ALL event handlers, callbacks, useEffect hooks, and store connections unchanged.
- Keep ALL conditional rendering logic unchanged.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -40</automated>
    <manual>Verify no file imports GLASS_CARD from cortex-theme anymore</manual>
  </verify>
  <done>All 12 files use GlassCard/GlassChat/GlassModal from ui/ instead of GLASS_CARD from cortex-theme. RepoManager and OverviewTab use staggerContainer/staggerItem from motion.ts. QAPanel renders via GlassChat. ExportDialog renders via GlassModal.</done>
</task>

<task type="auto">
  <name>Task 2: Replace GLASS_SURFACE usages, add PluginHeader, clean cortex-theme.ts, add loading/empty states</name>
  <files>
    src/renderer/src/plugins/cortex/CortexView.tsx
    src/renderer/src/plugins/cortex/components/InsightsPanel.tsx
    src/renderer/src/plugins/cortex/components/DiagramsTab.tsx
    src/renderer/src/plugins/cortex/components/FlowsTab.tsx
    src/renderer/src/plugins/cortex/components/ValidationPanel.tsx
    src/renderer/src/plugins/cortex/cortex-theme.ts
  </files>
  <action>
**GLASS_SURFACE replacement (6 files):**
Files: CortexView, InsightsPanel, DiagramsTab, FlowsTab, ValidationPanel (plus QAPanel, MindGraphTab, DesignDocTab, APIListTab if they also import GLASS_SURFACE — check task 1 already handled those)

1. Remove `GLASS_SURFACE` from the cortex-theme import. If it was the only import, delete the entire import line.
2. Add `import { GlassSurface } from '@renderer/components/ui'` (or add to existing ui import).
3. Replace every `<div className={`${GLASS_SURFACE} ...`}>` with `<GlassSurface className="...">`. For full-width headers/toolbars, add `className="rounded-none border-x-0 border-t-0"` to avoid rounded corners at edges.

**CortexView.tsx — PluginHeader migration:**
- Import `PluginHeader` from `@renderer/components/ui`. Import `Brain` from `lucide-react`.
- Replace the existing header + custom tab bar with `<PluginHeader icon={Brain} title="Cortex" tabs={TABS} activeTab={activeTab} onTabChange={setActiveTab} statusIndicator={...} />`. Map existing tab definitions to `{ id, label, icon }` format.
- Keep existing `AnimatePresence` for tab content switching. Use `pageTransition` from motion.ts if not already.
- Preserve ALL store connections, routing logic, and state management.

**cortex-theme.ts cleanup:**
- Delete: `GLASS_CARD` constant, `GLASS_SURFACE` constant, `cardVariants` object, `useCardVariants` function.
- Keep: `KindColorSet` interface, `KIND_COLORS` record, `getKindColor` function, `MethodColorSet` interface, `METHOD_COLORS` record, `getMethodColor` function, `REPO_TYPE_GRADIENTS` record.
- The file should shrink from ~97 lines to ~50 lines.
- Run `grep -r "GLASS_CARD\|GLASS_SURFACE\|cardVariants\|useCardVariants" src/renderer/src/plugins/cortex/` to verify ZERO remaining references.

**Loading/empty states across all Cortex components:**
- InsightsPanel: If no insights, show `<EmptyState icon={LayoutDashboard} title="No insights yet" description="Analyze a repository to see insights" actionLabel="Go to Repos" onAction={() => setActiveTab('repos')} />`
- RepoManager: If no repos and not loading, show `<EmptyState icon={FolderGit2} title="No repositories" description="Add a repository to get started" actionLabel="Add Repository" onAction={openAddDialog} />`
- OverviewTab: If loading, show `<GlassSkeleton variant="card" />` (3 cards).
- MindGraphTab toolbar: Replace any bare toolbar div with `<GlassSurface>` + `GlassInput` for search + `GlassButton` for controls.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30 && grep -r "GLASS_CARD\|GLASS_SURFACE\|cardVariants\|useCardVariants" src/renderer/src/plugins/cortex/ || echo "CLEAN: No legacy cortex-theme references found"</automated>
    <manual>Verify cortex-theme.ts only exports KIND_COLORS, METHOD_COLORS, REPO_TYPE_GRADIENTS and their helpers</manual>
  </verify>
  <done>All GLASS_SURFACE usages replaced with GlassSurface. CortexView uses PluginHeader. cortex-theme.ts cleaned of all inline glass constants. Loading/empty states added. Zero legacy cortex-theme glass references remain in any file.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `grep -r "GLASS_CARD\|GLASS_SURFACE\|cardVariants\|useCardVariants" src/renderer/src/plugins/cortex/` returns zero matches
- cortex-theme.ts only exports: KindColorSet, KIND_COLORS, getKindColor, MethodColorSet, METHOD_COLORS, getMethodColor, REPO_TYPE_GRADIENTS
- CortexView has PluginHeader with gradient Brain icon title
- QAPanel uses GlassChat component
- ExportDialog uses GlassModal
- All loading states use GlassSkeleton, all empty states use EmptyState
</verification>

<success_criteria>
- Zero inline glass patterns remain in Cortex — all use shared ui/ components
- cortex-theme.ts is cleaned to functional-colors-only
- Cortex is visually consistent with TextCraft and core pages
- All existing functionality preserved (analysis, QA streaming, export, MindGraph)
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-migration/04-02-SUMMARY.md`
</output>
