---
phase: 04-plugin-migration
plan: 5
type: execute
wave: 3
depends_on: [04-01]
files_modified:
  - src/renderer/src/plugins/launchpad/LaunchpadView.tsx
  - src/renderer/src/plugins/launchpad/ProviderSelector.tsx
  - src/renderer/src/plugins/launchpad/ResourceConfigurator.tsx
  - src/renderer/src/plugins/launchpad/ServiceCatalog.tsx
  - src/renderer/src/plugins/launchpad/EstimationSummary.tsx
  - src/renderer/src/plugins/launchpad/AiAdvisor.tsx
  - src/renderer/src/plugins/launchpad/ComparisonView.tsx
  - src/renderer/src/plugins/launchpad/EstimationHistory.tsx
autonomous: true
requirements: [PLUG-04]

must_haves:
  truths:
    - "Launchpad renders a PluginHeader with Rocket icon, gradient title, and GlassTab bar (Estimator, AI Advisor, History, Compare)"
    - "Provider selector shows GlassCard per provider with brand accent color on hover glow (AWS=orange, GCP=blue, Azure=cyan)"
    - "Resource configurator uses GlassCard form sections with GlassInput/GlassSelect"
    - "Estimation summary uses AnimatedCounter for total cost in a sticky GlassCard sidebar"
    - "AI Advisor uses GlassChat component for the AI chat interface"
    - "Compare view shows side-by-side GlassCards per provider with green/red cost highlights"
    - "All loading states use GlassSkeleton, empty states use EmptyState"
  artifacts:
    - path: "src/renderer/src/plugins/launchpad/LaunchpadView.tsx"
      provides: "Main view with PluginHeader"
      min_lines: 40
    - path: "src/renderer/src/plugins/launchpad/ProviderSelector.tsx"
      provides: "Glass provider cards with brand colors"
      min_lines: 30
  key_links:
    - from: "src/renderer/src/plugins/launchpad/LaunchpadView.tsx"
      to: "PluginHeader"
      via: "import from ui/"
      pattern: "import.*PluginHeader.*from.*ui"
    - from: "src/renderer/src/plugins/launchpad/AiAdvisor.tsx"
      to: "GlassChat"
      via: "import from ui/"
      pattern: "import.*GlassChat.*from.*ui"
---

<objective>
Migrate all 8 Launchpad files to the Obsidian Glass design system. Key feature: provider cards with brand-specific hover glows and AnimatedCounter for cost estimates.

Purpose: Launchpad's cost estimation screens benefit most from glass styling — financial data needs visual hierarchy and clarity.
Output: All 8 Launchpad files migrated with glass components.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-migration/04-00-SUMMARY.md
@.planning/phases/04-plugin-migration/04-01-SUMMARY.md
@src/renderer/src/plugins/launchpad/LaunchpadView.tsx
@src/renderer/src/plugins/launchpad/ProviderSelector.tsx
@src/renderer/src/plugins/launchpad/EstimationSummary.tsx
@src/renderer/src/plugins/launchpad/AiAdvisor.tsx
@src/renderer/src/stores/launchpad-store.ts
@src/renderer/src/components/ui/index.ts
@src/renderer/src/components/ui/AnimatedCounter.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate LaunchpadView, ProviderSelector, ResourceConfigurator, and ServiceCatalog</name>
  <files>
    src/renderer/src/plugins/launchpad/LaunchpadView.tsx
    src/renderer/src/plugins/launchpad/ProviderSelector.tsx
    src/renderer/src/plugins/launchpad/ResourceConfigurator.tsx
    src/renderer/src/plugins/launchpad/ServiceCatalog.tsx
  </files>
  <action>
**LaunchpadView.tsx:**
- Import `PluginHeader` from `@renderer/components/ui`. Import `Rocket` from `lucide-react`.
- Replace header + custom tab bar with `<PluginHeader icon={Rocket} title="Launchpad" tabs={[{ id: 'estimator', label: 'Estimator', icon: Calculator }, { id: 'advisor', label: 'AI Advisor', icon: MessageSquare }, { id: 'history', label: 'History', icon: Clock }, { id: 'compare', label: 'Compare', icon: GitCompare }]} activeTab={activeTab} onTabChange={setActiveTab} />`.
- Replace `animate-tab-enter` CSS class with framer-motion `AnimatePresence` + `pageTransition` variants.
- Preserve ALL store connections and tab routing logic.

**ProviderSelector.tsx:**
- Import `GlassCard, GlassBadge` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Define brand colors map: `{ aws: 'rgba(255,153,0,0.15)', gcp: 'rgba(66,133,244,0.15)', azure: 'rgba(0,188,212,0.15)' }` for hover glow.
- Each provider: `<motion.div variants={staggerItem}><GlassCard variant="interactive" className="cursor-pointer" style={{ '--hover-glow': brandColors[provider.id] }}>` with custom hover glow using inline style or a `hover:shadow-[0_0_20px_var(--hover-glow)]` approach.
- Show provider logo/icon, name, and a brief description. Selected provider gets accent border.
- Wrap list in `motion.div variants={staggerContainer}`.
- If no providers available, show `EmptyState icon={Rocket} title="Select a Provider" description="Choose a cloud provider to start estimating"`.
- Preserve all selection handlers.

**ResourceConfigurator.tsx:**
- Import `GlassCard, GlassInput, GlassSelect, GlassButton` from `@renderer/components/ui`.
- Group configuration into `GlassCard` sections per resource type (e.g., Compute, Storage, Network).
- Replace native inputs with `GlassInput` (for instance count, region name, etc.) and selects with `GlassSelect` (for instance type, region, storage tier).
- Add section headers inside each GlassCard: `<div className="text-xs font-medium text-text-secondary uppercase tracking-wider pb-2">Section Name</div>`.
- Preserve all form state, validation, and calculation triggers.

**ServiceCatalog.tsx:**
- Import `GlassCard, GlassBadge, GlassSkeleton` from `@renderer/components/ui`.
- Each service: `GlassCard variant="interactive"` with service name, category `GlassBadge`, and pricing preview.
- Loading: `GlassSkeleton variant="card"`.
- Preserve all service selection and filtering logic.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Launchpad shows PluginHeader, provider cards have brand-colored hover glow, forms use GlassInput/GlassSelect</manual>
  </verify>
  <done>LaunchpadView uses PluginHeader. ProviderSelector shows brand-colored GlassCards with stagger. ResourceConfigurator uses GlassCard sections with GlassInput/GlassSelect. ServiceCatalog has interactive GlassCards.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate EstimationSummary, AiAdvisor, ComparisonView, and EstimationHistory</name>
  <files>
    src/renderer/src/plugins/launchpad/EstimationSummary.tsx
    src/renderer/src/plugins/launchpad/AiAdvisor.tsx
    src/renderer/src/plugins/launchpad/ComparisonView.tsx
    src/renderer/src/plugins/launchpad/EstimationHistory.tsx
  </files>
  <action>
**EstimationSummary.tsx:**
- Import `GlassCard, GlassBadge, AnimatedCounter` from `@renderer/components/ui`.
- Sticky sidebar: `GlassCard className="sticky top-4"` containing:
  - Total cost heading with `AnimatedCounter` displaying the dollar amount (import from `@renderer/components/ui/AnimatedCounter`).
  - Per-service breakdown: a list of rows with service name and cost, using `GlassBadge` for service type labels.
  - Monthly/yearly toggle with glass-styled segmented control.
- If no estimate calculated yet, show a subdued message or `EmptyState`.
- Preserve all cost calculation logic and store connections.

**AiAdvisor.tsx:**
- Import `GlassChat` from `@renderer/components/ui`.
- Replace custom chat UI with `<GlassChat messages={...} onSend={...} isStreaming={...} placeholder="Ask about cloud costs..." renderContent={markdownRenderer} />`.
- Map existing message data to GlassChat's message format.
- If no provider selected, show `EmptyState icon={MessageSquare} title="Select a provider first" description="Choose a cloud provider to get AI cost advice"`.
- Preserve all store connections and streaming logic.

**ComparisonView.tsx:**
- Import `GlassCard, GlassBadge, GlassTable` from `@renderer/components/ui`.
- Side-by-side layout: flex row with `GlassCard` per provider.
- Each provider card shows: provider name header, per-service cost rows.
- Cost comparison highlights: cheaper values get `text-emerald-400` (green), more expensive get `text-red-400` (red).
- If comparing and one is clearly cheaper, show a `GlassBadge variant="success"` "Best Value" badge on that card.
- If no comparison data, show `EmptyState icon={GitCompare} title="Nothing to compare" description="Estimate costs for multiple providers to compare"`.

**EstimationHistory.tsx:**
- Import `GlassCard, GlassSkeleton, EmptyState, GlassBadge` from `@renderer/components/ui`. Import `staggerContainer, staggerItem` from `@renderer/lib/motion`.
- Each history entry: `GlassCard variant="interactive"` with estimation name/date, provider `GlassBadge`, total cost, and resource count.
- Stagger entrance animation.
- Loading: `GlassSkeleton variant="card"`.
- Empty: `<EmptyState icon={Clock} title="No estimation history" description="Your cost estimates will appear here" />`
- Preserve all history loading, click-to-restore, and delete handlers.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>EstimationSummary has AnimatedCounter for cost, AI Advisor uses GlassChat, Compare shows side-by-side GlassCards</manual>
  </verify>
  <done>EstimationSummary uses AnimatedCounter in sticky GlassCard. AiAdvisor uses GlassChat. ComparisonView shows side-by-side GlassCards with cost color coding. EstimationHistory has stagger GlassCards. All loading/empty states handled.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- No `animate-tab-enter` CSS class references remain in Launchpad files
- All 8 files use shared ui/ components
- AI Advisor uses GlassChat component
- EstimationSummary uses AnimatedCounter
- Provider cards have brand-specific hover glow colors
- All loading states use GlassSkeleton
- All empty states use EmptyState with context-specific CTAs
</verification>

<success_criteria>
- Launchpad is visually consistent with all other migrated plugins
- No inline glass patterns — all use shared ui/ components
- Provider brand colors preserved on hover glow
- AnimatedCounter animates cost changes smoothly
- All existing cost estimation, AI advice, and comparison functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-migration/04-05-SUMMARY.md`
</output>
