---
phase: 21-intake-grooming-improvements-comments
plan: "05"
type: execute
wave: 4
depends_on: ["21-03", "21-04"]
files_modified: []
autonomous: false
requirements: [GROOM-03, GROOM-04]

must_haves:
  truths:
    - "The app compiles with zero TypeScript errors in both tsconfig.node.json and tsconfig.web.json"
    - "The grooming button shows stage labels during active batch grooming"
    - "The task detail modal has two tabs: Grooming Results and Notes"
    - "Comments persist across re-groom operations"
    - "The integration status ribbon shows 4 icons with correct opacity treatment"
  artifacts:
    - path: "src/main/taskgroomer/grooming-agent.ts"
      provides: "Two-pass groomTask with sourcesUsed + onStage callback"
      contains: "sourcesUsed"
    - path: "src/main/taskgroomer/database.ts"
      provides: "comments column + addComment/updateComment/deleteComment"
      contains: "TaskComment"
    - path: "src/renderer/src/plugins/task-groomer/TaskSidePanel.tsx"
      provides: "Notes tab + SourcesRibbon"
      contains: "Notes"
  key_links:
    - from: "Two-pass agent"
      to: "TaskGroomerView stage labels"
      via: "groomStage in store → stage-label in button"
      pattern: "Analyzing tasks"
    - from: "Task.comments"
      to: "Notes tab"
      via: "store addComment → IPC → DB → updated task state → Notes tab re-render"
      pattern: "addComment"
---

<objective>
Full compile verification across both TypeScript configs and a human verification checkpoint for all three Phase 21 features.

Purpose: Ensure the two-pass grooming, comments, and integration ribbon all work end-to-end before phase completion.

Output: Phase 21 verified and ready to ship.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full compile verification</name>
  <files></files>
  <action>
Run both TypeScript configs and confirm zero errors:

```bash
cd /Users/prafullsaxena/Desktop/Development/zenith
npx tsc --noEmit --project tsconfig.node.json 2>&1
npx tsc --noEmit --project tsconfig.web.json 2>&1
```

If any errors exist, fix them before proceeding to the human checkpoint.

Common issues to check if errors appear:
- sourcesUsed field missing from Task interface in electron.d.ts
- TaskComment interface not declared globally (should be in electron.d.ts, not imported)
- groomStage field missing from TaskGroomerState
- addComment/updateComment/deleteComment not added to ElectronAPI.taskgroomer
- Lucide icon (e.g. Ticket) not available in installed version — substitute with Tag or ExternalLink
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit --project tsconfig.node.json 2>&1 | grep -c "error TS" && npx tsc --noEmit --project tsconfig.web.json 2>&1 | grep -c "error TS"</automated>
    <manual>Both counts must be 0</manual>
  </verify>
  <done>Both tsconfig.node.json and tsconfig.web.json compile with zero TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 21 features</name>
  <action>
Start the app (pm2 restart all) and verify all four test scenarios listed in how-to-verify. Do not proceed until all pass.
  </action>
  <what-built>
Phase 21 complete:
1. Two-pass grooming agent — AI first decides which sources a task needs, then queries only those, then summarizes. Simple tasks get one AI call total. Stage labels (Analyzing tasks... / Querying sources... / Summarizing...) show in the Groom button during runs.
2. Task comments — Notes tab in the task detail modal with add/edit/delete timestamped entries. Comments survive re-grooming.
3. Integration status ribbon — 4 icons (AI / Jira / Confluence / Google) at the top of Grooming Results, showing which sources were actually queried.
  </what-built>
  <how-to-verify>
Start the app: pm2 restart all, then open Zenith.

**Test 1 — Stage labels:**
1. Navigate to Task Groomer plugin
2. Make sure at least one task is in Dump status
3. Click the Groom button
4. Observe the button label: should cycle through "Analyzing tasks..." then "Querying sources..." then "Summarizing..." as each task is processed
5. After run completes, button returns to "Groom"

**Test 2 — Integration status ribbon:**
1. Click on any groomed task to open its detail modal
2. In Grooming Results tab, verify a ribbon of 4 icons appears at the top: AI, Jira, Confluence, Google
3. Icons for sources that were used appear in color; unused ones appear faded/grayscale
4. (If no groomed tasks, run Re-groom on any task first)

**Test 3 — Notes tab:**
1. Open any task detail modal
2. Click the "Notes" tab
3. Type a note and click Save — note appears with timestamp
4. Click Edit on the note — inline textarea appears pre-filled
5. Change the text, click Save — note updates with "(edited)" marker
6. Add a second note, then click Delete on the first — it disappears
7. Trigger Re-groom on the task (Re-groom button in the modal header)
8. After re-groom completes, switch to Notes tab — remaining notes still present

**Test 4 — Comments count badge:**
1. Open a task with at least one note
2. The Notes tab label shows a small count badge
  </how-to-verify>
  <verify>
    <automated>MISSING — verification is manual for this checkpoint task</automated>
    <manual>All four test scenarios pass as described in how-to-verify</manual>
  </verify>
  <done>Human has approved all four test scenarios: stage labels visible during grooming, ribbon renders correctly, Notes tab CRUD works, comments survive re-groom.</done>
  <resume-signal>Type "approved" if all four tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Both TypeScript configs compile with zero errors.
Human verified: stage labels, ribbon, Notes tab add/edit/delete, re-groom preserves comments.
</verification>

<success_criteria>
- Zero TypeScript errors in both tsconfig.node.json and tsconfig.web.json
- Human approves all four test scenarios
- Phase 21 ready for commit
</success_criteria>

<output>
After completion, create `.planning/phases/21-intake-grooming-improvements-comments/21-05-SUMMARY.md`
</output>
