---
phase: 09-calculator-store
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/stores/launchpad-store.ts
autonomous: true
requirements: [CALC-04]
gap_closure: true

must_haves:
  truths:
    - "Calling getTotalCost twice with the same selections and region returns the memoized result without re-invoking calculateTotalCost"
    - "Changing region, provider, or triggering a sync cache refresh clears memoCache so stale results are never served"
  artifacts:
    - path: "src/renderer/src/stores/launchpad-store.ts"
      provides: "getTotalCost with memoCache read/write path active"
      contains: "memoCache.get(key)"
  key_links:
    - from: "getTotalCost"
      to: "memoCache"
      via: "composite key built from all selection serviceIds + stableHash(config) + region"
      pattern: "memoCache\\.get\\(key\\)"
---

<objective>
Wire the existing `memoCache` Map into `getTotalCost` in `launchpad-store.ts` so that calculator-result memoization is active per CALC-04.

Purpose: The infrastructure (memoCache Map, stableHash function, cache-clear calls in setRegion/setProvider/refreshPricingCache) was built in plan 09-02 but the read/write path inside getTotalCost was never connected. This is the only gap blocking CALC-04.

Output: A patched `getTotalCost` that builds a composite cache key from all selections and region, returns cached `{ monthly, yearly }` on a hit, and writes to memoCache on a miss.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calculator-store/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire memoCache read/write path in getTotalCost</name>
  <files>src/renderer/src/stores/launchpad-store.ts</files>
  <action>
    Locate the `getTotalCost` getter (currently lines 173-180). Replace the body so that:

    1. Build a composite cache key over ALL current selections. For each selection in `selectedServices`, concatenate `${selection.serviceId}:${stableHash(selection.config)}`. Join with `|`, then append `:${pricingCache.region}`. Example key shape: `"ec2:{"instanceType":"t3.micro"}|s3:{}:us-east-1"`.

    2. Check `memoCache.get(key)`. If hit, return `{ monthly: cached.monthly, yearly: cached.yearly }` immediately without calling `calculateTotalCost`.

    3. On a miss, call `calculateTotalCost(selectedServices, pricingCache.rates, pricingCache.region)`, store `{ monthly: result.totalMonthly, yearly: result.totalYearly, breakdown: result.items }` in `memoCache.set(key, ...)`, and return `{ monthly: result.totalMonthly, yearly: result.totalYearly }`.

    The early-return guards (empty selections, null rates) remain unchanged — cache lookup only happens when `calculateTotalCost` would otherwise be called.

    Do NOT change memoCache type declaration, stableHash function, or any cache-clear call sites (setRegion, setProvider, refreshPricingCache). Touch only the `getTotalCost` body.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit -p tsconfig.web.json 2>&1 | tail -5</automated>
    <manual>Grep confirms the read path: grep -n "memoCache.get" src/renderer/src/stores/launchpad-store.ts — must show a line inside getTotalCost. Grep confirms the write path: grep -n "memoCache.set" src/renderer/src/stores/launchpad-store.ts — must show a line inside getTotalCost.</manual>
  </verify>
  <done>TypeScript compiles with zero errors. `memoCache.get(key)` and `memoCache.set(key, ...)` both appear inside the getTotalCost body. The memoCache Map is no longer declared-but-unused in the calculation path.</done>
</task>

</tasks>

<verification>
After task completion:

1. TypeScript zero errors: `npx tsc --noEmit -p tsconfig.web.json` exits 0.
2. Cache read path present: `grep -n "memoCache.get" src/renderer/src/stores/launchpad-store.ts` returns a result inside getTotalCost.
3. Cache write path present: `grep -n "memoCache.set" src/renderer/src/stores/launchpad-store.ts` returns a result inside getTotalCost.
4. Cache-clear calls unchanged: `grep -n "memoCache.clear" src/renderer/src/stores/launchpad-store.ts` still shows 3 results (setProvider, setRegion, refreshPricingCache).
</verification>

<success_criteria>
- CALC-04 satisfied: same selections + same region → memoCache hit, calculateTotalCost not called a second time
- Phase 09 verification score rises from 9/10 to 10/10
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/09-calculator-store/09-03-SUMMARY.md` following the standard summary template.
</output>
