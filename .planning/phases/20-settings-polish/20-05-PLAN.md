---
phase: 20-settings-polish
plan: 05
type: execute
wave: 2
depends_on:
  - "20-01"
  - "20-02"
  - "20-03"
  - "20-04"
files_modified: []
autonomous: false
requirements:
  - DATA-02
  - INT-04

must_haves:
  truths:
    - "User visually confirms all Phase 20 deliverables are working and polished"
    - "Integration health dashboard visible and correct in Settings"
    - "Jira link opens external browser"
    - "Failure banner appears when groom run has failures"
    - "Capture popup has submit button"
    - "No TypeScript errors in the build"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 20 polish deliverables before shipping.

Purpose: Phase 20 is visual/interactive — automated tests cannot confirm look and feel. This single checkpoint verifies all four parallel plans together.
Output: User sign-off or issue list for fixes.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-settings-polish/20-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final compile verification</name>
  <files>.</files>
  <action>
    Run the TypeScript compiler across the full project to confirm zero errors from all Phase 20 changes:
    ```bash
    cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit
    ```
    If any errors exist: fix them in the relevant files before proceeding to the human verification checkpoint.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>npx tsc --noEmit exits with code 0 (zero TypeScript errors).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Phase 20 polish</name>
  <what-built>
    Phase 20 complete implementation:
    - Plan 01: Integration health dashboard in Settings → Task Groomer (4 service icons, active = vivid, inactive = dimmed)
    - Plan 02: Failure banner in TaskGroomerView when groom fails + amber warning icon on failed task cards
    - Plan 03: Jira link uses window.api.app.openExternal, polished empty state in TaskSidePanel, a11y focus rings
    - Plan 04: GroomDigest visual consistency (text-sm task rows), polished capture popup with submit button, StatusDropdown focus ring
  </what-built>
  <action>Present the verification steps below to the user and wait for approval.</action>
  <how-to-verify>
    1. Open Settings → Task Groomer. Verify: compact icon row at top showing 4 service icons. If Jira is configured → Jira icon is vivid blue. If not → all non-web-search icons are dimmed/grayscale. Web Search icon always vivid green.

    2. In Settings → Task Groomer → Integrations: confirm existing credential forms still work (no regression from Plan 01).

    3. Open Task Groomer plugin. Click any task. In the side panel: verify Jira link (if present) is a button (not anchor). Click it — should open browser, not navigate in Electron. Also verify the empty state card appears for tasks with no grooming data (not bare italic text).

    4. Tab through the side panel using keyboard: Re-groom button, Jira link, research links — all should show visible purple focus rings.

    5. Press Cmd+Shift+D to open capture popup. Verify: "Capture ⏎" button at bottom right. Click it to capture a task. Verify press animation (scale). Enter key also still works.

    6. Open Task Groomer and trigger a groom run. If any tasks fail: verify amber banner appears above tab content with "N tasks failed" text, Retry button, and X dismiss button. Verify failed task cards show AlertCircle icon.

    7. Open GroomDigest (after a groom run): task text should be text-sm (slightly larger than before). Close button should have visible focus ring when tabbed to.

    8. StatusDropdown badge on task cards: tab to it and press Space to open — verify focus ring visible on the pill trigger.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual verification required by human</automated>
    <manual>User confirms all 8 checklist items above pass. Type "approved" or describe issues.</manual>
  </verify>
  <done>User types "approved" confirming all Phase 20 deliverables work correctly.</done>
  <resume-signal>Type "approved" if all checks pass, or list specific issues found (e.g., "Jira icon not dimming", "focus ring missing on X button")</resume-signal>
</task>

</tasks>

<verification>
Human approval confirms all Phase 20 deliverables are complete and production-ready.
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- User approves all visual/interactive deliverables
- Phase 20 marked complete in STATE.md
</success_criteria>

<output>
After completion, create `.planning/phases/20-settings-polish/20-05-SUMMARY.md`

Update STATE.md:
- Phase: 20-settings-polish — Complete
- Progress: [██████████] 100% (7/7 phases complete — v3.0 DONE)
- Milestone: v3.0 Task Groomer Plugin — SHIPPED
</output>
