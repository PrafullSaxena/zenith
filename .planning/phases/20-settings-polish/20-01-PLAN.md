---
phase: 20-settings-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/task-groomer/TaskGroomerSettings.tsx
autonomous: true
requirements:
  - DATA-02
  - INT-04

must_haves:
  truths:
    - "The top of TaskGroomerSettings shows a compact icon row: AI Agent, Jira, Confluence, Web Search — each icon full-color when configured, dimmed/desaturated at ~30% opacity when not configured"
    - "Tapping an icon does nothing (display-only)"
    - "The health dashboard renders above the AI Agent section"
  artifacts:
    - path: "src/renderer/src/plugins/task-groomer/TaskGroomerSettings.tsx"
      provides: "Integration health dashboard + existing schedule/settings sections"
      contains: "IntegrationHealthDashboard"
  key_links:
    - from: "IntegrationHealthDashboard"
      to: "jiraStatus.configured"
      via: "prop/derived value"
      pattern: "jiraStatus\\.configured"
    - from: "IntegrationHealthDashboard"
      to: "confluenceStatus.configured"
      via: "prop/derived value"
      pattern: "confluenceStatus\\.configured"
---

<objective>
Add integration health dashboard to TaskGroomerSettings as a compact icon-based status row at the very top of the settings panel — above the AI Agent section.

Purpose: Give the user an at-a-glance view of which integrations are configured without needing to scroll. This satisfies the "integration health dashboard" deliverable from CONTEXT.md.
Output: Updated TaskGroomerSettings.tsx with IntegrationHealthDashboard sub-component at top.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-settings-polish/20-CONTEXT.md
@src/renderer/src/plugins/task-groomer/TaskGroomerSettings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IntegrationHealthDashboard sub-component to TaskGroomerSettings</name>
  <files>src/renderer/src/plugins/task-groomer/TaskGroomerSettings.tsx</files>
  <action>
    Add a new `IntegrationHealthDashboard` sub-component at the top of TaskGroomerSettings.tsx (rendered as the first child inside `&lt;div className="space-y-6"&gt;`, before the AI Agent section).

    Visual design (from CONTEXT.md locked decision):
    - Compact single row of 4 service icons: AI Agent (Brain or Bot from lucide-react), Jira (Ticket from lucide-react), Confluence (FileText from lucide-react), Web Search (Globe from lucide-react)
    - Each icon is 20px size, wrapped in a 36x36 rounded-lg container
    - Configured/connected: full-color vivid icon — use `text-primary` for AI Agent, `text-blue-400` for Jira, `text-blue-300` for Confluence, `text-green-400` for Web Search
    - Not configured: same icon, `opacity-30 grayscale` Tailwind classes (desaturated + dimmed)
    - No text labels, no click handler — purely informational display
    - Container: `rounded-xl border border-white/6 bg-white/[0.04] p-4` with a small label "Integration Status" (text-[11px] text-muted-foreground uppercase tracking-wider mb-3)
    - Row: `flex items-center gap-3`

    Props the component needs (pass down from parent where state already lives):
    - `aiConfigured: boolean` — `availableProviders.length > 0` (AI agent connected = at least one provider available)
    - `jiraConfigured: boolean` — `jiraStatus.configured`
    - `confluenceConfigured: boolean` — `confluenceStatus.configured`
    - `webSearchConfigured: boolean` — always `true` (Web Search is always available per Phase 17 decision)

    Implementation:
    ```tsx
    function IntegrationHealthDashboard({
      aiConfigured,
      jiraConfigured,
      confluenceConfigured,
      webSearchConfigured
    }: {
      aiConfigured: boolean
      jiraConfigured: boolean
      confluenceConfigured: boolean
      webSearchConfigured: boolean
    }): React.JSX.Element {
      const services = [
        { icon: Bot, label: 'AI Agent', configured: aiConfigured, color: 'text-primary' },
        { icon: Ticket, label: 'Jira', configured: jiraConfigured, color: 'text-blue-400' },
        { icon: FileText, label: 'Confluence', configured: confluenceConfigured, color: 'text-blue-300' },
        { icon: Globe, label: 'Web Search', configured: webSearchConfigured, color: 'text-green-400' }
      ]
      return (
        &lt;div className="rounded-xl border border-white/6 bg-white/[0.04] p-4"&gt;
          &lt;p className="text-[11px] text-muted-foreground uppercase tracking-wider mb-3"&gt;Integration Status&lt;/p&gt;
          &lt;div className="flex items-center gap-3"&gt;
            {services.map(({ icon: Icon, label, configured, color }) =&gt; (
              &lt;div
                key={label}
                title={label}
                className={cn(
                  'flex h-9 w-9 items-center justify-center rounded-lg border',
                  configured
                    ? 'border-white/10 bg-white/[0.04]'
                    : 'border-white/6 bg-white/[0.02] opacity-30 grayscale'
                )}
                aria-label={`${label}: ${configured ? 'configured' : 'not configured'}`}
              &gt;
                &lt;Icon size={18} className={configured ? color : 'text-muted-foreground'} /&gt;
              &lt;/div&gt;
            ))}
          &lt;/div&gt;
        &lt;/div&gt;
      )
    }
    ```

    Add to imports from lucide-react: `Bot`, `Ticket`, `FileText`, `Globe` (in addition to existing imports).
    Add `IntegrationHealthDashboard` as the first item inside `&lt;div className="space-y-6"&gt;` with appropriate props:
    ```tsx
    &lt;IntegrationHealthDashboard
      aiConfigured={availableProviders.length &gt; 0}
      jiraConfigured={jiraStatus.configured}
      confluenceConfigured={confluenceStatus.configured}
      webSearchConfigured={true}
    /&gt;
    ```
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Open Settings → Task Groomer. Verify: 4 icons appear at top above AI Agent section. Before any integration configured: all icons dimmed/grayscale. After configuring Jira: Jira icon becomes vivid blue. Clicking icons does nothing.</manual>
  </verify>
  <done>IntegrationHealthDashboard renders as compact 4-icon row above AI Agent section; configured icons are vivid/colored; unconfigured icons are opacity-30 grayscale; no click action; TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` in project root — zero TypeScript errors on modified file.
Visually confirm the health dashboard renders in Settings → Task Groomer above the AI Agent section.
</verification>

<success_criteria>
- IntegrationHealthDashboard sub-component exists in TaskGroomerSettings.tsx
- 4 icons: Bot (AI), Ticket (Jira), FileText (Confluence), Globe (Web Search)
- Configured = full-color icon; unconfigured = opacity-30 grayscale
- No click handler on icons
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-settings-polish/20-01-SUMMARY.md`
</output>
