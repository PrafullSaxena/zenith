---
phase: 20-settings-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/task-groomer/GroomDigest.tsx
  - src/renderer/src/plugins/capture/CapturePopup.tsx
  - src/renderer/src/plugins/capture/capture.css
  - src/renderer/src/plugins/task-groomer/StatusDropdown.tsx
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "GroomDigest panel uses the same border radius, background tone, and text sizing as TaskSidePanel — visually consistent right-side panel"
    - "GroomDigest close button (X) gains a focus-visible ring — tabbing to it shows a ring matching primary/60"
    - "Capture popup submit button has a visible press state and matches Zenith dark aesthetic — polished keyboard hint footer"
    - "StatusDropdown DropdownMenuContent has a consistent open animation matching other dropdowns in the app"
  artifacts:
    - path: "src/renderer/src/plugins/task-groomer/GroomDigest.tsx"
      provides: "Visually consistent digest panel with a11y"
      contains: "border-l border-white/8"
    - path: "src/renderer/src/plugins/capture/CapturePopup.tsx"
      provides: "Polished capture popup matching Zenith aesthetic"
    - path: "src/renderer/src/plugins/capture/capture.css"
      provides: "Refined capture card styles"
    - path: "src/renderer/src/plugins/task-groomer/StatusDropdown.tsx"
      provides: "Smooth status dropdown with focus ring on trigger"
  key_links:
    - from: "GroomDigest.tsx"
      to: "TaskSidePanel visual language"
      via: "matching Tailwind tokens"
      pattern: "bg-background border-l border-white"
---

<objective>
Polish GroomDigest visual consistency, refine the capture popup aesthetic, and add focus ring to StatusDropdown trigger.

Purpose: CONTEXT.md calls for GroomDigest to visually match TaskSidePanel and for the capture popup to feel as refined as a spotlight/launcher. StatusDropdown needs a focus ring for a11y completeness.
Output: Updated GroomDigest.tsx, CapturePopup.tsx, capture.css, StatusDropdown.tsx.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-settings-polish/20-CONTEXT.md
@src/renderer/src/plugins/task-groomer/GroomDigest.tsx
@src/renderer/src/plugins/capture/CapturePopup.tsx
@src/renderer/src/plugins/capture/capture.css
@src/renderer/src/plugins/task-groomer/StatusDropdown.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish GroomDigest visual consistency and StatusDropdown a11y</name>
  <files>
    src/renderer/src/plugins/task-groomer/GroomDigest.tsx
    src/renderer/src/plugins/task-groomer/StatusDropdown.tsx
  </files>
  <action>
    **GroomDigest.tsx — visual consistency with TaskSidePanel:**

    The digest panel already uses `bg-background border-l border-white/8 shadow-xl`. Align these details:

    1. Header padding: TaskSidePanel uses `p-6 pt-10`. GroomDigest header uses `px-6 py-4 pt-10 border-b border-white/6`. This is fine — keep as is.

    2. Task row hover: current `hover:bg-white/4` — matches TaskCard. Keep.

    3. Footer border: `border-t border-white/6` — matches. Keep.

    4. Task row text sizing: `text-xs text-foreground` — matches TaskCard `text-sm`. Unify: change task row text from `text-xs` to `text-sm` for the task text span:
    ```tsx
    &lt;span className="flex-1 text-sm text-foreground truncate group-hover:text-primary transition-colors"&gt;
    ```

    5. Digest close button a11y: verify `aria-label="Close digest"` is present (it is from Phase 19). Add focus ring:
    ```tsx
    className="rounded-md p-1.5 text-muted-foreground hover:text-foreground hover:bg-white/8 transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/60"
    ```

    6. Digest slide-in timing: current `duration-300` — matches Sheet animation. Keep.

    7. Task row click button: add focus ring for a11y:
    ```tsx
    className="flex items-center gap-2 px-4 py-2.5 text-left hover:bg-white/4 transition-colors group focus-visible:outline-none focus-visible:bg-white/4"
    ```

    **StatusDropdown.tsx — add focus ring to trigger button:**

    Find the DropdownMenuTrigger button:
    ```tsx
    className={cn(
      'inline-flex items-center rounded-full px-2 py-0.5',
      'text-[10px] font-medium border shrink-0',
      'transition-opacity hover:opacity-80 focus:outline-none',
      config.color,
      config.bgColor
    )}
    ```

    Replace `focus:outline-none` with `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/60 focus-visible:ring-offset-1 focus-visible:ring-offset-background`:
    ```tsx
    className={cn(
      'inline-flex items-center rounded-full px-2 py-0.5',
      'text-[10px] font-medium border shrink-0',
      'transition-opacity hover:opacity-80',
      'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/60 focus-visible:ring-offset-1 focus-visible:ring-offset-background',
      config.color,
      config.bgColor
    )}
    ```
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Open GroomDigest after a groom run — task text is text-sm (larger than before). Close button has visible focus ring when tabbed to. StatusDropdown pill button shows focus ring when tabbed to in both TaskCard and TaskSidePanel contexts. No visual regressions in non-focused state.</manual>
  </verify>
  <done>GroomDigest task text unified to text-sm; close button and row buttons have focus rings; StatusDropdown trigger has focus-visible ring; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Polish capture popup to match Zenith spotlight aesthetic</name>
  <files>
    src/renderer/src/plugins/capture/CapturePopup.tsx
    src/renderer/src/plugins/capture/capture.css
  </files>
  <action>
    The capture popup already has good bones from Phase 15 (dark background, border, animations). Apply polish to match the Zenith spotlight/launcher aesthetic:

    **capture.css changes:**

    1. Submit button: Add an explicit submit button below the textarea (currently only keyboard Enter works). Add CSS for it:
    ```css
    .capture-actions {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 8px;
      margin-top: 10px;
    }

    .capture-submit {
      display: inline-flex;
      align-items: center;
      gap: 5px;
      padding: 4px 12px;
      background: rgba(139, 92, 246, 0.85);
      border: 1px solid rgba(139, 92, 246, 0.5);
      border-radius: 7px;
      font-size: 11px;
      font-weight: 500;
      color: rgba(255, 255, 255, 0.92);
      cursor: pointer;
      transition: background 120ms ease, transform 80ms ease;
      user-select: none;
    }

    .capture-submit:hover {
      background: rgba(139, 92, 246, 0.95);
    }

    .capture-submit:active {
      transform: scale(0.97);
    }

    .capture-submit:disabled {
      opacity: 0.45;
      cursor: not-allowed;
      transform: none;
    }

    .capture-submit:focus-visible {
      outline: 2px solid rgba(139, 92, 246, 0.8);
      outline-offset: 2px;
    }
    ```

    2. Card subtle inner glow: update `.capture-card` box-shadow to add a subtle top highlight:
    ```css
    .capture-card {
      /* ... existing ... */
      box-shadow:
        0 24px 64px rgba(0, 0, 0, 0.6),
        0 0 0 0.5px rgba(255, 255, 255, 0.06),
        inset 0 1px 0 rgba(255, 255, 255, 0.04);  /* subtle top highlight */
    }
    ```

    3. Input focus caret: already `caret-color: #8b5cf6` — keep.

    **CapturePopup.tsx changes:**

    Replace the existing `capture-hint` div with an `capture-actions` div that has the submit button + the hint text:

    Before (current):
    ```tsx
    &lt;div className="capture-hint"&gt;
      &lt;span&gt;⏎ to capture&lt;/span&gt;
      &lt;span&gt;esc to dismiss&lt;/span&gt;
    &lt;/div&gt;
    ```

    After:
    ```tsx
    &lt;div className="capture-actions"&gt;
      &lt;span style={{ fontSize: '10px', color: 'rgba(255,255,255,0.22)' }}&gt;esc to dismiss&lt;/span&gt;
      &lt;button
        type="button"
        className="capture-submit"
        onClick={handleSubmit}
        disabled={submitting || !text.trim()}
        aria-label="Capture task"
      &gt;
        {submitting ? 'Capturing...' : 'Capture ⏎'}
      &lt;/button&gt;
    &lt;/div&gt;
    ```

    Keep all existing keyboard handling (Enter to submit, Escape to close) unchanged.

    The hint row changes from a two-sided display to: left = "esc to dismiss" caption, right = styled Capture button. This gives the popup a more polished feel while preserving keyboard-first workflow.
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Press Cmd+Shift+D to open capture popup. Verify: "Capture ⏎" button appears at bottom right. Clicking it submits. Button has active press scale. Enter key still submits. Escape still closes. Button is disabled when text field is empty.</manual>
  </verify>
  <done>Capture popup has styled submit button with press state; inner glow on card; keyboard behavior unchanged; TypeScript compiles.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero errors on all modified files.
Visual: open capture popup and verify submit button + press state. Open GroomDigest and verify text sizing + close button focus ring. Tab through StatusDropdown to verify focus ring.
</verification>

<success_criteria>
- GroomDigest task row text is text-sm (matching TaskCard)
- GroomDigest close button has focus-visible ring
- StatusDropdown trigger has focus-visible ring
- Capture popup has visible "Capture ⏎" submit button with active transform
- Capture popup card has subtle inner top highlight
- Keyboard behavior (Enter/Escape) unchanged in capture popup
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-settings-polish/20-04-SUMMARY.md`
</output>
