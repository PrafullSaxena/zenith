---
phase: 04-screen-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
  - src/renderer/src/plugins/db-inspector/ConnectionManager.tsx
  - src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx
  - src/renderer/src/plugins/db-inspector/QueryConsole.tsx
  - src/renderer/src/plugins/db-inspector/QueryTab.tsx
  - src/renderer/src/plugins/db-inspector/ResultsGrid.tsx
autonomous: true
requirements:
  - DBIP-01
  - DBIP-02
  - DBIP-03

must_haves:
  truths:
    - "DbInspectorView uses PluginShell with tabs for Query, AI, ER, History"
    - "ConnectionManager shows Card with Select for connections, connect Button, StatusBadge"
    - "SchemaExplorer renders Accordion tree (databases > schemas > tables > columns) with Tooltip for types"
    - "QueryConsole uses shared CodeEditor (execute mode with SQL dialect) + DataTable for results"
  artifacts:
    - path: "src/renderer/src/plugins/db-inspector/DbInspectorView.tsx"
      provides: "DB Inspector plugin shell"
      contains: "import.*PluginShell"
    - path: "src/renderer/src/plugins/db-inspector/QueryConsole.tsx"
      provides: "SQL editor + results"
      contains: "import.*CodeEditor"
    - path: "src/renderer/src/plugins/db-inspector/ResultsGrid.tsx"
      provides: "Query results table"
      contains: "import.*DataTable"
  key_links:
    - from: "src/renderer/src/plugins/db-inspector/QueryConsole.tsx"
      to: "shared code-editor"
      via: "CodeEditor with execute mode"
      pattern: "import.*CodeEditor.*from.*shared"
    - from: "src/renderer/src/plugins/db-inspector/ResultsGrid.tsx"
      to: "shared data-table"
      via: "DataTable for results"
      pattern: "import.*DataTable.*from.*shared"
---

<objective>
Migrate DB Inspector main view, ConnectionManager, SchemaExplorer, and QueryConsole from Glass components to shadcn/shared components.

Purpose: QueryConsole is the primary interaction surface for DB Inspector and must use shared CodeEditor + DataTable.
Output: 6 rewritten DB Inspector component files.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/plugin-shell.tsx
@src/renderer/src/components/shared/code-editor.tsx
@src/renderer/src/components/shared/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate DbInspectorView + ConnectionManager + SchemaExplorer</name>
  <files>
    src/renderer/src/plugins/db-inspector/DbInspectorView.tsx
    src/renderer/src/plugins/db-inspector/ConnectionManager.tsx
    src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx
  </files>
  <action>
**DbInspectorView.tsx:**
1. Read existing file to understand layout (typically split: left sidebar with ConnectionManager+SchemaExplorer, right tabbed content)
2. Remove all Glass imports
3. Import PluginShell from @renderer/components/shared/plugin-shell, SplitPanel from @renderer/components/shared/split-panel
4. Use SplitPanel (direction="horizontal", sizes=[25, 75]) for left sidebar / right content
5. Left panel: ConnectionManager at top + SchemaExplorer below in a flex column
6. Right panel: Tabs (Query, AI, ER, History) -- or use PluginShell tabs if the layout allows
7. Keep ALL store connections (useDbStore), active connection state, tab management

**ConnectionManager.tsx:**
1. Read existing file
2. Replace Glass components with Card (rounded-[22px])
3. Select from ui/select for connection dropdown
4. Button (primary) for "Connect", Button (ghost) for "New Connection"
5. Badge for connection status (success=connected, destructive=disconnected, warning=connecting)
6. Keep ALL IPC calls for connecting, testing, creating connections

**SchemaExplorer.tsx:**
1. Read existing file
2. Replace with Accordion from ui/accordion
3. Hierarchy: AccordionItem per database > AccordionItem per schema > AccordionItem per table > list of columns
4. Column items: name + Tooltip from ui/tooltip showing data type, nullable, default, constraints
5. Use ScrollArea from ui/scroll-area for the entire explorer
6. Clicking a table name could populate query editor -- keep that behavior
7. Keep ALL schema fetching IPC calls and store connections
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>DB Inspector shows split layout with ConnectionManager + SchemaExplorer on left</manual>
  </verify>
  <done>DbInspectorView uses SplitPanel layout. ConnectionManager uses Card+Select+Badge. SchemaExplorer uses Accordion+Tooltip. All store/IPC connections preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate QueryConsole + QueryTab + ResultsGrid</name>
  <files>
    src/renderer/src/plugins/db-inspector/QueryConsole.tsx
    src/renderer/src/plugins/db-inspector/QueryTab.tsx
    src/renderer/src/plugins/db-inspector/ResultsGrid.tsx
  </files>
  <action>
**QueryConsole.tsx:**
1. Read existing file -- this is the main query editing + execution area
2. Remove SqlEditor import, replace with CodeEditor from @renderer/components/shared/code-editor
3. CodeEditor props: language="sql", dialect based on connection type (postgresql/mysql), onExecute handler for Mod+Enter, schemaCompletions from store (tables + columns for autocomplete)
4. Layout: CodeEditor on top, ResultsGrid below (vertical split using SplitPanel or simple flex layout)
5. Keep ALL query execution logic, result handling, error display, store connections

**QueryTab.tsx:**
1. Read existing file -- manages multiple query tabs
2. Replace with Tabs from ui/tabs
3. Each tab: TabsTrigger with query name + close Button
4. "+" Button to add new query tab
5. TabsContent renders QueryConsole for that tab
6. Keep ALL tab state management

**ResultsGrid.tsx:**
1. Read existing file -- renders query results as a table
2. Replace with DataTable from @renderer/components/shared/data-table
3. Columns generated dynamically from result column names
4. Pass sortable=true, paginated=true, virtualScroll=true for large results
5. Keep the CellModal for expanding large cell content (or use DataTable's built-in cell expand if available)
6. Keep ALL result data handling
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Query tab shows CodeEditor with SQL highlighting, results show in DataTable below</manual>
  </verify>
  <done>QueryConsole uses shared CodeEditor (execute mode, SQL dialect, schema completions). QueryTab uses Tabs. ResultsGrid uses shared DataTable with pagination. All query execution logic preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassTab\|SqlEditor\|GlassTable" src/renderer/src/plugins/db-inspector/DbInspectorView.tsx src/renderer/src/plugins/db-inspector/ConnectionManager.tsx src/renderer/src/plugins/db-inspector/SchemaExplorer.tsx src/renderer/src/plugins/db-inspector/QueryConsole.tsx src/renderer/src/plugins/db-inspector/QueryTab.tsx src/renderer/src/plugins/db-inspector/ResultsGrid.tsx` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- DB Inspector renders split layout with connection manager + schema explorer on left
- QueryConsole uses shared CodeEditor with SQL dialect and execute mode
- ResultsGrid uses shared DataTable with sorting, pagination, virtual scroll
- Zero Glass* or SqlEditor imports in modified files
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-05-SUMMARY.md`
</output>
