---
phase: 04-screen-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/settings/SettingsLayout.tsx
  - src/renderer/src/components/settings/GeneralSettings.tsx
  - src/renderer/src/components/settings/AIAgentsSettings.tsx
  - src/renderer/src/components/settings/AgentRow.tsx
  - src/renderer/src/components/settings/AddCustomAgentForm.tsx
  - src/renderer/src/components/settings/MCPSettings.tsx
  - src/renderer/src/components/settings/PluginSettings.tsx
  - src/renderer/src/components/settings/ConnectionListEditor.tsx
  - src/renderer/src/components/settings/RepoListEditor.tsx
  - src/renderer/src/components/settings/SettingsField.tsx
autonomous: true
requirements:
  - SETT-01
  - SETT-02
  - SETT-03
  - SETT-04
  - SETT-05
  - SETT-06

must_haves:
  truths:
    - "Settings has vertical Tabs navigation on the left with animated indicator"
    - "General settings shows theme selector grid with single active theme"
    - "AI Agents panel shows DataTable with StatusBadge and API key inputs"
    - "MCP panel shows Card list with Switch toggles and DropdownMenu actions"
    - "Plugin settings renders dynamic Accordion forms"
    - "Connection and Repo editors are restyled with Card forms"
  artifacts:
    - path: "src/renderer/src/components/settings/SettingsLayout.tsx"
      provides: "Settings shell with vertical Tabs"
      contains: "import.*Tabs.*from.*@renderer/components/ui/tabs"
    - path: "src/renderer/src/components/settings/AIAgentsSettings.tsx"
      provides: "Agent management with DataTable"
      contains: "import.*DataTable"
    - path: "src/renderer/src/components/settings/MCPSettings.tsx"
      provides: "MCP server management with Switch and DropdownMenu"
      contains: "import.*Switch"
  key_links:
    - from: "src/renderer/src/components/settings/SettingsLayout.tsx"
      to: "sub-panels"
      via: "Tabs rendering each settings panel"
      pattern: "TabsContent"
    - from: "src/renderer/src/components/settings/AIAgentsSettings.tsx"
      to: "stores"
      via: "settings store for agent config"
      pattern: "useSettingsStore|useAgentStore"
---

<objective>
Migrate the Settings screen from Glass components to shadcn/ui with vertical Tabs navigation and all sub-panels restyled.

Purpose: Settings is a complex multi-panel screen that needs consistent form styling across all sub-sections.
Output: 10 rewritten settings component files using Tabs, Card, Input, Select, Switch, Accordion, DataTable.
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@design-system/zenith/MASTER.md
@src/renderer/src/components/shared/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate SettingsLayout + GeneralSettings + SettingsField</name>
  <files>
    src/renderer/src/components/settings/SettingsLayout.tsx
    src/renderer/src/components/settings/GeneralSettings.tsx
    src/renderer/src/components/settings/SettingsField.tsx
  </files>
  <action>
**SettingsLayout.tsx:**
1. Read existing file to understand current tab structure, routing, and store connections
2. Remove all Glass imports (GlassTab, PluginHeader, etc.)
3. Import Tabs, TabsList, TabsTrigger, TabsContent from @renderer/components/ui/tabs
4. Build a vertical tabs layout: left sidebar with TabsList (flex-col, w-48) + right content area with TabsContent
5. Tabs: General, AI Agents, MCP Servers, Plugins, Connections, Repos (match existing tab structure)
6. TabsTrigger styling: text-left justify-start rounded-2xl, active gets primary/18 bg
7. Wrap in a Card with rounded-[28px] or use bare layout with page-level styling
8. Keep ALL store connections and state management

**GeneralSettings.tsx:**
1. Read existing file
2. Replace Glass inputs with shadcn Input, Select, Label from ui/
3. Theme selector: grid of Cards (1 active "zenith-violet" + placeholder slots showing "Coming soon" with muted styling)
4. Code highlight theme: Select dropdown
5. Working directory picker: Input with Button trigger for file dialog
6. Use Card for section grouping, rounded-[22px] for nested cards

**SettingsField.tsx:**
1. Read existing file -- likely a reusable form field wrapper
2. Restyle using Label from ui/label, consistent spacing (gap-2), error states with destructive color
3. Keep any validation logic
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Settings opens with vertical tabs on left, General tab shows theme grid</manual>
  </verify>
  <done>SettingsLayout renders vertical Tabs. GeneralSettings shows theme selector grid. SettingsField uses Label + consistent styling. Zero Glass imports.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AIAgents, MCP, Plugin, Connection, Repo panels</name>
  <files>
    src/renderer/src/components/settings/AIAgentsSettings.tsx
    src/renderer/src/components/settings/AgentRow.tsx
    src/renderer/src/components/settings/AddCustomAgentForm.tsx
    src/renderer/src/components/settings/MCPSettings.tsx
    src/renderer/src/components/settings/PluginSettings.tsx
    src/renderer/src/components/settings/ConnectionListEditor.tsx
    src/renderer/src/components/settings/RepoListEditor.tsx
  </files>
  <action>
**AIAgentsSettings.tsx + AgentRow.tsx + AddCustomAgentForm.tsx:**
1. Read all three files to understand agent data structure, store, and IPC calls
2. Replace table rendering with DataTable from @renderer/components/shared/data-table (columns: name, provider, model, status, actions)
3. Status column: Badge with success/warning/destructive variant
4. API key cells: Input with type="password" and show/hide toggle Button
5. Actions: Button for test, Button (destructive) for remove
6. AddCustomAgentForm: Dialog from ui/dialog with Input fields, Select for provider, Button submit
7. Keep ALL store subscriptions, IPC calls for testing connections, API key save logic

**MCPSettings.tsx:**
1. Read existing file
2. Replace with Card list layout -- each MCP server gets a Card (rounded-[22px])
3. Card content: server name, status Badge, Switch from ui/switch for enable/disable, DropdownMenu from ui/dropdown-menu for actions (configure, restart, remove)
4. Keep ALL IPC calls for server management

**PluginSettings.tsx:**
1. Read existing file
2. Use Accordion from ui/accordion -- each plugin gets an AccordionItem
3. Inside each: dynamic form fields using Input, Select, Textarea, Switch from ui/
4. Keep ALL store connections

**ConnectionListEditor.tsx + RepoListEditor.tsx:**
1. Read both files
2. Restyle as Card-based forms
3. Connection editor: Card with Input for host/port/db, Select for type, Button for test with StatusBadge result
4. Repo editor: inline editable rows with Input, Add Button (primary), Remove Button (ghost/destructive)
5. Keep ALL store connections and IPC calls
  </action>
  <verify>
    <automated>cd /Users/prafullsaxena/Desktop/Development/zenith && npx pnpm exec tsc --noEmit --project tsconfig.web.json 2>&1 | head -30</automated>
    <manual>Each settings sub-panel renders with new components, all toggles/buttons functional</manual>
  </verify>
  <done>AI Agents panel uses DataTable with StatusBadges. MCP panel uses Card+Switch+DropdownMenu. Plugin panel uses Accordion. Connection/Repo editors use Card forms. All store/IPC connections preserved.</done>
</task>

</tasks>

<verification>
- `grep -r "GlassCard\|GlassTab\|GlassInput\|GlassButton\|PluginHeader\|GlassSurface" src/renderer/src/components/settings/` returns no matches
- TypeScript compiles: `npx pnpm exec tsc --noEmit --project tsconfig.web.json`
</verification>

<success_criteria>
- Settings screen renders with vertical Tabs navigation
- All 6 sub-panels (General, AI Agents, MCP, Plugins, Connections, Repos) render with shadcn components
- Zero Glass* imports in settings/
- All store connections and IPC calls preserved
</success_criteria>

<output>
After completion, create `.planning/phases/04-screen-migration/04-02-SUMMARY.md`
</output>
