---
phase: 10-service-catalog
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/data/cloud-pricing/aws.ts
  - src/renderer/src/data/cloud-pricing/gcp.ts
  - src/renderer/src/data/cloud-pricing/azure.ts
  - src/renderer/src/data/cloud-pricing/equivalences.ts
autonomous: true
requirements: [CAT-01, CAT-02, CAT-04]

must_haves:
  truths:
    - "AWS catalog has ~34 services across exactly 8 categories: Compute, Storage, Database, Networking, ML/AI, Analytics & Streaming, Messaging/Integration, Security & Identity"
    - "GCP catalog has ~34 services across the same 8 categories with equivalent coverage"
    - "Azure catalog has ~34 services across the same 8 categories with equivalent coverage"
    - "SERVICE_EQUIVALENCES has ~25 service families, each with AWS + GCP + Azure entries"
    - "The DB re-seeds cleanly from the expanded TS catalogs on next app launch"
  artifacts:
    - path: "src/renderer/src/data/cloud-pricing/aws.ts"
      provides: "AWS_CATALOG with 8 categories, ~34 services total"
      contains: "ML/AI, Analytics & Streaming, Messaging/Integration, Security & Identity categories"
    - path: "src/renderer/src/data/cloud-pricing/gcp.ts"
      provides: "GCP_CATALOG with 8 categories, ~34 services total"
    - path: "src/renderer/src/data/cloud-pricing/azure.ts"
      provides: "AZURE_CATALOG with 8 categories, ~34 services total"
    - path: "src/renderer/src/data/cloud-pricing/equivalences.ts"
      provides: "SERVICE_EQUIVALENCES with ~25 families"
      contains: "sagemaker, bedrock, sns, sqs, cognito, waf families"
  key_links:
    - from: "src/main/pricing/seed.ts"
      to: "aws.ts / gcp.ts / azure.ts"
      via: "direct TS import — seed reads catalog categories/services"
      pattern: "AWS_CATALOG|GCP_CATALOG|AZURE_CATALOG"
    - from: "src/renderer/src/data/cloud-pricing/equivalences.ts"
      to: "ComparisonView.tsx"
      via: "getEquivalentServiceId() lookups"
      pattern: "SERVICE_EQUIVALENCES"
---

<objective>
Expand the hardcoded TS catalogs from ~17 services per provider to ~34 services per provider across exactly 8 canonical categories, and grow the cross-provider equivalence map from 11 to ~25 service families.

Purpose: CAT-01 and CAT-02 require ~100 services across 8 categories for all 3 providers. CAT-04 requires ~25 equivalence families for ComparisonView. The DB is seeded from these TS catalogs (seed.ts imports them directly), so expanding the TS files automatically gives the DB richer data on next launch.
Output: Expanded aws.ts, gcp.ts, azure.ts (8 categories each), expanded equivalences.ts (~25 families).
</objective>

<execution_context>
@/Users/prafullsaxena/.claude/get-shit-done/workflows/execute-plan.md
@/Users/prafullsaxena/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/renderer/src/data/cloud-pricing/aws.ts
@src/renderer/src/data/cloud-pricing/gcp.ts
@src/renderer/src/data/cloud-pricing/azure.ts
@src/renderer/src/data/cloud-pricing/equivalences.ts
@src/renderer/src/data/cloud-pricing/types.ts
@src/main/pricing/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand AWS_CATALOG to 8 categories with ~34 services</name>
  <files>src/renderer/src/data/cloud-pricing/aws.ts</files>
  <action>
Rewrite AWS_CATALOG to have exactly 8 top-level categories. Keep all existing services intact — add new ones.

Target 8 categories and their services (retain existing IDs exactly, add new ones):

1. **Compute** (keep: ec2, lambda; add: ~2 more)
   - `elastic-beanstalk` — App platform PaaS, no configSchema pricing needed (flat $0/hr env fee), configSchema: { quantity: {type: 'number', label: 'Environments', default: 1, min: 1, max: 50} }, pricePerUnit (use a `rate` key): 0.0 (environment is free, compute billed via EC2 — document this)
   - `lightsail` — Simplified VMs, configSchema: { plan: { type: 'select', label: 'Plan', options: [{ label: '$5/mo (1 vCPU, 1GB)', value: 'nano', pricePerHour: 0.00685 }, { label: '$10/mo (1 vCPU, 2GB)', value: 'micro', pricePerHour: 0.0137 }, { label: '$20/mo (2 vCPU, 4GB)', value: 'small', pricePerHour: 0.0274 }] } }

2. **Storage** (keep: s3, ebs; add: ~2 more)
   - `glacier` — Long-term archival, configSchema: { storageGB: { type: 'number', label: 'Storage (GB)', default: 100, min: 1, max: 1000000 } } rate key `glacier_gb`: 0.004
   - `efs` — Elastic File System, configSchema: { storageGB: { type: 'number', label: 'Storage (GB)', default: 50, min: 1, max: 1000000 } } rate key `efs_gb`: 0.30

3. **Database** (keep: rds, dynamodb; add: ~2 more)
   - `elasticache` — In-memory Redis/Memcached, configSchema: { nodeType: { type: 'select', label: 'Node Type', options: [{ label: 'cache.t3.micro', value: 'micro', pricePerHour: 0.017 }, { label: 'cache.t3.small', value: 'small', pricePerHour: 0.034 }, { label: 'cache.m5.large', value: 'large', pricePerHour: 0.156 }] }, quantity: { type: 'number', label: 'Nodes', default: 1, min: 1, max: 20 } }
   - `redshift` — Data warehouse, configSchema: { nodeType: { type: 'select', label: 'Node Type', options: [{ label: 'dc2.large (2 vCPU, 15 GB)', value: 'dc2.large', pricePerHour: 0.25 }, { label: 'ra3.xlplus (4 vCPU, 32 GB)', value: 'ra3.xlplus', pricePerHour: 1.086 }] }, quantity: { type: 'number', label: 'Nodes', default: 2, min: 1, max: 32 } }

4. **Networking** (keep: data-transfer, cloudfront; add: ~2 more)
   - `vpc-nat-gateway` — NAT Gateway, configSchema: { quantity: { type: 'number', label: 'NAT Gateways', default: 1, min: 1, max: 20 }, dataProcessedGB: { type: 'number', label: 'Data processed (GB/mo)', default: 100, min: 0, max: 100000 } } rates: nat_gateway_hourly: 0.045, nat_data_gb: 0.045
   - `route53` — DNS, configSchema: { hostedZones: { type: 'number', label: 'Hosted Zones', default: 1, min: 1, max: 500 }, queriesMillions: { type: 'number', label: 'DNS Queries (millions/mo)', default: 1, min: 0, max: 10000 } } rates: zone: 0.50, query_m: 0.40

5. **Containers** (keep: eks, fargate — rename category from 'containers' → keep 'containers' ID)
   - `ecr` — Container registry, configSchema: { storageGB: { type: 'number', label: 'Storage (GB)', default: 10, min: 1, max: 10000 }, dataTransferGB: { type: 'number', label: 'Data Transfer Out (GB/mo)', default: 10, min: 0, max: 10000 } } rates: ecr_gb: 0.10, ecr_transfer_gb: 0.09
   - `app-runner` — Managed app runner, configSchema: { vcpu: { type: 'number', label: 'vCPU', default: 0.25, min: 0.25, max: 4 }, memoryGB: { type: 'number', label: 'Memory (GB)', default: 0.5, min: 0.5, max: 12 } } rates: app_runner_vcpu: 0.064, app_runner_gb: 0.007

   NOTE: Rename "containers" category name to "Containers" if not already. Keep category id: 'containers'.

6. **ML/AI** (new category, id: 'mlai', name: 'ML/AI')
   - `sagemaker` — ML training & inference, configSchema: { instanceType: { type: 'select', label: 'Instance Type', options: [{ label: 'ml.t3.medium', value: 'ml.t3.medium', pricePerHour: 0.0464 }, { label: 'ml.m5.xlarge', value: 'ml.m5.xlarge', pricePerHour: 0.23 }, { label: 'ml.p3.2xlarge (GPU)', value: 'ml.p3.2xlarge', pricePerHour: 3.825 }] }, usageHoursPerMonth: { type: 'number', label: 'Usage hours/mo', default: 100, min: 1, max: 730 } }
   - `bedrock` — Generative AI API, configSchema: { model: { type: 'select', label: 'Model', options: [{ label: 'Claude 3 Haiku', value: 'haiku', pricePerHour: 0 }, { label: 'Claude 3 Sonnet', value: 'sonnet', pricePerHour: 0 }, { label: 'Titan Text Express', value: 'titan', pricePerHour: 0 }] }, inputTokensMillions: { type: 'number', label: 'Input tokens (millions/mo)', default: 1, min: 0.1, max: 10000 }, outputTokensMillions: { type: 'number', label: 'Output tokens (millions/mo)', default: 0.5, min: 0.1, max: 10000 } } rates: bedrock_input_m: 0.25, bedrock_output_m: 1.25
   - `rekognition` — Image/video analysis, configSchema: { imagesThousands: { type: 'number', label: 'Images analyzed (thousands/mo)', default: 10, min: 1, max: 100000 } } rates: rekognition_k_images: 1.00
   - `comprehend` — NLP / text analysis, configSchema: { unitsThousands: { type: 'number', label: 'Text units (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: comprehend_k_units: 0.10

7. **Analytics & Streaming** (new category, id: 'analytics', name: 'Analytics & Streaming')
   - `kinesis` — Data streaming, configSchema: { shards: { type: 'number', label: 'Shards', default: 1, min: 1, max: 200 }, dataGB: { type: 'number', label: 'Data throughput (GB/mo)', default: 10, min: 1, max: 10000 } } rates: kinesis_shard_hr: 0.015, kinesis_gb: 0.085
   - `emr` — Managed Hadoop/Spark, configSchema: { instanceType: { type: 'select', label: 'Master Instance', options: [{ label: 'm5.xlarge', value: 'm5.xlarge', pricePerHour: 0.192 }, { label: 'r5.2xlarge', value: 'r5.2xlarge', pricePerHour: 0.504 }] }, coreNodes: { type: 'number', label: 'Core Nodes', default: 2, min: 1, max: 50 }, usageHoursPerMonth: { type: 'number', label: 'Usage hours/mo', default: 100, min: 1, max: 730 } }
   - `athena` — Serverless query, configSchema: { dataScannedTB: { type: 'number', label: 'Data scanned (TB/mo)', default: 0.1, min: 0.01, max: 1000 } } rates: athena_tb: 5.00
   - `glue` — ETL service, configSchema: { dpuHours: { type: 'number', label: 'DPU hours/mo', default: 10, min: 1, max: 10000 } } rates: glue_dpu_hr: 0.44

8. **Messaging/Integration** (new category, id: 'messaging', name: 'Messaging/Integration')
   - `sqs` — Message queue, configSchema: { requestsMillions: { type: 'number', label: 'Requests (millions/mo)', default: 10, min: 0.1, max: 100000 } } rates: sqs_m_requests: 0.40
   - `sns` — Push notifications, configSchema: { requestsMillions: { type: 'number', label: 'Publishes (millions/mo)', default: 1, min: 0.1, max: 100000 } } rates: sns_m_pub: 0.50
   - `eventbridge` — Event bus, configSchema: { eventsMillions: { type: 'number', label: 'Events (millions/mo)', default: 1, min: 0.1, max: 100000 } } rates: eventbridge_m: 1.00
   - `step-functions` — Workflow orchestration, configSchema: { transitionsThousands: { type: 'number', label: 'State transitions (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: step_fn_k_transitions: 0.025

   Remove the old 'serverless' category (api-gateway was there) — move api-gateway into Networking (or Messaging). Place it under Messaging/Integration: `api-gateway` — keep existing configSchema.

9. **Security & Identity** (new category, id: 'security', name: 'Security & Identity')
   - `cognito` — User auth, configSchema: { mau: { type: 'number', label: 'Monthly Active Users', default: 1000, min: 0, max: 10000000 } } rates: cognito_mau: 0.0055 (first 50k free — keep simple, just use the rate)
   - `waf` — Web firewall, configSchema: { webACLs: { type: 'number', label: 'Web ACLs', default: 1, min: 1, max: 100 }, requestsMillions: { type: 'number', label: 'Requests (millions/mo)', default: 10, min: 0.1, max: 100000 } } rates: waf_acl: 5.00, waf_m_requests: 0.60
   - `secrets-manager` — Secrets storage, configSchema: { secrets: { type: 'number', label: 'Secrets stored', default: 10, min: 1, max: 100000 }, apiCallsThousands: { type: 'number', label: 'API calls (thousands/mo)', default: 10, min: 0, max: 1000000 } } rates: sm_secret: 0.40, sm_k_calls: 0.05
   - `kms` — Key management, configSchema: { keys: { type: 'number', label: 'Customer Managed Keys', default: 1, min: 1, max: 10000 }, apiCallsThousands: { type: 'number', label: 'API calls (thousands/mo)', default: 20, min: 0, max: 1000000 } } rates: kms_key: 1.00, kms_k_calls: 0.03

IMPORTANT implementation notes:
- Keep all existing service definitions EXACTLY as they are (ec2, lambda, eks, fargate, s3, ebs, rds, dynamodb, data-transfer, cloudfront, api-gateway). Do NOT change their IDs, configSchema, or pricePerHour values.
- Move api-gateway from the old 'serverless' category into 'messaging' category.
- The old 'containers' category (eks, fargate) stays as category id 'containers'.
- Total target: 8 categories, ~34 services.
- For rate-based services (not pricePerHour select): use a `rates` object on the service definition if you need it, or use a placeholder pricePerHour: 0 with rate keys documented. The calculator.ts uses RateMap fallback for non-select services — keeping a sensible default for the configSchema is what matters for the UI.
- Descriptions should be 1 short sentence (≤12 words).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/prafullsaxena/Desktop/Development/zenith/tsconfig.web.json 2>&1 | grep -E "aws\.ts|gcp\.ts|azure\.ts|error" | head -20</automated>
    <manual>grep -c "id:" src/renderer/src/data/cloud-pricing/aws.ts — should show ~42 (categories + services)</manual>
  </verify>
  <done>aws.ts has 8 categories and ~34 services; TypeScript compiles without errors in aws.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Expand GCP and Azure catalogs to 8 categories + expand equivalences to ~25 families</name>
  <files>
    src/renderer/src/data/cloud-pricing/gcp.ts
    src/renderer/src/data/cloud-pricing/azure.ts
    src/renderer/src/data/cloud-pricing/equivalences.ts
  </files>
  <action>
**GCP_CATALOG — expand to 8 categories mirroring AWS structure:**

Keep all existing services (compute-engine, cloud-functions, cloud-storage, persistent-disk, cloud-sql, firestore, gcp-data-transfer-out, cloud-cdn, cloud-run). Move cloud-run to Containers category.

1. **Compute** (compute-engine, cloud-functions; add: app-engine, cloud-batch)
   - `app-engine` — PaaS, configSchema: { instances: { type: 'number', label: 'Instances', default: 1, min: 1, max: 100 } } rate: app_engine_instance_hr: 0.05
   - `cloud-batch` — Batch jobs, configSchema: { vcpuHours: { type: 'number', label: 'vCPU-hours/mo', default: 100, min: 1, max: 100000 } } rates: batch_vcpu_hr: 0.048

2. **Storage** (cloud-storage, persistent-disk; add: filestore, cloud-backup)
   - `filestore` — Managed NFS, configSchema: { storageGB: { type: 'number', label: 'Storage (GB)', default: 1024, min: 1024, max: 100000 } } rates: filestore_gb: 0.20
   - `cloud-backup` — Backup storage, configSchema: { storageGB: { type: 'number', label: 'Backup storage (GB)', default: 100, min: 1, max: 1000000 } } rates: backup_gb: 0.023

3. **Database** (cloud-sql, firestore; add: bigtable, spanner)
   - `bigtable` — Wide-column NoSQL, configSchema: { nodes: { type: 'number', label: 'Nodes', default: 1, min: 1, max: 100 }, storageGB: { type: 'number', label: 'Storage (GB)', default: 100, min: 1, max: 1000000 } } rates: bigtable_node_hr: 0.65, bigtable_gb: 0.017
   - `spanner` — Globally distributed SQL, configSchema: { processingUnits: { type: 'number', label: 'Processing Units', default: 100, min: 100, max: 10000 } } rates: spanner_pu_hr: 0.09

4. **Networking** (gcp-data-transfer-out, cloud-cdn; add: cloud-nat, cloud-armor)
   - `cloud-nat` — NAT Gateway, configSchema: { gatewayHours: { type: 'number', label: 'Gateway hours/mo', default: 730, min: 1, max: 730 }, dataGB: { type: 'number', label: 'Data processed (GB)', default: 100, min: 0, max: 100000 } } rates: nat_hr: 0.044, nat_data_gb: 0.045
   - `cloud-armor` — DDoS/WAF, configSchema: { policies: { type: 'number', label: 'Security Policies', default: 1, min: 1, max: 100 }, requestsMillions: { type: 'number', label: 'Requests (millions/mo)', default: 10, min: 0.1, max: 100000 } } rates: armor_policy: 5.00, armor_m_req: 0.75

5. **Containers** (cloud-run, gke; add: cloud-run-jobs, artifact-registry)
   - Keep existing: cloud-run, gke (id: 'gke')
   - `artifact-registry` — Container registry, configSchema: { storageGB: { type: 'number', label: 'Storage (GB)', default: 10, min: 1, max: 10000 } } rates: artifact_gb: 0.10

6. **ML/AI** (new, id: 'mlai')
   - `vertex-ai` — ML platform, configSchema: { machineType: { type: 'select', label: 'Machine Type', options: [{ label: 'n1-standard-4', value: 'n1-std-4', pricePerHour: 0.19 }, { label: 'n1-highmem-8', value: 'n1-highmem-8', pricePerHour: 0.57 }, { label: 'a2-highgpu-1g (A100)', value: 'a2-highgpu-1g', pricePerHour: 3.67 }] }, usageHoursPerMonth: { type: 'number', label: 'Usage hours/mo', default: 100, min: 1, max: 730 } }
   - `vision-ai` — Image analysis, configSchema: { unitsThousands: { type: 'number', label: 'Images (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: vision_k: 1.50
   - `natural-language-ai` — NLP API, configSchema: { unitsThousands: { type: 'number', label: 'Text units (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: nlp_k: 1.00
   - `translation-ai` — Translation, configSchema: { charsMillions: { type: 'number', label: 'Characters (millions/mo)', default: 1, min: 0.1, max: 10000 } } rates: translate_m_chars: 20.00

7. **Analytics & Streaming** (new, id: 'analytics')
   - `bigquery` — Serverless analytics, configSchema: { queryTB: { type: 'number', label: 'Query data scanned (TB/mo)', default: 1, min: 0.01, max: 1000 }, storageGB: { type: 'number', label: 'Storage (GB)', default: 100, min: 1, max: 1000000 } } rates: bq_query_tb: 5.00, bq_storage_gb: 0.02
   - `pub-sub` — Messaging, configSchema: { dataGB: { type: 'number', label: 'Message data (GB/mo)', default: 10, min: 0.1, max: 100000 } } rates: pubsub_gb: 0.04
   - `dataflow` — Streaming ETL, configSchema: { vcpuHours: { type: 'number', label: 'vCPU-hours/mo', default: 100, min: 1, max: 100000 } } rates: dataflow_vcpu_hr: 0.056
   - `looker-studio` — BI / reporting, configSchema: { reportCount: { type: 'number', label: 'Reports', default: 1, min: 1, max: 1000 } } rates: looker_report: 0 (free tier — document)

8. **Messaging/Integration** (new, id: 'messaging') — Note: pub-sub belongs in Analytics, use Cloud Tasks here
   - `cloud-tasks` — Task queue, configSchema: { tasksMillions: { type: 'number', label: 'Tasks (millions/mo)', default: 10, min: 0.1, max: 100000 } } rates: cloud_tasks_m: 0.40
   - `cloud-scheduler` — Cron jobs, configSchema: { jobs: { type: 'number', label: 'Jobs', default: 3, min: 1, max: 1000 } } rates: scheduler_job: 0.10
   - `eventarc` — Event routing, configSchema: { eventsMillions: { type: 'number', label: 'Events (millions/mo)', default: 1, min: 0.1, max: 100000 } } rates: eventarc_m: 0.40
   - `workflows` — Serverless orchestration, configSchema: { stepsThousands: { type: 'number', label: 'Steps executed (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: workflows_k_steps: 0.01

   NOTE for GCP Security: Put IAM/security items under a **Security & Identity** category too:

9. **Security & Identity** (new, id: 'security')
   - `cloud-armor` — ALREADY added to Networking above. Move it here instead. Keep cloud-armor under Security.
   - `secret-manager` — Secret storage, configSchema: { secrets: { type: 'number', label: 'Secrets stored', default: 10, min: 1, max: 100000 }, accessesThousands: { type: 'number', label: 'Secret accesses (thousands/mo)', default: 10, min: 0, max: 1000000 } } rates: secret_version: 0.06, secret_k_access: 0.03
   - `cloud-kms` — Key management, configSchema: { keyVersions: { type: 'number', label: 'Key versions', default: 5, min: 1, max: 10000 }, operationsThousands: { type: 'number', label: 'Crypto operations (thousands/mo)', default: 10, min: 0, max: 1000000 } } rates: kms_version: 0.06, kms_k_ops: 0.03
   - `identity-platform` — Auth, configSchema: { mau: { type: 'number', label: 'Monthly Active Users', default: 1000, min: 0, max: 10000000 } } rates: idp_mau: 0.0055

SIMPLIFICATION: Keep cloud-armor in Security & Identity only (not Networking). For Networking use cloud-nat and cloud-dns instead:
   - `cloud-dns` — Managed DNS, configSchema: { zones: { type: 'number', label: 'Managed Zones', default: 1, min: 1, max: 10000 }, queriesMillions: { type: 'number', label: 'Queries (millions/mo)', default: 1, min: 0.1, max: 100000 } } rates: dns_zone: 0.20, dns_m_queries: 0.40

---

**AZURE_CATALOG — expand to 8 categories mirroring AWS structure:**

Keep all existing services (azure-vm, azure-functions, blob-storage, premium-ssd, azure-sql, cosmos-db, azure-data-transfer-out, azure-cdn, app-service). Move app-service to Containers/Compute.

1. **Compute** (azure-vm, azure-functions; add: azure-spring-apps, batch)
   - `azure-spring-apps` — Java PaaS, configSchema: { instances: { type: 'number', label: 'Instances', default: 1, min: 1, max: 100 } } rates: spring_apps_hr: 0.05
   - `azure-batch` — Batch compute, configSchema: { vcpuHours: { type: 'number', label: 'vCPU-hours/mo', default: 100, min: 1, max: 100000 } } rates: batch_vcpu_hr: 0.048

2. **Storage** (blob-storage, premium-ssd; add: azure-files, archive-storage)
   - `azure-files` — SMB file shares, configSchema: { storageGB: { type: 'number', label: 'Storage (GB)', default: 100, min: 1, max: 1000000 } } rates: files_gb: 0.20
   - `archive-storage` — Cold archive, configSchema: { storageGB: { type: 'number', label: 'Storage (GB)', default: 500, min: 1, max: 10000000 } } rates: archive_gb: 0.002

3. **Database** (azure-sql, cosmos-db; add: azure-cache-redis, azure-synapse)
   - `azure-cache-redis` — Redis cache, configSchema: { tier: { type: 'select', label: 'Tier', options: [{ label: 'Basic C0 (250MB)', value: 'c0', pricePerHour: 0.017 }, { label: 'Standard C1 (1GB)', value: 'c1', pricePerHour: 0.05 }, { label: 'Premium P1 (6GB)', value: 'p1', pricePerHour: 0.323 }] } }
   - `azure-synapse` — Analytics warehouse, configSchema: { dwu: { type: 'select', label: 'DWU', options: [{ label: 'DW100c', value: 'dw100c', pricePerHour: 1.51 }, { label: 'DW300c', value: 'dw300c', pricePerHour: 4.52 }, { label: 'DW1000c', value: 'dw1000c', pricePerHour: 15.07 }] } }

4. **Networking** (azure-data-transfer-out, azure-cdn; add: azure-nat-gateway, azure-dns)
   - `azure-nat-gateway` — Outbound NAT, configSchema: { quantity: { type: 'number', label: 'Gateways', default: 1, min: 1, max: 10 }, dataGB: { type: 'number', label: 'Data processed (GB/mo)', default: 100, min: 0, max: 100000 } } rates: nat_hr: 0.045, nat_data_gb: 0.045
   - `azure-dns` — DNS zones, configSchema: { zones: { type: 'number', label: 'DNS Zones', default: 1, min: 1, max: 10000 }, queriesMillions: { type: 'number', label: 'Queries (millions/mo)', default: 1, min: 0.1, max: 100000 } } rates: dns_zone: 0.50, dns_m_queries: 0.40

5. **Containers** (aks, azure-container-instances; add: app-service, azure-container-registry)
   - Keep: aks (id: 'aks'), azure-container-instances
   - `app-service` — Web app PaaS (move from old location), keep existing configSchema
   - `azure-container-registry` — ACR, configSchema: { storageGB: { type: 'number', label: 'Storage (GB)', default: 10, min: 1, max: 10000 } } rates: acr_gb: 0.10

6. **ML/AI** (new, id: 'mlai')
   - `azure-openai` — OpenAI on Azure, configSchema: { model: { type: 'select', label: 'Model', options: [{ label: 'GPT-3.5 Turbo', value: 'gpt35', pricePerHour: 0 }, { label: 'GPT-4', value: 'gpt4', pricePerHour: 0 }, { label: 'GPT-4o', value: 'gpt4o', pricePerHour: 0 }] }, inputTokensMillions: { type: 'number', label: 'Input tokens (millions/mo)', default: 1, min: 0.1, max: 10000 }, outputTokensMillions: { type: 'number', label: 'Output tokens (millions/mo)', default: 0.5, min: 0.1, max: 10000 } } rates: openai_input_m: 0.50, openai_output_m: 1.50
   - `azure-machine-learning` — ML workspace, configSchema: { computeHours: { type: 'number', label: 'Compute hours/mo', default: 100, min: 1, max: 730 } } rates: aml_hr: 0.18
   - `cognitive-services` — Vision/NLP APIs, configSchema: { callsThousands: { type: 'number', label: 'API calls (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: cog_k_calls: 1.50
   - `azure-bot-service` — Bot Framework, configSchema: { messagesThousands: { type: 'number', label: 'Messages (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: bot_k_msg: 0.50

7. **Analytics & Streaming** (new, id: 'analytics')
   - `event-hubs` — Event streaming (Kafka-compatible), configSchema: { throughputUnits: { type: 'number', label: 'Throughput Units', default: 1, min: 1, max: 40 } } rates: eh_tu_hr: 0.028, eh_ingress_m: 0.028
   - `stream-analytics` — Real-time processing, configSchema: { streamingUnits: { type: 'number', label: 'Streaming Units', default: 1, min: 1, max: 192 } } rates: sa_su_hr: 0.031
   - `azure-data-factory` — ETL pipelines, configSchema: { pipelineRuns: { type: 'number', label: 'Pipeline runs/mo', default: 100, min: 1, max: 100000 } } rates: adf_run: 0.001
   - `hdinsight` — Managed Hadoop/Spark, configSchema: { nodeHours: { type: 'number', label: 'Node-hours/mo', default: 200, min: 1, max: 100000 } } rates: hdi_node_hr: 0.192

8. **Messaging/Integration** (new, id: 'messaging')
   - `service-bus` — Enterprise messaging, configSchema: { messagesMillions: { type: 'number', label: 'Messages (millions/mo)', default: 1, min: 0.1, max: 100000 } } rates: sb_m_msg: 0.80
   - `azure-notification-hubs` — Push notifications, configSchema: { pushesMillions: { type: 'number', label: 'Pushes (millions/mo)', default: 1, min: 0.1, max: 10000 } } rates: anh_m_push: 0.50
   - `logic-apps` — Workflow automation, configSchema: { actionsThousands: { type: 'number', label: 'Actions (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: logic_apps_k_actions: 0.25
   - `api-management` — API gateway, configSchema: { tier: { type: 'select', label: 'Tier', options: [{ label: 'Developer', value: 'developer', pricePerHour: 0.07 }, { label: 'Basic', value: 'basic', pricePerHour: 0.21 }, { label: 'Standard', value: 'standard', pricePerHour: 0.95 }] } }

9. **Security & Identity** (new, id: 'security')
   - `azure-active-directory` — Identity, configSchema: { mau: { type: 'number', label: 'MAU (Premium P1)', default: 1000, min: 0, max: 10000000 } } rates: aad_mau: 0.006
   - `azure-key-vault` — Secret/key management, configSchema: { operations: { type: 'number', label: 'Operations (thousands/mo)', default: 10, min: 1, max: 1000000 } } rates: kv_k_ops: 0.03
   - `microsoft-defender` — Cloud security, configSchema: { servers: { type: 'number', label: 'Servers protected', default: 10, min: 1, max: 10000 } } rates: defender_server: 15.00
   - `azure-firewall` — Network firewall, configSchema: { deploymentHours: { type: 'number', label: 'Deployment hours/mo', default: 730, min: 1, max: 730 }, dataGB: { type: 'number', label: 'Data processed (GB/mo)', default: 100, min: 0, max: 100000 } } rates: fw_hr: 1.25, fw_data_gb: 0.016

---

**equivalences.ts — expand SERVICE_EQUIVALENCES to ~25 families:**

Keep all 11 existing entries unchanged. Add 14 new families:

```
// New equivalences to add:
'elasticache':     { aws: 'elasticache',        gcp: 'bigtable',           azure: 'azure-cache-redis'  },
'redshift':        { aws: 'redshift',            gcp: 'bigquery',           azure: 'azure-synapse'      },
'sagemaker':       { aws: 'sagemaker',           gcp: 'vertex-ai',          azure: 'azure-machine-learning' },
'bedrock':         { aws: 'bedrock',             gcp: 'vertex-ai',          azure: 'azure-openai'       },
'rekognition':     { aws: 'rekognition',         gcp: 'vision-ai',          azure: 'cognitive-services' },
'kinesis':         { aws: 'kinesis',             gcp: 'pub-sub',            azure: 'event-hubs'         },
'glue':            { aws: 'glue',                gcp: 'dataflow',           azure: 'azure-data-factory' },
'sns':             { aws: 'sns',                 gcp: 'pub-sub',            azure: 'azure-notification-hubs' },
'sqs':             { aws: 'sqs',                 gcp: 'cloud-tasks',        azure: 'service-bus'        },
'step-functions':  { aws: 'step-functions',      gcp: 'workflows',          azure: 'logic-apps'         },
'cognito':         { aws: 'cognito',             gcp: 'identity-platform',  azure: 'azure-active-directory' },
'waf':             { aws: 'waf',                 gcp: 'cloud-armor',        azure: 'azure-firewall'     },
'secrets-manager': { aws: 'secrets-manager',    gcp: 'secret-manager',     azure: 'azure-key-vault'    },
'ecr':             { aws: 'ecr',                 gcp: 'artifact-registry',  azure: 'azure-container-registry' },
```

This brings the total to 25 families.

NOTE: Some GCP/Azure IDs in these equivalences point to services that appear in ML/AI or other categories — that is intentional (e.g., bigquery handles analytics but maps to redshift for data warehouse equivalence).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p /Users/prafullsaxena/Desktop/Development/zenith/tsconfig.web.json 2>&1 | grep -E "gcp\.ts|azure\.ts|equivalences\.ts|error" | head -20</automated>
    <manual>Count: grep "id: '" src/renderer/src/data/cloud-pricing/gcp.ts | wc -l — expect ~42. grep "aws:" src/renderer/src/data/cloud-pricing/equivalences.ts | wc -l — expect 25.</manual>
  </verify>
  <done>gcp.ts and azure.ts each have 8 categories with ~34 services; equivalences.ts has 25 families; TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npx tsc --noEmit -p tsconfig.web.json` passes with zero errors in catalog files
- `npx tsc --noEmit -p tsconfig.node.json` passes (seed.ts imports these catalogs)
- Each catalog file has exactly 8 category IDs: compute, storage, database, networking, containers, mlai, analytics, messaging, security (wait — that's 9; keep 8 by merging wisely — use the 8 from CONTEXT.md: Compute, Storage, Database, Networking, ML/AI, Analytics & Streaming, Messaging/Integration, Security & Identity — so 8 categories including containers under compute or as standalone; adjust to keep containers within compute OR as a separate category making 9 — DECISION: use 8 as per CONTEXT.md; merge containers into compute)
- SERVICE_EQUIVALENCES has exactly 25 entries
</verification>

<success_criteria>
1. AWS, GCP, Azure catalogs each have ~34 services spanning 8 categories matching CONTEXT.md names
2. equivalences.ts has 25 service families
3. Zero TypeScript errors in both web and node tsconfig targets
4. seed.ts can import all three expanded catalogs without error (verified by tsconfig.node.json compile)
</success_criteria>

<output>
After completion, create `.planning/phases/10-service-catalog/10-01-SUMMARY.md`
</output>
